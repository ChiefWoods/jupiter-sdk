/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { GOVERNANCE_PROGRAM_ADDRESS } from "../programs";

export const SET_VOTING_REWARD_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([227, 241, 48, 137, 30, 26, 104, 70]);

export function getSetVotingRewardDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_VOTING_REWARD_DISCRIMINATOR,
  );
}

export type SetVotingRewardInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountGovernor extends string | AccountMeta<string> = string,
  TAccountRewardMint extends string | AccountMeta<string> = string,
  TAccountSmartWallet extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGovernor extends string
        ? WritableAccount<TAccountGovernor>
        : TAccountGovernor,
      TAccountRewardMint extends string
        ? ReadonlyAccount<TAccountRewardMint>
        : TAccountRewardMint,
      TAccountSmartWallet extends string
        ? ReadonlySignerAccount<TAccountSmartWallet> &
            AccountSignerMeta<TAccountSmartWallet>
        : TAccountSmartWallet,
      ...TRemainingAccounts,
    ]
  >;

export type SetVotingRewardInstructionData = {
  discriminator: ReadonlyUint8Array;
  rewardPerProposal: bigint;
};

export type SetVotingRewardInstructionDataArgs = {
  rewardPerProposal: number | bigint;
};

export function getSetVotingRewardInstructionDataEncoder(): FixedSizeEncoder<SetVotingRewardInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["rewardPerProposal", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_VOTING_REWARD_DISCRIMINATOR }),
  );
}

export function getSetVotingRewardInstructionDataDecoder(): FixedSizeDecoder<SetVotingRewardInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["rewardPerProposal", getU64Decoder()],
  ]);
}

export function getSetVotingRewardInstructionDataCodec(): FixedSizeCodec<
  SetVotingRewardInstructionDataArgs,
  SetVotingRewardInstructionData
> {
  return combineCodec(
    getSetVotingRewardInstructionDataEncoder(),
    getSetVotingRewardInstructionDataDecoder(),
  );
}

export type SetVotingRewardInput<
  TAccountGovernor extends string = string,
  TAccountRewardMint extends string = string,
  TAccountSmartWallet extends string = string,
> = {
  /** The [Governor] */
  governor: Address<TAccountGovernor>;
  /** reward mint */
  rewardMint: Address<TAccountRewardMint>;
  /** The Smart Wallet. */
  smartWallet: TransactionSigner<TAccountSmartWallet>;
  rewardPerProposal: SetVotingRewardInstructionDataArgs["rewardPerProposal"];
};

export function getSetVotingRewardInstruction<
  TAccountGovernor extends string,
  TAccountRewardMint extends string,
  TAccountSmartWallet extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: SetVotingRewardInput<
    TAccountGovernor,
    TAccountRewardMint,
    TAccountSmartWallet
  >,
  config?: { programAddress?: TProgramAddress },
): SetVotingRewardInstruction<
  TProgramAddress,
  TAccountGovernor,
  TAccountRewardMint,
  TAccountSmartWallet
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governor: { value: input.governor ?? null, isWritable: true },
    rewardMint: { value: input.rewardMint ?? null, isWritable: false },
    smartWallet: { value: input.smartWallet ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("governor", accounts.governor),
      getAccountMeta("rewardMint", accounts.rewardMint),
      getAccountMeta("smartWallet", accounts.smartWallet),
    ],
    data: getSetVotingRewardInstructionDataEncoder().encode(
      args as SetVotingRewardInstructionDataArgs,
    ),
    programAddress,
  } as SetVotingRewardInstruction<
    TProgramAddress,
    TAccountGovernor,
    TAccountRewardMint,
    TAccountSmartWallet
  >);
}

export type ParsedSetVotingRewardInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The [Governor] */
    governor: TAccountMetas[0];
    /** reward mint */
    rewardMint: TAccountMetas[1];
    /** The Smart Wallet. */
    smartWallet: TAccountMetas[2];
  };
  data: SetVotingRewardInstructionData;
};

export function parseSetVotingRewardInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetVotingRewardInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      governor: getNextAccount(),
      rewardMint: getNextAccount(),
      smartWallet: getNextAccount(),
    },
    data: getSetVotingRewardInstructionDataDecoder().decode(instruction.data),
  };
}
