/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LIQUIDITY_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_CLAIM_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([241, 146, 203, 216, 58, 222, 91, 118]);

export function getCloseClaimAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_CLAIM_ACCOUNT_DISCRIMINATOR,
  );
}

export type CloseClaimAccountInstruction<
  TProgram extends string = typeof LIQUIDITY_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountClaimAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountClaimAccount extends string
        ? WritableAccount<TAccountClaimAccount>
        : TAccountClaimAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseClaimAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  mint: Address;
};

export type CloseClaimAccountInstructionDataArgs = { mint: Address };

export function getCloseClaimAccountInstructionDataEncoder(): FixedSizeEncoder<CloseClaimAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mint", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CLOSE_CLAIM_ACCOUNT_DISCRIMINATOR }),
  );
}

export function getCloseClaimAccountInstructionDataDecoder(): FixedSizeDecoder<CloseClaimAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mint", getAddressDecoder()],
  ]);
}

export function getCloseClaimAccountInstructionDataCodec(): FixedSizeCodec<
  CloseClaimAccountInstructionDataArgs,
  CloseClaimAccountInstructionData
> {
  return combineCodec(
    getCloseClaimAccountInstructionDataEncoder(),
    getCloseClaimAccountInstructionDataDecoder(),
  );
}

export type CloseClaimAccountInput<
  TAccountUser extends string = string,
  TAccountClaimAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  claimAccount: Address<TAccountClaimAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  mint: CloseClaimAccountInstructionDataArgs["mint"];
};

export function getCloseClaimAccountInstruction<
  TAccountUser extends string,
  TAccountClaimAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: CloseClaimAccountInput<
    TAccountUser,
    TAccountClaimAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseClaimAccountInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountClaimAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    claimAccount: { value: input.claimAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("user", accounts.user),
      getAccountMeta("claimAccount", accounts.claimAccount),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCloseClaimAccountInstructionDataEncoder().encode(
      args as CloseClaimAccountInstructionDataArgs,
    ),
    programAddress,
  } as CloseClaimAccountInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountClaimAccount,
    TAccountSystemProgram
  >);
}

export type ParsedCloseClaimAccountInstruction<
  TProgram extends string = typeof LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    claimAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CloseClaimAccountInstructionData;
};

export function parseCloseClaimAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseClaimAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      claimAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCloseClaimAccountInstructionDataDecoder().decode(instruction.data),
  };
}
