/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_CLAIM_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [238, 50, 157, 85, 151, 58, 231, 45],
);

export function getLogClaimEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_CLAIM_EVENT_DISCRIMINATOR,
  );
}

export type LogClaimEvent = {
  user: Address;
  token: Address;
  recipient: Address;
  amount: bigint;
};

export type LogClaimEventArgs = {
  user: Address;
  token: Address;
  recipient: Address;
  amount: number | bigint;
};

/** Gets the encoder for {@link LogClaimEventArgs} event data. */
export function getLogClaimEventEncoder(): FixedSizeEncoder<LogClaimEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["user", getAddressEncoder()],
      ["token", getAddressEncoder()],
      ["recipient", getAddressEncoder()],
      ["amount", getU64Encoder()],
    ]),
    [getConstantEncoder(LOG_CLAIM_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogClaimEvent} event data. */
export function getLogClaimEventDecoder(): FixedSizeDecoder<LogClaimEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["user", getAddressDecoder()],
      ["token", getAddressDecoder()],
      ["recipient", getAddressDecoder()],
      ["amount", getU64Decoder()],
    ]),
    [getConstantDecoder(LOG_CLAIM_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogClaimEvent} event data. */
export function getLogClaimEventCodec(): FixedSizeCodec<
  LogClaimEventArgs,
  LogClaimEvent
> {
  return combineCodec(getLogClaimEventEncoder(), getLogClaimEventDecoder());
}

/** Parses the provided bytes into {@link LogClaimEvent} event data. */
export function parseLogClaimEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogClaimEvent {
  if (containsBytes(data, LOG_CLAIM_EVENT_DISCRIMINATOR, 0)) {
    return getLogClaimEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogClaimEvent" event discriminators.',
  );
}
