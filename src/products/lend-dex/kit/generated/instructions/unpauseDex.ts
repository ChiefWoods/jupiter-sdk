/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { DEX_PROGRAM_ADDRESS } from "../programs";

export const UNPAUSE_DEX_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  88, 52, 175, 105, 210, 116, 178, 218,
]);

export function getUnpauseDexDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(UNPAUSE_DEX_DISCRIMINATOR);
}

export type UnpauseDexInstruction<
  TProgram extends string = typeof DEX_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TAccountDex extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDexAdmin extends string
        ? ReadonlyAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      TAccountDex extends string ? WritableAccount<TAccountDex> : TAccountDex,
      ...TRemainingAccounts,
    ]
  >;

export type UnpauseDexInstructionData = { discriminator: ReadonlyUint8Array };

export type UnpauseDexInstructionDataArgs = {};

export function getUnpauseDexInstructionDataEncoder(): FixedSizeEncoder<UnpauseDexInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: UNPAUSE_DEX_DISCRIMINATOR }),
  );
}

export function getUnpauseDexInstructionDataDecoder(): FixedSizeDecoder<UnpauseDexInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getUnpauseDexInstructionDataCodec(): FixedSizeCodec<
  UnpauseDexInstructionDataArgs,
  UnpauseDexInstructionData
> {
  return combineCodec(
    getUnpauseDexInstructionDataEncoder(),
    getUnpauseDexInstructionDataDecoder(),
  );
}

export type UnpauseDexInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountDex extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  dex: Address<TAccountDex>;
};

export function getUnpauseDexInstruction<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TAccountDex extends string,
  TProgramAddress extends Address = typeof DEX_PROGRAM_ADDRESS,
>(
  input: UnpauseDexInput<TAccountAuthority, TAccountDexAdmin, TAccountDex>,
  config?: { programAddress?: TProgramAddress },
): UnpauseDexInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDexAdmin,
  TAccountDex
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: false },
    dex: { value: input.dex ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("dex", accounts.dex),
    ],
    data: getUnpauseDexInstructionDataEncoder().encode({}),
    programAddress,
  } as UnpauseDexInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex
  >);
}

export type ParsedUnpauseDexInstruction<
  TProgram extends string = typeof DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
    dex: TAccountMetas[2];
  };
  data: UnpauseDexInstructionData;
};

export function parseUnpauseDexInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUnpauseDexInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      dexAdmin: getNextAccount(),
      dex: getNextAccount(),
    },
    data: getUnpauseDexInstructionDataDecoder().decode(instruction.data),
  };
}
