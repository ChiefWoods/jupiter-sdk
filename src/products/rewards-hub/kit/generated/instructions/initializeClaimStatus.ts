/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findClaimStatusPda } from "../pdas";
import { GENIE_DISTRIBUTOR_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_CLAIM_STATUS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([133, 6, 250, 212, 133, 145, 69, 136]);

export function getInitializeClaimStatusDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CLAIM_STATUS_DISCRIMINATOR,
  );
}

export type InitializeClaimStatusInstruction<
  TProgram extends string = typeof GENIE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountCampaign extends string | AccountMeta<string> = string,
  TAccountClaimStatus extends string | AccountMeta<string> = string,
  TAccountClaimant extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCampaign extends string
        ? WritableAccount<TAccountCampaign>
        : TAccountCampaign,
      TAccountClaimStatus extends string
        ? WritableAccount<TAccountClaimStatus>
        : TAccountClaimStatus,
      TAccountClaimant extends string
        ? WritableSignerAccount<TAccountClaimant> &
            AccountSignerMeta<TAccountClaimant>
        : TAccountClaimant,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeClaimStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeClaimStatusInstructionDataArgs = {};

export function getInitializeClaimStatusInstructionDataEncoder(): FixedSizeEncoder<InitializeClaimStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_CLAIM_STATUS_DISCRIMINATOR,
    }),
  );
}

export function getInitializeClaimStatusInstructionDataDecoder(): FixedSizeDecoder<InitializeClaimStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeClaimStatusInstructionDataCodec(): FixedSizeCodec<
  InitializeClaimStatusInstructionDataArgs,
  InitializeClaimStatusInstructionData
> {
  return combineCodec(
    getInitializeClaimStatusInstructionDataEncoder(),
    getInitializeClaimStatusInstructionDataDecoder(),
  );
}

export type InitializeClaimStatusAsyncInput<
  TAccountCampaign extends string = string,
  TAccountClaimStatus extends string = string,
  TAccountClaimant extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The [GenieDistributor]. */
  campaign: Address<TAccountCampaign>;
  /** Claim status PDA */
  claimStatus?: Address<TAccountClaimStatus>;
  /** Who is claiming the tokens. */
  claimant: TransactionSigner<TAccountClaimant>;
  /** The [System] program. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeClaimStatusInstructionAsync<
  TAccountCampaign extends string,
  TAccountClaimStatus extends string,
  TAccountClaimant extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GENIE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: InitializeClaimStatusAsyncInput<
    TAccountCampaign,
    TAccountClaimStatus,
    TAccountClaimant,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeClaimStatusInstruction<
    TProgramAddress,
    TAccountCampaign,
    TAccountClaimStatus,
    TAccountClaimant,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GENIE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    campaign: { value: input.campaign ?? null, isWritable: true },
    claimStatus: { value: input.claimStatus ?? null, isWritable: true },
    claimant: { value: input.claimant ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.claimStatus.value) {
    accounts.claimStatus.value = await findClaimStatusPda({
      claimant: getAddressFromResolvedInstructionAccount(
        "claimant",
        accounts.claimant.value,
      ),
      campaign: getAddressFromResolvedInstructionAccount(
        "campaign",
        accounts.campaign.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("campaign", accounts.campaign),
      getAccountMeta("claimStatus", accounts.claimStatus),
      getAccountMeta("claimant", accounts.claimant),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeClaimStatusInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeClaimStatusInstruction<
    TProgramAddress,
    TAccountCampaign,
    TAccountClaimStatus,
    TAccountClaimant,
    TAccountSystemProgram
  >);
}

export type InitializeClaimStatusInput<
  TAccountCampaign extends string = string,
  TAccountClaimStatus extends string = string,
  TAccountClaimant extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The [GenieDistributor]. */
  campaign: Address<TAccountCampaign>;
  /** Claim status PDA */
  claimStatus: Address<TAccountClaimStatus>;
  /** Who is claiming the tokens. */
  claimant: TransactionSigner<TAccountClaimant>;
  /** The [System] program. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeClaimStatusInstruction<
  TAccountCampaign extends string,
  TAccountClaimStatus extends string,
  TAccountClaimant extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GENIE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: InitializeClaimStatusInput<
    TAccountCampaign,
    TAccountClaimStatus,
    TAccountClaimant,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeClaimStatusInstruction<
  TProgramAddress,
  TAccountCampaign,
  TAccountClaimStatus,
  TAccountClaimant,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GENIE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    campaign: { value: input.campaign ?? null, isWritable: true },
    claimStatus: { value: input.claimStatus ?? null, isWritable: true },
    claimant: { value: input.claimant ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("campaign", accounts.campaign),
      getAccountMeta("claimStatus", accounts.claimStatus),
      getAccountMeta("claimant", accounts.claimant),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeClaimStatusInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeClaimStatusInstruction<
    TProgramAddress,
    TAccountCampaign,
    TAccountClaimStatus,
    TAccountClaimant,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeClaimStatusInstruction<
  TProgram extends string = typeof GENIE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The [GenieDistributor]. */
    campaign: TAccountMetas[0];
    /** Claim status PDA */
    claimStatus: TAccountMetas[1];
    /** Who is claiming the tokens. */
    claimant: TAccountMetas[2];
    /** The [System] program. */
    systemProgram: TAccountMetas[3];
  };
  data: InitializeClaimStatusInstructionData;
};

export function parseInitializeClaimStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeClaimStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      campaign: getNextAccount(),
      claimStatus: getNextAccount(),
      claimant: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeClaimStatusInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
