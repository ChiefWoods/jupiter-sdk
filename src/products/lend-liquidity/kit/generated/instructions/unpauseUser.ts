/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_LIQUIDITY_PROGRAM_ADDRESS } from "../programs";

export const UNPAUSE_USER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  71, 115, 128, 252, 182, 126, 234, 62,
]);

export function getUnpauseUserDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UNPAUSE_USER_DISCRIMINATOR,
  );
}

export type UnpauseUserInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAuthList extends string | AccountMeta<string> = string,
  TAccountUserSupplyPosition extends string | AccountMeta<string> = string,
  TAccountUserBorrowPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAuthList extends string
        ? ReadonlyAccount<TAccountAuthList>
        : TAccountAuthList,
      TAccountUserSupplyPosition extends string
        ? WritableAccount<TAccountUserSupplyPosition>
        : TAccountUserSupplyPosition,
      TAccountUserBorrowPosition extends string
        ? WritableAccount<TAccountUserBorrowPosition>
        : TAccountUserBorrowPosition,
      ...TRemainingAccounts,
    ]
  >;

export type UnpauseUserInstructionData = {
  discriminator: ReadonlyUint8Array;
  protocol: Address;
  supplyMint: Address;
  borrowMint: Address;
  supplyStatus: Option<number>;
  borrowStatus: Option<number>;
};

export type UnpauseUserInstructionDataArgs = {
  protocol: Address;
  supplyMint: Address;
  borrowMint: Address;
  supplyStatus: OptionOrNullable<number>;
  borrowStatus: OptionOrNullable<number>;
};

export function getUnpauseUserInstructionDataEncoder(): Encoder<UnpauseUserInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["protocol", getAddressEncoder()],
      ["supplyMint", getAddressEncoder()],
      ["borrowMint", getAddressEncoder()],
      ["supplyStatus", getOptionEncoder(getU8Encoder())],
      ["borrowStatus", getOptionEncoder(getU8Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UNPAUSE_USER_DISCRIMINATOR }),
  );
}

export function getUnpauseUserInstructionDataDecoder(): Decoder<UnpauseUserInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["protocol", getAddressDecoder()],
    ["supplyMint", getAddressDecoder()],
    ["borrowMint", getAddressDecoder()],
    ["supplyStatus", getOptionDecoder(getU8Decoder())],
    ["borrowStatus", getOptionDecoder(getU8Decoder())],
  ]);
}

export function getUnpauseUserInstructionDataCodec(): Codec<
  UnpauseUserInstructionDataArgs,
  UnpauseUserInstructionData
> {
  return combineCodec(
    getUnpauseUserInstructionDataEncoder(),
    getUnpauseUserInstructionDataDecoder(),
  );
}

export type UnpauseUserInput<
  TAccountAuthority extends string = string,
  TAccountAuthList extends string = string,
  TAccountUserSupplyPosition extends string = string,
  TAccountUserBorrowPosition extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  authList: Address<TAccountAuthList>;
  userSupplyPosition: Address<TAccountUserSupplyPosition>;
  userBorrowPosition: Address<TAccountUserBorrowPosition>;
  protocol: UnpauseUserInstructionDataArgs["protocol"];
  supplyMint: UnpauseUserInstructionDataArgs["supplyMint"];
  borrowMint: UnpauseUserInstructionDataArgs["borrowMint"];
  supplyStatus: UnpauseUserInstructionDataArgs["supplyStatus"];
  borrowStatus: UnpauseUserInstructionDataArgs["borrowStatus"];
};

export function getUnpauseUserInstruction<
  TAccountAuthority extends string,
  TAccountAuthList extends string,
  TAccountUserSupplyPosition extends string,
  TAccountUserBorrowPosition extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: UnpauseUserInput<
    TAccountAuthority,
    TAccountAuthList,
    TAccountUserSupplyPosition,
    TAccountUserBorrowPosition
  >,
  config?: { programAddress?: TProgramAddress },
): UnpauseUserInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAuthList,
  TAccountUserSupplyPosition,
  TAccountUserBorrowPosition
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    authList: { value: input.authList ?? null, isWritable: false },
    userSupplyPosition: {
      value: input.userSupplyPosition ?? null,
      isWritable: true,
    },
    userBorrowPosition: {
      value: input.userBorrowPosition ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("authList", accounts.authList),
      getAccountMeta("userSupplyPosition", accounts.userSupplyPosition),
      getAccountMeta("userBorrowPosition", accounts.userBorrowPosition),
    ],
    data: getUnpauseUserInstructionDataEncoder().encode(
      args as UnpauseUserInstructionDataArgs,
    ),
    programAddress,
  } as UnpauseUserInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAuthList,
    TAccountUserSupplyPosition,
    TAccountUserBorrowPosition
  >);
}

export type ParsedUnpauseUserInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    authList: TAccountMetas[1];
    userSupplyPosition: TAccountMetas[2];
    userBorrowPosition: TAccountMetas[3];
  };
  data: UnpauseUserInstructionData;
};

export function parseUnpauseUserInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUnpauseUserInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      authList: getNextAccount(),
      userSupplyPosition: getNextAccount(),
      userBorrowPosition: getNextAccount(),
    },
    data: getUnpauseUserInstructionDataDecoder().decode(instruction.data),
  };
}
