/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CAMPAIGN_CLOSED_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([141, 16, 13, 204, 158, 184, 112, 201]);

export function getCampaignClosedEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CAMPAIGN_CLOSED_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type CampaignClosedEventEvent = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** Amount of tokens left in the vault. */
  remainingAmount: bigint;
  /** Receiver of the remaining tokens. */
  receiver: Address;
};

export type CampaignClosedEventEventArgs = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** Amount of tokens left in the vault. */
  remainingAmount: number | bigint;
  /** Receiver of the remaining tokens. */
  receiver: Address;
};

/** Gets the encoder for {@link CampaignClosedEventEventArgs} event data. */
export function getCampaignClosedEventEventEncoder(): Encoder<CampaignClosedEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["campaignId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["base", getAddressEncoder()],
      ["remainingAmount", getU64Encoder()],
      ["receiver", getAddressEncoder()],
    ]),
    [getConstantEncoder(CAMPAIGN_CLOSED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CampaignClosedEventEvent} event data. */
export function getCampaignClosedEventEventDecoder(): Decoder<CampaignClosedEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["campaignId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["base", getAddressDecoder()],
      ["remainingAmount", getU64Decoder()],
      ["receiver", getAddressDecoder()],
    ]),
    [getConstantDecoder(CAMPAIGN_CLOSED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CampaignClosedEventEvent} event data. */
export function getCampaignClosedEventEventCodec(): Codec<
  CampaignClosedEventEventArgs,
  CampaignClosedEventEvent
> {
  return combineCodec(
    getCampaignClosedEventEventEncoder(),
    getCampaignClosedEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link CampaignClosedEventEvent} event data. */
export function parseCampaignClosedEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CampaignClosedEventEvent {
  if (containsBytes(data, CAMPAIGN_CLOSED_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getCampaignClosedEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CampaignClosedEventEvent" event discriminators.',
  );
}
