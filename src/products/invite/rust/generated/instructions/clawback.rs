//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CLAWBACK_DISCRIMINATOR: [u8; 8] = [111, 92, 142, 79, 33, 234, 82, 27];

/// Accounts.
#[derive(Debug)]
pub struct Clawback {
    pub invite_info: solana_address::Address,

    pub sender: solana_address::Address,
}

impl Clawback {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.invite_info,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.sender, true));
        accounts.extend_from_slice(remaining_accounts);
        let data = ClawbackInstructionData::new().try_to_vec().unwrap();

        solana_instruction::Instruction {
            program_id: crate::INVITE_ESCROW_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ClawbackInstructionData {
    discriminator: [u8; 8],
}

impl ClawbackInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [111, 92, 142, 79, 33, 234, 82, 27],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for ClawbackInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `Clawback`.
///
/// ### Accounts:
///
///   0. `[writable]` invite_info
///   1. `[writable, signer]` sender
#[derive(Clone, Debug, Default)]
pub struct ClawbackBuilder {
    invite_info: Option<solana_address::Address>,
    sender: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ClawbackBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn invite_info(&mut self, invite_info: solana_address::Address) -> &mut Self {
        self.invite_info = Some(invite_info);
        self
    }
    #[inline(always)]
    pub fn sender(&mut self, sender: solana_address::Address) -> &mut Self {
        self.sender = Some(sender);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = Clawback {
            invite_info: self.invite_info.expect("invite_info is not set"),
            sender: self.sender.expect("sender is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `clawback` CPI accounts.
pub struct ClawbackCpiAccounts<'a, 'b> {
    pub invite_info: &'b solana_account_info::AccountInfo<'a>,

    pub sender: &'b solana_account_info::AccountInfo<'a>,
}

/// `clawback` CPI instruction.
pub struct ClawbackCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub invite_info: &'b solana_account_info::AccountInfo<'a>,

    pub sender: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> ClawbackCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: ClawbackCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            invite_info: accounts.invite_info,
            sender: accounts.sender,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.invite_info.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.sender.key, true));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let data = ClawbackInstructionData::new().try_to_vec().unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::INVITE_ESCROW_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.invite_info.clone());
        account_infos.push(self.sender.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `Clawback` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` invite_info
///   1. `[writable, signer]` sender
#[derive(Clone, Debug)]
pub struct ClawbackCpiBuilder<'a, 'b> {
    instruction: Box<ClawbackCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ClawbackCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ClawbackCpiBuilderInstruction {
            __program: program,
            invite_info: None,
            sender: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn invite_info(
        &mut self,
        invite_info: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.invite_info = Some(invite_info);
        self
    }
    #[inline(always)]
    pub fn sender(&mut self, sender: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.sender = Some(sender);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = ClawbackCpi {
            __program: self.instruction.__program,

            invite_info: self
                .instruction
                .invite_info
                .expect("invite_info is not set"),

            sender: self.instruction.sender.expect("sender is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ClawbackCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    invite_info: Option<&'b solana_account_info::AccountInfo<'a>>,
    sender: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
