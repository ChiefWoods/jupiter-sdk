/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const VAULT_ADMIN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  88, 97, 160, 117, 102, 39, 103, 44,
]);

export function getVaultAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_ADMIN_DISCRIMINATOR);
}

export type VaultAdmin = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  liquidityProgram: Address;
  nextVaultId: number;
  auths: Array<Address>;
  bump: number;
};

export type VaultAdminArgs = {
  authority: Address;
  liquidityProgram: Address;
  nextVaultId: number;
  auths: Array<Address>;
  bump: number;
};

/** Gets the encoder for {@link VaultAdminArgs} account data. */
export function getVaultAdminEncoder(): Encoder<VaultAdminArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["liquidityProgram", getAddressEncoder()],
      ["nextVaultId", getU16Encoder()],
      ["auths", getArrayEncoder(getAddressEncoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VAULT_ADMIN_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VaultAdmin} account data. */
export function getVaultAdminDecoder(): Decoder<VaultAdmin> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["liquidityProgram", getAddressDecoder()],
    ["nextVaultId", getU16Decoder()],
    ["auths", getArrayDecoder(getAddressDecoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link VaultAdmin} account data. */
export function getVaultAdminCodec(): Codec<VaultAdminArgs, VaultAdmin> {
  return combineCodec(getVaultAdminEncoder(), getVaultAdminDecoder());
}

export function decodeVaultAdmin<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VaultAdmin, TAddress>;
export function decodeVaultAdmin<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VaultAdmin, TAddress>;
export function decodeVaultAdmin<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<VaultAdmin, TAddress> | MaybeAccount<VaultAdmin, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVaultAdminDecoder(),
  );
}

export async function fetchVaultAdmin<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VaultAdmin, TAddress>> {
  const maybeAccount = await fetchMaybeVaultAdmin(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVaultAdmin<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VaultAdmin, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVaultAdmin(maybeAccount);
}

export async function fetchAllVaultAdmin(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VaultAdmin>[]> {
  const maybeAccounts = await fetchAllMaybeVaultAdmin(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVaultAdmin(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VaultAdmin>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVaultAdmin(maybeAccount));
}
