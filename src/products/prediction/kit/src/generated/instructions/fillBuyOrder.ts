/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { PREDICTION_MARKET_PROGRAM_ADDRESS } from "../programs";

export const FILL_BUY_ORDER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  179, 201, 221, 22, 91, 16, 208, 4,
]);

export function getFillBuyOrderDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FILL_BUY_ORDER_DISCRIMINATOR,
  );
}

export type FillBuyOrderInstruction<
  TProgram extends string = typeof PREDICTION_MARKET_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSecondaryAuthority extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountOrder extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountOrderAta extends string | AccountMeta<string> = string,
  TAccountIntegratorTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSecondaryAuthority extends string
        ? WritableSignerAccount<TAccountSecondaryAuthority> &
            AccountSignerMeta<TAccountSecondaryAuthority>
        : TAccountSecondaryAuthority,
      TAccountOwner extends string
        ? WritableAccount<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountOrder extends string
        ? WritableAccount<TAccountOrder>
        : TAccountOrder,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountOrderAta extends string
        ? WritableAccount<TAccountOrderAta>
        : TAccountOrderAta,
      TAccountIntegratorTokenAccount extends string
        ? WritableAccount<TAccountIntegratorTokenAccount>
        : TAccountIntegratorTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FillBuyOrderInstructionData = {
  discriminator: ReadonlyUint8Array;
  filledContracts: bigint;
  totalCostUsd: bigint;
  venueFeeUsd: bigint;
  orderId: Option<string>;
};

export type FillBuyOrderInstructionDataArgs = {
  filledContracts: number | bigint;
  totalCostUsd: number | bigint;
  venueFeeUsd: number | bigint;
  orderId: OptionOrNullable<string>;
};

export function getFillBuyOrderInstructionDataEncoder(): Encoder<FillBuyOrderInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["filledContracts", getU64Encoder()],
      ["totalCostUsd", getU64Encoder()],
      ["venueFeeUsd", getU64Encoder()],
      [
        "orderId",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: FILL_BUY_ORDER_DISCRIMINATOR }),
  );
}

export function getFillBuyOrderInstructionDataDecoder(): Decoder<FillBuyOrderInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["filledContracts", getU64Decoder()],
    ["totalCostUsd", getU64Decoder()],
    ["venueFeeUsd", getU64Decoder()],
    [
      "orderId",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

export function getFillBuyOrderInstructionDataCodec(): Codec<
  FillBuyOrderInstructionDataArgs,
  FillBuyOrderInstructionData
> {
  return combineCodec(
    getFillBuyOrderInstructionDataEncoder(),
    getFillBuyOrderInstructionDataDecoder(),
  );
}

export type FillBuyOrderInput<
  TAccountAuthority extends string = string,
  TAccountSecondaryAuthority extends string = string,
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountPosition extends string = string,
  TAccountOrder extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountOrderAta extends string = string,
  TAccountIntegratorTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  secondaryAuthority: TransactionSigner<TAccountSecondaryAuthority>;
  owner: Address<TAccountOwner>;
  vault: Address<TAccountVault>;
  position: Address<TAccountPosition>;
  order: Address<TAccountOrder>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  orderAta: Address<TAccountOrderAta>;
  integratorTokenAccount?: Address<TAccountIntegratorTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  filledContracts: FillBuyOrderInstructionDataArgs["filledContracts"];
  totalCostUsd: FillBuyOrderInstructionDataArgs["totalCostUsd"];
  venueFeeUsd: FillBuyOrderInstructionDataArgs["venueFeeUsd"];
  orderId: FillBuyOrderInstructionDataArgs["orderId"];
};

export function getFillBuyOrderInstruction<
  TAccountAuthority extends string,
  TAccountSecondaryAuthority extends string,
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountPosition extends string,
  TAccountOrder extends string,
  TAccountVaultTokenAccount extends string,
  TAccountOrderAta extends string,
  TAccountIntegratorTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof PREDICTION_MARKET_PROGRAM_ADDRESS,
>(
  input: FillBuyOrderInput<
    TAccountAuthority,
    TAccountSecondaryAuthority,
    TAccountOwner,
    TAccountVault,
    TAccountPosition,
    TAccountOrder,
    TAccountVaultTokenAccount,
    TAccountOrderAta,
    TAccountIntegratorTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): FillBuyOrderInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountSecondaryAuthority,
  TAccountOwner,
  TAccountVault,
  TAccountPosition,
  TAccountOrder,
  TAccountVaultTokenAccount,
  TAccountOrderAta,
  TAccountIntegratorTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PREDICTION_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    secondaryAuthority: {
      value: input.secondaryAuthority ?? null,
      isWritable: true,
    },
    owner: { value: input.owner ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    order: { value: input.order ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    orderAta: { value: input.orderAta ?? null, isWritable: true },
    integratorTokenAccount: {
      value: input.integratorTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("secondaryAuthority", accounts.secondaryAuthority),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("position", accounts.position),
      getAccountMeta("order", accounts.order),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("orderAta", accounts.orderAta),
      getAccountMeta("integratorTokenAccount", accounts.integratorTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getFillBuyOrderInstructionDataEncoder().encode(
      args as FillBuyOrderInstructionDataArgs,
    ),
    programAddress,
  } as FillBuyOrderInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountSecondaryAuthority,
    TAccountOwner,
    TAccountVault,
    TAccountPosition,
    TAccountOrder,
    TAccountVaultTokenAccount,
    TAccountOrderAta,
    TAccountIntegratorTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedFillBuyOrderInstruction<
  TProgram extends string = typeof PREDICTION_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    secondaryAuthority: TAccountMetas[1];
    owner: TAccountMetas[2];
    vault: TAccountMetas[3];
    position: TAccountMetas[4];
    order: TAccountMetas[5];
    vaultTokenAccount: TAccountMetas[6];
    orderAta: TAccountMetas[7];
    integratorTokenAccount?: TAccountMetas[8] | undefined;
    tokenProgram: TAccountMetas[9];
  };
  data: FillBuyOrderInstructionData;
};

export function parseFillBuyOrderInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFillBuyOrderInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === PREDICTION_MARKET_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      secondaryAuthority: getNextAccount(),
      owner: getNextAccount(),
      vault: getNextAccount(),
      position: getNextAccount(),
      order: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      orderAta: getNextAccount(),
      integratorTokenAccount: getNextOptionalAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getFillBuyOrderInstructionDataDecoder().decode(instruction.data),
  };
}
