//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::GovernanceParameters;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const SET_GOVERNANCE_PARAMS_DISCRIMINATOR: [u8; 8] = [175, 187, 3, 73, 8, 251, 67, 178];

/// Accounts.
#[derive(Debug)]
pub struct SetGovernanceParams {
    /// The [Governor]
    pub governor: solana_address::Address,
    /// The Smart Wallet.
    pub smart_wallet: solana_address::Address,
}

impl SetGovernanceParams {
    pub fn instruction(
        &self,
        args: SetGovernanceParamsInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SetGovernanceParamsInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.governor, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.smart_wallet,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SetGovernanceParamsInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::GOVERNANCE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetGovernanceParamsInstructionData {
    discriminator: [u8; 8],
}

impl SetGovernanceParamsInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [175, 187, 3, 73, 8, 251, 67, 178],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SetGovernanceParamsInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetGovernanceParamsInstructionArgs {
    pub params: GovernanceParameters,
}

impl SetGovernanceParamsInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SetGovernanceParams`.
///
/// ### Accounts:
///
///   0. `[writable]` governor
///   1. `[signer]` smart_wallet
#[derive(Clone, Debug, Default)]
pub struct SetGovernanceParamsBuilder {
    governor: Option<solana_address::Address>,
    smart_wallet: Option<solana_address::Address>,
    params: Option<GovernanceParameters>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetGovernanceParamsBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The [Governor]
    #[inline(always)]
    pub fn governor(&mut self, governor: solana_address::Address) -> &mut Self {
        self.governor = Some(governor);
        self
    }
    /// The Smart Wallet.
    #[inline(always)]
    pub fn smart_wallet(&mut self, smart_wallet: solana_address::Address) -> &mut Self {
        self.smart_wallet = Some(smart_wallet);
        self
    }
    #[inline(always)]
    pub fn params(&mut self, params: GovernanceParameters) -> &mut Self {
        self.params = Some(params);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SetGovernanceParams {
            governor: self.governor.expect("governor is not set"),
            smart_wallet: self.smart_wallet.expect("smart_wallet is not set"),
        };
        let args = SetGovernanceParamsInstructionArgs {
            params: self.params.clone().expect("params is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `set_governance_params` CPI accounts.
pub struct SetGovernanceParamsCpiAccounts<'a, 'b> {
    /// The [Governor]
    pub governor: &'b solana_account_info::AccountInfo<'a>,
    /// The Smart Wallet.
    pub smart_wallet: &'b solana_account_info::AccountInfo<'a>,
}

/// `set_governance_params` CPI instruction.
pub struct SetGovernanceParamsCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The [Governor]
    pub governor: &'b solana_account_info::AccountInfo<'a>,
    /// The Smart Wallet.
    pub smart_wallet: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: SetGovernanceParamsInstructionArgs,
}

impl<'a, 'b> SetGovernanceParamsCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SetGovernanceParamsCpiAccounts<'a, 'b>,
        args: SetGovernanceParamsInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            governor: accounts.governor,
            smart_wallet: accounts.smart_wallet,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.governor.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.smart_wallet.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = SetGovernanceParamsInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::GOVERNANCE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.governor.clone());
        account_infos.push(self.smart_wallet.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SetGovernanceParams` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` governor
///   1. `[signer]` smart_wallet
#[derive(Clone, Debug)]
pub struct SetGovernanceParamsCpiBuilder<'a, 'b> {
    instruction: Box<SetGovernanceParamsCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetGovernanceParamsCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SetGovernanceParamsCpiBuilderInstruction {
            __program: program,
            governor: None,
            smart_wallet: None,
            params: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The [Governor]
    #[inline(always)]
    pub fn governor(&mut self, governor: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.governor = Some(governor);
        self
    }
    /// The Smart Wallet.
    #[inline(always)]
    pub fn smart_wallet(
        &mut self,
        smart_wallet: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.smart_wallet = Some(smart_wallet);
        self
    }
    #[inline(always)]
    pub fn params(&mut self, params: GovernanceParameters) -> &mut Self {
        self.instruction.params = Some(params);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SetGovernanceParamsInstructionArgs {
            params: self.instruction.params.clone().expect("params is not set"),
        };
        let instruction = SetGovernanceParamsCpi {
            __program: self.instruction.__program,

            governor: self.instruction.governor.expect("governor is not set"),

            smart_wallet: self
                .instruction
                .smart_wallet
                .expect("smart_wallet is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SetGovernanceParamsCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    governor: Option<&'b solana_account_info::AccountInfo<'a>>,
    smart_wallet: Option<&'b solana_account_info::AccountInfo<'a>>,
    params: Option<GovernanceParameters>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
