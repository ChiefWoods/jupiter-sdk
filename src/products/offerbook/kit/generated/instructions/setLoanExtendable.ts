/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findEventAuthorityPda } from "../pdas";
import { OFFERBOOK_PROGRAM_ADDRESS } from "../programs";

export const SET_LOAN_EXTENDABLE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([130, 168, 137, 148, 29, 17, 239, 228]);

export function getSetLoanExtendableDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_LOAN_EXTENDABLE_DISCRIMINATOR,
  );
}

export type SetLoanExtendableInstruction<
  TProgram extends string = typeof OFFERBOOK_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountLoan extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountLoan extends string
        ? WritableAccount<TAccountLoan>
        : TAccountLoan,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SetLoanExtendableInstructionData = {
  discriminator: ReadonlyUint8Array;
  allow: boolean;
};

export type SetLoanExtendableInstructionDataArgs = { allow: boolean };

export function getSetLoanExtendableInstructionDataEncoder(): FixedSizeEncoder<SetLoanExtendableInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["allow", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_LOAN_EXTENDABLE_DISCRIMINATOR }),
  );
}

export function getSetLoanExtendableInstructionDataDecoder(): FixedSizeDecoder<SetLoanExtendableInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["allow", getBooleanDecoder()],
  ]);
}

export function getSetLoanExtendableInstructionDataCodec(): FixedSizeCodec<
  SetLoanExtendableInstructionDataArgs,
  SetLoanExtendableInstructionData
> {
  return combineCodec(
    getSetLoanExtendableInstructionDataEncoder(),
    getSetLoanExtendableInstructionDataDecoder(),
  );
}

export type SetLoanExtendableAsyncInput<
  TAccountSigner extends string = string,
  TAccountLoan extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  loan: Address<TAccountLoan>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  allow: SetLoanExtendableInstructionDataArgs["allow"];
};

export async function getSetLoanExtendableInstructionAsync<
  TAccountSigner extends string,
  TAccountLoan extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof OFFERBOOK_PROGRAM_ADDRESS,
>(
  input: SetLoanExtendableAsyncInput<
    TAccountSigner,
    TAccountLoan,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetLoanExtendableInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLoan,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? OFFERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    loan: { value: input.loan ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await findEventAuthorityPda({
      programAddress,
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("loan", accounts.loan),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getSetLoanExtendableInstructionDataEncoder().encode(
      args as SetLoanExtendableInstructionDataArgs,
    ),
    programAddress,
  } as SetLoanExtendableInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLoan,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type SetLoanExtendableInput<
  TAccountSigner extends string = string,
  TAccountLoan extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  loan: Address<TAccountLoan>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  allow: SetLoanExtendableInstructionDataArgs["allow"];
};

export function getSetLoanExtendableInstruction<
  TAccountSigner extends string,
  TAccountLoan extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof OFFERBOOK_PROGRAM_ADDRESS,
>(
  input: SetLoanExtendableInput<
    TAccountSigner,
    TAccountLoan,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SetLoanExtendableInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountLoan,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? OFFERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    loan: { value: input.loan ?? null, isWritable: true },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("loan", accounts.loan),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getSetLoanExtendableInstructionDataEncoder().encode(
      args as SetLoanExtendableInstructionDataArgs,
    ),
    programAddress,
  } as SetLoanExtendableInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLoan,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedSetLoanExtendableInstruction<
  TProgram extends string = typeof OFFERBOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    loan: TAccountMetas[1];
    eventAuthority: TAccountMetas[2];
    program: TAccountMetas[3];
  };
  data: SetLoanExtendableInstructionData;
};

export function parseSetLoanExtendableInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetLoanExtendableInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      loan: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getSetLoanExtendableInstructionDataDecoder().decode(instruction.data),
  };
}
