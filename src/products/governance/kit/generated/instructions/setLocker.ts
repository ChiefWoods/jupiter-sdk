/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { GOVERNANCE_PROGRAM_ADDRESS } from "../programs";

export const SET_LOCKER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  17, 6, 101, 72, 250, 23, 152, 96,
]);

export function getSetLockerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SET_LOCKER_DISCRIMINATOR);
}

export type SetLockerInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountGovernor extends string | AccountMeta<string> = string,
  TAccountSmartWallet extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGovernor extends string
        ? WritableAccount<TAccountGovernor>
        : TAccountGovernor,
      TAccountSmartWallet extends string
        ? ReadonlySignerAccount<TAccountSmartWallet> &
            AccountSignerMeta<TAccountSmartWallet>
        : TAccountSmartWallet,
      ...TRemainingAccounts,
    ]
  >;

export type SetLockerInstructionData = {
  discriminator: ReadonlyUint8Array;
  newLocker: Address;
};

export type SetLockerInstructionDataArgs = { newLocker: Address };

export function getSetLockerInstructionDataEncoder(): FixedSizeEncoder<SetLockerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newLocker", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_LOCKER_DISCRIMINATOR }),
  );
}

export function getSetLockerInstructionDataDecoder(): FixedSizeDecoder<SetLockerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newLocker", getAddressDecoder()],
  ]);
}

export function getSetLockerInstructionDataCodec(): FixedSizeCodec<
  SetLockerInstructionDataArgs,
  SetLockerInstructionData
> {
  return combineCodec(
    getSetLockerInstructionDataEncoder(),
    getSetLockerInstructionDataDecoder(),
  );
}

export type SetLockerInput<
  TAccountGovernor extends string = string,
  TAccountSmartWallet extends string = string,
> = {
  /** The [Governor] */
  governor: Address<TAccountGovernor>;
  /** The Smart Wallet. */
  smartWallet: TransactionSigner<TAccountSmartWallet>;
  newLocker: SetLockerInstructionDataArgs["newLocker"];
};

export function getSetLockerInstruction<
  TAccountGovernor extends string,
  TAccountSmartWallet extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: SetLockerInput<TAccountGovernor, TAccountSmartWallet>,
  config?: { programAddress?: TProgramAddress },
): SetLockerInstruction<
  TProgramAddress,
  TAccountGovernor,
  TAccountSmartWallet
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    governor: { value: input.governor ?? null, isWritable: true },
    smartWallet: { value: input.smartWallet ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("governor", accounts.governor),
      getAccountMeta("smartWallet", accounts.smartWallet),
    ],
    data: getSetLockerInstructionDataEncoder().encode(
      args as SetLockerInstructionDataArgs,
    ),
    programAddress,
  } as SetLockerInstruction<
    TProgramAddress,
    TAccountGovernor,
    TAccountSmartWallet
  >);
}

export type ParsedSetLockerInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The [Governor] */
    governor: TAccountMetas[0];
    /** The Smart Wallet. */
    smartWallet: TAccountMetas[1];
  };
  data: SetLockerInstructionData;
};

export function parseSetLockerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetLockerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { governor: getNextAccount(), smartWallet: getNextAccount() },
    data: getSetLockerInstructionDataDecoder().decode(instruction.data),
  };
}
