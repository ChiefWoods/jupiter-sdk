/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findEventAuthorityPda } from "../pdas";
import { OFFERBOOK_PROGRAM_ADDRESS } from "../programs";

export const CREATE_TOKEN_PRINCIPAL_OFFER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([78, 9, 69, 142, 189, 64, 171, 13]);

export function getCreateTokenPrincipalOfferDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_TOKEN_PRINCIPAL_OFFER_DISCRIMINATOR,
  );
}

export type CreateTokenPrincipalOfferInstruction<
  TProgram extends string = typeof OFFERBOOK_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSignerUser extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountOffer extends string | AccountMeta<string> = string,
  TAccountPrincipalMint extends string | AccountMeta<string> = string,
  TAccountCollateralMint extends string | AccountMeta<string> = string,
  TAccountCounteredOffer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSignerUser extends string
        ? WritableAccount<TAccountSignerUser>
        : TAccountSignerUser,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountOffer extends string
        ? WritableAccount<TAccountOffer>
        : TAccountOffer,
      TAccountPrincipalMint extends string
        ? ReadonlyAccount<TAccountPrincipalMint>
        : TAccountPrincipalMint,
      TAccountCollateralMint extends string
        ? ReadonlyAccount<TAccountCollateralMint>
        : TAccountCollateralMint,
      TAccountCounteredOffer extends string
        ? ReadonlyAccount<TAccountCounteredOffer>
        : TAccountCounteredOffer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateTokenPrincipalOfferInstructionData = {
  discriminator: ReadonlyUint8Array;
  principalAmount: bigint;
  collateralAmount: bigint;
  apy: number;
  duration: number;
  expiry: number;
  allowPartialFill: boolean;
  minFillAmount: bigint;
};

export type CreateTokenPrincipalOfferInstructionDataArgs = {
  principalAmount: number | bigint;
  collateralAmount: number | bigint;
  apy: number;
  duration: number;
  expiry: number;
  allowPartialFill: boolean;
  minFillAmount: number | bigint;
};

export function getCreateTokenPrincipalOfferInstructionDataEncoder(): FixedSizeEncoder<CreateTokenPrincipalOfferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["principalAmount", getU64Encoder()],
      ["collateralAmount", getU64Encoder()],
      ["apy", getU32Encoder()],
      ["duration", getU32Encoder()],
      ["expiry", getU32Encoder()],
      ["allowPartialFill", getBooleanEncoder()],
      ["minFillAmount", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CREATE_TOKEN_PRINCIPAL_OFFER_DISCRIMINATOR,
    }),
  );
}

export function getCreateTokenPrincipalOfferInstructionDataDecoder(): FixedSizeDecoder<CreateTokenPrincipalOfferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["principalAmount", getU64Decoder()],
    ["collateralAmount", getU64Decoder()],
    ["apy", getU32Decoder()],
    ["duration", getU32Decoder()],
    ["expiry", getU32Decoder()],
    ["allowPartialFill", getBooleanDecoder()],
    ["minFillAmount", getU64Decoder()],
  ]);
}

export function getCreateTokenPrincipalOfferInstructionDataCodec(): FixedSizeCodec<
  CreateTokenPrincipalOfferInstructionDataArgs,
  CreateTokenPrincipalOfferInstructionData
> {
  return combineCodec(
    getCreateTokenPrincipalOfferInstructionDataEncoder(),
    getCreateTokenPrincipalOfferInstructionDataDecoder(),
  );
}

export type CreateTokenPrincipalOfferAsyncInput<
  TAccountSigner extends string = string,
  TAccountSignerUser extends string = string,
  TAccountConfig extends string = string,
  TAccountOffer extends string = string,
  TAccountPrincipalMint extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountCounteredOffer extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** The lender creating the offer; pays for the offer account allocation. */
  signer: TransactionSigner<TAccountSigner>;
  /**
   * Registered user profile for the signer.
   * We want to ensure that the signer is a registered user before creating an offer.
   */
  signerUser: Address<TAccountSignerUser>;
  /**
   * Global protocol configuration that defines valid offer parameter ranges
   * (duration, expiry, APY, minimum amounts) and pause state.
   */
  config: Address<TAccountConfig>;
  /** Newly initialized offer account that will store all offer terms. */
  offer: Address<TAccountOffer>;
  /**
   * The token mint the lender is offering to lend. Must differ from
   * the collateral mint.
   */
  principalMint: Address<TAccountPrincipalMint>;
  /**
   * The token mint the lender expects as collateral. Must differ from
   * the principal mint.
   */
  collateralMint: Address<TAccountCollateralMint>;
  /**
   * Optional: the offer this one is countering. When present, must be
   * on the opposite side (Collateral), created by someone other than
   * the signer, still fillable (Active or PartiallyFilled), and for the
   * same asset pair (same principal mint, same collateral mint).
   * When absent, the field stays at Pubkey::default().
   */
  counteredOffer?: Address<TAccountCounteredOffer>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  principalAmount: CreateTokenPrincipalOfferInstructionDataArgs["principalAmount"];
  collateralAmount: CreateTokenPrincipalOfferInstructionDataArgs["collateralAmount"];
  apy: CreateTokenPrincipalOfferInstructionDataArgs["apy"];
  duration: CreateTokenPrincipalOfferInstructionDataArgs["duration"];
  expiry: CreateTokenPrincipalOfferInstructionDataArgs["expiry"];
  allowPartialFill: CreateTokenPrincipalOfferInstructionDataArgs["allowPartialFill"];
  minFillAmount: CreateTokenPrincipalOfferInstructionDataArgs["minFillAmount"];
};

export async function getCreateTokenPrincipalOfferInstructionAsync<
  TAccountSigner extends string,
  TAccountSignerUser extends string,
  TAccountConfig extends string,
  TAccountOffer extends string,
  TAccountPrincipalMint extends string,
  TAccountCollateralMint extends string,
  TAccountCounteredOffer extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof OFFERBOOK_PROGRAM_ADDRESS,
>(
  input: CreateTokenPrincipalOfferAsyncInput<
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCollateralMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateTokenPrincipalOfferInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCollateralMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? OFFERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    signerUser: { value: input.signerUser ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    offer: { value: input.offer ?? null, isWritable: true },
    principalMint: { value: input.principalMint ?? null, isWritable: false },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    counteredOffer: { value: input.counteredOffer ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await findEventAuthorityPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("signerUser", accounts.signerUser),
      getAccountMeta("config", accounts.config),
      getAccountMeta("offer", accounts.offer),
      getAccountMeta("principalMint", accounts.principalMint),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("counteredOffer", accounts.counteredOffer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getCreateTokenPrincipalOfferInstructionDataEncoder().encode(
      args as CreateTokenPrincipalOfferInstructionDataArgs,
    ),
    programAddress,
  } as CreateTokenPrincipalOfferInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCollateralMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type CreateTokenPrincipalOfferInput<
  TAccountSigner extends string = string,
  TAccountSignerUser extends string = string,
  TAccountConfig extends string = string,
  TAccountOffer extends string = string,
  TAccountPrincipalMint extends string = string,
  TAccountCollateralMint extends string = string,
  TAccountCounteredOffer extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** The lender creating the offer; pays for the offer account allocation. */
  signer: TransactionSigner<TAccountSigner>;
  /**
   * Registered user profile for the signer.
   * We want to ensure that the signer is a registered user before creating an offer.
   */
  signerUser: Address<TAccountSignerUser>;
  /**
   * Global protocol configuration that defines valid offer parameter ranges
   * (duration, expiry, APY, minimum amounts) and pause state.
   */
  config: Address<TAccountConfig>;
  /** Newly initialized offer account that will store all offer terms. */
  offer: Address<TAccountOffer>;
  /**
   * The token mint the lender is offering to lend. Must differ from
   * the collateral mint.
   */
  principalMint: Address<TAccountPrincipalMint>;
  /**
   * The token mint the lender expects as collateral. Must differ from
   * the principal mint.
   */
  collateralMint: Address<TAccountCollateralMint>;
  /**
   * Optional: the offer this one is countering. When present, must be
   * on the opposite side (Collateral), created by someone other than
   * the signer, still fillable (Active or PartiallyFilled), and for the
   * same asset pair (same principal mint, same collateral mint).
   * When absent, the field stays at Pubkey::default().
   */
  counteredOffer?: Address<TAccountCounteredOffer>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  principalAmount: CreateTokenPrincipalOfferInstructionDataArgs["principalAmount"];
  collateralAmount: CreateTokenPrincipalOfferInstructionDataArgs["collateralAmount"];
  apy: CreateTokenPrincipalOfferInstructionDataArgs["apy"];
  duration: CreateTokenPrincipalOfferInstructionDataArgs["duration"];
  expiry: CreateTokenPrincipalOfferInstructionDataArgs["expiry"];
  allowPartialFill: CreateTokenPrincipalOfferInstructionDataArgs["allowPartialFill"];
  minFillAmount: CreateTokenPrincipalOfferInstructionDataArgs["minFillAmount"];
};

export function getCreateTokenPrincipalOfferInstruction<
  TAccountSigner extends string,
  TAccountSignerUser extends string,
  TAccountConfig extends string,
  TAccountOffer extends string,
  TAccountPrincipalMint extends string,
  TAccountCollateralMint extends string,
  TAccountCounteredOffer extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof OFFERBOOK_PROGRAM_ADDRESS,
>(
  input: CreateTokenPrincipalOfferInput<
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCollateralMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateTokenPrincipalOfferInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountSignerUser,
  TAccountConfig,
  TAccountOffer,
  TAccountPrincipalMint,
  TAccountCollateralMint,
  TAccountCounteredOffer,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? OFFERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    signerUser: { value: input.signerUser ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    offer: { value: input.offer ?? null, isWritable: true },
    principalMint: { value: input.principalMint ?? null, isWritable: false },
    collateralMint: { value: input.collateralMint ?? null, isWritable: false },
    counteredOffer: { value: input.counteredOffer ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("signerUser", accounts.signerUser),
      getAccountMeta("config", accounts.config),
      getAccountMeta("offer", accounts.offer),
      getAccountMeta("principalMint", accounts.principalMint),
      getAccountMeta("collateralMint", accounts.collateralMint),
      getAccountMeta("counteredOffer", accounts.counteredOffer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getCreateTokenPrincipalOfferInstructionDataEncoder().encode(
      args as CreateTokenPrincipalOfferInstructionDataArgs,
    ),
    programAddress,
  } as CreateTokenPrincipalOfferInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCollateralMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedCreateTokenPrincipalOfferInstruction<
  TProgram extends string = typeof OFFERBOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The lender creating the offer; pays for the offer account allocation. */
    signer: TAccountMetas[0];
    /**
     * Registered user profile for the signer.
     * We want to ensure that the signer is a registered user before creating an offer.
     */
    signerUser: TAccountMetas[1];
    /**
     * Global protocol configuration that defines valid offer parameter ranges
     * (duration, expiry, APY, minimum amounts) and pause state.
     */
    config: TAccountMetas[2];
    /** Newly initialized offer account that will store all offer terms. */
    offer: TAccountMetas[3];
    /**
     * The token mint the lender is offering to lend. Must differ from
     * the collateral mint.
     */
    principalMint: TAccountMetas[4];
    /**
     * The token mint the lender expects as collateral. Must differ from
     * the principal mint.
     */
    collateralMint: TAccountMetas[5];
    /**
     * Optional: the offer this one is countering. When present, must be
     * on the opposite side (Collateral), created by someone other than
     * the signer, still fillable (Active or PartiallyFilled), and for the
     * same asset pair (same principal mint, same collateral mint).
     * When absent, the field stays at Pubkey::default().
     */
    counteredOffer?: TAccountMetas[6] | undefined;
    systemProgram: TAccountMetas[7];
    eventAuthority: TAccountMetas[8];
    program: TAccountMetas[9];
  };
  data: CreateTokenPrincipalOfferInstructionData;
};

export function parseCreateTokenPrincipalOfferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateTokenPrincipalOfferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === OFFERBOOK_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      signerUser: getNextAccount(),
      config: getNextAccount(),
      offer: getNextAccount(),
      principalMint: getNextAccount(),
      collateralMint: getNextAccount(),
      counteredOffer: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getCreateTokenPrincipalOfferInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
