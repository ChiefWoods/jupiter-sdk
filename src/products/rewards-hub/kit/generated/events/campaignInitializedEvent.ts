/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CAMPAIGN_INITIALIZED_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([62, 247, 61, 179, 223, 177, 191, 212]);

export function getCampaignInitializedEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CAMPAIGN_INITIALIZED_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type CampaignInitializedEventEvent = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** Mint of the token to be distributed. */
  mint: Address;
  /** Allocated amounts per mission. */
  allocatedAmounts: Array<bigint>;
  /** Start timestamp. */
  startTs: bigint;
  /** End timestamp. */
  endTs: bigint;
  /** Clawback receiver. */
  clawbackReceiver: Address;
};

export type CampaignInitializedEventEventArgs = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** Mint of the token to be distributed. */
  mint: Address;
  /** Allocated amounts per mission. */
  allocatedAmounts: Array<number | bigint>;
  /** Start timestamp. */
  startTs: number | bigint;
  /** End timestamp. */
  endTs: number | bigint;
  /** Clawback receiver. */
  clawbackReceiver: Address;
};

/** Gets the encoder for {@link CampaignInitializedEventEventArgs} event data. */
export function getCampaignInitializedEventEventEncoder(): Encoder<CampaignInitializedEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["campaignId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["base", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["allocatedAmounts", getArrayEncoder(getU64Encoder(), { size: 5 })],
      ["startTs", getI64Encoder()],
      ["endTs", getI64Encoder()],
      ["clawbackReceiver", getAddressEncoder()],
    ]),
    [getConstantEncoder(CAMPAIGN_INITIALIZED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CampaignInitializedEventEvent} event data. */
export function getCampaignInitializedEventEventDecoder(): Decoder<CampaignInitializedEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["campaignId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["base", getAddressDecoder()],
      ["mint", getAddressDecoder()],
      ["allocatedAmounts", getArrayDecoder(getU64Decoder(), { size: 5 })],
      ["startTs", getI64Decoder()],
      ["endTs", getI64Decoder()],
      ["clawbackReceiver", getAddressDecoder()],
    ]),
    [getConstantDecoder(CAMPAIGN_INITIALIZED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CampaignInitializedEventEvent} event data. */
export function getCampaignInitializedEventEventCodec(): Codec<
  CampaignInitializedEventEventArgs,
  CampaignInitializedEventEvent
> {
  return combineCodec(
    getCampaignInitializedEventEventEncoder(),
    getCampaignInitializedEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link CampaignInitializedEventEvent} event data. */
export function parseCampaignInitializedEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CampaignInitializedEventEvent {
  if (containsBytes(data, CAMPAIGN_INITIALIZED_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getCampaignInitializedEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CampaignInitializedEventEvent" event discriminators.',
  );
}
