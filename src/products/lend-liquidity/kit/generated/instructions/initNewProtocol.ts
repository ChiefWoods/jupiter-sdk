/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findUserBorrowPositionPda, findUserSupplyPositionPda } from "../pdas";
import { LEND_LIQUIDITY_PROGRAM_ADDRESS } from "../programs";

export const INIT_NEW_PROTOCOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([193, 147, 5, 32, 138, 135, 213, 158]);

export function getInitNewProtocolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_NEW_PROTOCOL_DISCRIMINATOR,
  );
}

export type InitNewProtocolInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAuthList extends string | AccountMeta<string> = string,
  TAccountUserSupplyPosition extends string | AccountMeta<string> = string,
  TAccountUserBorrowPosition extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAuthList extends string
        ? ReadonlyAccount<TAccountAuthList>
        : TAccountAuthList,
      TAccountUserSupplyPosition extends string
        ? WritableAccount<TAccountUserSupplyPosition>
        : TAccountUserSupplyPosition,
      TAccountUserBorrowPosition extends string
        ? WritableAccount<TAccountUserBorrowPosition>
        : TAccountUserBorrowPosition,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitNewProtocolInstructionData = {
  discriminator: ReadonlyUint8Array;
  supplyMint: Address;
  borrowMint: Address;
  protocol: Address;
};

export type InitNewProtocolInstructionDataArgs = {
  supplyMint: Address;
  borrowMint: Address;
  protocol: Address;
};

export function getInitNewProtocolInstructionDataEncoder(): FixedSizeEncoder<InitNewProtocolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["supplyMint", getAddressEncoder()],
      ["borrowMint", getAddressEncoder()],
      ["protocol", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_NEW_PROTOCOL_DISCRIMINATOR }),
  );
}

export function getInitNewProtocolInstructionDataDecoder(): FixedSizeDecoder<InitNewProtocolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["supplyMint", getAddressDecoder()],
    ["borrowMint", getAddressDecoder()],
    ["protocol", getAddressDecoder()],
  ]);
}

export function getInitNewProtocolInstructionDataCodec(): FixedSizeCodec<
  InitNewProtocolInstructionDataArgs,
  InitNewProtocolInstructionData
> {
  return combineCodec(
    getInitNewProtocolInstructionDataEncoder(),
    getInitNewProtocolInstructionDataDecoder(),
  );
}

export type InitNewProtocolAsyncInput<
  TAccountAuthority extends string = string,
  TAccountAuthList extends string = string,
  TAccountUserSupplyPosition extends string = string,
  TAccountUserBorrowPosition extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  authList: Address<TAccountAuthList>;
  userSupplyPosition?: Address<TAccountUserSupplyPosition>;
  userBorrowPosition?: Address<TAccountUserBorrowPosition>;
  systemProgram?: Address<TAccountSystemProgram>;
  supplyMint: InitNewProtocolInstructionDataArgs["supplyMint"];
  borrowMint: InitNewProtocolInstructionDataArgs["borrowMint"];
  protocol: InitNewProtocolInstructionDataArgs["protocol"];
};

export async function getInitNewProtocolInstructionAsync<
  TAccountAuthority extends string,
  TAccountAuthList extends string,
  TAccountUserSupplyPosition extends string,
  TAccountUserBorrowPosition extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: InitNewProtocolAsyncInput<
    TAccountAuthority,
    TAccountAuthList,
    TAccountUserSupplyPosition,
    TAccountUserBorrowPosition,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitNewProtocolInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAuthList,
    TAccountUserSupplyPosition,
    TAccountUserBorrowPosition,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    authList: { value: input.authList ?? null, isWritable: false },
    userSupplyPosition: {
      value: input.userSupplyPosition ?? null,
      isWritable: true,
    },
    userBorrowPosition: {
      value: input.userBorrowPosition ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userSupplyPosition.value) {
    accounts.userSupplyPosition.value = await findUserSupplyPositionPda({
      supplyMint: getNonNullResolvedInstructionInput(
        "supplyMint",
        args.supplyMint,
      ),
      protocol: getNonNullResolvedInstructionInput("protocol", args.protocol),
    });
  }
  if (!accounts.userBorrowPosition.value) {
    accounts.userBorrowPosition.value = await findUserBorrowPositionPda({
      borrowMint: getNonNullResolvedInstructionInput(
        "borrowMint",
        args.borrowMint,
      ),
      protocol: getNonNullResolvedInstructionInput("protocol", args.protocol),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("authList", accounts.authList),
      getAccountMeta("userSupplyPosition", accounts.userSupplyPosition),
      getAccountMeta("userBorrowPosition", accounts.userBorrowPosition),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitNewProtocolInstructionDataEncoder().encode(
      args as InitNewProtocolInstructionDataArgs,
    ),
    programAddress,
  } as InitNewProtocolInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAuthList,
    TAccountUserSupplyPosition,
    TAccountUserBorrowPosition,
    TAccountSystemProgram
  >);
}

export type InitNewProtocolInput<
  TAccountAuthority extends string = string,
  TAccountAuthList extends string = string,
  TAccountUserSupplyPosition extends string = string,
  TAccountUserBorrowPosition extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  authList: Address<TAccountAuthList>;
  userSupplyPosition: Address<TAccountUserSupplyPosition>;
  userBorrowPosition: Address<TAccountUserBorrowPosition>;
  systemProgram?: Address<TAccountSystemProgram>;
  supplyMint: InitNewProtocolInstructionDataArgs["supplyMint"];
  borrowMint: InitNewProtocolInstructionDataArgs["borrowMint"];
  protocol: InitNewProtocolInstructionDataArgs["protocol"];
};

export function getInitNewProtocolInstruction<
  TAccountAuthority extends string,
  TAccountAuthList extends string,
  TAccountUserSupplyPosition extends string,
  TAccountUserBorrowPosition extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: InitNewProtocolInput<
    TAccountAuthority,
    TAccountAuthList,
    TAccountUserSupplyPosition,
    TAccountUserBorrowPosition,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitNewProtocolInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAuthList,
  TAccountUserSupplyPosition,
  TAccountUserBorrowPosition,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    authList: { value: input.authList ?? null, isWritable: false },
    userSupplyPosition: {
      value: input.userSupplyPosition ?? null,
      isWritable: true,
    },
    userBorrowPosition: {
      value: input.userBorrowPosition ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("authList", accounts.authList),
      getAccountMeta("userSupplyPosition", accounts.userSupplyPosition),
      getAccountMeta("userBorrowPosition", accounts.userBorrowPosition),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitNewProtocolInstructionDataEncoder().encode(
      args as InitNewProtocolInstructionDataArgs,
    ),
    programAddress,
  } as InitNewProtocolInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAuthList,
    TAccountUserSupplyPosition,
    TAccountUserBorrowPosition,
    TAccountSystemProgram
  >);
}

export type ParsedInitNewProtocolInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    authList: TAccountMetas[1];
    userSupplyPosition: TAccountMetas[2];
    userBorrowPosition: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitNewProtocolInstructionData;
};

export function parseInitNewProtocolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitNewProtocolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      authList: getNextAccount(),
      userSupplyPosition: getNextAccount(),
      userBorrowPosition: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitNewProtocolInstructionDataDecoder().decode(instruction.data),
  };
}
