/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { PREDICTION_MARKET_PROGRAM_ADDRESS } from "../programs";

export const CLOSE_LOST_POSITION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([235, 157, 191, 130, 227, 214, 104, 178]);

export function getCloseLostPositionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_LOST_POSITION_DISCRIMINATOR,
  );
}

export type CloseLostPositionInstruction<
  TProgram extends string = typeof PREDICTION_MARKET_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TAccountMarketResult extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      TAccountMarketResult extends string
        ? ReadonlyAccount<TAccountMarketResult>
        : TAccountMarketResult,
      ...TRemainingAccounts,
    ]
  >;

export type CloseLostPositionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseLostPositionInstructionDataArgs = {};

export function getCloseLostPositionInstructionDataEncoder(): FixedSizeEncoder<CloseLostPositionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_LOST_POSITION_DISCRIMINATOR }),
  );
}

export function getCloseLostPositionInstructionDataDecoder(): FixedSizeDecoder<CloseLostPositionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseLostPositionInstructionDataCodec(): FixedSizeCodec<
  CloseLostPositionInstructionDataArgs,
  CloseLostPositionInstructionData
> {
  return combineCodec(
    getCloseLostPositionInstructionDataEncoder(),
    getCloseLostPositionInstructionDataDecoder(),
  );
}

export type CloseLostPositionInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
  TAccountPosition extends string = string,
  TAccountMarketResult extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vault: Address<TAccountVault>;
  position: Address<TAccountPosition>;
  marketResult: Address<TAccountMarketResult>;
};

export function getCloseLostPositionInstruction<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TAccountPosition extends string,
  TAccountMarketResult extends string,
  TProgramAddress extends Address = typeof PREDICTION_MARKET_PROGRAM_ADDRESS,
>(
  input: CloseLostPositionInput<
    TAccountAuthority,
    TAccountVault,
    TAccountPosition,
    TAccountMarketResult
  >,
  config?: { programAddress?: TProgramAddress },
): CloseLostPositionInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountVault,
  TAccountPosition,
  TAccountMarketResult
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PREDICTION_MARKET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    position: { value: input.position ?? null, isWritable: true },
    marketResult: { value: input.marketResult ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("position", accounts.position),
      getAccountMeta("marketResult", accounts.marketResult),
    ],
    data: getCloseLostPositionInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseLostPositionInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault,
    TAccountPosition,
    TAccountMarketResult
  >);
}

export type ParsedCloseLostPositionInstruction<
  TProgram extends string = typeof PREDICTION_MARKET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vault: TAccountMetas[1];
    position: TAccountMetas[2];
    marketResult: TAccountMetas[3];
  };
  data: CloseLostPositionInstructionData;
};

export function parseCloseLostPositionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseLostPositionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      vault: getNextAccount(),
      position: getNextAccount(),
      marketResult: getNextAccount(),
    },
    data: getCloseLostPositionInstructionDataDecoder().decode(instruction.data),
  };
}
