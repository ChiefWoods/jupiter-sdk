/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI128Decoder,
  getI128Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_ARBITRAGE_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([105, 165, 52, 9, 218, 211, 46, 13]);

export function getLogArbitrageEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_ARBITRAGE_EVENT_DISCRIMINATOR,
  );
}

export type LogArbitrageEvent = {
  dexId: number;
  routing: bigint;
  amtOut: bigint;
};

export type LogArbitrageEventArgs = {
  dexId: number;
  routing: number | bigint;
  amtOut: number | bigint;
};

/** Gets the encoder for {@link LogArbitrageEventArgs} event data. */
export function getLogArbitrageEventEncoder(): FixedSizeEncoder<LogArbitrageEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["dexId", getU16Encoder()],
      ["routing", getI128Encoder()],
      ["amtOut", getU64Encoder()],
    ]),
    [getConstantEncoder(LOG_ARBITRAGE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogArbitrageEvent} event data. */
export function getLogArbitrageEventDecoder(): FixedSizeDecoder<LogArbitrageEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["dexId", getU16Decoder()],
      ["routing", getI128Decoder()],
      ["amtOut", getU64Decoder()],
    ]),
    [getConstantDecoder(LOG_ARBITRAGE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogArbitrageEvent} event data. */
export function getLogArbitrageEventCodec(): FixedSizeCodec<
  LogArbitrageEventArgs,
  LogArbitrageEvent
> {
  return combineCodec(
    getLogArbitrageEventEncoder(),
    getLogArbitrageEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LogArbitrageEvent} event data. */
export function parseLogArbitrageEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogArbitrageEvent {
  if (containsBytes(data, LOG_ARBITRAGE_EVENT_DISCRIMINATOR, 0)) {
    return getLogArbitrageEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogArbitrageEvent" event discriminators.',
  );
}
