/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const INITIALIZE_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([18, 233, 50, 5, 97, 190, 167, 209]);

export function getInitializeEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_EVENT_DISCRIMINATOR,
  );
}

export type InitializeEvent = {
  sender: Address;
  inviteSigner: Address;
  amount: bigint;
  expiration: bigint;
};

export type InitializeEventArgs = {
  sender: Address;
  inviteSigner: Address;
  amount: number | bigint;
  expiration: number | bigint;
};

/** Gets the encoder for {@link InitializeEventArgs} event data. */
export function getInitializeEventEncoder(): FixedSizeEncoder<InitializeEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["sender", getAddressEncoder()],
      ["inviteSigner", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["expiration", getI64Encoder()],
    ]),
    [getConstantEncoder(INITIALIZE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link InitializeEvent} event data. */
export function getInitializeEventDecoder(): FixedSizeDecoder<InitializeEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["sender", getAddressDecoder()],
      ["inviteSigner", getAddressDecoder()],
      ["amount", getU64Decoder()],
      ["expiration", getI64Decoder()],
    ]),
    [getConstantDecoder(INITIALIZE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link InitializeEvent} event data. */
export function getInitializeEventCodec(): FixedSizeCodec<
  InitializeEventArgs,
  InitializeEvent
> {
  return combineCodec(getInitializeEventEncoder(), getInitializeEventDecoder());
}

/** Parses the provided bytes into {@link InitializeEvent} event data. */
export function parseInitializeEvent(
  data: ReadonlyUint8Array | Uint8Array,
): InitializeEvent {
  if (containsBytes(data, INITIALIZE_EVENT_DISCRIMINATOR, 0)) {
    return getInitializeEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "InitializeEvent" event discriminators.',
  );
}
