//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

pub const UNPAUSE_USER_DISCRIMINATOR: [u8; 8] = [71, 115, 128, 252, 182, 126, 234, 62];

/// Accounts.
#[derive(Debug)]
pub struct UnpauseUser {
    pub authority: solana_address::Address,

    pub auth_list: solana_address::Address,

    pub user_supply_position: solana_address::Address,

    pub user_borrow_position: solana_address::Address,
}

impl UnpauseUser {
    pub fn instruction(&self, args: UnpauseUserInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: UnpauseUserInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.authority,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.auth_list,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.user_supply_position,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.user_borrow_position,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = UnpauseUserInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::LEND_LIQUIDITY_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UnpauseUserInstructionData {
    discriminator: [u8; 8],
}

impl UnpauseUserInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [71, 115, 128, 252, 182, 126, 234, 62],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for UnpauseUserInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UnpauseUserInstructionArgs {
    pub protocol: Address,
    pub supply_mint: Address,
    pub borrow_mint: Address,
    pub supply_status: Option<u8>,
    pub borrow_status: Option<u8>,
}

impl UnpauseUserInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `UnpauseUser`.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[]` auth_list
///   2. `[writable]` user_supply_position
///   3. `[writable]` user_borrow_position
#[derive(Clone, Debug, Default)]
pub struct UnpauseUserBuilder {
    authority: Option<solana_address::Address>,
    auth_list: Option<solana_address::Address>,
    user_supply_position: Option<solana_address::Address>,
    user_borrow_position: Option<solana_address::Address>,
    protocol: Option<Address>,
    supply_mint: Option<Address>,
    borrow_mint: Option<Address>,
    supply_status: Option<u8>,
    borrow_status: Option<u8>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UnpauseUserBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_address::Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn auth_list(&mut self, auth_list: solana_address::Address) -> &mut Self {
        self.auth_list = Some(auth_list);
        self
    }
    #[inline(always)]
    pub fn user_supply_position(
        &mut self,
        user_supply_position: solana_address::Address,
    ) -> &mut Self {
        self.user_supply_position = Some(user_supply_position);
        self
    }
    #[inline(always)]
    pub fn user_borrow_position(
        &mut self,
        user_borrow_position: solana_address::Address,
    ) -> &mut Self {
        self.user_borrow_position = Some(user_borrow_position);
        self
    }
    #[inline(always)]
    pub fn protocol(&mut self, protocol: Address) -> &mut Self {
        self.protocol = Some(protocol);
        self
    }
    #[inline(always)]
    pub fn supply_mint(&mut self, supply_mint: Address) -> &mut Self {
        self.supply_mint = Some(supply_mint);
        self
    }
    #[inline(always)]
    pub fn borrow_mint(&mut self, borrow_mint: Address) -> &mut Self {
        self.borrow_mint = Some(borrow_mint);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn supply_status(&mut self, supply_status: u8) -> &mut Self {
        self.supply_status = Some(supply_status);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn borrow_status(&mut self, borrow_status: u8) -> &mut Self {
        self.borrow_status = Some(borrow_status);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = UnpauseUser {
            authority: self.authority.expect("authority is not set"),
            auth_list: self.auth_list.expect("auth_list is not set"),
            user_supply_position: self
                .user_supply_position
                .expect("user_supply_position is not set"),
            user_borrow_position: self
                .user_borrow_position
                .expect("user_borrow_position is not set"),
        };
        let args = UnpauseUserInstructionArgs {
            protocol: self.protocol.clone().expect("protocol is not set"),
            supply_mint: self.supply_mint.clone().expect("supply_mint is not set"),
            borrow_mint: self.borrow_mint.clone().expect("borrow_mint is not set"),
            supply_status: self.supply_status.clone(),
            borrow_status: self.borrow_status.clone(),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `unpause_user` CPI accounts.
pub struct UnpauseUserCpiAccounts<'a, 'b> {
    pub authority: &'b solana_account_info::AccountInfo<'a>,

    pub auth_list: &'b solana_account_info::AccountInfo<'a>,

    pub user_supply_position: &'b solana_account_info::AccountInfo<'a>,

    pub user_borrow_position: &'b solana_account_info::AccountInfo<'a>,
}

/// `unpause_user` CPI instruction.
pub struct UnpauseUserCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub authority: &'b solana_account_info::AccountInfo<'a>,

    pub auth_list: &'b solana_account_info::AccountInfo<'a>,

    pub user_supply_position: &'b solana_account_info::AccountInfo<'a>,

    pub user_borrow_position: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: UnpauseUserInstructionArgs,
}

impl<'a, 'b> UnpauseUserCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: UnpauseUserCpiAccounts<'a, 'b>,
        args: UnpauseUserInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            auth_list: accounts.auth_list,
            user_supply_position: accounts.user_supply_position,
            user_borrow_position: accounts.user_borrow_position,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.auth_list.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.user_supply_position.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.user_borrow_position.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = UnpauseUserInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::LEND_LIQUIDITY_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.auth_list.clone());
        account_infos.push(self.user_supply_position.clone());
        account_infos.push(self.user_borrow_position.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `UnpauseUser` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[]` auth_list
///   2. `[writable]` user_supply_position
///   3. `[writable]` user_borrow_position
#[derive(Clone, Debug)]
pub struct UnpauseUserCpiBuilder<'a, 'b> {
    instruction: Box<UnpauseUserCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UnpauseUserCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(UnpauseUserCpiBuilderInstruction {
            __program: program,
            authority: None,
            auth_list: None,
            user_supply_position: None,
            user_borrow_position: None,
            protocol: None,
            supply_mint: None,
            borrow_mint: None,
            supply_status: None,
            borrow_status: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn auth_list(&mut self, auth_list: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.auth_list = Some(auth_list);
        self
    }
    #[inline(always)]
    pub fn user_supply_position(
        &mut self,
        user_supply_position: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.user_supply_position = Some(user_supply_position);
        self
    }
    #[inline(always)]
    pub fn user_borrow_position(
        &mut self,
        user_borrow_position: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.user_borrow_position = Some(user_borrow_position);
        self
    }
    #[inline(always)]
    pub fn protocol(&mut self, protocol: Address) -> &mut Self {
        self.instruction.protocol = Some(protocol);
        self
    }
    #[inline(always)]
    pub fn supply_mint(&mut self, supply_mint: Address) -> &mut Self {
        self.instruction.supply_mint = Some(supply_mint);
        self
    }
    #[inline(always)]
    pub fn borrow_mint(&mut self, borrow_mint: Address) -> &mut Self {
        self.instruction.borrow_mint = Some(borrow_mint);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn supply_status(&mut self, supply_status: u8) -> &mut Self {
        self.instruction.supply_status = Some(supply_status);
        self
    }
    /// `[optional argument]`
    #[inline(always)]
    pub fn borrow_status(&mut self, borrow_status: u8) -> &mut Self {
        self.instruction.borrow_status = Some(borrow_status);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = UnpauseUserInstructionArgs {
            protocol: self
                .instruction
                .protocol
                .clone()
                .expect("protocol is not set"),
            supply_mint: self
                .instruction
                .supply_mint
                .clone()
                .expect("supply_mint is not set"),
            borrow_mint: self
                .instruction
                .borrow_mint
                .clone()
                .expect("borrow_mint is not set"),
            supply_status: self.instruction.supply_status.clone(),
            borrow_status: self.instruction.borrow_status.clone(),
        };
        let instruction = UnpauseUserCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            auth_list: self.instruction.auth_list.expect("auth_list is not set"),

            user_supply_position: self
                .instruction
                .user_supply_position
                .expect("user_supply_position is not set"),

            user_borrow_position: self
                .instruction
                .user_borrow_position
                .expect("user_borrow_position is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct UnpauseUserCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    auth_list: Option<&'b solana_account_info::AccountInfo<'a>>,
    user_supply_position: Option<&'b solana_account_info::AccountInfo<'a>>,
    user_borrow_position: Option<&'b solana_account_info::AccountInfo<'a>>,
    protocol: Option<Address>,
    supply_mint: Option<Address>,
    borrow_mint: Option<Address>,
    supply_status: Option<u8>,
    borrow_status: Option<u8>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
