/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CLOSE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([24, 49, 90, 170, 80, 200, 243, 186]);

export function getCloseClaimStatusEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type CloseClaimStatusEventEvent = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** Claim status public key. */
  claimsPubkey: Address;
};

export type CloseClaimStatusEventEventArgs = CloseClaimStatusEventEvent;

/** Gets the encoder for {@link CloseClaimStatusEventEventArgs} event data. */
export function getCloseClaimStatusEventEventEncoder(): Encoder<CloseClaimStatusEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["campaignId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["base", getAddressEncoder()],
      ["claimsPubkey", getAddressEncoder()],
    ]),
    [getConstantEncoder(CLOSE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CloseClaimStatusEventEvent} event data. */
export function getCloseClaimStatusEventEventDecoder(): Decoder<CloseClaimStatusEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["campaignId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["base", getAddressDecoder()],
      ["claimsPubkey", getAddressDecoder()],
    ]),
    [getConstantDecoder(CLOSE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CloseClaimStatusEventEvent} event data. */
export function getCloseClaimStatusEventEventCodec(): Codec<
  CloseClaimStatusEventEventArgs,
  CloseClaimStatusEventEvent
> {
  return combineCodec(
    getCloseClaimStatusEventEventEncoder(),
    getCloseClaimStatusEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link CloseClaimStatusEventEvent} event data. */
export function parseCloseClaimStatusEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CloseClaimStatusEventEvent {
  if (containsBytes(data, CLOSE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getCloseClaimStatusEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CloseClaimStatusEventEvent" event discriminators.',
  );
}
