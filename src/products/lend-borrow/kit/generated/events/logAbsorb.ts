/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_ABSORB_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([177, 119, 143, 137, 184, 63, 197, 215]);

export function getLogAbsorbEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_ABSORB_EVENT_DISCRIMINATOR,
  );
}

export type LogAbsorbEvent = { colAmount: bigint; debtAmount: bigint };

export type LogAbsorbEventArgs = {
  colAmount: number | bigint;
  debtAmount: number | bigint;
};

/** Gets the encoder for {@link LogAbsorbEventArgs} event data. */
export function getLogAbsorbEventEncoder(): FixedSizeEncoder<LogAbsorbEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["colAmount", getU64Encoder()],
      ["debtAmount", getU64Encoder()],
    ]),
    [getConstantEncoder(LOG_ABSORB_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogAbsorbEvent} event data. */
export function getLogAbsorbEventDecoder(): FixedSizeDecoder<LogAbsorbEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["colAmount", getU64Decoder()],
      ["debtAmount", getU64Decoder()],
    ]),
    [getConstantDecoder(LOG_ABSORB_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogAbsorbEvent} event data. */
export function getLogAbsorbEventCodec(): FixedSizeCodec<
  LogAbsorbEventArgs,
  LogAbsorbEvent
> {
  return combineCodec(getLogAbsorbEventEncoder(), getLogAbsorbEventDecoder());
}

/** Parses the provided bytes into {@link LogAbsorbEvent} event data. */
export function parseLogAbsorbEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogAbsorbEvent {
  if (containsBytes(data, LOG_ABSORB_EVENT_DISCRIMINATOR, 0)) {
    return getLogAbsorbEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogAbsorbEvent" event discriminators.',
  );
}
