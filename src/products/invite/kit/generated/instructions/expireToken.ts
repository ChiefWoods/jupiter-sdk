/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { INVITE_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const EXPIRE_TOKEN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  90, 208, 189, 130, 72, 252, 204, 11,
]);

export function getExpireTokenDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXPIRE_TOKEN_DISCRIMINATOR,
  );
}

export type ExpireTokenInstruction<
  TProgram extends string = typeof INVITE_ESCROW_PROGRAM_ADDRESS,
  TAccountInviteInfo extends string | AccountMeta<string> = string,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountSenderTokenAccount extends string | AccountMeta<string> = string,
  TAccountEscrowTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInviteInfo extends string
        ? WritableAccount<TAccountInviteInfo>
        : TAccountInviteInfo,
      TAccountSender extends string
        ? WritableAccount<TAccountSender>
        : TAccountSender,
      TAccountSenderTokenAccount extends string
        ? WritableAccount<TAccountSenderTokenAccount>
        : TAccountSenderTokenAccount,
      TAccountEscrowTokenAccount extends string
        ? WritableAccount<TAccountEscrowTokenAccount>
        : TAccountEscrowTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountMint extends string
        ? WritableAccount<TAccountMint>
        : TAccountMint,
      TAccountKeeper extends string
        ? WritableSignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireTokenInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireTokenInstructionDataArgs = {};

export function getExpireTokenInstructionDataEncoder(): FixedSizeEncoder<ExpireTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXPIRE_TOKEN_DISCRIMINATOR }),
  );
}

export function getExpireTokenInstructionDataDecoder(): FixedSizeDecoder<ExpireTokenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpireTokenInstructionDataCodec(): FixedSizeCodec<
  ExpireTokenInstructionDataArgs,
  ExpireTokenInstructionData
> {
  return combineCodec(
    getExpireTokenInstructionDataEncoder(),
    getExpireTokenInstructionDataDecoder(),
  );
}

export type ExpireTokenInput<
  TAccountInviteInfo extends string = string,
  TAccountSender extends string = string,
  TAccountSenderTokenAccount extends string = string,
  TAccountEscrowTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountMint extends string = string,
  TAccountKeeper extends string = string,
> = {
  inviteInfo: Address<TAccountInviteInfo>;
  sender: Address<TAccountSender>;
  senderTokenAccount: Address<TAccountSenderTokenAccount>;
  escrowTokenAccount: Address<TAccountEscrowTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
  associatedTokenProgram: Address<TAccountAssociatedTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  mint: Address<TAccountMint>;
  keeper: TransactionSigner<TAccountKeeper>;
};

export function getExpireTokenInstruction<
  TAccountInviteInfo extends string,
  TAccountSender extends string,
  TAccountSenderTokenAccount extends string,
  TAccountEscrowTokenAccount extends string,
  TAccountTokenProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountSystemProgram extends string,
  TAccountMint extends string,
  TAccountKeeper extends string,
  TProgramAddress extends Address = typeof INVITE_ESCROW_PROGRAM_ADDRESS,
>(
  input: ExpireTokenInput<
    TAccountInviteInfo,
    TAccountSender,
    TAccountSenderTokenAccount,
    TAccountEscrowTokenAccount,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram,
    TAccountMint,
    TAccountKeeper
  >,
  config?: { programAddress?: TProgramAddress },
): ExpireTokenInstruction<
  TProgramAddress,
  TAccountInviteInfo,
  TAccountSender,
  TAccountSenderTokenAccount,
  TAccountEscrowTokenAccount,
  TAccountTokenProgram,
  TAccountAssociatedTokenProgram,
  TAccountSystemProgram,
  TAccountMint,
  TAccountKeeper
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? INVITE_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    inviteInfo: { value: input.inviteInfo ?? null, isWritable: true },
    sender: { value: input.sender ?? null, isWritable: true },
    senderTokenAccount: {
      value: input.senderTokenAccount ?? null,
      isWritable: true,
    },
    escrowTokenAccount: {
      value: input.escrowTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: true },
    keeper: { value: input.keeper ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("inviteInfo", accounts.inviteInfo),
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("senderTokenAccount", accounts.senderTokenAccount),
      getAccountMeta("escrowTokenAccount", accounts.escrowTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getExpireTokenInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireTokenInstruction<
    TProgramAddress,
    TAccountInviteInfo,
    TAccountSender,
    TAccountSenderTokenAccount,
    TAccountEscrowTokenAccount,
    TAccountTokenProgram,
    TAccountAssociatedTokenProgram,
    TAccountSystemProgram,
    TAccountMint,
    TAccountKeeper
  >);
}

export type ParsedExpireTokenInstruction<
  TProgram extends string = typeof INVITE_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    inviteInfo: TAccountMetas[0];
    sender: TAccountMetas[1];
    senderTokenAccount: TAccountMetas[2];
    escrowTokenAccount: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
    associatedTokenProgram: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
    mint: TAccountMetas[7];
    keeper: TAccountMetas[8];
  };
  data: ExpireTokenInstructionData;
};

export function parseExpireTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpireTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      inviteInfo: getNextAccount(),
      sender: getNextAccount(),
      senderTokenAccount: getNextAccount(),
      escrowTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
      mint: getNextAccount(),
      keeper: getNextAccount(),
    },
    data: getExpireTokenInstructionDataDecoder().decode(instruction.data),
  };
}
