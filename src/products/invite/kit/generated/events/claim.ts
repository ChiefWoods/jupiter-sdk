/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CLAIM_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  133, 98, 9, 238, 133, 207, 191, 113,
]);

export function getClaimEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_EVENT_DISCRIMINATOR);
}

export type ClaimEvent = {
  claimer: Address;
  sender: Address;
  inviteSigner: Address;
  amount: bigint;
};

export type ClaimEventArgs = {
  claimer: Address;
  sender: Address;
  inviteSigner: Address;
  amount: number | bigint;
};

/** Gets the encoder for {@link ClaimEventArgs} event data. */
export function getClaimEventEncoder(): FixedSizeEncoder<ClaimEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["claimer", getAddressEncoder()],
      ["sender", getAddressEncoder()],
      ["inviteSigner", getAddressEncoder()],
      ["amount", getU64Encoder()],
    ]),
    [getConstantEncoder(CLAIM_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ClaimEvent} event data. */
export function getClaimEventDecoder(): FixedSizeDecoder<ClaimEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["claimer", getAddressDecoder()],
      ["sender", getAddressDecoder()],
      ["inviteSigner", getAddressDecoder()],
      ["amount", getU64Decoder()],
    ]),
    [getConstantDecoder(CLAIM_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ClaimEvent} event data. */
export function getClaimEventCodec(): FixedSizeCodec<
  ClaimEventArgs,
  ClaimEvent
> {
  return combineCodec(getClaimEventEncoder(), getClaimEventDecoder());
}

/** Parses the provided bytes into {@link ClaimEvent} event data. */
export function parseClaimEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ClaimEvent {
  if (containsBytes(data, CLAIM_EVENT_DISCRIMINATOR, 0)) {
    return getClaimEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ClaimEvent" event discriminators.',
  );
}
