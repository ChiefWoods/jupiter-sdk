/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ORACLE_ADMIN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  239, 232, 8, 20, 254, 63, 25, 246,
]);

export function getOracleAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ORACLE_ADMIN_DISCRIMINATOR,
  );
}

export type OracleAdmin = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  auths: Array<Address>;
};

export type OracleAdminArgs = { authority: Address; auths: Array<Address> };

/** Gets the encoder for {@link OracleAdminArgs} account data. */
export function getOracleAdminEncoder(): Encoder<OracleAdminArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["auths", getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: ORACLE_ADMIN_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link OracleAdmin} account data. */
export function getOracleAdminDecoder(): Decoder<OracleAdmin> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["auths", getArrayDecoder(getAddressDecoder())],
  ]);
}

/** Gets the codec for {@link OracleAdmin} account data. */
export function getOracleAdminCodec(): Codec<OracleAdminArgs, OracleAdmin> {
  return combineCodec(getOracleAdminEncoder(), getOracleAdminDecoder());
}

export function decodeOracleAdmin<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<OracleAdmin, TAddress>;
export function decodeOracleAdmin<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<OracleAdmin, TAddress>;
export function decodeOracleAdmin<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<OracleAdmin, TAddress> | MaybeAccount<OracleAdmin, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getOracleAdminDecoder(),
  );
}

export async function fetchOracleAdmin<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<OracleAdmin, TAddress>> {
  const maybeAccount = await fetchMaybeOracleAdmin(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeOracleAdmin<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<OracleAdmin, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeOracleAdmin(maybeAccount);
}

export async function fetchAllOracleAdmin(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<OracleAdmin>[]> {
  const maybeAccounts = await fetchAllMaybeOracleAdmin(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeOracleAdmin(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<OracleAdmin>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeOracleAdmin(maybeAccount));
}
