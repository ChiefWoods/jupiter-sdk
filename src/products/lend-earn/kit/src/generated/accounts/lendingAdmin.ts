/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LENDING_ADMIN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  42, 8, 33, 220, 163, 40, 210, 5,
]);

export function getLendingAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LENDING_ADMIN_DISCRIMINATOR,
  );
}

export type LendingAdmin = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  liquidityProgram: Address;
  rebalancer: Address;
  nextLendingId: number;
  auths: Array<Address>;
  bump: number;
};

export type LendingAdminArgs = {
  authority: Address;
  liquidityProgram: Address;
  rebalancer: Address;
  nextLendingId: number;
  auths: Array<Address>;
  bump: number;
};

/** Gets the encoder for {@link LendingAdminArgs} account data. */
export function getLendingAdminEncoder(): Encoder<LendingAdminArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["liquidityProgram", getAddressEncoder()],
      ["rebalancer", getAddressEncoder()],
      ["nextLendingId", getU16Encoder()],
      ["auths", getArrayEncoder(getAddressEncoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: LENDING_ADMIN_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link LendingAdmin} account data. */
export function getLendingAdminDecoder(): Decoder<LendingAdmin> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["liquidityProgram", getAddressDecoder()],
    ["rebalancer", getAddressDecoder()],
    ["nextLendingId", getU16Decoder()],
    ["auths", getArrayDecoder(getAddressDecoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link LendingAdmin} account data. */
export function getLendingAdminCodec(): Codec<LendingAdminArgs, LendingAdmin> {
  return combineCodec(getLendingAdminEncoder(), getLendingAdminDecoder());
}

export function decodeLendingAdmin<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<LendingAdmin, TAddress>;
export function decodeLendingAdmin<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<LendingAdmin, TAddress>;
export function decodeLendingAdmin<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<LendingAdmin, TAddress> | MaybeAccount<LendingAdmin, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getLendingAdminDecoder(),
  );
}

export async function fetchLendingAdmin<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<LendingAdmin, TAddress>> {
  const maybeAccount = await fetchMaybeLendingAdmin(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeLendingAdmin<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<LendingAdmin, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeLendingAdmin(maybeAccount);
}

export async function fetchAllLendingAdmin(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<LendingAdmin>[]> {
  const maybeAccounts = await fetchAllMaybeLendingAdmin(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeLendingAdmin(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<LendingAdmin>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeLendingAdmin(maybeAccount));
}
