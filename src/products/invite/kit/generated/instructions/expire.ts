/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { INVITE_ESCROW_PROGRAM_ADDRESS } from "../programs";

export const EXPIRE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  243, 83, 205, 58, 57, 201, 247, 146,
]);

export function getExpireDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(EXPIRE_DISCRIMINATOR);
}

export type ExpireInstruction<
  TProgram extends string = typeof INVITE_ESCROW_PROGRAM_ADDRESS,
  TAccountInviteInfo extends string | AccountMeta<string> = string,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountInviteInfo extends string
        ? WritableAccount<TAccountInviteInfo>
        : TAccountInviteInfo,
      TAccountSender extends string
        ? WritableAccount<TAccountSender>
        : TAccountSender,
      TAccountKeeper extends string
        ? WritableSignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireInstructionDataArgs = {};

export function getExpireInstructionDataEncoder(): FixedSizeEncoder<ExpireInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXPIRE_DISCRIMINATOR }),
  );
}

export function getExpireInstructionDataDecoder(): FixedSizeDecoder<ExpireInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpireInstructionDataCodec(): FixedSizeCodec<
  ExpireInstructionDataArgs,
  ExpireInstructionData
> {
  return combineCodec(
    getExpireInstructionDataEncoder(),
    getExpireInstructionDataDecoder(),
  );
}

export type ExpireInput<
  TAccountInviteInfo extends string = string,
  TAccountSender extends string = string,
  TAccountKeeper extends string = string,
> = {
  inviteInfo: Address<TAccountInviteInfo>;
  sender: Address<TAccountSender>;
  keeper: TransactionSigner<TAccountKeeper>;
};

export function getExpireInstruction<
  TAccountInviteInfo extends string,
  TAccountSender extends string,
  TAccountKeeper extends string,
  TProgramAddress extends Address = typeof INVITE_ESCROW_PROGRAM_ADDRESS,
>(
  input: ExpireInput<TAccountInviteInfo, TAccountSender, TAccountKeeper>,
  config?: { programAddress?: TProgramAddress },
): ExpireInstruction<
  TProgramAddress,
  TAccountInviteInfo,
  TAccountSender,
  TAccountKeeper
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? INVITE_ESCROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    inviteInfo: { value: input.inviteInfo ?? null, isWritable: true },
    sender: { value: input.sender ?? null, isWritable: true },
    keeper: { value: input.keeper ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("inviteInfo", accounts.inviteInfo),
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getExpireInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireInstruction<
    TProgramAddress,
    TAccountInviteInfo,
    TAccountSender,
    TAccountKeeper
  >);
}

export type ParsedExpireInstruction<
  TProgram extends string = typeof INVITE_ESCROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    inviteInfo: TAccountMetas[0];
    sender: TAccountMetas[1];
    keeper: TAccountMetas[2];
  };
  data: ExpireInstructionData;
};

export function parseExpireInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpireInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      inviteInfo: getNextAccount(),
      sender: getNextAccount(),
      keeper: getNextAccount(),
    },
    data: getExpireInstructionDataDecoder().decode(instruction.data),
  };
}
