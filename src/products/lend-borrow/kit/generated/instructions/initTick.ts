/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI32Decoder,
  getI32Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_BORROW_PROGRAM_ADDRESS } from "../programs";

export const INIT_TICK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  22, 13, 62, 141, 73, 89, 178, 29,
]);

export function getInitTickDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_TICK_DISCRIMINATOR);
}

export type InitTickInstruction<
  TProgram extends string = typeof LEND_BORROW_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountVaultConfig extends string | AccountMeta<string> = string,
  TAccountTickData extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountVaultConfig extends string
        ? ReadonlyAccount<TAccountVaultConfig>
        : TAccountVaultConfig,
      TAccountTickData extends string
        ? WritableAccount<TAccountTickData>
        : TAccountTickData,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitTickInstructionData = {
  discriminator: ReadonlyUint8Array;
  vaultId: number;
  tick: number;
};

export type InitTickInstructionDataArgs = { vaultId: number; tick: number };

export function getInitTickInstructionDataEncoder(): FixedSizeEncoder<InitTickInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vaultId", getU16Encoder()],
      ["tick", getI32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_TICK_DISCRIMINATOR }),
  );
}

export function getInitTickInstructionDataDecoder(): FixedSizeDecoder<InitTickInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vaultId", getU16Decoder()],
    ["tick", getI32Decoder()],
  ]);
}

export function getInitTickInstructionDataCodec(): FixedSizeCodec<
  InitTickInstructionDataArgs,
  InitTickInstructionData
> {
  return combineCodec(
    getInitTickInstructionDataEncoder(),
    getInitTickInstructionDataDecoder(),
  );
}

export type InitTickInput<
  TAccountSigner extends string = string,
  TAccountVaultConfig extends string = string,
  TAccountTickData extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  /** @dev Verification inside instruction logic */
  vaultConfig: Address<TAccountVaultConfig>;
  tickData: Address<TAccountTickData>;
  systemProgram?: Address<TAccountSystemProgram>;
  vaultId: InitTickInstructionDataArgs["vaultId"];
  tick: InitTickInstructionDataArgs["tick"];
};

export function getInitTickInstruction<
  TAccountSigner extends string,
  TAccountVaultConfig extends string,
  TAccountTickData extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_BORROW_PROGRAM_ADDRESS,
>(
  input: InitTickInput<
    TAccountSigner,
    TAccountVaultConfig,
    TAccountTickData,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitTickInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountVaultConfig,
  TAccountTickData,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_BORROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    vaultConfig: { value: input.vaultConfig ?? null, isWritable: false },
    tickData: { value: input.tickData ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("vaultConfig", accounts.vaultConfig),
      getAccountMeta("tickData", accounts.tickData),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitTickInstructionDataEncoder().encode(
      args as InitTickInstructionDataArgs,
    ),
    programAddress,
  } as InitTickInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVaultConfig,
    TAccountTickData,
    TAccountSystemProgram
  >);
}

export type ParsedInitTickInstruction<
  TProgram extends string = typeof LEND_BORROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    /** @dev Verification inside instruction logic */
    vaultConfig: TAccountMetas[1];
    tickData: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitTickInstructionData;
};

export function parseInitTickInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitTickInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      vaultConfig: getNextAccount(),
      tickData: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitTickInstructionDataDecoder().decode(instruction.data),
  };
}
