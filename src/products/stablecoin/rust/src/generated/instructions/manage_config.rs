//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::ConfigManagementAction;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const MANAGE_CONFIG_DISCRIMINATOR: [u8; 8] = [119, 51, 144, 55, 24, 242, 232, 231];

/// Accounts.
#[derive(Debug)]
pub struct ManageConfig {
    pub operator_authority: solana_address::Address,

    pub operator: solana_address::Address,

    pub config: solana_address::Address,
}

impl ManageConfig {
    pub fn instruction(
        &self,
        args: ManageConfigInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: ManageConfigInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            self.operator_authority,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.operator,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.config, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = ManageConfigInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::JUP_STABLE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ManageConfigInstructionData {
    discriminator: [u8; 8],
}

impl ManageConfigInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [119, 51, 144, 55, 24, 242, 232, 231],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for ManageConfigInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ManageConfigInstructionArgs {
    pub action: ConfigManagementAction,
}

impl ManageConfigInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `ManageConfig`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` operator_authority
///   1. `[]` operator
///   2. `[writable]` config
#[derive(Clone, Debug, Default)]
pub struct ManageConfigBuilder {
    operator_authority: Option<solana_address::Address>,
    operator: Option<solana_address::Address>,
    config: Option<solana_address::Address>,
    action: Option<ConfigManagementAction>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ManageConfigBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn operator_authority(&mut self, operator_authority: solana_address::Address) -> &mut Self {
        self.operator_authority = Some(operator_authority);
        self
    }
    #[inline(always)]
    pub fn operator(&mut self, operator: solana_address::Address) -> &mut Self {
        self.operator = Some(operator);
        self
    }
    #[inline(always)]
    pub fn config(&mut self, config: solana_address::Address) -> &mut Self {
        self.config = Some(config);
        self
    }
    #[inline(always)]
    pub fn action(&mut self, action: ConfigManagementAction) -> &mut Self {
        self.action = Some(action);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = ManageConfig {
            operator_authority: self
                .operator_authority
                .expect("operator_authority is not set"),
            operator: self.operator.expect("operator is not set"),
            config: self.config.expect("config is not set"),
        };
        let args = ManageConfigInstructionArgs {
            action: self.action.clone().expect("action is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `manage_config` CPI accounts.
pub struct ManageConfigCpiAccounts<'a, 'b> {
    pub operator_authority: &'b solana_account_info::AccountInfo<'a>,

    pub operator: &'b solana_account_info::AccountInfo<'a>,

    pub config: &'b solana_account_info::AccountInfo<'a>,
}

/// `manage_config` CPI instruction.
pub struct ManageConfigCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub operator_authority: &'b solana_account_info::AccountInfo<'a>,

    pub operator: &'b solana_account_info::AccountInfo<'a>,

    pub config: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: ManageConfigInstructionArgs,
}

impl<'a, 'b> ManageConfigCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: ManageConfigCpiAccounts<'a, 'b>,
        args: ManageConfigInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            operator_authority: accounts.operator_authority,
            operator: accounts.operator,
            config: accounts.config,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.operator_authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.operator.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.config.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = ManageConfigInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::JUP_STABLE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.operator_authority.clone());
        account_infos.push(self.operator.clone());
        account_infos.push(self.config.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `ManageConfig` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` operator_authority
///   1. `[]` operator
///   2. `[writable]` config
#[derive(Clone, Debug)]
pub struct ManageConfigCpiBuilder<'a, 'b> {
    instruction: Box<ManageConfigCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ManageConfigCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ManageConfigCpiBuilderInstruction {
            __program: program,
            operator_authority: None,
            operator: None,
            config: None,
            action: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn operator_authority(
        &mut self,
        operator_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.operator_authority = Some(operator_authority);
        self
    }
    #[inline(always)]
    pub fn operator(&mut self, operator: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.operator = Some(operator);
        self
    }
    #[inline(always)]
    pub fn config(&mut self, config: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.config = Some(config);
        self
    }
    #[inline(always)]
    pub fn action(&mut self, action: ConfigManagementAction) -> &mut Self {
        self.instruction.action = Some(action);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = ManageConfigInstructionArgs {
            action: self.instruction.action.clone().expect("action is not set"),
        };
        let instruction = ManageConfigCpi {
            __program: self.instruction.__program,

            operator_authority: self
                .instruction
                .operator_authority
                .expect("operator_authority is not set"),

            operator: self.instruction.operator.expect("operator is not set"),

            config: self.instruction.config.expect("config is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ManageConfigCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    operator_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    operator: Option<&'b solana_account_info::AccountInfo<'a>>,
    config: Option<&'b solana_account_info::AccountInfo<'a>>,
    action: Option<ConfigManagementAction>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
