/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_LIQUIDITY_PROGRAM_ADDRESS } from "../programs";

export const PAUSE_TOKEN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  226, 150, 72, 211, 159, 51, 226, 39,
]);

export function getPauseTokenDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PAUSE_TOKEN_DISCRIMINATOR);
}

export type PauseTokenInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAuthList extends string | AccountMeta<string> = string,
  TAccountTokenReserve extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAuthList extends string
        ? ReadonlyAccount<TAccountAuthList>
        : TAccountAuthList,
      TAccountTokenReserve extends string
        ? WritableAccount<TAccountTokenReserve>
        : TAccountTokenReserve,
      ...TRemainingAccounts,
    ]
  >;

export type PauseTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  mint: Address;
};

export type PauseTokenInstructionDataArgs = { mint: Address };

export function getPauseTokenInstructionDataEncoder(): FixedSizeEncoder<PauseTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mint", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: PAUSE_TOKEN_DISCRIMINATOR }),
  );
}

export function getPauseTokenInstructionDataDecoder(): FixedSizeDecoder<PauseTokenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mint", getAddressDecoder()],
  ]);
}

export function getPauseTokenInstructionDataCodec(): FixedSizeCodec<
  PauseTokenInstructionDataArgs,
  PauseTokenInstructionData
> {
  return combineCodec(
    getPauseTokenInstructionDataEncoder(),
    getPauseTokenInstructionDataDecoder(),
  );
}

export type PauseTokenInput<
  TAccountAuthority extends string = string,
  TAccountAuthList extends string = string,
  TAccountTokenReserve extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  authList: Address<TAccountAuthList>;
  tokenReserve: Address<TAccountTokenReserve>;
  mint: PauseTokenInstructionDataArgs["mint"];
};

export function getPauseTokenInstruction<
  TAccountAuthority extends string,
  TAccountAuthList extends string,
  TAccountTokenReserve extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: PauseTokenInput<
    TAccountAuthority,
    TAccountAuthList,
    TAccountTokenReserve
  >,
  config?: { programAddress?: TProgramAddress },
): PauseTokenInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAuthList,
  TAccountTokenReserve
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    authList: { value: input.authList ?? null, isWritable: false },
    tokenReserve: { value: input.tokenReserve ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("authList", accounts.authList),
      getAccountMeta("tokenReserve", accounts.tokenReserve),
    ],
    data: getPauseTokenInstructionDataEncoder().encode(
      args as PauseTokenInstructionDataArgs,
    ),
    programAddress,
  } as PauseTokenInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAuthList,
    TAccountTokenReserve
  >);
}

export type ParsedPauseTokenInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    authList: TAccountMetas[1];
    tokenReserve: TAccountMetas[2];
  };
  data: PauseTokenInstructionData;
};

export function parsePauseTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPauseTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      authList: getNextAccount(),
      tokenReserve: getNextAccount(),
    },
    data: getPauseTokenInstructionDataDecoder().decode(instruction.data),
  };
}
