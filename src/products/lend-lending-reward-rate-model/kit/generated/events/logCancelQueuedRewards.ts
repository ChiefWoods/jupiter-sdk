/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_CANCEL_QUEUED_REWARDS_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([177, 173, 63, 139, 228, 173, 187, 204]);

export function getLogCancelQueuedRewardsEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_CANCEL_QUEUED_REWARDS_EVENT_DISCRIMINATOR,
  );
}

export type LogCancelQueuedRewardsEvent = { mint: Address };

export type LogCancelQueuedRewardsEventArgs = LogCancelQueuedRewardsEvent;

/** Gets the encoder for {@link LogCancelQueuedRewardsEventArgs} event data. */
export function getLogCancelQueuedRewardsEventEncoder(): FixedSizeEncoder<LogCancelQueuedRewardsEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([["mint", getAddressEncoder()]]),
    [getConstantEncoder(LOG_CANCEL_QUEUED_REWARDS_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogCancelQueuedRewardsEvent} event data. */
export function getLogCancelQueuedRewardsEventDecoder(): FixedSizeDecoder<LogCancelQueuedRewardsEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([["mint", getAddressDecoder()]]),
    [getConstantDecoder(LOG_CANCEL_QUEUED_REWARDS_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogCancelQueuedRewardsEvent} event data. */
export function getLogCancelQueuedRewardsEventCodec(): FixedSizeCodec<
  LogCancelQueuedRewardsEventArgs,
  LogCancelQueuedRewardsEvent
> {
  return combineCodec(
    getLogCancelQueuedRewardsEventEncoder(),
    getLogCancelQueuedRewardsEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LogCancelQueuedRewardsEvent} event data. */
export function parseLogCancelQueuedRewardsEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogCancelQueuedRewardsEvent {
  if (containsBytes(data, LOG_CANCEL_QUEUED_REWARDS_EVENT_DISCRIMINATOR, 0)) {
    return getLogCancelQueuedRewardsEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogCancelQueuedRewardsEvent" event discriminators.',
  );
}
