/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const GET_CENTER_PRICE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([60, 51, 11, 241, 151, 180, 192, 27]);

export function getGetCenterPriceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_CENTER_PRICE_DISCRIMINATOR,
  );
}

export type GetCenterPriceInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountOracle extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOracle extends string
        ? ReadonlyAccount<TAccountOracle>
        : TAccountOracle,
      ...TRemainingAccounts,
    ]
  >;

export type GetCenterPriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  nonce: number;
};

export type GetCenterPriceInstructionDataArgs = { nonce: number };

export function getGetCenterPriceInstructionDataEncoder(): FixedSizeEncoder<GetCenterPriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nonce", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: GET_CENTER_PRICE_DISCRIMINATOR }),
  );
}

export function getGetCenterPriceInstructionDataDecoder(): FixedSizeDecoder<GetCenterPriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nonce", getU16Decoder()],
  ]);
}

export function getGetCenterPriceInstructionDataCodec(): FixedSizeCodec<
  GetCenterPriceInstructionDataArgs,
  GetCenterPriceInstructionData
> {
  return combineCodec(
    getGetCenterPriceInstructionDataEncoder(),
    getGetCenterPriceInstructionDataDecoder(),
  );
}

export type GetCenterPriceInput<TAccountOracle extends string = string> = {
  oracle: Address<TAccountOracle>;
  nonce: GetCenterPriceInstructionDataArgs["nonce"];
};

export function getGetCenterPriceInstruction<
  TAccountOracle extends string,
  TProgramAddress extends Address = typeof LEND_ORACLE_PROGRAM_ADDRESS,
>(
  input: GetCenterPriceInput<TAccountOracle>,
  config?: { programAddress?: TProgramAddress },
): GetCenterPriceInstruction<TProgramAddress, TAccountOracle> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    oracle: { value: input.oracle ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("oracle", accounts.oracle)],
    data: getGetCenterPriceInstructionDataEncoder().encode(
      args as GetCenterPriceInstructionDataArgs,
    ),
    programAddress,
  } as GetCenterPriceInstruction<TProgramAddress, TAccountOracle>);
}

export type ParsedGetCenterPriceInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    oracle: TAccountMetas[0];
  };
  data: GetCenterPriceInstructionData;
};

export function parseGetCenterPriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetCenterPriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { oracle: getNextAccount() },
    data: getGetCenterPriceInstructionDataDecoder().decode(instruction.data),
  };
}
