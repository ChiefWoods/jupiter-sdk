/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDexMetadataPda, findDexPda } from "../pdas";
import { LEND_DEX_PROGRAM_ADDRESS } from "../programs";

export const INIT_DEX_METADATA_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([114, 167, 144, 220, 143, 73, 224, 8]);

export function getInitDexMetadataDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_DEX_METADATA_DISCRIMINATOR,
  );
}

export type InitDexMetadataInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TAccountDex extends string | AccountMeta<string> = string,
  TAccountDexMetadata extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDexAdmin extends string
        ? ReadonlyAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      TAccountDex extends string ? ReadonlyAccount<TAccountDex> : TAccountDex,
      TAccountDexMetadata extends string
        ? WritableAccount<TAccountDexMetadata>
        : TAccountDexMetadata,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitDexMetadataInstructionData = {
  discriminator: ReadonlyUint8Array;
  dexId: number;
  lookupTable: Address;
};

export type InitDexMetadataInstructionDataArgs = {
  dexId: number;
  lookupTable: Address;
};

export function getInitDexMetadataInstructionDataEncoder(): FixedSizeEncoder<InitDexMetadataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["dexId", getU16Encoder()],
      ["lookupTable", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_DEX_METADATA_DISCRIMINATOR }),
  );
}

export function getInitDexMetadataInstructionDataDecoder(): FixedSizeDecoder<InitDexMetadataInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["dexId", getU16Decoder()],
    ["lookupTable", getAddressDecoder()],
  ]);
}

export function getInitDexMetadataInstructionDataCodec(): FixedSizeCodec<
  InitDexMetadataInstructionDataArgs,
  InitDexMetadataInstructionData
> {
  return combineCodec(
    getInitDexMetadataInstructionDataEncoder(),
    getInitDexMetadataInstructionDataDecoder(),
  );
}

export type InitDexMetadataAsyncInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountDex extends string = string,
  TAccountDexMetadata extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  dex?: Address<TAccountDex>;
  dexMetadata?: Address<TAccountDexMetadata>;
  systemProgram?: Address<TAccountSystemProgram>;
  dexId: InitDexMetadataInstructionDataArgs["dexId"];
  lookupTable: InitDexMetadataInstructionDataArgs["lookupTable"];
};

export async function getInitDexMetadataInstructionAsync<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TAccountDex extends string,
  TAccountDexMetadata extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_DEX_PROGRAM_ADDRESS,
>(
  input: InitDexMetadataAsyncInput<
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountDexMetadata,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitDexMetadataInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountDexMetadata,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: false },
    dex: { value: input.dex ?? null, isWritable: false },
    dexMetadata: { value: input.dexMetadata ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.dex.value) {
    accounts.dex.value = await findDexPda(
      { dexId: getNonNullResolvedInstructionInput("dexId", args.dexId) },
      { programAddress },
    );
  }
  if (!accounts.dexMetadata.value) {
    accounts.dexMetadata.value = await findDexMetadataPda(
      { dexId: getNonNullResolvedInstructionInput("dexId", args.dexId) },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("dex", accounts.dex),
      getAccountMeta("dexMetadata", accounts.dexMetadata),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitDexMetadataInstructionDataEncoder().encode(
      args as InitDexMetadataInstructionDataArgs,
    ),
    programAddress,
  } as InitDexMetadataInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountDexMetadata,
    TAccountSystemProgram
  >);
}

export type InitDexMetadataInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountDex extends string = string,
  TAccountDexMetadata extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  dex: Address<TAccountDex>;
  dexMetadata: Address<TAccountDexMetadata>;
  systemProgram?: Address<TAccountSystemProgram>;
  dexId: InitDexMetadataInstructionDataArgs["dexId"];
  lookupTable: InitDexMetadataInstructionDataArgs["lookupTable"];
};

export function getInitDexMetadataInstruction<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TAccountDex extends string,
  TAccountDexMetadata extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_DEX_PROGRAM_ADDRESS,
>(
  input: InitDexMetadataInput<
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountDexMetadata,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitDexMetadataInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDexAdmin,
  TAccountDex,
  TAccountDexMetadata,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: false },
    dex: { value: input.dex ?? null, isWritable: false },
    dexMetadata: { value: input.dexMetadata ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("dex", accounts.dex),
      getAccountMeta("dexMetadata", accounts.dexMetadata),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitDexMetadataInstructionDataEncoder().encode(
      args as InitDexMetadataInstructionDataArgs,
    ),
    programAddress,
  } as InitDexMetadataInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountDexMetadata,
    TAccountSystemProgram
  >);
}

export type ParsedInitDexMetadataInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
    dex: TAccountMetas[2];
    dexMetadata: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitDexMetadataInstructionData;
};

export function parseInitDexMetadataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitDexMetadataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      dexAdmin: getNextAccount(),
      dex: getNextAccount(),
      dexMetadata: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitDexMetadataInstructionDataDecoder().decode(instruction.data),
  };
}
