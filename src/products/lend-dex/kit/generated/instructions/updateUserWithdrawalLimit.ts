/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { DEX_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_USER_WITHDRAWAL_LIMIT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([162, 9, 186, 9, 213, 30, 173, 78]);

export function getUpdateUserWithdrawalLimitDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_USER_WITHDRAWAL_LIMIT_DISCRIMINATOR,
  );
}

export type UpdateUserWithdrawalLimitInstruction<
  TProgram extends string = typeof DEX_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TAccountDex extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDexAdmin extends string
        ? ReadonlyAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      TAccountDex extends string ? ReadonlyAccount<TAccountDex> : TAccountDex,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateUserWithdrawalLimitInstructionData = {
  discriminator: ReadonlyUint8Array;
  newLimit: bigint;
};

export type UpdateUserWithdrawalLimitInstructionDataArgs = {
  newLimit: number | bigint;
};

export function getUpdateUserWithdrawalLimitInstructionDataEncoder(): FixedSizeEncoder<UpdateUserWithdrawalLimitInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newLimit", getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_USER_WITHDRAWAL_LIMIT_DISCRIMINATOR,
    }),
  );
}

export function getUpdateUserWithdrawalLimitInstructionDataDecoder(): FixedSizeDecoder<UpdateUserWithdrawalLimitInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newLimit", getU64Decoder()],
  ]);
}

export function getUpdateUserWithdrawalLimitInstructionDataCodec(): FixedSizeCodec<
  UpdateUserWithdrawalLimitInstructionDataArgs,
  UpdateUserWithdrawalLimitInstructionData
> {
  return combineCodec(
    getUpdateUserWithdrawalLimitInstructionDataEncoder(),
    getUpdateUserWithdrawalLimitInstructionDataDecoder(),
  );
}

export type UpdateUserWithdrawalLimitInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountDex extends string = string,
  TAccountPosition extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  dex: Address<TAccountDex>;
  position: Address<TAccountPosition>;
  newLimit: UpdateUserWithdrawalLimitInstructionDataArgs["newLimit"];
};

export function getUpdateUserWithdrawalLimitInstruction<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TAccountDex extends string,
  TAccountPosition extends string,
  TProgramAddress extends Address = typeof DEX_PROGRAM_ADDRESS,
>(
  input: UpdateUserWithdrawalLimitInput<
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountPosition
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateUserWithdrawalLimitInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDexAdmin,
  TAccountDex,
  TAccountPosition
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: false },
    dex: { value: input.dex ?? null, isWritable: false },
    position: { value: input.position ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("dex", accounts.dex),
      getAccountMeta("position", accounts.position),
    ],
    data: getUpdateUserWithdrawalLimitInstructionDataEncoder().encode(
      args as UpdateUserWithdrawalLimitInstructionDataArgs,
    ),
    programAddress,
  } as UpdateUserWithdrawalLimitInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountPosition
  >);
}

export type ParsedUpdateUserWithdrawalLimitInstruction<
  TProgram extends string = typeof DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
    dex: TAccountMetas[2];
    position: TAccountMetas[3];
  };
  data: UpdateUserWithdrawalLimitInstructionData;
};

export function parseUpdateUserWithdrawalLimitInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateUserWithdrawalLimitInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      dexAdmin: getNextAccount(),
      dex: getNextAccount(),
      position: getNextAccount(),
    },
    data: getUpdateUserWithdrawalLimitInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
