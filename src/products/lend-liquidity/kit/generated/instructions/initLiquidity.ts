/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findAuthListPda, findLiquidityPda } from "../pdas";
import { LEND_LIQUIDITY_PROGRAM_ADDRESS } from "../programs";

export const INIT_LIQUIDITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  95, 189, 216, 183, 188, 62, 244, 108,
]);

export function getInitLiquidityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_LIQUIDITY_DISCRIMINATOR,
  );
}

export type InitLiquidityInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountLiquidity extends string | AccountMeta<string> = string,
  TAccountAuthList extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountLiquidity extends string
        ? WritableAccount<TAccountLiquidity>
        : TAccountLiquidity,
      TAccountAuthList extends string
        ? WritableAccount<TAccountAuthList>
        : TAccountAuthList,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitLiquidityInstructionData = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  revenueCollector: Address;
};

export type InitLiquidityInstructionDataArgs = {
  authority: Address;
  revenueCollector: Address;
};

export function getInitLiquidityInstructionDataEncoder(): FixedSizeEncoder<InitLiquidityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["revenueCollector", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_LIQUIDITY_DISCRIMINATOR }),
  );
}

export function getInitLiquidityInstructionDataDecoder(): FixedSizeDecoder<InitLiquidityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["revenueCollector", getAddressDecoder()],
  ]);
}

export function getInitLiquidityInstructionDataCodec(): FixedSizeCodec<
  InitLiquidityInstructionDataArgs,
  InitLiquidityInstructionData
> {
  return combineCodec(
    getInitLiquidityInstructionDataEncoder(),
    getInitLiquidityInstructionDataDecoder(),
  );
}

export type InitLiquidityAsyncInput<
  TAccountSigner extends string = string,
  TAccountLiquidity extends string = string,
  TAccountAuthList extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  liquidity?: Address<TAccountLiquidity>;
  authList?: Address<TAccountAuthList>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitLiquidityInstructionDataArgs["authority"];
  revenueCollector: InitLiquidityInstructionDataArgs["revenueCollector"];
};

export async function getInitLiquidityInstructionAsync<
  TAccountSigner extends string,
  TAccountLiquidity extends string,
  TAccountAuthList extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: InitLiquidityAsyncInput<
    TAccountSigner,
    TAccountLiquidity,
    TAccountAuthList,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitLiquidityInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLiquidity,
    TAccountAuthList,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    liquidity: { value: input.liquidity ?? null, isWritable: true },
    authList: { value: input.authList ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.liquidity.value) {
    accounts.liquidity.value = await findLiquidityPda();
  }
  if (!accounts.authList.value) {
    accounts.authList.value = await findAuthListPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("liquidity", accounts.liquidity),
      getAccountMeta("authList", accounts.authList),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitLiquidityInstructionDataEncoder().encode(
      args as InitLiquidityInstructionDataArgs,
    ),
    programAddress,
  } as InitLiquidityInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLiquidity,
    TAccountAuthList,
    TAccountSystemProgram
  >);
}

export type InitLiquidityInput<
  TAccountSigner extends string = string,
  TAccountLiquidity extends string = string,
  TAccountAuthList extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  liquidity: Address<TAccountLiquidity>;
  authList: Address<TAccountAuthList>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitLiquidityInstructionDataArgs["authority"];
  revenueCollector: InitLiquidityInstructionDataArgs["revenueCollector"];
};

export function getInitLiquidityInstruction<
  TAccountSigner extends string,
  TAccountLiquidity extends string,
  TAccountAuthList extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: InitLiquidityInput<
    TAccountSigner,
    TAccountLiquidity,
    TAccountAuthList,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitLiquidityInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountLiquidity,
  TAccountAuthList,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    liquidity: { value: input.liquidity ?? null, isWritable: true },
    authList: { value: input.authList ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("liquidity", accounts.liquidity),
      getAccountMeta("authList", accounts.authList),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitLiquidityInstructionDataEncoder().encode(
      args as InitLiquidityInstructionDataArgs,
    ),
    programAddress,
  } as InitLiquidityInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLiquidity,
    TAccountAuthList,
    TAccountSystemProgram
  >);
}

export type ParsedInitLiquidityInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    liquidity: TAccountMetas[1];
    authList: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitLiquidityInstructionData;
};

export function parseInitLiquidityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitLiquidityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      liquidity: getNextAccount(),
      authList: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitLiquidityInstructionDataDecoder().decode(instruction.data),
  };
}
