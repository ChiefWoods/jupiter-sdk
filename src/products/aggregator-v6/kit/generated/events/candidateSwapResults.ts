/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getCandidateSwapResultDecoder,
  getCandidateSwapResultEncoder,
  type CandidateSwapResult,
  type CandidateSwapResultArgs,
} from "../types";

export const CANDIDATE_SWAP_RESULTS_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([45, 9, 244, 30, 229, 52, 168, 123]);

export function getCandidateSwapResultsEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANDIDATE_SWAP_RESULTS_EVENT_DISCRIMINATOR,
  );
}

export type CandidateSwapResultsEvent = { results: Array<CandidateSwapResult> };

export type CandidateSwapResultsEventArgs = {
  results: Array<CandidateSwapResultArgs>;
};

/** Gets the encoder for {@link CandidateSwapResultsEventArgs} event data. */
export function getCandidateSwapResultsEventEncoder(): Encoder<CandidateSwapResultsEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["results", getArrayEncoder(getCandidateSwapResultEncoder())],
    ]),
    [getConstantEncoder(CANDIDATE_SWAP_RESULTS_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CandidateSwapResultsEvent} event data. */
export function getCandidateSwapResultsEventDecoder(): Decoder<CandidateSwapResultsEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["results", getArrayDecoder(getCandidateSwapResultDecoder())],
    ]),
    [getConstantDecoder(CANDIDATE_SWAP_RESULTS_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CandidateSwapResultsEvent} event data. */
export function getCandidateSwapResultsEventCodec(): Codec<
  CandidateSwapResultsEventArgs,
  CandidateSwapResultsEvent
> {
  return combineCodec(
    getCandidateSwapResultsEventEncoder(),
    getCandidateSwapResultsEventDecoder(),
  );
}

/** Parses the provided bytes into {@link CandidateSwapResultsEvent} event data. */
export function parseCandidateSwapResultsEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CandidateSwapResultsEvent {
  if (containsBytes(data, CANDIDATE_SWAP_RESULTS_EVENT_DISCRIMINATOR, 0)) {
    return getCandidateSwapResultsEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CandidateSwapResultsEvent" event discriminators.',
  );
}
