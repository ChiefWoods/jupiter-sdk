/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { PREDICTION_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_TICKET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  201, 80, 119, 145, 208, 184, 168, 70,
]);

export function getSettleTicketDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_TICKET_DISCRIMINATOR,
  );
}

export type SettleTicketInstruction<
  TProgram extends string = typeof PREDICTION_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountTicket extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountTicket extends string
        ? WritableAccount<TAccountTicket>
        : TAccountTicket,
      ...TRemainingAccounts,
    ]
  >;

export type SettleTicketInstructionData = {
  discriminator: ReadonlyUint8Array;
  payoutUsd: bigint;
};

export type SettleTicketInstructionDataArgs = { payoutUsd: number | bigint };

export function getSettleTicketInstructionDataEncoder(): FixedSizeEncoder<SettleTicketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["payoutUsd", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SETTLE_TICKET_DISCRIMINATOR }),
  );
}

export function getSettleTicketInstructionDataDecoder(): FixedSizeDecoder<SettleTicketInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["payoutUsd", getU64Decoder()],
  ]);
}

export function getSettleTicketInstructionDataCodec(): FixedSizeCodec<
  SettleTicketInstructionDataArgs,
  SettleTicketInstructionData
> {
  return combineCodec(
    getSettleTicketInstructionDataEncoder(),
    getSettleTicketInstructionDataDecoder(),
  );
}

export type SettleTicketInput<
  TAccountAuthority extends string = string,
  TAccountVault extends string = string,
  TAccountTicket extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vault: Address<TAccountVault>;
  ticket: Address<TAccountTicket>;
  payoutUsd: SettleTicketInstructionDataArgs["payoutUsd"];
};

export function getSettleTicketInstruction<
  TAccountAuthority extends string,
  TAccountVault extends string,
  TAccountTicket extends string,
  TProgramAddress extends Address = typeof PREDICTION_PROGRAM_ADDRESS,
>(
  input: SettleTicketInput<TAccountAuthority, TAccountVault, TAccountTicket>,
  config?: { programAddress?: TProgramAddress },
): SettleTicketInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountVault,
  TAccountTicket
> {
  // Program address.
  const programAddress = config?.programAddress ?? PREDICTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: false },
    ticket: { value: input.ticket ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("ticket", accounts.ticket),
    ],
    data: getSettleTicketInstructionDataEncoder().encode(
      args as SettleTicketInstructionDataArgs,
    ),
    programAddress,
  } as SettleTicketInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVault,
    TAccountTicket
  >);
}

export type ParsedSettleTicketInstruction<
  TProgram extends string = typeof PREDICTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vault: TAccountMetas[1];
    ticket: TAccountMetas[2];
  };
  data: SettleTicketInstructionData;
};

export function parseSettleTicketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleTicketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      vault: getNextAccount(),
      ticket: getNextAccount(),
    },
    data: getSettleTicketInstructionDataDecoder().decode(instruction.data),
  };
}
