/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_INIT_VAULT_STATE_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([140, 108, 65, 38, 128, 26, 194, 28]);

export function getLogInitVaultStateEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_INIT_VAULT_STATE_EVENT_DISCRIMINATOR,
  );
}

export type LogInitVaultStateEvent = { vaultState: Address };

export type LogInitVaultStateEventArgs = LogInitVaultStateEvent;

/** Gets the encoder for {@link LogInitVaultStateEventArgs} event data. */
export function getLogInitVaultStateEventEncoder(): FixedSizeEncoder<LogInitVaultStateEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([["vaultState", getAddressEncoder()]]),
    [getConstantEncoder(LOG_INIT_VAULT_STATE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogInitVaultStateEvent} event data. */
export function getLogInitVaultStateEventDecoder(): FixedSizeDecoder<LogInitVaultStateEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([["vaultState", getAddressDecoder()]]),
    [getConstantDecoder(LOG_INIT_VAULT_STATE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogInitVaultStateEvent} event data. */
export function getLogInitVaultStateEventCodec(): FixedSizeCodec<
  LogInitVaultStateEventArgs,
  LogInitVaultStateEvent
> {
  return combineCodec(
    getLogInitVaultStateEventEncoder(),
    getLogInitVaultStateEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LogInitVaultStateEvent} event data. */
export function parseLogInitVaultStateEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogInitVaultStateEvent {
  if (containsBytes(data, LOG_INIT_VAULT_STATE_EVENT_DISCRIMINATOR, 0)) {
    return getLogInitVaultStateEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogInitVaultStateEvent" event discriminators.',
  );
}
