/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CLAWBACK_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([47, 23, 60, 84, 245, 114, 178, 169]);

export function getClawbackEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAWBACK_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type ClawbackEventEvent = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** Amount clawed back. */
  clawedBackAmount: bigint;
  /** Receiver of the clawback. */
  receiver: Address;
};

export type ClawbackEventEventArgs = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** Amount clawed back. */
  clawedBackAmount: number | bigint;
  /** Receiver of the clawback. */
  receiver: Address;
};

/** Gets the encoder for {@link ClawbackEventEventArgs} event data. */
export function getClawbackEventEventEncoder(): Encoder<ClawbackEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["campaignId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["base", getAddressEncoder()],
      ["clawedBackAmount", getU64Encoder()],
      ["receiver", getAddressEncoder()],
    ]),
    [getConstantEncoder(CLAWBACK_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ClawbackEventEvent} event data. */
export function getClawbackEventEventDecoder(): Decoder<ClawbackEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["campaignId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["base", getAddressDecoder()],
      ["clawedBackAmount", getU64Decoder()],
      ["receiver", getAddressDecoder()],
    ]),
    [getConstantDecoder(CLAWBACK_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ClawbackEventEvent} event data. */
export function getClawbackEventEventCodec(): Codec<
  ClawbackEventEventArgs,
  ClawbackEventEvent
> {
  return combineCodec(
    getClawbackEventEventEncoder(),
    getClawbackEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link ClawbackEventEvent} event data. */
export function parseClawbackEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ClawbackEventEvent {
  if (containsBytes(data, CLAWBACK_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getClawbackEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ClawbackEventEvent" event discriminators.',
  );
}
