/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_DEX_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_THRESHOLD_PERCENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([177, 125, 99, 134, 42, 254, 140, 234]);

export function getUpdateThresholdPercentDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_THRESHOLD_PERCENT_DISCRIMINATOR,
  );
}

export type UpdateThresholdPercentInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TAccountDex extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDexAdmin extends string
        ? ReadonlyAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      TAccountDex extends string ? WritableAccount<TAccountDex> : TAccountDex,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateThresholdPercentInstructionData = {
  discriminator: ReadonlyUint8Array;
  upperThresholdPercent: number;
  lowerThresholdPercent: number;
  thresholdShiftTime: number;
  shiftTime: number;
};

export type UpdateThresholdPercentInstructionDataArgs = {
  upperThresholdPercent: number;
  lowerThresholdPercent: number;
  thresholdShiftTime: number;
  shiftTime: number;
};

export function getUpdateThresholdPercentInstructionDataEncoder(): FixedSizeEncoder<UpdateThresholdPercentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["upperThresholdPercent", getU32Encoder()],
      ["lowerThresholdPercent", getU32Encoder()],
      ["thresholdShiftTime", getU32Encoder()],
      ["shiftTime", getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_THRESHOLD_PERCENT_DISCRIMINATOR,
    }),
  );
}

export function getUpdateThresholdPercentInstructionDataDecoder(): FixedSizeDecoder<UpdateThresholdPercentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["upperThresholdPercent", getU32Decoder()],
    ["lowerThresholdPercent", getU32Decoder()],
    ["thresholdShiftTime", getU32Decoder()],
    ["shiftTime", getU32Decoder()],
  ]);
}

export function getUpdateThresholdPercentInstructionDataCodec(): FixedSizeCodec<
  UpdateThresholdPercentInstructionDataArgs,
  UpdateThresholdPercentInstructionData
> {
  return combineCodec(
    getUpdateThresholdPercentInstructionDataEncoder(),
    getUpdateThresholdPercentInstructionDataDecoder(),
  );
}

export type UpdateThresholdPercentInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountDex extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  dex: Address<TAccountDex>;
  upperThresholdPercent: UpdateThresholdPercentInstructionDataArgs["upperThresholdPercent"];
  lowerThresholdPercent: UpdateThresholdPercentInstructionDataArgs["lowerThresholdPercent"];
  thresholdShiftTime: UpdateThresholdPercentInstructionDataArgs["thresholdShiftTime"];
  shiftTime: UpdateThresholdPercentInstructionDataArgs["shiftTime"];
};

export function getUpdateThresholdPercentInstruction<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TAccountDex extends string,
  TProgramAddress extends Address = typeof LEND_DEX_PROGRAM_ADDRESS,
>(
  input: UpdateThresholdPercentInput<
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateThresholdPercentInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDexAdmin,
  TAccountDex
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: false },
    dex: { value: input.dex ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("dex", accounts.dex),
    ],
    data: getUpdateThresholdPercentInstructionDataEncoder().encode(
      args as UpdateThresholdPercentInstructionDataArgs,
    ),
    programAddress,
  } as UpdateThresholdPercentInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex
  >);
}

export type ParsedUpdateThresholdPercentInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
    dex: TAccountMetas[2];
  };
  data: UpdateThresholdPercentInstructionData;
};

export function parseUpdateThresholdPercentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateThresholdPercentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      dexAdmin: getNextAccount(),
      dex: getNextAccount(),
    },
    data: getUpdateThresholdPercentInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
