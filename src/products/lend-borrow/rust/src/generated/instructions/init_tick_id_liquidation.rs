//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const INIT_TICK_ID_LIQUIDATION_DISCRIMINATOR: [u8; 8] = [56, 110, 121, 169, 152, 241, 86, 183];

/// Accounts.
#[derive(Debug)]
pub struct InitTickIdLiquidation {
    pub signer: solana_address::Address,
    /// @dev Verification inside instruction logic
    pub tick_data: solana_address::Address,

    pub tick_id_liquidation: solana_address::Address,

    pub system_program: solana_address::Address,
}

impl InitTickIdLiquidation {
    pub fn instruction(
        &self,
        args: InitTickIdLiquidationInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: InitTickIdLiquidationInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.signer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.tick_data,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.tick_id_liquidation,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = InitTickIdLiquidationInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::VAULTS_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitTickIdLiquidationInstructionData {
    discriminator: [u8; 8],
}

impl InitTickIdLiquidationInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [56, 110, 121, 169, 152, 241, 86, 183],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for InitTickIdLiquidationInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitTickIdLiquidationInstructionArgs {
    pub vault_id: u16,
    pub tick: i32,
    pub total_ids: u32,
}

impl InitTickIdLiquidationInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `InitTickIdLiquidation`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[]` tick_data
///   2. `[writable]` tick_id_liquidation
///   3. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct InitTickIdLiquidationBuilder {
    signer: Option<solana_address::Address>,
    tick_data: Option<solana_address::Address>,
    tick_id_liquidation: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    vault_id: Option<u16>,
    tick: Option<i32>,
    total_ids: Option<u32>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl InitTickIdLiquidationBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn signer(&mut self, signer: solana_address::Address) -> &mut Self {
        self.signer = Some(signer);
        self
    }
    /// @dev Verification inside instruction logic
    #[inline(always)]
    pub fn tick_data(&mut self, tick_data: solana_address::Address) -> &mut Self {
        self.tick_data = Some(tick_data);
        self
    }
    #[inline(always)]
    pub fn tick_id_liquidation(
        &mut self,
        tick_id_liquidation: solana_address::Address,
    ) -> &mut Self {
        self.tick_id_liquidation = Some(tick_id_liquidation);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn vault_id(&mut self, vault_id: u16) -> &mut Self {
        self.vault_id = Some(vault_id);
        self
    }
    #[inline(always)]
    pub fn tick(&mut self, tick: i32) -> &mut Self {
        self.tick = Some(tick);
        self
    }
    #[inline(always)]
    pub fn total_ids(&mut self, total_ids: u32) -> &mut Self {
        self.total_ids = Some(total_ids);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = InitTickIdLiquidation {
            signer: self.signer.expect("signer is not set"),
            tick_data: self.tick_data.expect("tick_data is not set"),
            tick_id_liquidation: self
                .tick_id_liquidation
                .expect("tick_id_liquidation is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = InitTickIdLiquidationInstructionArgs {
            vault_id: self.vault_id.clone().expect("vault_id is not set"),
            tick: self.tick.clone().expect("tick is not set"),
            total_ids: self.total_ids.clone().expect("total_ids is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `init_tick_id_liquidation` CPI accounts.
pub struct InitTickIdLiquidationCpiAccounts<'a, 'b> {
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// @dev Verification inside instruction logic
    pub tick_data: &'b solana_account_info::AccountInfo<'a>,

    pub tick_id_liquidation: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `init_tick_id_liquidation` CPI instruction.
pub struct InitTickIdLiquidationCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// @dev Verification inside instruction logic
    pub tick_data: &'b solana_account_info::AccountInfo<'a>,

    pub tick_id_liquidation: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: InitTickIdLiquidationInstructionArgs,
}

impl<'a, 'b> InitTickIdLiquidationCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: InitTickIdLiquidationCpiAccounts<'a, 'b>,
        args: InitTickIdLiquidationInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            signer: accounts.signer,
            tick_data: accounts.tick_data,
            tick_id_liquidation: accounts.tick_id_liquidation,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.signer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.tick_data.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.tick_id_liquidation.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = InitTickIdLiquidationInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::VAULTS_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.signer.clone());
        account_infos.push(self.tick_data.clone());
        account_infos.push(self.tick_id_liquidation.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `InitTickIdLiquidation` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[]` tick_data
///   2. `[writable]` tick_id_liquidation
///   3. `[]` system_program
#[derive(Clone, Debug)]
pub struct InitTickIdLiquidationCpiBuilder<'a, 'b> {
    instruction: Box<InitTickIdLiquidationCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> InitTickIdLiquidationCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(InitTickIdLiquidationCpiBuilderInstruction {
            __program: program,
            signer: None,
            tick_data: None,
            tick_id_liquidation: None,
            system_program: None,
            vault_id: None,
            tick: None,
            total_ids: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.signer = Some(signer);
        self
    }
    /// @dev Verification inside instruction logic
    #[inline(always)]
    pub fn tick_data(&mut self, tick_data: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.tick_data = Some(tick_data);
        self
    }
    #[inline(always)]
    pub fn tick_id_liquidation(
        &mut self,
        tick_id_liquidation: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.tick_id_liquidation = Some(tick_id_liquidation);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn vault_id(&mut self, vault_id: u16) -> &mut Self {
        self.instruction.vault_id = Some(vault_id);
        self
    }
    #[inline(always)]
    pub fn tick(&mut self, tick: i32) -> &mut Self {
        self.instruction.tick = Some(tick);
        self
    }
    #[inline(always)]
    pub fn total_ids(&mut self, total_ids: u32) -> &mut Self {
        self.instruction.total_ids = Some(total_ids);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = InitTickIdLiquidationInstructionArgs {
            vault_id: self
                .instruction
                .vault_id
                .clone()
                .expect("vault_id is not set"),
            tick: self.instruction.tick.clone().expect("tick is not set"),
            total_ids: self
                .instruction
                .total_ids
                .clone()
                .expect("total_ids is not set"),
        };
        let instruction = InitTickIdLiquidationCpi {
            __program: self.instruction.__program,

            signer: self.instruction.signer.expect("signer is not set"),

            tick_data: self.instruction.tick_data.expect("tick_data is not set"),

            tick_id_liquidation: self
                .instruction
                .tick_id_liquidation
                .expect("tick_id_liquidation is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct InitTickIdLiquidationCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    signer: Option<&'b solana_account_info::AccountInfo<'a>>,
    tick_data: Option<&'b solana_account_info::AccountInfo<'a>>,
    tick_id_liquidation: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    vault_id: Option<u16>,
    tick: Option<i32>,
    total_ids: Option<u32>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
