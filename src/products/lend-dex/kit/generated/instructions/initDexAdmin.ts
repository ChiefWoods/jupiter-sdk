/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findDexAdminPda } from "../pdas";
import { LEND_DEX_PROGRAM_ADDRESS } from "../programs";

export const INIT_DEX_ADMIN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  16, 61, 98, 61, 189, 243, 52, 252,
]);

export function getInitDexAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_DEX_ADMIN_DISCRIMINATOR,
  );
}

export type InitDexAdminInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountDexAdmin extends string
        ? WritableAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitDexAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  liquidity: Address;
  authority: Address;
};

export type InitDexAdminInstructionDataArgs = {
  liquidity: Address;
  authority: Address;
};

export function getInitDexAdminInstructionDataEncoder(): FixedSizeEncoder<InitDexAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["liquidity", getAddressEncoder()],
      ["authority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_DEX_ADMIN_DISCRIMINATOR }),
  );
}

export function getInitDexAdminInstructionDataDecoder(): FixedSizeDecoder<InitDexAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["liquidity", getAddressDecoder()],
    ["authority", getAddressDecoder()],
  ]);
}

export function getInitDexAdminInstructionDataCodec(): FixedSizeCodec<
  InitDexAdminInstructionDataArgs,
  InitDexAdminInstructionData
> {
  return combineCodec(
    getInitDexAdminInstructionDataEncoder(),
    getInitDexAdminInstructionDataDecoder(),
  );
}

export type InitDexAdminAsyncInput<
  TAccountSigner extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  dexAdmin?: Address<TAccountDexAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  liquidity: InitDexAdminInstructionDataArgs["liquidity"];
  authority: InitDexAdminInstructionDataArgs["authority"];
};

export async function getInitDexAdminInstructionAsync<
  TAccountSigner extends string,
  TAccountDexAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_DEX_PROGRAM_ADDRESS,
>(
  input: InitDexAdminAsyncInput<
    TAccountSigner,
    TAccountDexAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitDexAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDexAdmin,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.dexAdmin.value) {
    accounts.dexAdmin.value = await findDexAdminPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitDexAdminInstructionDataEncoder().encode(
      args as InitDexAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitDexAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDexAdmin,
    TAccountSystemProgram
  >);
}

export type InitDexAdminInput<
  TAccountSigner extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  dexAdmin: Address<TAccountDexAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  liquidity: InitDexAdminInstructionDataArgs["liquidity"];
  authority: InitDexAdminInstructionDataArgs["authority"];
};

export function getInitDexAdminInstruction<
  TAccountSigner extends string,
  TAccountDexAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_DEX_PROGRAM_ADDRESS,
>(
  input: InitDexAdminInput<
    TAccountSigner,
    TAccountDexAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitDexAdminInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountDexAdmin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitDexAdminInstructionDataEncoder().encode(
      args as InitDexAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitDexAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountDexAdmin,
    TAccountSystemProgram
  >);
}

export type ParsedInitDexAdminInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitDexAdminInstructionData;
};

export function parseInitDexAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitDexAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      dexAdmin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitDexAdminInstructionDataDecoder().decode(instruction.data),
  };
}
