/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_BORROW_PROGRAM_ADDRESS } from "../programs";

export const GET_EXCHANGE_PRICES_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([237, 128, 83, 152, 52, 21, 231, 86]);

export function getGetExchangePricesDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_EXCHANGE_PRICES_DISCRIMINATOR,
  );
}

export type GetExchangePricesInstruction<
  TProgram extends string = typeof LEND_BORROW_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountVaultConfig extends string | AccountMeta<string> = string,
  TAccountSupplyTokenReserves extends string | AccountMeta<string> = string,
  TAccountBorrowTokenReserves extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? ReadonlyAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountVaultConfig extends string
        ? ReadonlyAccount<TAccountVaultConfig>
        : TAccountVaultConfig,
      TAccountSupplyTokenReserves extends string
        ? ReadonlyAccount<TAccountSupplyTokenReserves>
        : TAccountSupplyTokenReserves,
      TAccountBorrowTokenReserves extends string
        ? ReadonlyAccount<TAccountBorrowTokenReserves>
        : TAccountBorrowTokenReserves,
      ...TRemainingAccounts,
    ]
  >;

export type GetExchangePricesInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type GetExchangePricesInstructionDataArgs = {};

export function getGetExchangePricesInstructionDataEncoder(): FixedSizeEncoder<GetExchangePricesInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: GET_EXCHANGE_PRICES_DISCRIMINATOR }),
  );
}

export function getGetExchangePricesInstructionDataDecoder(): FixedSizeDecoder<GetExchangePricesInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getGetExchangePricesInstructionDataCodec(): FixedSizeCodec<
  GetExchangePricesInstructionDataArgs,
  GetExchangePricesInstructionData
> {
  return combineCodec(
    getGetExchangePricesInstructionDataEncoder(),
    getGetExchangePricesInstructionDataDecoder(),
  );
}

export type GetExchangePricesInput<
  TAccountVaultState extends string = string,
  TAccountVaultConfig extends string = string,
  TAccountSupplyTokenReserves extends string = string,
  TAccountBorrowTokenReserves extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  vaultConfig: Address<TAccountVaultConfig>;
  supplyTokenReserves: Address<TAccountSupplyTokenReserves>;
  borrowTokenReserves: Address<TAccountBorrowTokenReserves>;
};

export function getGetExchangePricesInstruction<
  TAccountVaultState extends string,
  TAccountVaultConfig extends string,
  TAccountSupplyTokenReserves extends string,
  TAccountBorrowTokenReserves extends string,
  TProgramAddress extends Address = typeof LEND_BORROW_PROGRAM_ADDRESS,
>(
  input: GetExchangePricesInput<
    TAccountVaultState,
    TAccountVaultConfig,
    TAccountSupplyTokenReserves,
    TAccountBorrowTokenReserves
  >,
  config?: { programAddress?: TProgramAddress },
): GetExchangePricesInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountVaultConfig,
  TAccountSupplyTokenReserves,
  TAccountBorrowTokenReserves
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_BORROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: false },
    vaultConfig: { value: input.vaultConfig ?? null, isWritable: false },
    supplyTokenReserves: {
      value: input.supplyTokenReserves ?? null,
      isWritable: false,
    },
    borrowTokenReserves: {
      value: input.borrowTokenReserves ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("vaultConfig", accounts.vaultConfig),
      getAccountMeta("supplyTokenReserves", accounts.supplyTokenReserves),
      getAccountMeta("borrowTokenReserves", accounts.borrowTokenReserves),
    ],
    data: getGetExchangePricesInstructionDataEncoder().encode({}),
    programAddress,
  } as GetExchangePricesInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountVaultConfig,
    TAccountSupplyTokenReserves,
    TAccountBorrowTokenReserves
  >);
}

export type ParsedGetExchangePricesInstruction<
  TProgram extends string = typeof LEND_BORROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    vaultConfig: TAccountMetas[1];
    supplyTokenReserves: TAccountMetas[2];
    borrowTokenReserves: TAccountMetas[3];
  };
  data: GetExchangePricesInstructionData;
};

export function parseGetExchangePricesInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetExchangePricesInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      vaultConfig: getNextAccount(),
      supplyTokenReserves: getNextAccount(),
      borrowTokenReserves: getNextAccount(),
    },
    data: getGetExchangePricesInstructionDataDecoder().decode(instruction.data),
  };
}
