/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findOracleAdminPda } from "../pdas";
import { LEND_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const INIT_ADMIN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  97, 65, 97, 27, 200, 206, 72, 219,
]);

export function getInitAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_ADMIN_DISCRIMINATOR);
}

export type InitAdminInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountOracleAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountOracleAdmin extends string
        ? WritableAccount<TAccountOracleAdmin>
        : TAccountOracleAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
};

export type InitAdminInstructionDataArgs = { authority: Address };

export function getInitAdminInstructionDataEncoder(): FixedSizeEncoder<InitAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_ADMIN_DISCRIMINATOR }),
  );
}

export function getInitAdminInstructionDataDecoder(): FixedSizeDecoder<InitAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
  ]);
}

export function getInitAdminInstructionDataCodec(): FixedSizeCodec<
  InitAdminInstructionDataArgs,
  InitAdminInstructionData
> {
  return combineCodec(
    getInitAdminInstructionDataEncoder(),
    getInitAdminInstructionDataDecoder(),
  );
}

export type InitAdminAsyncInput<
  TAccountSigner extends string = string,
  TAccountOracleAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  oracleAdmin?: Address<TAccountOracleAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitAdminInstructionDataArgs["authority"];
};

export async function getInitAdminInstructionAsync<
  TAccountSigner extends string,
  TAccountOracleAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_ORACLE_PROGRAM_ADDRESS,
>(
  input: InitAdminAsyncInput<
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    oracleAdmin: { value: input.oracleAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.oracleAdmin.value) {
    accounts.oracleAdmin.value = await findOracleAdminPda({ programAddress });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("oracleAdmin", accounts.oracleAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitAdminInstructionDataEncoder().encode(
      args as InitAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountSystemProgram
  >);
}

export type InitAdminInput<
  TAccountSigner extends string = string,
  TAccountOracleAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  oracleAdmin: Address<TAccountOracleAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitAdminInstructionDataArgs["authority"];
};

export function getInitAdminInstruction<
  TAccountSigner extends string,
  TAccountOracleAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_ORACLE_PROGRAM_ADDRESS,
>(
  input: InitAdminInput<
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitAdminInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountOracleAdmin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    oracleAdmin: { value: input.oracleAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("oracleAdmin", accounts.oracleAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitAdminInstructionDataEncoder().encode(
      args as InitAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountSystemProgram
  >);
}

export type ParsedInitAdminInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    oracleAdmin: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitAdminInstructionData;
};

export function parseInitAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      oracleAdmin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitAdminInstructionDataDecoder().decode(instruction.data),
  };
}
