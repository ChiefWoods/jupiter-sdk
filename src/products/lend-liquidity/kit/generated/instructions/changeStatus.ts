/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_LIQUIDITY_PROGRAM_ADDRESS } from "../programs";

export const CHANGE_STATUS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  236, 145, 131, 228, 227, 17, 192, 255,
]);

export function getChangeStatusDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CHANGE_STATUS_DISCRIMINATOR,
  );
}

export type ChangeStatusInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountLiquidity extends string | AccountMeta<string> = string,
  TAccountAuthList extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountLiquidity extends string
        ? WritableAccount<TAccountLiquidity>
        : TAccountLiquidity,
      TAccountAuthList extends string
        ? ReadonlyAccount<TAccountAuthList>
        : TAccountAuthList,
      ...TRemainingAccounts,
    ]
  >;

export type ChangeStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
  status: boolean;
};

export type ChangeStatusInstructionDataArgs = { status: boolean };

export function getChangeStatusInstructionDataEncoder(): FixedSizeEncoder<ChangeStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["status", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: CHANGE_STATUS_DISCRIMINATOR }),
  );
}

export function getChangeStatusInstructionDataDecoder(): FixedSizeDecoder<ChangeStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["status", getBooleanDecoder()],
  ]);
}

export function getChangeStatusInstructionDataCodec(): FixedSizeCodec<
  ChangeStatusInstructionDataArgs,
  ChangeStatusInstructionData
> {
  return combineCodec(
    getChangeStatusInstructionDataEncoder(),
    getChangeStatusInstructionDataDecoder(),
  );
}

export type ChangeStatusInput<
  TAccountAuthority extends string = string,
  TAccountLiquidity extends string = string,
  TAccountAuthList extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  liquidity: Address<TAccountLiquidity>;
  authList: Address<TAccountAuthList>;
  status: ChangeStatusInstructionDataArgs["status"];
};

export function getChangeStatusInstruction<
  TAccountAuthority extends string,
  TAccountLiquidity extends string,
  TAccountAuthList extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: ChangeStatusInput<
    TAccountAuthority,
    TAccountLiquidity,
    TAccountAuthList
  >,
  config?: { programAddress?: TProgramAddress },
): ChangeStatusInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountLiquidity,
  TAccountAuthList
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    liquidity: { value: input.liquidity ?? null, isWritable: true },
    authList: { value: input.authList ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("liquidity", accounts.liquidity),
      getAccountMeta("authList", accounts.authList),
    ],
    data: getChangeStatusInstructionDataEncoder().encode(
      args as ChangeStatusInstructionDataArgs,
    ),
    programAddress,
  } as ChangeStatusInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountLiquidity,
    TAccountAuthList
  >);
}

export type ParsedChangeStatusInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    liquidity: TAccountMetas[1];
    authList: TAccountMetas[2];
  };
  data: ChangeStatusInstructionData;
};

export function parseChangeStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedChangeStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      liquidity: getNextAccount(),
      authList: getNextAccount(),
    },
    data: getChangeStatusInstructionDataDecoder().decode(instruction.data),
  };
}
