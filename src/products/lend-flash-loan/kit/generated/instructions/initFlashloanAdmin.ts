/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findFlashloanAdminPda } from "../pdas";
import { LEND_FLASH_LOAN_PROGRAM_ADDRESS } from "../programs";

export const INIT_FLASHLOAN_ADMIN_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([185, 117, 154, 56, 95, 12, 187, 139]);

export function getInitFlashloanAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_FLASHLOAN_ADMIN_DISCRIMINATOR,
  );
}

export type InitFlashloanAdminInstruction<
  TProgram extends string = typeof LEND_FLASH_LOAN_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountFlashloanAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountFlashloanAdmin extends string
        ? WritableAccount<TAccountFlashloanAdmin>
        : TAccountFlashloanAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitFlashloanAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  flashloanFee: number;
  liquidityProgram: Address;
};

export type InitFlashloanAdminInstructionDataArgs = {
  authority: Address;
  flashloanFee: number;
  liquidityProgram: Address;
};

export function getInitFlashloanAdminInstructionDataEncoder(): FixedSizeEncoder<InitFlashloanAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["flashloanFee", getU16Encoder()],
      ["liquidityProgram", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INIT_FLASHLOAN_ADMIN_DISCRIMINATOR,
    }),
  );
}

export function getInitFlashloanAdminInstructionDataDecoder(): FixedSizeDecoder<InitFlashloanAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["flashloanFee", getU16Decoder()],
    ["liquidityProgram", getAddressDecoder()],
  ]);
}

export function getInitFlashloanAdminInstructionDataCodec(): FixedSizeCodec<
  InitFlashloanAdminInstructionDataArgs,
  InitFlashloanAdminInstructionData
> {
  return combineCodec(
    getInitFlashloanAdminInstructionDataEncoder(),
    getInitFlashloanAdminInstructionDataDecoder(),
  );
}

export type InitFlashloanAdminAsyncInput<
  TAccountSigner extends string = string,
  TAccountFlashloanAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  flashloanAdmin?: Address<TAccountFlashloanAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitFlashloanAdminInstructionDataArgs["authority"];
  flashloanFee: InitFlashloanAdminInstructionDataArgs["flashloanFee"];
  liquidityProgram: InitFlashloanAdminInstructionDataArgs["liquidityProgram"];
};

export async function getInitFlashloanAdminInstructionAsync<
  TAccountSigner extends string,
  TAccountFlashloanAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_FLASH_LOAN_PROGRAM_ADDRESS,
>(
  input: InitFlashloanAdminAsyncInput<
    TAccountSigner,
    TAccountFlashloanAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitFlashloanAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountFlashloanAdmin,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_FLASH_LOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    flashloanAdmin: { value: input.flashloanAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.flashloanAdmin.value) {
    accounts.flashloanAdmin.value = await findFlashloanAdminPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("flashloanAdmin", accounts.flashloanAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitFlashloanAdminInstructionDataEncoder().encode(
      args as InitFlashloanAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitFlashloanAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountFlashloanAdmin,
    TAccountSystemProgram
  >);
}

export type InitFlashloanAdminInput<
  TAccountSigner extends string = string,
  TAccountFlashloanAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  flashloanAdmin: Address<TAccountFlashloanAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitFlashloanAdminInstructionDataArgs["authority"];
  flashloanFee: InitFlashloanAdminInstructionDataArgs["flashloanFee"];
  liquidityProgram: InitFlashloanAdminInstructionDataArgs["liquidityProgram"];
};

export function getInitFlashloanAdminInstruction<
  TAccountSigner extends string,
  TAccountFlashloanAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_FLASH_LOAN_PROGRAM_ADDRESS,
>(
  input: InitFlashloanAdminInput<
    TAccountSigner,
    TAccountFlashloanAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitFlashloanAdminInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountFlashloanAdmin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_FLASH_LOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    flashloanAdmin: { value: input.flashloanAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("flashloanAdmin", accounts.flashloanAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitFlashloanAdminInstructionDataEncoder().encode(
      args as InitFlashloanAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitFlashloanAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountFlashloanAdmin,
    TAccountSystemProgram
  >);
}

export type ParsedInitFlashloanAdminInstruction<
  TProgram extends string = typeof LEND_FLASH_LOAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    flashloanAdmin: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitFlashloanAdminInstructionData;
};

export function parseInitFlashloanAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitFlashloanAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      flashloanAdmin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitFlashloanAdminInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
