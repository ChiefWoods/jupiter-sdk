/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CLAIM_TOKEN_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([193, 131, 59, 210, 91, 145, 198, 255]);

export function getClaimTokenEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_TOKEN_EVENT_DISCRIMINATOR,
  );
}

export type ClaimTokenEvent = {
  claimer: Address;
  sender: Address;
  inviteSigner: Address;
  amount: bigint;
  mint: Address;
};

export type ClaimTokenEventArgs = {
  claimer: Address;
  sender: Address;
  inviteSigner: Address;
  amount: number | bigint;
  mint: Address;
};

/** Gets the encoder for {@link ClaimTokenEventArgs} event data. */
export function getClaimTokenEventEncoder(): FixedSizeEncoder<ClaimTokenEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["claimer", getAddressEncoder()],
      ["sender", getAddressEncoder()],
      ["inviteSigner", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["mint", getAddressEncoder()],
    ]),
    [getConstantEncoder(CLAIM_TOKEN_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ClaimTokenEvent} event data. */
export function getClaimTokenEventDecoder(): FixedSizeDecoder<ClaimTokenEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["claimer", getAddressDecoder()],
      ["sender", getAddressDecoder()],
      ["inviteSigner", getAddressDecoder()],
      ["amount", getU64Decoder()],
      ["mint", getAddressDecoder()],
    ]),
    [getConstantDecoder(CLAIM_TOKEN_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ClaimTokenEvent} event data. */
export function getClaimTokenEventCodec(): FixedSizeCodec<
  ClaimTokenEventArgs,
  ClaimTokenEvent
> {
  return combineCodec(getClaimTokenEventEncoder(), getClaimTokenEventDecoder());
}

/** Parses the provided bytes into {@link ClaimTokenEvent} event data. */
export function parseClaimTokenEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ClaimTokenEvent {
  if (containsBytes(data, CLAIM_TOKEN_EVENT_DISCRIMINATOR, 0)) {
    return getClaimTokenEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ClaimTokenEvent" event discriminators.',
  );
}
