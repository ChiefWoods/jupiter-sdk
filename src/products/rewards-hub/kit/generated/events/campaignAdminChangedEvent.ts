/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CAMPAIGN_ADMIN_CHANGED_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([49, 222, 104, 253, 31, 101, 68, 181]);

export function getCampaignAdminChangedEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CAMPAIGN_ADMIN_CHANGED_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type CampaignAdminChangedEventEvent = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** New admin. */
  newAdmin: Address;
};

export type CampaignAdminChangedEventEventArgs = CampaignAdminChangedEventEvent;

/** Gets the encoder for {@link CampaignAdminChangedEventEventArgs} event data. */
export function getCampaignAdminChangedEventEventEncoder(): Encoder<CampaignAdminChangedEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["campaignId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["base", getAddressEncoder()],
      ["newAdmin", getAddressEncoder()],
    ]),
    [getConstantEncoder(CAMPAIGN_ADMIN_CHANGED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link CampaignAdminChangedEventEvent} event data. */
export function getCampaignAdminChangedEventEventDecoder(): Decoder<CampaignAdminChangedEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["campaignId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["base", getAddressDecoder()],
      ["newAdmin", getAddressDecoder()],
    ]),
    [getConstantDecoder(CAMPAIGN_ADMIN_CHANGED_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link CampaignAdminChangedEventEvent} event data. */
export function getCampaignAdminChangedEventEventCodec(): Codec<
  CampaignAdminChangedEventEventArgs,
  CampaignAdminChangedEventEvent
> {
  return combineCodec(
    getCampaignAdminChangedEventEventEncoder(),
    getCampaignAdminChangedEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link CampaignAdminChangedEventEvent} event data. */
export function parseCampaignAdminChangedEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): CampaignAdminChangedEventEvent {
  if (
    containsBytes(data, CAMPAIGN_ADMIN_CHANGED_EVENT_EVENT_DISCRIMINATOR, 0)
  ) {
    return getCampaignAdminChangedEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "CampaignAdminChangedEventEvent" event discriminators.',
  );
}
