/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_DEX_PROGRAM_ADDRESS } from "../programs";

export const INIT_DEX_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  222, 187, 81, 48, 89, 117, 230, 164,
]);

export function getInitDexDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_DEX_DISCRIMINATOR);
}

export type InitDexInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TAccountDex extends string | AccountMeta<string> = string,
  TAccountDexMetadata extends string | AccountMeta<string> = string,
  TAccountToken0 extends string | AccountMeta<string> = string,
  TAccountToken1 extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDexAdmin extends string
        ? WritableAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      TAccountDex extends string ? WritableAccount<TAccountDex> : TAccountDex,
      TAccountDexMetadata extends string
        ? WritableAccount<TAccountDexMetadata>
        : TAccountDexMetadata,
      TAccountToken0 extends string
        ? ReadonlyAccount<TAccountToken0>
        : TAccountToken0,
      TAccountToken1 extends string
        ? ReadonlyAccount<TAccountToken1>
        : TAccountToken1,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitDexInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** Initial center price: a normalized ratio scaled by PRICE_PRECISION (10^15) */
  centerPrice: bigint;
  /** Fee in 4 decimals: 1% = 10000, max 10% = 100000 */
  fee: number;
  /** Revenue cut in 4 decimals (e.g., 100000 = 10%). Divided by 10000 for storage. */
  revenueCut: number;
  /** Upper range percent in 4 decimals: 1% = 10000 */
  upperPercent: number;
  /** Lower range percent in 4 decimals: 1% = 10000 */
  lowerPercent: number;
  /** Upper shift threshold percent in 4 decimals. Divided by 1000 for storage. */
  upperShiftThreshold: number;
  /** Lower shift threshold percent in 4 decimals. Divided by 1000 for storage. */
  lowerShiftThreshold: number;
  /** Time (seconds) for threshold-based center price shift */
  thresholdShiftTime: number;
  /** Maximum center price */
  maxCenterPrice: bigint;
  /** Minimum center price */
  minCenterPrice: bigint;
};

export type InitDexInstructionDataArgs = {
  /** Initial center price: a normalized ratio scaled by PRICE_PRECISION (10^15) */
  centerPrice: number | bigint;
  /** Fee in 4 decimals: 1% = 10000, max 10% = 100000 */
  fee: number;
  /** Revenue cut in 4 decimals (e.g., 100000 = 10%). Divided by 10000 for storage. */
  revenueCut: number;
  /** Upper range percent in 4 decimals: 1% = 10000 */
  upperPercent: number;
  /** Lower range percent in 4 decimals: 1% = 10000 */
  lowerPercent: number;
  /** Upper shift threshold percent in 4 decimals. Divided by 1000 for storage. */
  upperShiftThreshold: number;
  /** Lower shift threshold percent in 4 decimals. Divided by 1000 for storage. */
  lowerShiftThreshold: number;
  /** Time (seconds) for threshold-based center price shift */
  thresholdShiftTime: number;
  /** Maximum center price */
  maxCenterPrice: number | bigint;
  /** Minimum center price */
  minCenterPrice: number | bigint;
};

export function getInitDexInstructionDataEncoder(): FixedSizeEncoder<InitDexInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["centerPrice", getU64Encoder()],
      ["fee", getU32Encoder()],
      ["revenueCut", getU32Encoder()],
      ["upperPercent", getU32Encoder()],
      ["lowerPercent", getU32Encoder()],
      ["upperShiftThreshold", getU32Encoder()],
      ["lowerShiftThreshold", getU32Encoder()],
      ["thresholdShiftTime", getU32Encoder()],
      ["maxCenterPrice", getU64Encoder()],
      ["minCenterPrice", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_DEX_DISCRIMINATOR }),
  );
}

export function getInitDexInstructionDataDecoder(): FixedSizeDecoder<InitDexInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["centerPrice", getU64Decoder()],
    ["fee", getU32Decoder()],
    ["revenueCut", getU32Decoder()],
    ["upperPercent", getU32Decoder()],
    ["lowerPercent", getU32Decoder()],
    ["upperShiftThreshold", getU32Decoder()],
    ["lowerShiftThreshold", getU32Decoder()],
    ["thresholdShiftTime", getU32Decoder()],
    ["maxCenterPrice", getU64Decoder()],
    ["minCenterPrice", getU64Decoder()],
  ]);
}

export function getInitDexInstructionDataCodec(): FixedSizeCodec<
  InitDexInstructionDataArgs,
  InitDexInstructionData
> {
  return combineCodec(
    getInitDexInstructionDataEncoder(),
    getInitDexInstructionDataDecoder(),
  );
}

export type InitDexInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountDex extends string = string,
  TAccountDexMetadata extends string = string,
  TAccountToken0 extends string = string,
  TAccountToken1 extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  dex: Address<TAccountDex>;
  dexMetadata: Address<TAccountDexMetadata>;
  token0: Address<TAccountToken0>;
  token1: Address<TAccountToken1>;
  systemProgram?: Address<TAccountSystemProgram>;
  centerPrice: InitDexInstructionDataArgs["centerPrice"];
  fee: InitDexInstructionDataArgs["fee"];
  revenueCut: InitDexInstructionDataArgs["revenueCut"];
  upperPercent: InitDexInstructionDataArgs["upperPercent"];
  lowerPercent: InitDexInstructionDataArgs["lowerPercent"];
  upperShiftThreshold: InitDexInstructionDataArgs["upperShiftThreshold"];
  lowerShiftThreshold: InitDexInstructionDataArgs["lowerShiftThreshold"];
  thresholdShiftTime: InitDexInstructionDataArgs["thresholdShiftTime"];
  maxCenterPrice: InitDexInstructionDataArgs["maxCenterPrice"];
  minCenterPrice: InitDexInstructionDataArgs["minCenterPrice"];
};

export function getInitDexInstruction<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TAccountDex extends string,
  TAccountDexMetadata extends string,
  TAccountToken0 extends string,
  TAccountToken1 extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_DEX_PROGRAM_ADDRESS,
>(
  input: InitDexInput<
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountDexMetadata,
    TAccountToken0,
    TAccountToken1,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitDexInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDexAdmin,
  TAccountDex,
  TAccountDexMetadata,
  TAccountToken0,
  TAccountToken1,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: true },
    dex: { value: input.dex ?? null, isWritable: true },
    dexMetadata: { value: input.dexMetadata ?? null, isWritable: true },
    token0: { value: input.token0 ?? null, isWritable: false },
    token1: { value: input.token1 ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("dex", accounts.dex),
      getAccountMeta("dexMetadata", accounts.dexMetadata),
      getAccountMeta("token0", accounts.token0),
      getAccountMeta("token1", accounts.token1),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitDexInstructionDataEncoder().encode(
      args as InitDexInstructionDataArgs,
    ),
    programAddress,
  } as InitDexInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountDexMetadata,
    TAccountToken0,
    TAccountToken1,
    TAccountSystemProgram
  >);
}

export type ParsedInitDexInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
    dex: TAccountMetas[2];
    dexMetadata: TAccountMetas[3];
    token0: TAccountMetas[4];
    token1: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: InitDexInstructionData;
};

export function parseInitDexInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitDexInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      dexAdmin: getNextAccount(),
      dex: getNextAccount(),
      dexMetadata: getNextAccount(),
      token0: getNextAccount(),
      token1: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitDexInstructionDataDecoder().decode(instruction.data),
  };
}
