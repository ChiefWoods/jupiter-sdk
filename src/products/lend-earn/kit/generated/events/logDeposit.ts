/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_DEPOSIT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([176, 243, 1, 56, 142, 206, 1, 106]);

export function getLogDepositEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_DEPOSIT_EVENT_DISCRIMINATOR,
  );
}

export type LogDepositEvent = {
  sender: Address;
  receiver: Address;
  assets: bigint;
  sharesMinted: bigint;
};

export type LogDepositEventArgs = {
  sender: Address;
  receiver: Address;
  assets: number | bigint;
  sharesMinted: number | bigint;
};

/** Gets the encoder for {@link LogDepositEventArgs} event data. */
export function getLogDepositEventEncoder(): FixedSizeEncoder<LogDepositEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["sender", getAddressEncoder()],
      ["receiver", getAddressEncoder()],
      ["assets", getU64Encoder()],
      ["sharesMinted", getU64Encoder()],
    ]),
    [getConstantEncoder(LOG_DEPOSIT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogDepositEvent} event data. */
export function getLogDepositEventDecoder(): FixedSizeDecoder<LogDepositEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["sender", getAddressDecoder()],
      ["receiver", getAddressDecoder()],
      ["assets", getU64Decoder()],
      ["sharesMinted", getU64Decoder()],
    ]),
    [getConstantDecoder(LOG_DEPOSIT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogDepositEvent} event data. */
export function getLogDepositEventCodec(): FixedSizeCodec<
  LogDepositEventArgs,
  LogDepositEvent
> {
  return combineCodec(getLogDepositEventEncoder(), getLogDepositEventDecoder());
}

/** Parses the provided bytes into {@link LogDepositEvent} event data. */
export function parseLogDepositEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogDepositEvent {
  if (containsBytes(data, LOG_DEPOSIT_EVENT_DISCRIMINATOR, 0)) {
    return getLogDepositEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogDepositEvent" event discriminators.',
  );
}
