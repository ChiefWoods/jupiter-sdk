/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const INVITE_INFO_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  221, 16, 207, 116, 22, 143, 244, 211,
]);

export function getInviteInfoDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INVITE_INFO_DISCRIMINATOR);
}

export type InviteInfo = {
  discriminator: ReadonlyUint8Array;
  sender: Address;
  expiry: bigint;
};

export type InviteInfoArgs = { sender: Address; expiry: number | bigint };

/** Gets the encoder for {@link InviteInfoArgs} account data. */
export function getInviteInfoEncoder(): FixedSizeEncoder<InviteInfoArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["sender", getAddressEncoder()],
      ["expiry", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INVITE_INFO_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link InviteInfo} account data. */
export function getInviteInfoDecoder(): FixedSizeDecoder<InviteInfo> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["sender", getAddressDecoder()],
    ["expiry", getI64Decoder()],
  ]);
}

/** Gets the codec for {@link InviteInfo} account data. */
export function getInviteInfoCodec(): FixedSizeCodec<
  InviteInfoArgs,
  InviteInfo
> {
  return combineCodec(getInviteInfoEncoder(), getInviteInfoDecoder());
}

export function decodeInviteInfo<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<InviteInfo, TAddress>;
export function decodeInviteInfo<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<InviteInfo, TAddress>;
export function decodeInviteInfo<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<InviteInfo, TAddress> | MaybeAccount<InviteInfo, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getInviteInfoDecoder(),
  );
}

export async function fetchInviteInfo<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<InviteInfo, TAddress>> {
  const maybeAccount = await fetchMaybeInviteInfo(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeInviteInfo<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<InviteInfo, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeInviteInfo(maybeAccount);
}

export async function fetchAllInviteInfo(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<InviteInfo>[]> {
  const maybeAccounts = await fetchAllMaybeInviteInfo(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeInviteInfo(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<InviteInfo>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeInviteInfo(maybeAccount));
}

export function getInviteInfoSize(): number {
  return 48;
}
