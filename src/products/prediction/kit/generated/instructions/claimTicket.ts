/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { PREDICTION_PROGRAM_ADDRESS } from "../programs";

export const CLAIM_TICKET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  122, 229, 144, 167, 42, 113, 16, 220,
]);

export function getClaimTicketDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_TICKET_DISCRIMINATOR,
  );
}

export type ClaimTicketInstruction<
  TProgram extends string = typeof PREDICTION_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountTicket extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountOwnerTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountOwner extends string
        ? ReadonlyAccount<TAccountOwner>
        : TAccountOwner,
      TAccountVault extends string
        ? ReadonlyAccount<TAccountVault>
        : TAccountVault,
      TAccountTicket extends string
        ? WritableAccount<TAccountTicket>
        : TAccountTicket,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountOwnerTokenAccount extends string
        ? WritableAccount<TAccountOwnerTokenAccount>
        : TAccountOwnerTokenAccount,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimTicketInstructionData = { discriminator: ReadonlyUint8Array };

export type ClaimTicketInstructionDataArgs = {};

export function getClaimTicketInstructionDataEncoder(): FixedSizeEncoder<ClaimTicketInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLAIM_TICKET_DISCRIMINATOR }),
  );
}

export function getClaimTicketInstructionDataDecoder(): FixedSizeDecoder<ClaimTicketInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClaimTicketInstructionDataCodec(): FixedSizeCodec<
  ClaimTicketInstructionDataArgs,
  ClaimTicketInstructionData
> {
  return combineCodec(
    getClaimTicketInstructionDataEncoder(),
    getClaimTicketInstructionDataDecoder(),
  );
}

export type ClaimTicketInput<
  TAccountAuthority extends string = string,
  TAccountOwner extends string = string,
  TAccountVault extends string = string,
  TAccountTicket extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountOwnerTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  owner: Address<TAccountOwner>;
  vault: Address<TAccountVault>;
  ticket: Address<TAccountTicket>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  ownerTokenAccount: Address<TAccountOwnerTokenAccount>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getClaimTicketInstruction<
  TAccountAuthority extends string,
  TAccountOwner extends string,
  TAccountVault extends string,
  TAccountTicket extends string,
  TAccountVaultTokenAccount extends string,
  TAccountOwnerTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof PREDICTION_PROGRAM_ADDRESS,
>(
  input: ClaimTicketInput<
    TAccountAuthority,
    TAccountOwner,
    TAccountVault,
    TAccountTicket,
    TAccountVaultTokenAccount,
    TAccountOwnerTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimTicketInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountOwner,
  TAccountVault,
  TAccountTicket,
  TAccountVaultTokenAccount,
  TAccountOwnerTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PREDICTION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: false },
    ticket: { value: input.ticket ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    ownerTokenAccount: {
      value: input.ownerTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("vault", accounts.vault),
      getAccountMeta("ticket", accounts.ticket),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("ownerTokenAccount", accounts.ownerTokenAccount),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getClaimTicketInstructionDataEncoder().encode({}),
    programAddress,
  } as ClaimTicketInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountOwner,
    TAccountVault,
    TAccountTicket,
    TAccountVaultTokenAccount,
    TAccountOwnerTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedClaimTicketInstruction<
  TProgram extends string = typeof PREDICTION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    owner: TAccountMetas[1];
    vault: TAccountMetas[2];
    ticket: TAccountMetas[3];
    vaultTokenAccount: TAccountMetas[4];
    ownerTokenAccount: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
  };
  data: ClaimTicketInstructionData;
};

export function parseClaimTicketInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimTicketInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      owner: getNextAccount(),
      vault: getNextAccount(),
      ticket: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      ownerTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getClaimTicketInstructionDataDecoder().decode(instruction.data),
  };
}
