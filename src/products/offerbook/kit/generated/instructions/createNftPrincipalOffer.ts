/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findEventAuthorityPda } from "../pdas";
import { OFFERBOOK_PROGRAM_ADDRESS } from "../programs";
import {
  getNftCollateralDecoder,
  getNftCollateralEncoder,
  type NftCollateral,
  type NftCollateralArgs,
} from "../types";

export const CREATE_NFT_PRINCIPAL_OFFER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([2, 36, 83, 39, 177, 98, 126, 75]);

export function getCreateNftPrincipalOfferDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_NFT_PRINCIPAL_OFFER_DISCRIMINATOR,
  );
}

export type CreateNftPrincipalOfferInstruction<
  TProgram extends string = typeof OFFERBOOK_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSignerUser extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountOffer extends string | AccountMeta<string> = string,
  TAccountPrincipalMint extends string | AccountMeta<string> = string,
  TAccountCounteredOffer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSignerUser extends string
        ? WritableAccount<TAccountSignerUser>
        : TAccountSignerUser,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountOffer extends string
        ? WritableAccount<TAccountOffer>
        : TAccountOffer,
      TAccountPrincipalMint extends string
        ? ReadonlyAccount<TAccountPrincipalMint>
        : TAccountPrincipalMint,
      TAccountCounteredOffer extends string
        ? ReadonlyAccount<TAccountCounteredOffer>
        : TAccountCounteredOffer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountEventAuthority extends string
        ? ReadonlyAccount<TAccountEventAuthority>
        : TAccountEventAuthority,
      TAccountProgram extends string
        ? ReadonlyAccount<TAccountProgram>
        : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateNftPrincipalOfferInstructionData = {
  discriminator: ReadonlyUint8Array;
  principalAmount: bigint;
  apy: number;
  duration: number;
  expiry: number;
  allowExtend: boolean;
  collateral: NftCollateral;
};

export type CreateNftPrincipalOfferInstructionDataArgs = {
  principalAmount: number | bigint;
  apy: number;
  duration: number;
  expiry: number;
  allowExtend: boolean;
  collateral: NftCollateralArgs;
};

export function getCreateNftPrincipalOfferInstructionDataEncoder(): Encoder<CreateNftPrincipalOfferInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["principalAmount", getU64Encoder()],
      ["apy", getU32Encoder()],
      ["duration", getU32Encoder()],
      ["expiry", getU32Encoder()],
      ["allowExtend", getBooleanEncoder()],
      ["collateral", getNftCollateralEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CREATE_NFT_PRINCIPAL_OFFER_DISCRIMINATOR,
    }),
  );
}

export function getCreateNftPrincipalOfferInstructionDataDecoder(): Decoder<CreateNftPrincipalOfferInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["principalAmount", getU64Decoder()],
    ["apy", getU32Decoder()],
    ["duration", getU32Decoder()],
    ["expiry", getU32Decoder()],
    ["allowExtend", getBooleanDecoder()],
    ["collateral", getNftCollateralDecoder()],
  ]);
}

export function getCreateNftPrincipalOfferInstructionDataCodec(): Codec<
  CreateNftPrincipalOfferInstructionDataArgs,
  CreateNftPrincipalOfferInstructionData
> {
  return combineCodec(
    getCreateNftPrincipalOfferInstructionDataEncoder(),
    getCreateNftPrincipalOfferInstructionDataDecoder(),
  );
}

export type CreateNftPrincipalOfferAsyncInput<
  TAccountSigner extends string = string,
  TAccountSignerUser extends string = string,
  TAccountConfig extends string = string,
  TAccountOffer extends string = string,
  TAccountPrincipalMint extends string = string,
  TAccountCounteredOffer extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** The lender creating the offer; pays for the offer account allocation. */
  signer: TransactionSigner<TAccountSigner>;
  /**
   * Registered user profile for the signer.
   * We want to ensure that the signer is a registered user before creating an offer.
   */
  signerUser: Address<TAccountSignerUser>;
  /**
   * Global protocol configuration that defines valid offer parameter ranges
   * (duration, expiry, APY, minimum amounts) and pause state.
   */
  config: Address<TAccountConfig>;
  /** Newly initialized offer account that will store all offer terms. */
  offer: Address<TAccountOffer>;
  /** The token mint the lender is offering to lend. */
  principalMint: Address<TAccountPrincipalMint>;
  /**
   * Optional: the offer this one is countering. When present, must be
   * on the opposite side (Collateral), created by someone other than
   * the signer, still fillable (Active or PartiallyFilled), and for the
   * same principal token. The collateral asset is carried in the IX
   * params rather than as an account, so collateral-side consistency
   * isn't enforceable at the constraint level.
   * When absent, the field stays at Pubkey::default().
   */
  counteredOffer?: Address<TAccountCounteredOffer>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority?: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  principalAmount: CreateNftPrincipalOfferInstructionDataArgs["principalAmount"];
  apy: CreateNftPrincipalOfferInstructionDataArgs["apy"];
  duration: CreateNftPrincipalOfferInstructionDataArgs["duration"];
  expiry: CreateNftPrincipalOfferInstructionDataArgs["expiry"];
  allowExtend: CreateNftPrincipalOfferInstructionDataArgs["allowExtend"];
  collateral: CreateNftPrincipalOfferInstructionDataArgs["collateral"];
};

export async function getCreateNftPrincipalOfferInstructionAsync<
  TAccountSigner extends string,
  TAccountSignerUser extends string,
  TAccountConfig extends string,
  TAccountOffer extends string,
  TAccountPrincipalMint extends string,
  TAccountCounteredOffer extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof OFFERBOOK_PROGRAM_ADDRESS,
>(
  input: CreateNftPrincipalOfferAsyncInput<
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateNftPrincipalOfferInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? OFFERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    signerUser: { value: input.signerUser ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    offer: { value: input.offer ?? null, isWritable: true },
    principalMint: { value: input.principalMint ?? null, isWritable: false },
    counteredOffer: { value: input.counteredOffer ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.eventAuthority.value) {
    accounts.eventAuthority.value = await findEventAuthorityPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("signerUser", accounts.signerUser),
      getAccountMeta("config", accounts.config),
      getAccountMeta("offer", accounts.offer),
      getAccountMeta("principalMint", accounts.principalMint),
      getAccountMeta("counteredOffer", accounts.counteredOffer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getCreateNftPrincipalOfferInstructionDataEncoder().encode(
      args as CreateNftPrincipalOfferInstructionDataArgs,
    ),
    programAddress,
  } as CreateNftPrincipalOfferInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type CreateNftPrincipalOfferInput<
  TAccountSigner extends string = string,
  TAccountSignerUser extends string = string,
  TAccountConfig extends string = string,
  TAccountOffer extends string = string,
  TAccountPrincipalMint extends string = string,
  TAccountCounteredOffer extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** The lender creating the offer; pays for the offer account allocation. */
  signer: TransactionSigner<TAccountSigner>;
  /**
   * Registered user profile for the signer.
   * We want to ensure that the signer is a registered user before creating an offer.
   */
  signerUser: Address<TAccountSignerUser>;
  /**
   * Global protocol configuration that defines valid offer parameter ranges
   * (duration, expiry, APY, minimum amounts) and pause state.
   */
  config: Address<TAccountConfig>;
  /** Newly initialized offer account that will store all offer terms. */
  offer: Address<TAccountOffer>;
  /** The token mint the lender is offering to lend. */
  principalMint: Address<TAccountPrincipalMint>;
  /**
   * Optional: the offer this one is countering. When present, must be
   * on the opposite side (Collateral), created by someone other than
   * the signer, still fillable (Active or PartiallyFilled), and for the
   * same principal token. The collateral asset is carried in the IX
   * params rather than as an account, so collateral-side consistency
   * isn't enforceable at the constraint level.
   * When absent, the field stays at Pubkey::default().
   */
  counteredOffer?: Address<TAccountCounteredOffer>;
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  principalAmount: CreateNftPrincipalOfferInstructionDataArgs["principalAmount"];
  apy: CreateNftPrincipalOfferInstructionDataArgs["apy"];
  duration: CreateNftPrincipalOfferInstructionDataArgs["duration"];
  expiry: CreateNftPrincipalOfferInstructionDataArgs["expiry"];
  allowExtend: CreateNftPrincipalOfferInstructionDataArgs["allowExtend"];
  collateral: CreateNftPrincipalOfferInstructionDataArgs["collateral"];
};

export function getCreateNftPrincipalOfferInstruction<
  TAccountSigner extends string,
  TAccountSignerUser extends string,
  TAccountConfig extends string,
  TAccountOffer extends string,
  TAccountPrincipalMint extends string,
  TAccountCounteredOffer extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof OFFERBOOK_PROGRAM_ADDRESS,
>(
  input: CreateNftPrincipalOfferInput<
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateNftPrincipalOfferInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountSignerUser,
  TAccountConfig,
  TAccountOffer,
  TAccountPrincipalMint,
  TAccountCounteredOffer,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? OFFERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    signerUser: { value: input.signerUser ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    offer: { value: input.offer ?? null, isWritable: true },
    principalMint: { value: input.principalMint ?? null, isWritable: false },
    counteredOffer: { value: input.counteredOffer ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("signerUser", accounts.signerUser),
      getAccountMeta("config", accounts.config),
      getAccountMeta("offer", accounts.offer),
      getAccountMeta("principalMint", accounts.principalMint),
      getAccountMeta("counteredOffer", accounts.counteredOffer),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("eventAuthority", accounts.eventAuthority),
      getAccountMeta("program", accounts.program),
    ],
    data: getCreateNftPrincipalOfferInstructionDataEncoder().encode(
      args as CreateNftPrincipalOfferInstructionDataArgs,
    ),
    programAddress,
  } as CreateNftPrincipalOfferInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountConfig,
    TAccountOffer,
    TAccountPrincipalMint,
    TAccountCounteredOffer,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedCreateNftPrincipalOfferInstruction<
  TProgram extends string = typeof OFFERBOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The lender creating the offer; pays for the offer account allocation. */
    signer: TAccountMetas[0];
    /**
     * Registered user profile for the signer.
     * We want to ensure that the signer is a registered user before creating an offer.
     */
    signerUser: TAccountMetas[1];
    /**
     * Global protocol configuration that defines valid offer parameter ranges
     * (duration, expiry, APY, minimum amounts) and pause state.
     */
    config: TAccountMetas[2];
    /** Newly initialized offer account that will store all offer terms. */
    offer: TAccountMetas[3];
    /** The token mint the lender is offering to lend. */
    principalMint: TAccountMetas[4];
    /**
     * Optional: the offer this one is countering. When present, must be
     * on the opposite side (Collateral), created by someone other than
     * the signer, still fillable (Active or PartiallyFilled), and for the
     * same principal token. The collateral asset is carried in the IX
     * params rather than as an account, so collateral-side consistency
     * isn't enforceable at the constraint level.
     * When absent, the field stays at Pubkey::default().
     */
    counteredOffer?: TAccountMetas[5] | undefined;
    systemProgram: TAccountMetas[6];
    eventAuthority: TAccountMetas[7];
    program: TAccountMetas[8];
  };
  data: CreateNftPrincipalOfferInstructionData;
};

export function parseCreateNftPrincipalOfferInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateNftPrincipalOfferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === OFFERBOOK_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      signerUser: getNextAccount(),
      config: getNextAccount(),
      offer: getNextAccount(),
      principalMint: getNextAccount(),
      counteredOffer: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getCreateNftPrincipalOfferInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
