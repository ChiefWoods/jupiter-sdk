/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findLendingAdminPda } from "../pdas";
import { LEND_EARN_PROGRAM_ADDRESS } from "../programs";

export const INIT_LENDING_ADMIN_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([203, 185, 241, 165, 56, 254, 33, 9]);

export function getInitLendingAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_LENDING_ADMIN_DISCRIMINATOR,
  );
}

export type InitLendingAdminInstruction<
  TProgram extends string = typeof LEND_EARN_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountLendingAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountLendingAdmin extends string
        ? WritableAccount<TAccountLendingAdmin>
        : TAccountLendingAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitLendingAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  liquidityProgram: Address;
  rebalancer: Address;
  authority: Address;
};

export type InitLendingAdminInstructionDataArgs = {
  liquidityProgram: Address;
  rebalancer: Address;
  authority: Address;
};

export function getInitLendingAdminInstructionDataEncoder(): FixedSizeEncoder<InitLendingAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["liquidityProgram", getAddressEncoder()],
      ["rebalancer", getAddressEncoder()],
      ["authority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_LENDING_ADMIN_DISCRIMINATOR }),
  );
}

export function getInitLendingAdminInstructionDataDecoder(): FixedSizeDecoder<InitLendingAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["liquidityProgram", getAddressDecoder()],
    ["rebalancer", getAddressDecoder()],
    ["authority", getAddressDecoder()],
  ]);
}

export function getInitLendingAdminInstructionDataCodec(): FixedSizeCodec<
  InitLendingAdminInstructionDataArgs,
  InitLendingAdminInstructionData
> {
  return combineCodec(
    getInitLendingAdminInstructionDataEncoder(),
    getInitLendingAdminInstructionDataDecoder(),
  );
}

export type InitLendingAdminAsyncInput<
  TAccountAuthority extends string = string,
  TAccountLendingAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  lendingAdmin?: Address<TAccountLendingAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  liquidityProgram: InitLendingAdminInstructionDataArgs["liquidityProgram"];
  rebalancer: InitLendingAdminInstructionDataArgs["rebalancer"];
  authorityArg: InitLendingAdminInstructionDataArgs["authority"];
};

export async function getInitLendingAdminInstructionAsync<
  TAccountAuthority extends string,
  TAccountLendingAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_EARN_PROGRAM_ADDRESS,
>(
  input: InitLendingAdminAsyncInput<
    TAccountAuthority,
    TAccountLendingAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitLendingAdminInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountLendingAdmin,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_EARN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    lendingAdmin: { value: input.lendingAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input, authority: input.authorityArg };

  // Resolve default values.
  if (!accounts.lendingAdmin.value) {
    accounts.lendingAdmin.value = await findLendingAdminPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("lendingAdmin", accounts.lendingAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitLendingAdminInstructionDataEncoder().encode(
      args as InitLendingAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitLendingAdminInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountLendingAdmin,
    TAccountSystemProgram
  >);
}

export type InitLendingAdminInput<
  TAccountAuthority extends string = string,
  TAccountLendingAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  lendingAdmin: Address<TAccountLendingAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  liquidityProgram: InitLendingAdminInstructionDataArgs["liquidityProgram"];
  rebalancer: InitLendingAdminInstructionDataArgs["rebalancer"];
  authorityArg: InitLendingAdminInstructionDataArgs["authority"];
};

export function getInitLendingAdminInstruction<
  TAccountAuthority extends string,
  TAccountLendingAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_EARN_PROGRAM_ADDRESS,
>(
  input: InitLendingAdminInput<
    TAccountAuthority,
    TAccountLendingAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitLendingAdminInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountLendingAdmin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_EARN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    lendingAdmin: { value: input.lendingAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input, authority: input.authorityArg };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("lendingAdmin", accounts.lendingAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitLendingAdminInstructionDataEncoder().encode(
      args as InitLendingAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitLendingAdminInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountLendingAdmin,
    TAccountSystemProgram
  >);
}

export type ParsedInitLendingAdminInstruction<
  TProgram extends string = typeof LEND_EARN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    lendingAdmin: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitLendingAdminInstructionData;
};

export function parseInitLendingAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitLendingAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      lendingAdmin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitLendingAdminInstructionDataDecoder().decode(instruction.data),
  };
}
