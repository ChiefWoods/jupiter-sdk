//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

pub const INIT_LENDING_ADMIN_DISCRIMINATOR: [u8; 8] = [203, 185, 241, 165, 56, 254, 33, 9];

/// Accounts.
#[derive(Debug)]
pub struct InitLendingAdmin {
    pub authority: solana_address::Address,

    pub lending_admin: solana_address::Address,

    pub system_program: solana_address::Address,
}

impl InitLendingAdmin {
    pub fn instruction(
        &self,
        args: InitLendingAdminInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: InitLendingAdminInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.authority, true));
        accounts.push(solana_instruction::AccountMeta::new(
            self.lending_admin,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = InitLendingAdminInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::LEND_EARN_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitLendingAdminInstructionData {
    discriminator: [u8; 8],
}

impl InitLendingAdminInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [203, 185, 241, 165, 56, 254, 33, 9],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for InitLendingAdminInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitLendingAdminInstructionArgs {
    pub liquidity_program: Address,
    pub rebalancer: Address,
    pub authority_arg: Address,
}

impl InitLendingAdminInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `InitLendingAdmin`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[writable]` lending_admin
///   2. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct InitLendingAdminBuilder {
    authority: Option<solana_address::Address>,
    lending_admin: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    liquidity_program: Option<Address>,
    rebalancer: Option<Address>,
    authority_arg: Option<Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl InitLendingAdminBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_address::Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn lending_admin(&mut self, lending_admin: solana_address::Address) -> &mut Self {
        self.lending_admin = Some(lending_admin);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn liquidity_program(&mut self, liquidity_program: Address) -> &mut Self {
        self.liquidity_program = Some(liquidity_program);
        self
    }
    #[inline(always)]
    pub fn rebalancer(&mut self, rebalancer: Address) -> &mut Self {
        self.rebalancer = Some(rebalancer);
        self
    }
    #[inline(always)]
    pub fn authority_arg(&mut self, authority_arg: Address) -> &mut Self {
        self.authority_arg = Some(authority_arg);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = InitLendingAdmin {
            authority: self.authority.expect("authority is not set"),
            lending_admin: self.lending_admin.expect("lending_admin is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = InitLendingAdminInstructionArgs {
            liquidity_program: self
                .liquidity_program
                .clone()
                .expect("liquidity_program is not set"),
            rebalancer: self.rebalancer.clone().expect("rebalancer is not set"),
            authority_arg: self
                .authority_arg
                .clone()
                .expect("authority_arg is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `init_lending_admin` CPI accounts.
pub struct InitLendingAdminCpiAccounts<'a, 'b> {
    pub authority: &'b solana_account_info::AccountInfo<'a>,

    pub lending_admin: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `init_lending_admin` CPI instruction.
pub struct InitLendingAdminCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub authority: &'b solana_account_info::AccountInfo<'a>,

    pub lending_admin: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: InitLendingAdminInstructionArgs,
}

impl<'a, 'b> InitLendingAdminCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: InitLendingAdminCpiAccounts<'a, 'b>,
        args: InitLendingAdminInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            lending_admin: accounts.lending_admin,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.lending_admin.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = InitLendingAdminInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::LEND_EARN_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.lending_admin.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `InitLendingAdmin` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[writable]` lending_admin
///   2. `[]` system_program
#[derive(Clone, Debug)]
pub struct InitLendingAdminCpiBuilder<'a, 'b> {
    instruction: Box<InitLendingAdminCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> InitLendingAdminCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(InitLendingAdminCpiBuilderInstruction {
            __program: program,
            authority: None,
            lending_admin: None,
            system_program: None,
            liquidity_program: None,
            rebalancer: None,
            authority_arg: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn lending_admin(
        &mut self,
        lending_admin: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.lending_admin = Some(lending_admin);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn liquidity_program(&mut self, liquidity_program: Address) -> &mut Self {
        self.instruction.liquidity_program = Some(liquidity_program);
        self
    }
    #[inline(always)]
    pub fn rebalancer(&mut self, rebalancer: Address) -> &mut Self {
        self.instruction.rebalancer = Some(rebalancer);
        self
    }
    #[inline(always)]
    pub fn authority_arg(&mut self, authority_arg: Address) -> &mut Self {
        self.instruction.authority_arg = Some(authority_arg);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = InitLendingAdminInstructionArgs {
            liquidity_program: self
                .instruction
                .liquidity_program
                .clone()
                .expect("liquidity_program is not set"),
            rebalancer: self
                .instruction
                .rebalancer
                .clone()
                .expect("rebalancer is not set"),
            authority_arg: self
                .instruction
                .authority_arg
                .clone()
                .expect("authority_arg is not set"),
        };
        let instruction = InitLendingAdminCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            lending_admin: self
                .instruction
                .lending_admin
                .expect("lending_admin is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct InitLendingAdminCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    lending_admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    liquidity_program: Option<Address>,
    rebalancer: Option<Address>,
    authority_arg: Option<Address>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
