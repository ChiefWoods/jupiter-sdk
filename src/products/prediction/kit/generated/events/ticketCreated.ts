/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const TICKET_CREATED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([122, 222, 128, 213, 63, 50, 51, 110]);

export function getTicketCreatedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TICKET_CREATED_EVENT_DISCRIMINATOR,
  );
}

export type TicketCreatedEvent = {
  ticket: Address;
  owner: Address;
  ticketId: string;
  marketId: string;
  stakeUsd: bigint;
  timestamp: bigint;
};

export type TicketCreatedEventArgs = {
  ticket: Address;
  owner: Address;
  ticketId: string;
  marketId: string;
  stakeUsd: number | bigint;
  timestamp: number | bigint;
};

/** Gets the encoder for {@link TicketCreatedEventArgs} event data. */
export function getTicketCreatedEventEncoder(): Encoder<TicketCreatedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["ticket", getAddressEncoder()],
      ["owner", getAddressEncoder()],
      ["ticketId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["marketId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["stakeUsd", getU64Encoder()],
      ["timestamp", getI64Encoder()],
    ]),
    [getConstantEncoder(TICKET_CREATED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link TicketCreatedEvent} event data. */
export function getTicketCreatedEventDecoder(): Decoder<TicketCreatedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["ticket", getAddressDecoder()],
      ["owner", getAddressDecoder()],
      ["ticketId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["marketId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["stakeUsd", getU64Decoder()],
      ["timestamp", getI64Decoder()],
    ]),
    [getConstantDecoder(TICKET_CREATED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link TicketCreatedEvent} event data. */
export function getTicketCreatedEventCodec(): Codec<
  TicketCreatedEventArgs,
  TicketCreatedEvent
> {
  return combineCodec(
    getTicketCreatedEventEncoder(),
    getTicketCreatedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link TicketCreatedEvent} event data. */
export function parseTicketCreatedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): TicketCreatedEvent {
  if (containsBytes(data, TICKET_CREATED_EVENT_DISCRIMINATOR, 0)) {
    return getTicketCreatedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "TicketCreatedEvent" event discriminators.',
  );
}
