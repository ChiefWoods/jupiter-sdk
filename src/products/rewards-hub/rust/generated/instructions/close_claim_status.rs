//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CLOSE_CLAIM_STATUS_DISCRIMINATOR: [u8; 8] = [163, 214, 191, 165, 245, 188, 17, 185];

/// Accounts.
#[derive(Debug)]
pub struct CloseClaimStatus {
    /// Campaign
    pub campaign: solana_address::Address,

    pub claim_status: solana_address::Address,

    pub closer: solana_address::Address,

    pub claimant: solana_address::Address,
}

impl CloseClaimStatus {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.campaign,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.claim_status,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.closer,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.claimant, false));
        accounts.extend_from_slice(remaining_accounts);
        let data = CloseClaimStatusInstructionData::new().try_to_vec().unwrap();

        solana_instruction::Instruction {
            program_id: crate::REWARDS_HUB_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CloseClaimStatusInstructionData {
    discriminator: [u8; 8],
}

impl CloseClaimStatusInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [163, 214, 191, 165, 245, 188, 17, 185],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CloseClaimStatusInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `CloseClaimStatus`.
///
/// ### Accounts:
///
///   0. `[]` campaign
///   1. `[writable]` claim_status
///   2. `[signer]` closer
///   3. `[writable]` claimant
#[derive(Clone, Debug, Default)]
pub struct CloseClaimStatusBuilder {
    campaign: Option<solana_address::Address>,
    claim_status: Option<solana_address::Address>,
    closer: Option<solana_address::Address>,
    claimant: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CloseClaimStatusBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Campaign
    #[inline(always)]
    pub fn campaign(&mut self, campaign: solana_address::Address) -> &mut Self {
        self.campaign = Some(campaign);
        self
    }
    #[inline(always)]
    pub fn claim_status(&mut self, claim_status: solana_address::Address) -> &mut Self {
        self.claim_status = Some(claim_status);
        self
    }
    #[inline(always)]
    pub fn closer(&mut self, closer: solana_address::Address) -> &mut Self {
        self.closer = Some(closer);
        self
    }
    #[inline(always)]
    pub fn claimant(&mut self, claimant: solana_address::Address) -> &mut Self {
        self.claimant = Some(claimant);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CloseClaimStatus {
            campaign: self.campaign.expect("campaign is not set"),
            claim_status: self.claim_status.expect("claim_status is not set"),
            closer: self.closer.expect("closer is not set"),
            claimant: self.claimant.expect("claimant is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `close_claim_status` CPI accounts.
pub struct CloseClaimStatusCpiAccounts<'a, 'b> {
    /// Campaign
    pub campaign: &'b solana_account_info::AccountInfo<'a>,

    pub claim_status: &'b solana_account_info::AccountInfo<'a>,

    pub closer: &'b solana_account_info::AccountInfo<'a>,

    pub claimant: &'b solana_account_info::AccountInfo<'a>,
}

/// `close_claim_status` CPI instruction.
pub struct CloseClaimStatusCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Campaign
    pub campaign: &'b solana_account_info::AccountInfo<'a>,

    pub claim_status: &'b solana_account_info::AccountInfo<'a>,

    pub closer: &'b solana_account_info::AccountInfo<'a>,

    pub claimant: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> CloseClaimStatusCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CloseClaimStatusCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            campaign: accounts.campaign,
            claim_status: accounts.claim_status,
            closer: accounts.closer,
            claimant: accounts.claimant,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.campaign.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.claim_status.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.closer.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.claimant.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let data = CloseClaimStatusInstructionData::new().try_to_vec().unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::REWARDS_HUB_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.campaign.clone());
        account_infos.push(self.claim_status.clone());
        account_infos.push(self.closer.clone());
        account_infos.push(self.claimant.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CloseClaimStatus` via CPI.
///
/// ### Accounts:
///
///   0. `[]` campaign
///   1. `[writable]` claim_status
///   2. `[signer]` closer
///   3. `[writable]` claimant
#[derive(Clone, Debug)]
pub struct CloseClaimStatusCpiBuilder<'a, 'b> {
    instruction: Box<CloseClaimStatusCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CloseClaimStatusCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CloseClaimStatusCpiBuilderInstruction {
            __program: program,
            campaign: None,
            claim_status: None,
            closer: None,
            claimant: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Campaign
    #[inline(always)]
    pub fn campaign(&mut self, campaign: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.campaign = Some(campaign);
        self
    }
    #[inline(always)]
    pub fn claim_status(
        &mut self,
        claim_status: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.claim_status = Some(claim_status);
        self
    }
    #[inline(always)]
    pub fn closer(&mut self, closer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.closer = Some(closer);
        self
    }
    #[inline(always)]
    pub fn claimant(&mut self, claimant: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.claimant = Some(claimant);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = CloseClaimStatusCpi {
            __program: self.instruction.__program,

            campaign: self.instruction.campaign.expect("campaign is not set"),

            claim_status: self
                .instruction
                .claim_status
                .expect("claim_status is not set"),

            closer: self.instruction.closer.expect("closer is not set"),

            claimant: self.instruction.claimant.expect("claimant is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CloseClaimStatusCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    campaign: Option<&'b solana_account_info::AccountInfo<'a>>,
    claim_status: Option<&'b solana_account_info::AccountInfo<'a>>,
    closer: Option<&'b solana_account_info::AccountInfo<'a>>,
    claimant: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
