/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findLendingRewardsAdminPda } from "../pdas";
import { LEND_LENDING_REWARD_RATE_MODEL_PROGRAM_ADDRESS } from "../programs";

export const INIT_LENDING_REWARDS_ADMIN_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([202, 36, 47, 209, 3, 201, 173, 94]);

export function getInitLendingRewardsAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_LENDING_REWARDS_ADMIN_DISCRIMINATOR,
  );
}

export type InitLendingRewardsAdminInstruction<
  TProgram extends string =
    typeof LEND_LENDING_REWARD_RATE_MODEL_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountLendingRewardsAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountLendingRewardsAdmin extends string
        ? WritableAccount<TAccountLendingRewardsAdmin>
        : TAccountLendingRewardsAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitLendingRewardsAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  lendingProgram: Address;
};

export type InitLendingRewardsAdminInstructionDataArgs = {
  authority: Address;
  lendingProgram: Address;
};

export function getInitLendingRewardsAdminInstructionDataEncoder(): FixedSizeEncoder<InitLendingRewardsAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["lendingProgram", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INIT_LENDING_REWARDS_ADMIN_DISCRIMINATOR,
    }),
  );
}

export function getInitLendingRewardsAdminInstructionDataDecoder(): FixedSizeDecoder<InitLendingRewardsAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["lendingProgram", getAddressDecoder()],
  ]);
}

export function getInitLendingRewardsAdminInstructionDataCodec(): FixedSizeCodec<
  InitLendingRewardsAdminInstructionDataArgs,
  InitLendingRewardsAdminInstructionData
> {
  return combineCodec(
    getInitLendingRewardsAdminInstructionDataEncoder(),
    getInitLendingRewardsAdminInstructionDataDecoder(),
  );
}

export type InitLendingRewardsAdminAsyncInput<
  TAccountSigner extends string = string,
  TAccountLendingRewardsAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  lendingRewardsAdmin?: Address<TAccountLendingRewardsAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitLendingRewardsAdminInstructionDataArgs["authority"];
  lendingProgram: InitLendingRewardsAdminInstructionDataArgs["lendingProgram"];
};

export async function getInitLendingRewardsAdminInstructionAsync<
  TAccountSigner extends string,
  TAccountLendingRewardsAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address =
    typeof LEND_LENDING_REWARD_RATE_MODEL_PROGRAM_ADDRESS,
>(
  input: InitLendingRewardsAdminAsyncInput<
    TAccountSigner,
    TAccountLendingRewardsAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitLendingRewardsAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLendingRewardsAdmin,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LENDING_REWARD_RATE_MODEL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    lendingRewardsAdmin: {
      value: input.lendingRewardsAdmin ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.lendingRewardsAdmin.value) {
    accounts.lendingRewardsAdmin.value = await findLendingRewardsAdminPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("lendingRewardsAdmin", accounts.lendingRewardsAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitLendingRewardsAdminInstructionDataEncoder().encode(
      args as InitLendingRewardsAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitLendingRewardsAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLendingRewardsAdmin,
    TAccountSystemProgram
  >);
}

export type InitLendingRewardsAdminInput<
  TAccountSigner extends string = string,
  TAccountLendingRewardsAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  lendingRewardsAdmin: Address<TAccountLendingRewardsAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  authority: InitLendingRewardsAdminInstructionDataArgs["authority"];
  lendingProgram: InitLendingRewardsAdminInstructionDataArgs["lendingProgram"];
};

export function getInitLendingRewardsAdminInstruction<
  TAccountSigner extends string,
  TAccountLendingRewardsAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address =
    typeof LEND_LENDING_REWARD_RATE_MODEL_PROGRAM_ADDRESS,
>(
  input: InitLendingRewardsAdminInput<
    TAccountSigner,
    TAccountLendingRewardsAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitLendingRewardsAdminInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountLendingRewardsAdmin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LENDING_REWARD_RATE_MODEL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    lendingRewardsAdmin: {
      value: input.lendingRewardsAdmin ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("lendingRewardsAdmin", accounts.lendingRewardsAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitLendingRewardsAdminInstructionDataEncoder().encode(
      args as InitLendingRewardsAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitLendingRewardsAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLendingRewardsAdmin,
    TAccountSystemProgram
  >);
}

export type ParsedInitLendingRewardsAdminInstruction<
  TProgram extends string =
    typeof LEND_LENDING_REWARD_RATE_MODEL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    lendingRewardsAdmin: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitLendingRewardsAdminInstructionData;
};

export function parseInitLendingRewardsAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitLendingRewardsAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      lendingRewardsAdmin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitLendingRewardsAdminInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
