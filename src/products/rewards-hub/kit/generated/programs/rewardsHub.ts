/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/kit/program-client-core";
import {
  getCampaignCodec,
  getClaimStatusCodec,
  type Campaign,
  type CampaignArgs,
  type ClaimStatus,
  type ClaimStatusArgs,
} from "../accounts";
import {
  getClaimInstructionAsync,
  getClawbackInstruction,
  getCloseClaimStatusInstruction,
  getIdempotentClaimInstruction,
  getInitializeCampaignInstructionAsync,
  getInitializeClaimStatusInstructionAsync,
  getSetAdminInstruction,
  getSetClawbackReceiverInstruction,
  parseClaimInstruction,
  parseClawbackInstruction,
  parseCloseClaimStatusInstruction,
  parseIdempotentClaimInstruction,
  parseInitializeCampaignInstruction,
  parseInitializeClaimStatusInstruction,
  parseSetAdminInstruction,
  parseSetClawbackReceiverInstruction,
  type ClaimAsyncInput,
  type ClawbackInput,
  type CloseClaimStatusInput,
  type IdempotentClaimInput,
  type InitializeCampaignAsyncInput,
  type InitializeClaimStatusAsyncInput,
  type ParsedClaimInstruction,
  type ParsedClawbackInstruction,
  type ParsedCloseClaimStatusInstruction,
  type ParsedIdempotentClaimInstruction,
  type ParsedInitializeCampaignInstruction,
  type ParsedInitializeClaimStatusInstruction,
  type ParsedSetAdminInstruction,
  type ParsedSetClawbackReceiverInstruction,
  type SetAdminInput,
  type SetClawbackReceiverInput,
} from "../instructions";
import { findCampaignPda, findClaimStatusPda } from "../pdas";

export const REWARDS_HUB_PROGRAM_ADDRESS =
  "GenieRGuCtgfDGThwjp2GLreQMFtJoG1fqFE8MF1gAzG" as Address<"GenieRGuCtgfDGThwjp2GLreQMFtJoG1fqFE8MF1gAzG">;

export enum RewardsHubAccount {
  Campaign,
  ClaimStatus,
}

export function identifyRewardsHubAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): RewardsHubAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([50, 40, 49, 11, 157, 220, 229, 192]),
      ),
      0,
    )
  ) {
    return RewardsHubAccount.Campaign;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([22, 183, 249, 157, 247, 95, 150, 96]),
      ),
      0,
    )
  ) {
    return RewardsHubAccount.ClaimStatus;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "rewardsHub" },
  );
}

export enum RewardsHubEvent {
  CampaignAdminChangedEvent,
  CampaignClawbackReceiverChangedEvent,
  CampaignClosedEvent,
  CampaignInitializedEvent,
  ClaimEvent,
  ClawbackEvent,
  CloseClaimStatusEvent,
  InitializeClaimStatusEvent,
}

export function identifyRewardsHubEvent(
  event: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): RewardsHubEvent {
  const data = "data" in event ? event.data : event;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([49, 222, 104, 253, 31, 101, 68, 181]),
      ),
      0,
    )
  ) {
    return RewardsHubEvent.CampaignAdminChangedEvent;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([211, 249, 83, 95, 82, 97, 135, 100]),
      ),
      0,
    )
  ) {
    return RewardsHubEvent.CampaignClawbackReceiverChangedEvent;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([141, 16, 13, 204, 158, 184, 112, 201]),
      ),
      0,
    )
  ) {
    return RewardsHubEvent.CampaignClosedEvent;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([62, 247, 61, 179, 223, 177, 191, 212]),
      ),
      0,
    )
  ) {
    return RewardsHubEvent.CampaignInitializedEvent;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([93, 15, 70, 170, 48, 140, 212, 219]),
      ),
      0,
    )
  ) {
    return RewardsHubEvent.ClaimEvent;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([47, 23, 60, 84, 245, 114, 178, 169]),
      ),
      0,
    )
  ) {
    return RewardsHubEvent.ClawbackEvent;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([24, 49, 90, 170, 80, 200, 243, 186]),
      ),
      0,
    )
  ) {
    return RewardsHubEvent.CloseClaimStatusEvent;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([121, 167, 244, 185, 162, 207, 107, 234]),
      ),
      0,
    )
  ) {
    return RewardsHubEvent.InitializeClaimStatusEvent;
  }
  throw new Error(
    "The provided event could not be identified as a rewardsHub event.",
  );
}

export enum RewardsHubInstruction {
  Claim,
  Clawback,
  CloseClaimStatus,
  IdempotentClaim,
  InitializeCampaign,
  InitializeClaimStatus,
  SetAdmin,
  SetClawbackReceiver,
}

export function identifyRewardsHubInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): RewardsHubInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([62, 198, 214, 193, 213, 159, 108, 210]),
      ),
      0,
    )
  ) {
    return RewardsHubInstruction.Claim;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([111, 92, 142, 79, 33, 234, 82, 27]),
      ),
      0,
    )
  ) {
    return RewardsHubInstruction.Clawback;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([163, 214, 191, 165, 245, 188, 17, 185]),
      ),
      0,
    )
  ) {
    return RewardsHubInstruction.CloseClaimStatus;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([229, 117, 181, 84, 217, 200, 61, 150]),
      ),
      0,
    )
  ) {
    return RewardsHubInstruction.IdempotentClaim;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([169, 88, 7, 6, 9, 165, 65, 132]),
      ),
      0,
    )
  ) {
    return RewardsHubInstruction.InitializeCampaign;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([133, 6, 250, 212, 133, 145, 69, 136]),
      ),
      0,
    )
  ) {
    return RewardsHubInstruction.InitializeClaimStatus;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([251, 163, 0, 52, 91, 194, 187, 92]),
      ),
      0,
    )
  ) {
    return RewardsHubInstruction.SetAdmin;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([153, 217, 34, 20, 19, 29, 229, 75]),
      ),
      0,
    )
  ) {
    return RewardsHubInstruction.SetClawbackReceiver;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "rewardsHub" },
  );
}

export type ParsedRewardsHubInstruction<
  TProgram extends string = "GenieRGuCtgfDGThwjp2GLreQMFtJoG1fqFE8MF1gAzG",
> =
  | ({
      instructionType: RewardsHubInstruction.Claim;
    } & ParsedClaimInstruction<TProgram>)
  | ({
      instructionType: RewardsHubInstruction.Clawback;
    } & ParsedClawbackInstruction<TProgram>)
  | ({
      instructionType: RewardsHubInstruction.CloseClaimStatus;
    } & ParsedCloseClaimStatusInstruction<TProgram>)
  | ({
      instructionType: RewardsHubInstruction.IdempotentClaim;
    } & ParsedIdempotentClaimInstruction<TProgram>)
  | ({
      instructionType: RewardsHubInstruction.InitializeCampaign;
    } & ParsedInitializeCampaignInstruction<TProgram>)
  | ({
      instructionType: RewardsHubInstruction.InitializeClaimStatus;
    } & ParsedInitializeClaimStatusInstruction<TProgram>)
  | ({
      instructionType: RewardsHubInstruction.SetAdmin;
    } & ParsedSetAdminInstruction<TProgram>)
  | ({
      instructionType: RewardsHubInstruction.SetClawbackReceiver;
    } & ParsedSetClawbackReceiverInstruction<TProgram>);

export function parseRewardsHubInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedRewardsHubInstruction<TProgram> {
  const instructionType = identifyRewardsHubInstruction(instruction);
  switch (instructionType) {
    case RewardsHubInstruction.Claim: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: RewardsHubInstruction.Claim,
        ...parseClaimInstruction(instruction),
      };
    }
    case RewardsHubInstruction.Clawback: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: RewardsHubInstruction.Clawback,
        ...parseClawbackInstruction(instruction),
      };
    }
    case RewardsHubInstruction.CloseClaimStatus: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: RewardsHubInstruction.CloseClaimStatus,
        ...parseCloseClaimStatusInstruction(instruction),
      };
    }
    case RewardsHubInstruction.IdempotentClaim: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: RewardsHubInstruction.IdempotentClaim,
        ...parseIdempotentClaimInstruction(instruction),
      };
    }
    case RewardsHubInstruction.InitializeCampaign: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: RewardsHubInstruction.InitializeCampaign,
        ...parseInitializeCampaignInstruction(instruction),
      };
    }
    case RewardsHubInstruction.InitializeClaimStatus: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: RewardsHubInstruction.InitializeClaimStatus,
        ...parseInitializeClaimStatusInstruction(instruction),
      };
    }
    case RewardsHubInstruction.SetAdmin: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: RewardsHubInstruction.SetAdmin,
        ...parseSetAdminInstruction(instruction),
      };
    }
    case RewardsHubInstruction.SetClawbackReceiver: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: RewardsHubInstruction.SetClawbackReceiver,
        ...parseSetClawbackReceiverInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "rewardsHub",
        },
      );
  }
}

export type RewardsHubPlugin = {
  accounts: RewardsHubPluginAccounts;
  instructions: RewardsHubPluginInstructions;
  pdas: RewardsHubPluginPdas;
  identifyAccount: typeof identifyRewardsHubAccount;
  identifyInstruction: typeof identifyRewardsHubInstruction;
  parseInstruction: typeof parseRewardsHubInstruction;
};

export type RewardsHubPluginAccounts = {
  campaign: ReturnType<typeof getCampaignCodec> &
    SelfFetchFunctions<CampaignArgs, Campaign>;
  claimStatus: ReturnType<typeof getClaimStatusCodec> &
    SelfFetchFunctions<ClaimStatusArgs, ClaimStatus>;
};

export type RewardsHubPluginInstructions = {
  claim: (
    input: ClaimAsyncInput,
  ) => ReturnType<typeof getClaimInstructionAsync> & SelfPlanAndSendFunctions;
  clawback: (
    input: ClawbackInput,
  ) => ReturnType<typeof getClawbackInstruction> & SelfPlanAndSendFunctions;
  closeClaimStatus: (
    input: CloseClaimStatusInput,
  ) => ReturnType<typeof getCloseClaimStatusInstruction> &
    SelfPlanAndSendFunctions;
  idempotentClaim: (
    input: IdempotentClaimInput,
  ) => ReturnType<typeof getIdempotentClaimInstruction> &
    SelfPlanAndSendFunctions;
  initializeCampaign: (
    input: InitializeCampaignAsyncInput,
  ) => ReturnType<typeof getInitializeCampaignInstructionAsync> &
    SelfPlanAndSendFunctions;
  initializeClaimStatus: (
    input: InitializeClaimStatusAsyncInput,
  ) => ReturnType<typeof getInitializeClaimStatusInstructionAsync> &
    SelfPlanAndSendFunctions;
  setAdmin: (
    input: SetAdminInput,
  ) => ReturnType<typeof getSetAdminInstruction> & SelfPlanAndSendFunctions;
  setClawbackReceiver: (
    input: SetClawbackReceiverInput,
  ) => ReturnType<typeof getSetClawbackReceiverInstruction> &
    SelfPlanAndSendFunctions;
};

export type RewardsHubPluginPdas = {
  claimStatus: typeof findClaimStatusPda;
  campaign: typeof findCampaignPda;
};

export type RewardsHubPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function rewardsHubProgram() {
  return <T extends RewardsHubPluginRequirements>(
    client: T,
  ): ExtendedClient<T, { rewardsHub: RewardsHubPlugin }> => {
    return extendClient(client, {
      rewardsHub: <RewardsHubPlugin>{
        accounts: {
          campaign: addSelfFetchFunctions(client, getCampaignCodec()),
          claimStatus: addSelfFetchFunctions(client, getClaimStatusCodec()),
        },
        instructions: {
          claim: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getClaimInstructionAsync(input),
            ),
          clawback: (input) =>
            addSelfPlanAndSendFunctions(client, getClawbackInstruction(input)),
          closeClaimStatus: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCloseClaimStatusInstruction(input),
            ),
          idempotentClaim: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getIdempotentClaimInstruction(input),
            ),
          initializeCampaign: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeCampaignInstructionAsync(input),
            ),
          initializeClaimStatus: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeClaimStatusInstructionAsync(input),
            ),
          setAdmin: (input) =>
            addSelfPlanAndSendFunctions(client, getSetAdminInstruction(input)),
          setClawbackReceiver: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getSetClawbackReceiverInstruction(input),
            ),
        },
        pdas: { claimStatus: findClaimStatusPda, campaign: findCampaignPda },
        identifyAccount: identifyRewardsHubAccount,
        identifyInstruction: identifyRewardsHubInstruction,
        parseInstruction: parseRewardsHubInstruction,
      },
    });
  };
}
