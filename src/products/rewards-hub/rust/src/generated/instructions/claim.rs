//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CLAIM_DISCRIMINATOR: [u8; 8] = [62, 198, 214, 193, 213, 159, 108, 210];

/// Accounts.
#[derive(Debug)]
pub struct Claim {
    /// The [Campaign].
    pub campaign: solana_address::Address,
    /// Claim status PDA
    pub claim_status: solana_address::Address,
    /// Distributor ATA containing the tokens to distribute.
    pub from: solana_address::Address,
    /// Account to send the claimed tokens to.
    pub to: solana_address::Address,
    /// Who is claiming the tokens.
    pub claimant: solana_address::Address,
    /// Genie pubkey must sign the claim
    pub claims_pubkey: solana_address::Address,
    /// SPL [Token] program.
    pub token_program: solana_address::Address,
}

impl Claim {
    pub fn instruction(&self, args: ClaimInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: ClaimInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(7 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.campaign, false));
        accounts.push(solana_instruction::AccountMeta::new(
            self.claim_status,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.from, false));
        accounts.push(solana_instruction::AccountMeta::new(self.to, false));
        accounts.push(solana_instruction::AccountMeta::new(self.claimant, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.claims_pubkey,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.token_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = ClaimInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::GENIE_DISTRIBUTOR_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ClaimInstructionData {
    discriminator: [u8; 8],
}

impl ClaimInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [62, 198, 214, 193, 213, 159, 108, 210],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for ClaimInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct ClaimInstructionArgs {
    pub amount_unlocked: [u64; 5],
    pub lootbox_info: [u64; 5],
}

impl ClaimInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `Claim`.
///
/// ### Accounts:
///
///   0. `[writable]` campaign
///   1. `[writable]` claim_status
///   2. `[writable]` from
///   3. `[writable]` to
///   4. `[writable, signer]` claimant
///   5. `[signer]` claims_pubkey
///   6. `[optional]` token_program (default to `TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA`)
#[derive(Clone, Debug, Default)]
pub struct ClaimBuilder {
    campaign: Option<solana_address::Address>,
    claim_status: Option<solana_address::Address>,
    from: Option<solana_address::Address>,
    to: Option<solana_address::Address>,
    claimant: Option<solana_address::Address>,
    claims_pubkey: Option<solana_address::Address>,
    token_program: Option<solana_address::Address>,
    amount_unlocked: Option<[u64; 5]>,
    lootbox_info: Option<[u64; 5]>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl ClaimBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The [Campaign].
    #[inline(always)]
    pub fn campaign(&mut self, campaign: solana_address::Address) -> &mut Self {
        self.campaign = Some(campaign);
        self
    }
    /// Claim status PDA
    #[inline(always)]
    pub fn claim_status(&mut self, claim_status: solana_address::Address) -> &mut Self {
        self.claim_status = Some(claim_status);
        self
    }
    /// Distributor ATA containing the tokens to distribute.
    #[inline(always)]
    pub fn from(&mut self, from: solana_address::Address) -> &mut Self {
        self.from = Some(from);
        self
    }
    /// Account to send the claimed tokens to.
    #[inline(always)]
    pub fn to(&mut self, to: solana_address::Address) -> &mut Self {
        self.to = Some(to);
        self
    }
    /// Who is claiming the tokens.
    #[inline(always)]
    pub fn claimant(&mut self, claimant: solana_address::Address) -> &mut Self {
        self.claimant = Some(claimant);
        self
    }
    /// Genie pubkey must sign the claim
    #[inline(always)]
    pub fn claims_pubkey(&mut self, claims_pubkey: solana_address::Address) -> &mut Self {
        self.claims_pubkey = Some(claims_pubkey);
        self
    }
    /// `[optional account, default to 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA']`
    /// SPL [Token] program.
    #[inline(always)]
    pub fn token_program(&mut self, token_program: solana_address::Address) -> &mut Self {
        self.token_program = Some(token_program);
        self
    }
    #[inline(always)]
    pub fn amount_unlocked(&mut self, amount_unlocked: [u64; 5]) -> &mut Self {
        self.amount_unlocked = Some(amount_unlocked);
        self
    }
    #[inline(always)]
    pub fn lootbox_info(&mut self, lootbox_info: [u64; 5]) -> &mut Self {
        self.lootbox_info = Some(lootbox_info);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = Claim {
            campaign: self.campaign.expect("campaign is not set"),
            claim_status: self.claim_status.expect("claim_status is not set"),
            from: self.from.expect("from is not set"),
            to: self.to.expect("to is not set"),
            claimant: self.claimant.expect("claimant is not set"),
            claims_pubkey: self.claims_pubkey.expect("claims_pubkey is not set"),
            token_program: self.token_program.unwrap_or(solana_address::address!(
                "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"
            )),
        };
        let args = ClaimInstructionArgs {
            amount_unlocked: self
                .amount_unlocked
                .clone()
                .expect("amount_unlocked is not set"),
            lootbox_info: self.lootbox_info.clone().expect("lootbox_info is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `claim` CPI accounts.
pub struct ClaimCpiAccounts<'a, 'b> {
    /// The [Campaign].
    pub campaign: &'b solana_account_info::AccountInfo<'a>,
    /// Claim status PDA
    pub claim_status: &'b solana_account_info::AccountInfo<'a>,
    /// Distributor ATA containing the tokens to distribute.
    pub from: &'b solana_account_info::AccountInfo<'a>,
    /// Account to send the claimed tokens to.
    pub to: &'b solana_account_info::AccountInfo<'a>,
    /// Who is claiming the tokens.
    pub claimant: &'b solana_account_info::AccountInfo<'a>,
    /// Genie pubkey must sign the claim
    pub claims_pubkey: &'b solana_account_info::AccountInfo<'a>,
    /// SPL [Token] program.
    pub token_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `claim` CPI instruction.
pub struct ClaimCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The [Campaign].
    pub campaign: &'b solana_account_info::AccountInfo<'a>,
    /// Claim status PDA
    pub claim_status: &'b solana_account_info::AccountInfo<'a>,
    /// Distributor ATA containing the tokens to distribute.
    pub from: &'b solana_account_info::AccountInfo<'a>,
    /// Account to send the claimed tokens to.
    pub to: &'b solana_account_info::AccountInfo<'a>,
    /// Who is claiming the tokens.
    pub claimant: &'b solana_account_info::AccountInfo<'a>,
    /// Genie pubkey must sign the claim
    pub claims_pubkey: &'b solana_account_info::AccountInfo<'a>,
    /// SPL [Token] program.
    pub token_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: ClaimInstructionArgs,
}

impl<'a, 'b> ClaimCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: ClaimCpiAccounts<'a, 'b>,
        args: ClaimInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            campaign: accounts.campaign,
            claim_status: accounts.claim_status,
            from: accounts.from,
            to: accounts.to,
            claimant: accounts.claimant,
            claims_pubkey: accounts.claims_pubkey,
            token_program: accounts.token_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(7 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.campaign.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.claim_status.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.from.key, false));
        accounts.push(solana_instruction::AccountMeta::new(*self.to.key, false));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.claimant.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.claims_pubkey.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.token_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = ClaimInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::GENIE_DISTRIBUTOR_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(8 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.campaign.clone());
        account_infos.push(self.claim_status.clone());
        account_infos.push(self.from.clone());
        account_infos.push(self.to.clone());
        account_infos.push(self.claimant.clone());
        account_infos.push(self.claims_pubkey.clone());
        account_infos.push(self.token_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `Claim` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` campaign
///   1. `[writable]` claim_status
///   2. `[writable]` from
///   3. `[writable]` to
///   4. `[writable, signer]` claimant
///   5. `[signer]` claims_pubkey
///   6. `[]` token_program
#[derive(Clone, Debug)]
pub struct ClaimCpiBuilder<'a, 'b> {
    instruction: Box<ClaimCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> ClaimCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(ClaimCpiBuilderInstruction {
            __program: program,
            campaign: None,
            claim_status: None,
            from: None,
            to: None,
            claimant: None,
            claims_pubkey: None,
            token_program: None,
            amount_unlocked: None,
            lootbox_info: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The [Campaign].
    #[inline(always)]
    pub fn campaign(&mut self, campaign: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.campaign = Some(campaign);
        self
    }
    /// Claim status PDA
    #[inline(always)]
    pub fn claim_status(
        &mut self,
        claim_status: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.claim_status = Some(claim_status);
        self
    }
    /// Distributor ATA containing the tokens to distribute.
    #[inline(always)]
    pub fn from(&mut self, from: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.from = Some(from);
        self
    }
    /// Account to send the claimed tokens to.
    #[inline(always)]
    pub fn to(&mut self, to: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.to = Some(to);
        self
    }
    /// Who is claiming the tokens.
    #[inline(always)]
    pub fn claimant(&mut self, claimant: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.claimant = Some(claimant);
        self
    }
    /// Genie pubkey must sign the claim
    #[inline(always)]
    pub fn claims_pubkey(
        &mut self,
        claims_pubkey: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.claims_pubkey = Some(claims_pubkey);
        self
    }
    /// SPL [Token] program.
    #[inline(always)]
    pub fn token_program(
        &mut self,
        token_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.token_program = Some(token_program);
        self
    }
    #[inline(always)]
    pub fn amount_unlocked(&mut self, amount_unlocked: [u64; 5]) -> &mut Self {
        self.instruction.amount_unlocked = Some(amount_unlocked);
        self
    }
    #[inline(always)]
    pub fn lootbox_info(&mut self, lootbox_info: [u64; 5]) -> &mut Self {
        self.instruction.lootbox_info = Some(lootbox_info);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = ClaimInstructionArgs {
            amount_unlocked: self
                .instruction
                .amount_unlocked
                .clone()
                .expect("amount_unlocked is not set"),
            lootbox_info: self
                .instruction
                .lootbox_info
                .clone()
                .expect("lootbox_info is not set"),
        };
        let instruction = ClaimCpi {
            __program: self.instruction.__program,

            campaign: self.instruction.campaign.expect("campaign is not set"),

            claim_status: self
                .instruction
                .claim_status
                .expect("claim_status is not set"),

            from: self.instruction.from.expect("from is not set"),

            to: self.instruction.to.expect("to is not set"),

            claimant: self.instruction.claimant.expect("claimant is not set"),

            claims_pubkey: self
                .instruction
                .claims_pubkey
                .expect("claims_pubkey is not set"),

            token_program: self
                .instruction
                .token_program
                .expect("token_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct ClaimCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    campaign: Option<&'b solana_account_info::AccountInfo<'a>>,
    claim_status: Option<&'b solana_account_info::AccountInfo<'a>>,
    from: Option<&'b solana_account_info::AccountInfo<'a>>,
    to: Option<&'b solana_account_info::AccountInfo<'a>>,
    claimant: Option<&'b solana_account_info::AccountInfo<'a>>,
    claims_pubkey: Option<&'b solana_account_info::AccountInfo<'a>>,
    token_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    amount_unlocked: Option<[u64; 5]>,
    lootbox_info: Option<[u64; 5]>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
