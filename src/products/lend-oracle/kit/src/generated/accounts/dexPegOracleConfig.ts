/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU128Decoder,
  getU128Encoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getDexPegOracleKindDecoder,
  getDexPegOracleKindEncoder,
  getSourcesDecoder,
  getSourcesEncoder,
  type DexPegOracleKind,
  type DexPegOracleKindArgs,
  type Sources,
  type SourcesArgs,
} from "../types";

export const DEX_PEG_ORACLE_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([59, 189, 99, 149, 128, 5, 255, 72]);

export function getDexPegOracleConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEX_PEG_ORACLE_CONFIG_DISCRIMINATOR,
  );
}

export type DexPegOracleConfig = {
  discriminator: ReadonlyUint8Array;
  nonce: number;
  dex: Address;
  positionToken0: Address;
  positionToken1: Address;
  tokenReserve0: Address;
  tokenReserve1: Address;
  /**
   * Which token the combined reserves are denominated in (EVM
   * `QUOTE_IN_TOKEN0`). Irrelevant for pure 1:1 pegs (both orientations sum
   * identically); meaningful once a [`Self::conversion_source`] is set.
   */
  quoteInToken0: boolean;
  /**
   * Optional reserves-conversion price source (EVM
   * `RESERVES_CONVERSION_ORACLE`), needed for non-1:1 pools like LST/SOL:
   * prices ONE unit of token0 in token1 units (1e15-scaled after the
   * entry's own `invert`). `source == Pubkey::default()` → hard 1:1 peg
   * (USDC/USDT style), no extra account.
   * Restricted to single-account source types (Pyth, StakePool, MsolPool,
   * Redstone, Chainlink, ChainlinkDataStreams).
   */
  conversionSource: Sources;
  pegBufferPercent: bigint;
  kind: DexPegOracleKind;
  bump: number;
  reserved: ReadonlyUint8Array;
};

export type DexPegOracleConfigArgs = {
  nonce: number;
  dex: Address;
  positionToken0: Address;
  positionToken1: Address;
  tokenReserve0: Address;
  tokenReserve1: Address;
  /**
   * Which token the combined reserves are denominated in (EVM
   * `QUOTE_IN_TOKEN0`). Irrelevant for pure 1:1 pegs (both orientations sum
   * identically); meaningful once a [`Self::conversion_source`] is set.
   */
  quoteInToken0: boolean;
  /**
   * Optional reserves-conversion price source (EVM
   * `RESERVES_CONVERSION_ORACLE`), needed for non-1:1 pools like LST/SOL:
   * prices ONE unit of token0 in token1 units (1e15-scaled after the
   * entry's own `invert`). `source == Pubkey::default()` → hard 1:1 peg
   * (USDC/USDT style), no extra account.
   * Restricted to single-account source types (Pyth, StakePool, MsolPool,
   * Redstone, Chainlink, ChainlinkDataStreams).
   */
  conversionSource: SourcesArgs;
  pegBufferPercent: number | bigint;
  kind: DexPegOracleKindArgs;
  bump: number;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link DexPegOracleConfigArgs} account data. */
export function getDexPegOracleConfigEncoder(): FixedSizeEncoder<DexPegOracleConfigArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nonce", getU16Encoder()],
      ["dex", getAddressEncoder()],
      ["positionToken0", getAddressEncoder()],
      ["positionToken1", getAddressEncoder()],
      ["tokenReserve0", getAddressEncoder()],
      ["tokenReserve1", getAddressEncoder()],
      ["quoteInToken0", getBooleanEncoder()],
      ["conversionSource", getSourcesEncoder()],
      ["pegBufferPercent", getU128Encoder()],
      ["kind", getDexPegOracleKindEncoder()],
      ["bump", getU8Encoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({
      ...value,
      discriminator: DEX_PEG_ORACLE_CONFIG_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link DexPegOracleConfig} account data. */
export function getDexPegOracleConfigDecoder(): FixedSizeDecoder<DexPegOracleConfig> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nonce", getU16Decoder()],
    ["dex", getAddressDecoder()],
    ["positionToken0", getAddressDecoder()],
    ["positionToken1", getAddressDecoder()],
    ["tokenReserve0", getAddressDecoder()],
    ["tokenReserve1", getAddressDecoder()],
    ["quoteInToken0", getBooleanDecoder()],
    ["conversionSource", getSourcesDecoder()],
    ["pegBufferPercent", getU128Decoder()],
    ["kind", getDexPegOracleKindDecoder()],
    ["bump", getU8Decoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link DexPegOracleConfig} account data. */
export function getDexPegOracleConfigCodec(): FixedSizeCodec<
  DexPegOracleConfigArgs,
  DexPegOracleConfig
> {
  return combineCodec(
    getDexPegOracleConfigEncoder(),
    getDexPegOracleConfigDecoder(),
  );
}

export function decodeDexPegOracleConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DexPegOracleConfig, TAddress>;
export function decodeDexPegOracleConfig<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DexPegOracleConfig, TAddress>;
export function decodeDexPegOracleConfig<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<DexPegOracleConfig, TAddress>
  | MaybeAccount<DexPegOracleConfig, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDexPegOracleConfigDecoder(),
  );
}

export async function fetchDexPegOracleConfig<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DexPegOracleConfig, TAddress>> {
  const maybeAccount = await fetchMaybeDexPegOracleConfig(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDexPegOracleConfig<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DexPegOracleConfig, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDexPegOracleConfig(maybeAccount);
}

export async function fetchAllDexPegOracleConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DexPegOracleConfig>[]> {
  const maybeAccounts = await fetchAllMaybeDexPegOracleConfig(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDexPegOracleConfig(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DexPegOracleConfig>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeDexPegOracleConfig(maybeAccount),
  );
}

export function getDexPegOracleConfigSize(): number {
  return 287;
}
