/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const USER_CLAIM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  228, 142, 195, 181, 228, 147, 32, 209,
]);

export function getUserClaimDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(USER_CLAIM_DISCRIMINATOR);
}

export type UserClaim = {
  discriminator: ReadonlyUint8Array;
  user: Address;
  amount: bigint;
  mint: Address;
};

export type UserClaimArgs = {
  user: Address;
  amount: number | bigint;
  mint: Address;
};

/** Gets the encoder for {@link UserClaimArgs} account data. */
export function getUserClaimEncoder(): FixedSizeEncoder<UserClaimArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["user", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["mint", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: USER_CLAIM_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link UserClaim} account data. */
export function getUserClaimDecoder(): FixedSizeDecoder<UserClaim> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["user", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["mint", getAddressDecoder()],
  ]);
}

/** Gets the codec for {@link UserClaim} account data. */
export function getUserClaimCodec(): FixedSizeCodec<UserClaimArgs, UserClaim> {
  return combineCodec(getUserClaimEncoder(), getUserClaimDecoder());
}

export function decodeUserClaim<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<UserClaim, TAddress>;
export function decodeUserClaim<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<UserClaim, TAddress>;
export function decodeUserClaim<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<UserClaim, TAddress> | MaybeAccount<UserClaim, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getUserClaimDecoder(),
  );
}

export async function fetchUserClaim<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<UserClaim, TAddress>> {
  const maybeAccount = await fetchMaybeUserClaim(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeUserClaim<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<UserClaim, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeUserClaim(maybeAccount);
}

export async function fetchAllUserClaim(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<UserClaim>[]> {
  const maybeAccounts = await fetchAllMaybeUserClaim(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeUserClaim(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<UserClaim>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeUserClaim(maybeAccount));
}

export function getUserClaimSize(): number {
  return 80;
}
