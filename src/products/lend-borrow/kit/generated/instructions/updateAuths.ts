/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { VAULTS_PROGRAM_ADDRESS } from "../programs";
import {
  getAddressBoolDecoder,
  getAddressBoolEncoder,
  type AddressBool,
  type AddressBoolArgs,
} from "../types";

export const UPDATE_AUTHS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  93, 96, 178, 156, 57, 117, 253, 209,
]);

export function getUpdateAuthsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_AUTHS_DISCRIMINATOR,
  );
}

export type UpdateAuthsInstruction<
  TProgram extends string = typeof VAULTS_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountVaultAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountVaultAdmin extends string
        ? WritableAccount<TAccountVaultAdmin>
        : TAccountVaultAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateAuthsInstructionData = {
  discriminator: ReadonlyUint8Array;
  authStatus: Array<AddressBool>;
};

export type UpdateAuthsInstructionDataArgs = {
  authStatus: Array<AddressBoolArgs>;
};

export function getUpdateAuthsInstructionDataEncoder(): Encoder<UpdateAuthsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authStatus", getArrayEncoder(getAddressBoolEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_AUTHS_DISCRIMINATOR }),
  );
}

export function getUpdateAuthsInstructionDataDecoder(): Decoder<UpdateAuthsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authStatus", getArrayDecoder(getAddressBoolDecoder())],
  ]);
}

export function getUpdateAuthsInstructionDataCodec(): Codec<
  UpdateAuthsInstructionDataArgs,
  UpdateAuthsInstructionData
> {
  return combineCodec(
    getUpdateAuthsInstructionDataEncoder(),
    getUpdateAuthsInstructionDataDecoder(),
  );
}

export type UpdateAuthsInput<
  TAccountSigner extends string = string,
  TAccountVaultAdmin extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  vaultAdmin: Address<TAccountVaultAdmin>;
  authStatus: UpdateAuthsInstructionDataArgs["authStatus"];
};

export function getUpdateAuthsInstruction<
  TAccountSigner extends string,
  TAccountVaultAdmin extends string,
  TProgramAddress extends Address = typeof VAULTS_PROGRAM_ADDRESS,
>(
  input: UpdateAuthsInput<TAccountSigner, TAccountVaultAdmin>,
  config?: { programAddress?: TProgramAddress },
): UpdateAuthsInstruction<TProgramAddress, TAccountSigner, TAccountVaultAdmin> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    vaultAdmin: { value: input.vaultAdmin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("vaultAdmin", accounts.vaultAdmin),
    ],
    data: getUpdateAuthsInstructionDataEncoder().encode(
      args as UpdateAuthsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateAuthsInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVaultAdmin
  >);
}

export type ParsedUpdateAuthsInstruction<
  TProgram extends string = typeof VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    vaultAdmin: TAccountMetas[1];
  };
  data: UpdateAuthsInstructionData;
};

export function parseUpdateAuthsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateAuthsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount(), vaultAdmin: getNextAccount() },
    data: getUpdateAuthsInstructionDataDecoder().decode(instruction.data),
  };
}
