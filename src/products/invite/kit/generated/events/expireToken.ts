/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const EXPIRE_TOKEN_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([218, 185, 151, 32, 227, 207, 182, 98]);

export function getExpireTokenEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXPIRE_TOKEN_EVENT_DISCRIMINATOR,
  );
}

export type ExpireTokenEvent = {
  sender: Address;
  amount: bigint;
  mint: Address;
  expiration: bigint;
};

export type ExpireTokenEventArgs = {
  sender: Address;
  amount: number | bigint;
  mint: Address;
  expiration: number | bigint;
};

/** Gets the encoder for {@link ExpireTokenEventArgs} event data. */
export function getExpireTokenEventEncoder(): FixedSizeEncoder<ExpireTokenEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["sender", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["mint", getAddressEncoder()],
      ["expiration", getI64Encoder()],
    ]),
    [getConstantEncoder(EXPIRE_TOKEN_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ExpireTokenEvent} event data. */
export function getExpireTokenEventDecoder(): FixedSizeDecoder<ExpireTokenEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["sender", getAddressDecoder()],
      ["amount", getU64Decoder()],
      ["mint", getAddressDecoder()],
      ["expiration", getI64Decoder()],
    ]),
    [getConstantDecoder(EXPIRE_TOKEN_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ExpireTokenEvent} event data. */
export function getExpireTokenEventCodec(): FixedSizeCodec<
  ExpireTokenEventArgs,
  ExpireTokenEvent
> {
  return combineCodec(
    getExpireTokenEventEncoder(),
    getExpireTokenEventDecoder(),
  );
}

/** Parses the provided bytes into {@link ExpireTokenEvent} event data. */
export function parseExpireTokenEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ExpireTokenEvent {
  if (containsBytes(data, EXPIRE_TOKEN_EVENT_DISCRIMINATOR, 0)) {
    return getExpireTokenEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ExpireTokenEvent" event discriminators.',
  );
}
