/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_LIQUIDITY_PROGRAM_ADDRESS } from "../programs";
import {
  getAddressU8Decoder,
  getAddressU8Encoder,
  type AddressU8,
  type AddressU8Args,
} from "../types";

export const UPDATE_USER_CLASS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([12, 206, 68, 135, 63, 212, 48, 119]);

export function getUpdateUserClassDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_USER_CLASS_DISCRIMINATOR,
  );
}

export type UpdateUserClassInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAuthList extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAuthList extends string
        ? WritableAccount<TAccountAuthList>
        : TAccountAuthList,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateUserClassInstructionData = {
  discriminator: ReadonlyUint8Array;
  userClass: Array<AddressU8>;
};

export type UpdateUserClassInstructionDataArgs = {
  userClass: Array<AddressU8Args>;
};

export function getUpdateUserClassInstructionDataEncoder(): Encoder<UpdateUserClassInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["userClass", getArrayEncoder(getAddressU8Encoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_USER_CLASS_DISCRIMINATOR }),
  );
}

export function getUpdateUserClassInstructionDataDecoder(): Decoder<UpdateUserClassInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["userClass", getArrayDecoder(getAddressU8Decoder())],
  ]);
}

export function getUpdateUserClassInstructionDataCodec(): Codec<
  UpdateUserClassInstructionDataArgs,
  UpdateUserClassInstructionData
> {
  return combineCodec(
    getUpdateUserClassInstructionDataEncoder(),
    getUpdateUserClassInstructionDataDecoder(),
  );
}

export type UpdateUserClassInput<
  TAccountAuthority extends string = string,
  TAccountAuthList extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  authList: Address<TAccountAuthList>;
  userClass: UpdateUserClassInstructionDataArgs["userClass"];
};

export function getUpdateUserClassInstruction<
  TAccountAuthority extends string,
  TAccountAuthList extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: UpdateUserClassInput<TAccountAuthority, TAccountAuthList>,
  config?: { programAddress?: TProgramAddress },
): UpdateUserClassInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAuthList
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    authList: { value: input.authList ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("authList", accounts.authList),
    ],
    data: getUpdateUserClassInstructionDataEncoder().encode(
      args as UpdateUserClassInstructionDataArgs,
    ),
    programAddress,
  } as UpdateUserClassInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAuthList
  >);
}

export type ParsedUpdateUserClassInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    authList: TAccountMetas[1];
  };
  data: UpdateUserClassInstructionData;
};

export function parseUpdateUserClassInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateUserClassInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), authList: getNextAccount() },
    data: getUpdateUserClassInstructionDataDecoder().decode(instruction.data),
  };
}
