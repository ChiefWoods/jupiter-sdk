/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_FLASH_LOAN_PROGRAM_ADDRESS } from "../programs";

export const PAUSE_PROTOCOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  144, 95, 0, 107, 119, 39, 248, 141,
]);

export function getPauseProtocolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PAUSE_PROTOCOL_DISCRIMINATOR,
  );
}

export type PauseProtocolInstruction<
  TProgram extends string = typeof LEND_FLASH_LOAN_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountFlashloanAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountFlashloanAdmin extends string
        ? WritableAccount<TAccountFlashloanAdmin>
        : TAccountFlashloanAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type PauseProtocolInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type PauseProtocolInstructionDataArgs = {};

export function getPauseProtocolInstructionDataEncoder(): FixedSizeEncoder<PauseProtocolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PAUSE_PROTOCOL_DISCRIMINATOR }),
  );
}

export function getPauseProtocolInstructionDataDecoder(): FixedSizeDecoder<PauseProtocolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPauseProtocolInstructionDataCodec(): FixedSizeCodec<
  PauseProtocolInstructionDataArgs,
  PauseProtocolInstructionData
> {
  return combineCodec(
    getPauseProtocolInstructionDataEncoder(),
    getPauseProtocolInstructionDataDecoder(),
  );
}

export type PauseProtocolInput<
  TAccountAuthority extends string = string,
  TAccountFlashloanAdmin extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  flashloanAdmin: Address<TAccountFlashloanAdmin>;
};

export function getPauseProtocolInstruction<
  TAccountAuthority extends string,
  TAccountFlashloanAdmin extends string,
  TProgramAddress extends Address = typeof LEND_FLASH_LOAN_PROGRAM_ADDRESS,
>(
  input: PauseProtocolInput<TAccountAuthority, TAccountFlashloanAdmin>,
  config?: { programAddress?: TProgramAddress },
): PauseProtocolInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountFlashloanAdmin
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_FLASH_LOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    flashloanAdmin: { value: input.flashloanAdmin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("flashloanAdmin", accounts.flashloanAdmin),
    ],
    data: getPauseProtocolInstructionDataEncoder().encode({}),
    programAddress,
  } as PauseProtocolInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountFlashloanAdmin
  >);
}

export type ParsedPauseProtocolInstruction<
  TProgram extends string = typeof LEND_FLASH_LOAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    flashloanAdmin: TAccountMetas[1];
  };
  data: PauseProtocolInstructionData;
};

export function parsePauseProtocolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPauseProtocolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), flashloanAdmin: getNextAccount() },
    data: getPauseProtocolInstructionDataDecoder().decode(instruction.data),
  };
}
