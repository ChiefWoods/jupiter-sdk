/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_LIQUIDITY_PROGRAM_ADDRESS } from "../programs";

export const UNPAUSE_TOKEN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  108, 117, 62, 30, 200, 92, 255, 202,
]);

export function getUnpauseTokenDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UNPAUSE_TOKEN_DISCRIMINATOR,
  );
}

export type UnpauseTokenInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountAuthList extends string | AccountMeta<string> = string,
  TAccountTokenReserve extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountAuthList extends string
        ? ReadonlyAccount<TAccountAuthList>
        : TAccountAuthList,
      TAccountTokenReserve extends string
        ? WritableAccount<TAccountTokenReserve>
        : TAccountTokenReserve,
      ...TRemainingAccounts,
    ]
  >;

export type UnpauseTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  mint: Address;
};

export type UnpauseTokenInstructionDataArgs = { mint: Address };

export function getUnpauseTokenInstructionDataEncoder(): FixedSizeEncoder<UnpauseTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mint", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UNPAUSE_TOKEN_DISCRIMINATOR }),
  );
}

export function getUnpauseTokenInstructionDataDecoder(): FixedSizeDecoder<UnpauseTokenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mint", getAddressDecoder()],
  ]);
}

export function getUnpauseTokenInstructionDataCodec(): FixedSizeCodec<
  UnpauseTokenInstructionDataArgs,
  UnpauseTokenInstructionData
> {
  return combineCodec(
    getUnpauseTokenInstructionDataEncoder(),
    getUnpauseTokenInstructionDataDecoder(),
  );
}

export type UnpauseTokenInput<
  TAccountAuthority extends string = string,
  TAccountAuthList extends string = string,
  TAccountTokenReserve extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  authList: Address<TAccountAuthList>;
  tokenReserve: Address<TAccountTokenReserve>;
  mint: UnpauseTokenInstructionDataArgs["mint"];
};

export function getUnpauseTokenInstruction<
  TAccountAuthority extends string,
  TAccountAuthList extends string,
  TAccountTokenReserve extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: UnpauseTokenInput<
    TAccountAuthority,
    TAccountAuthList,
    TAccountTokenReserve
  >,
  config?: { programAddress?: TProgramAddress },
): UnpauseTokenInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountAuthList,
  TAccountTokenReserve
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    authList: { value: input.authList ?? null, isWritable: false },
    tokenReserve: { value: input.tokenReserve ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("authList", accounts.authList),
      getAccountMeta("tokenReserve", accounts.tokenReserve),
    ],
    data: getUnpauseTokenInstructionDataEncoder().encode(
      args as UnpauseTokenInstructionDataArgs,
    ),
    programAddress,
  } as UnpauseTokenInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountAuthList,
    TAccountTokenReserve
  >);
}

export type ParsedUnpauseTokenInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    authList: TAccountMetas[1];
    tokenReserve: TAccountMetas[2];
  };
  data: UnpauseTokenInstructionData;
};

export function parseUnpauseTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUnpauseTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      authList: getNextAccount(),
      tokenReserve: getNextAccount(),
    },
    data: getUnpauseTokenInstructionDataDecoder().decode(instruction.data),
  };
}
