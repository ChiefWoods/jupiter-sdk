/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { FLASHLOAN_PROGRAM_ADDRESS } from "../programs";

export const SET_FLASHLOAN_FEE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([120, 248, 221, 70, 84, 216, 0, 149]);

export function getSetFlashloanFeeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_FLASHLOAN_FEE_DISCRIMINATOR,
  );
}

export type SetFlashloanFeeInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountFlashloanAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountFlashloanAdmin extends string
        ? WritableAccount<TAccountFlashloanAdmin>
        : TAccountFlashloanAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type SetFlashloanFeeInstructionData = {
  discriminator: ReadonlyUint8Array;
  flashloanFee: number;
};

export type SetFlashloanFeeInstructionDataArgs = { flashloanFee: number };

export function getSetFlashloanFeeInstructionDataEncoder(): FixedSizeEncoder<SetFlashloanFeeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flashloanFee", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_FLASHLOAN_FEE_DISCRIMINATOR }),
  );
}

export function getSetFlashloanFeeInstructionDataDecoder(): FixedSizeDecoder<SetFlashloanFeeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flashloanFee", getU16Decoder()],
  ]);
}

export function getSetFlashloanFeeInstructionDataCodec(): FixedSizeCodec<
  SetFlashloanFeeInstructionDataArgs,
  SetFlashloanFeeInstructionData
> {
  return combineCodec(
    getSetFlashloanFeeInstructionDataEncoder(),
    getSetFlashloanFeeInstructionDataDecoder(),
  );
}

export type SetFlashloanFeeInput<
  TAccountAuthority extends string = string,
  TAccountFlashloanAdmin extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  flashloanAdmin: Address<TAccountFlashloanAdmin>;
  flashloanFee: SetFlashloanFeeInstructionDataArgs["flashloanFee"];
};

export function getSetFlashloanFeeInstruction<
  TAccountAuthority extends string,
  TAccountFlashloanAdmin extends string,
  TProgramAddress extends Address = typeof FLASHLOAN_PROGRAM_ADDRESS,
>(
  input: SetFlashloanFeeInput<TAccountAuthority, TAccountFlashloanAdmin>,
  config?: { programAddress?: TProgramAddress },
): SetFlashloanFeeInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountFlashloanAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLASHLOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    flashloanAdmin: { value: input.flashloanAdmin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("flashloanAdmin", accounts.flashloanAdmin),
    ],
    data: getSetFlashloanFeeInstructionDataEncoder().encode(
      args as SetFlashloanFeeInstructionDataArgs,
    ),
    programAddress,
  } as SetFlashloanFeeInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountFlashloanAdmin
  >);
}

export type ParsedSetFlashloanFeeInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    flashloanAdmin: TAccountMetas[1];
  };
  data: SetFlashloanFeeInstructionData;
};

export function parseSetFlashloanFeeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetFlashloanFeeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), flashloanAdmin: getNextAccount() },
    data: getSetFlashloanFeeInstructionDataDecoder().decode(instruction.data),
  };
}
