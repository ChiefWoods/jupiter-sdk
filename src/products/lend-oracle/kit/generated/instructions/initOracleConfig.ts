/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findOraclePda } from "../pdas";
import { LEND_ORACLE_PROGRAM_ADDRESS } from "../programs";
import {
  getSourcesDecoder,
  getSourcesEncoder,
  type Sources,
  type SourcesArgs,
} from "../types";

export const INIT_ORACLE_CONFIG_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([77, 144, 180, 246, 217, 15, 118, 92]);

export function getInitOracleConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_ORACLE_CONFIG_DISCRIMINATOR,
  );
}

export type InitOracleConfigInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountOracleAdmin extends string | AccountMeta<string> = string,
  TAccountOracle extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountOracleAdmin extends string
        ? ReadonlyAccount<TAccountOracleAdmin>
        : TAccountOracleAdmin,
      TAccountOracle extends string
        ? WritableAccount<TAccountOracle>
        : TAccountOracle,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitOracleConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  sources: Array<Sources>;
  nonce: number;
};

export type InitOracleConfigInstructionDataArgs = {
  sources: Array<SourcesArgs>;
  nonce: number;
};

export function getInitOracleConfigInstructionDataEncoder(): Encoder<InitOracleConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["sources", getArrayEncoder(getSourcesEncoder())],
      ["nonce", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_ORACLE_CONFIG_DISCRIMINATOR }),
  );
}

export function getInitOracleConfigInstructionDataDecoder(): Decoder<InitOracleConfigInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["sources", getArrayDecoder(getSourcesDecoder())],
    ["nonce", getU16Decoder()],
  ]);
}

export function getInitOracleConfigInstructionDataCodec(): Codec<
  InitOracleConfigInstructionDataArgs,
  InitOracleConfigInstructionData
> {
  return combineCodec(
    getInitOracleConfigInstructionDataEncoder(),
    getInitOracleConfigInstructionDataDecoder(),
  );
}

export type InitOracleConfigAsyncInput<
  TAccountSigner extends string = string,
  TAccountOracleAdmin extends string = string,
  TAccountOracle extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  oracleAdmin: Address<TAccountOracleAdmin>;
  oracle?: Address<TAccountOracle>;
  systemProgram?: Address<TAccountSystemProgram>;
  sources: InitOracleConfigInstructionDataArgs["sources"];
  nonce: InitOracleConfigInstructionDataArgs["nonce"];
};

export async function getInitOracleConfigInstructionAsync<
  TAccountSigner extends string,
  TAccountOracleAdmin extends string,
  TAccountOracle extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_ORACLE_PROGRAM_ADDRESS,
>(
  input: InitOracleConfigAsyncInput<
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountOracle,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitOracleConfigInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountOracle,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    oracleAdmin: { value: input.oracleAdmin ?? null, isWritable: false },
    oracle: { value: input.oracle ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.oracle.value) {
    accounts.oracle.value = await findOraclePda(
      { nonce: getNonNullResolvedInstructionInput("nonce", args.nonce) },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("oracleAdmin", accounts.oracleAdmin),
      getAccountMeta("oracle", accounts.oracle),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitOracleConfigInstructionDataEncoder().encode(
      args as InitOracleConfigInstructionDataArgs,
    ),
    programAddress,
  } as InitOracleConfigInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountOracle,
    TAccountSystemProgram
  >);
}

export type InitOracleConfigInput<
  TAccountSigner extends string = string,
  TAccountOracleAdmin extends string = string,
  TAccountOracle extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  oracleAdmin: Address<TAccountOracleAdmin>;
  oracle: Address<TAccountOracle>;
  systemProgram?: Address<TAccountSystemProgram>;
  sources: InitOracleConfigInstructionDataArgs["sources"];
  nonce: InitOracleConfigInstructionDataArgs["nonce"];
};

export function getInitOracleConfigInstruction<
  TAccountSigner extends string,
  TAccountOracleAdmin extends string,
  TAccountOracle extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_ORACLE_PROGRAM_ADDRESS,
>(
  input: InitOracleConfigInput<
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountOracle,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitOracleConfigInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountOracleAdmin,
  TAccountOracle,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    oracleAdmin: { value: input.oracleAdmin ?? null, isWritable: false },
    oracle: { value: input.oracle ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("oracleAdmin", accounts.oracleAdmin),
      getAccountMeta("oracle", accounts.oracle),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitOracleConfigInstructionDataEncoder().encode(
      args as InitOracleConfigInstructionDataArgs,
    ),
    programAddress,
  } as InitOracleConfigInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountOracle,
    TAccountSystemProgram
  >);
}

export type ParsedInitOracleConfigInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    oracleAdmin: TAccountMetas[1];
    oracle: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitOracleConfigInstructionData;
};

export function parseInitOracleConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitOracleConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      oracleAdmin: getNextAccount(),
      oracle: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitOracleConfigInstructionDataDecoder().decode(instruction.data),
  };
}
