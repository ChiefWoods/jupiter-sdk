/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findBranchPda } from "../pdas";
import { LEND_BORROW_PROGRAM_ADDRESS } from "../programs";

export const INIT_BRANCH_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  162, 91, 57, 23, 228, 93, 111, 21,
]);

export function getInitBranchDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INIT_BRANCH_DISCRIMINATOR);
}

export type InitBranchInstruction<
  TProgram extends string = typeof LEND_BORROW_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountVaultConfig extends string | AccountMeta<string> = string,
  TAccountBranch extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountVaultConfig extends string
        ? ReadonlyAccount<TAccountVaultConfig>
        : TAccountVaultConfig,
      TAccountBranch extends string
        ? WritableAccount<TAccountBranch>
        : TAccountBranch,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitBranchInstructionData = {
  discriminator: ReadonlyUint8Array;
  vaultId: number;
  branchId: number;
};

export type InitBranchInstructionDataArgs = {
  vaultId: number;
  branchId: number;
};

export function getInitBranchInstructionDataEncoder(): FixedSizeEncoder<InitBranchInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vaultId", getU16Encoder()],
      ["branchId", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_BRANCH_DISCRIMINATOR }),
  );
}

export function getInitBranchInstructionDataDecoder(): FixedSizeDecoder<InitBranchInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vaultId", getU16Decoder()],
    ["branchId", getU32Decoder()],
  ]);
}

export function getInitBranchInstructionDataCodec(): FixedSizeCodec<
  InitBranchInstructionDataArgs,
  InitBranchInstructionData
> {
  return combineCodec(
    getInitBranchInstructionDataEncoder(),
    getInitBranchInstructionDataDecoder(),
  );
}

export type InitBranchAsyncInput<
  TAccountSigner extends string = string,
  TAccountVaultConfig extends string = string,
  TAccountBranch extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  /** @dev Verification inside instruction logic */
  vaultConfig: Address<TAccountVaultConfig>;
  branch?: Address<TAccountBranch>;
  systemProgram?: Address<TAccountSystemProgram>;
  vaultId: InitBranchInstructionDataArgs["vaultId"];
  branchId: InitBranchInstructionDataArgs["branchId"];
};

export async function getInitBranchInstructionAsync<
  TAccountSigner extends string,
  TAccountVaultConfig extends string,
  TAccountBranch extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_BORROW_PROGRAM_ADDRESS,
>(
  input: InitBranchAsyncInput<
    TAccountSigner,
    TAccountVaultConfig,
    TAccountBranch,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitBranchInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVaultConfig,
    TAccountBranch,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_BORROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    vaultConfig: { value: input.vaultConfig ?? null, isWritable: false },
    branch: { value: input.branch ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.branch.value) {
    accounts.branch.value = await findBranchPda(
      {
        vaultId: getNonNullResolvedInstructionInput("vaultId", args.vaultId),
        branchId: getNonNullResolvedInstructionInput("branchId", args.branchId),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("vaultConfig", accounts.vaultConfig),
      getAccountMeta("branch", accounts.branch),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitBranchInstructionDataEncoder().encode(
      args as InitBranchInstructionDataArgs,
    ),
    programAddress,
  } as InitBranchInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVaultConfig,
    TAccountBranch,
    TAccountSystemProgram
  >);
}

export type InitBranchInput<
  TAccountSigner extends string = string,
  TAccountVaultConfig extends string = string,
  TAccountBranch extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  /** @dev Verification inside instruction logic */
  vaultConfig: Address<TAccountVaultConfig>;
  branch: Address<TAccountBranch>;
  systemProgram?: Address<TAccountSystemProgram>;
  vaultId: InitBranchInstructionDataArgs["vaultId"];
  branchId: InitBranchInstructionDataArgs["branchId"];
};

export function getInitBranchInstruction<
  TAccountSigner extends string,
  TAccountVaultConfig extends string,
  TAccountBranch extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_BORROW_PROGRAM_ADDRESS,
>(
  input: InitBranchInput<
    TAccountSigner,
    TAccountVaultConfig,
    TAccountBranch,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitBranchInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountVaultConfig,
  TAccountBranch,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_BORROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    vaultConfig: { value: input.vaultConfig ?? null, isWritable: false },
    branch: { value: input.branch ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("vaultConfig", accounts.vaultConfig),
      getAccountMeta("branch", accounts.branch),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitBranchInstructionDataEncoder().encode(
      args as InitBranchInstructionDataArgs,
    ),
    programAddress,
  } as InitBranchInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVaultConfig,
    TAccountBranch,
    TAccountSystemProgram
  >);
}

export type ParsedInitBranchInstruction<
  TProgram extends string = typeof LEND_BORROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    /** @dev Verification inside instruction logic */
    vaultConfig: TAccountMetas[1];
    branch: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitBranchInstructionData;
};

export function parseInitBranchInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitBranchInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      vaultConfig: getNextAccount(),
      branch: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitBranchInstructionDataDecoder().decode(instruction.data),
  };
}
