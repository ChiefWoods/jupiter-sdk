/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI32Decoder,
  getI32Encoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { VAULTS_PROGRAM_ADDRESS } from "../programs";

export const INIT_TICK_ID_LIQUIDATION_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([56, 110, 121, 169, 152, 241, 86, 183]);

export function getInitTickIdLiquidationDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_TICK_ID_LIQUIDATION_DISCRIMINATOR,
  );
}

export type InitTickIdLiquidationInstruction<
  TProgram extends string = typeof VAULTS_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountTickData extends string | AccountMeta<string> = string,
  TAccountTickIdLiquidation extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountTickData extends string
        ? ReadonlyAccount<TAccountTickData>
        : TAccountTickData,
      TAccountTickIdLiquidation extends string
        ? WritableAccount<TAccountTickIdLiquidation>
        : TAccountTickIdLiquidation,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitTickIdLiquidationInstructionData = {
  discriminator: ReadonlyUint8Array;
  vaultId: number;
  tick: number;
  totalIds: number;
};

export type InitTickIdLiquidationInstructionDataArgs = {
  vaultId: number;
  tick: number;
  totalIds: number;
};

export function getInitTickIdLiquidationInstructionDataEncoder(): FixedSizeEncoder<InitTickIdLiquidationInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vaultId", getU16Encoder()],
      ["tick", getI32Encoder()],
      ["totalIds", getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INIT_TICK_ID_LIQUIDATION_DISCRIMINATOR,
    }),
  );
}

export function getInitTickIdLiquidationInstructionDataDecoder(): FixedSizeDecoder<InitTickIdLiquidationInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vaultId", getU16Decoder()],
    ["tick", getI32Decoder()],
    ["totalIds", getU32Decoder()],
  ]);
}

export function getInitTickIdLiquidationInstructionDataCodec(): FixedSizeCodec<
  InitTickIdLiquidationInstructionDataArgs,
  InitTickIdLiquidationInstructionData
> {
  return combineCodec(
    getInitTickIdLiquidationInstructionDataEncoder(),
    getInitTickIdLiquidationInstructionDataDecoder(),
  );
}

export type InitTickIdLiquidationInput<
  TAccountSigner extends string = string,
  TAccountTickData extends string = string,
  TAccountTickIdLiquidation extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  /** @dev Verification inside instruction logic */
  tickData: Address<TAccountTickData>;
  tickIdLiquidation: Address<TAccountTickIdLiquidation>;
  systemProgram?: Address<TAccountSystemProgram>;
  vaultId: InitTickIdLiquidationInstructionDataArgs["vaultId"];
  tick: InitTickIdLiquidationInstructionDataArgs["tick"];
  totalIds: InitTickIdLiquidationInstructionDataArgs["totalIds"];
};

export function getInitTickIdLiquidationInstruction<
  TAccountSigner extends string,
  TAccountTickData extends string,
  TAccountTickIdLiquidation extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULTS_PROGRAM_ADDRESS,
>(
  input: InitTickIdLiquidationInput<
    TAccountSigner,
    TAccountTickData,
    TAccountTickIdLiquidation,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitTickIdLiquidationInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountTickData,
  TAccountTickIdLiquidation,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    tickData: { value: input.tickData ?? null, isWritable: false },
    tickIdLiquidation: {
      value: input.tickIdLiquidation ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("tickData", accounts.tickData),
      getAccountMeta("tickIdLiquidation", accounts.tickIdLiquidation),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitTickIdLiquidationInstructionDataEncoder().encode(
      args as InitTickIdLiquidationInstructionDataArgs,
    ),
    programAddress,
  } as InitTickIdLiquidationInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountTickData,
    TAccountTickIdLiquidation,
    TAccountSystemProgram
  >);
}

export type ParsedInitTickIdLiquidationInstruction<
  TProgram extends string = typeof VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    /** @dev Verification inside instruction logic */
    tickData: TAccountMetas[1];
    tickIdLiquidation: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitTickIdLiquidationInstructionData;
};

export function parseInitTickIdLiquidationInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitTickIdLiquidationInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      tickData: getNextAccount(),
      tickIdLiquidation: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitTickIdLiquidationInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
