/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_BORROW_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_ORACLE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  112, 41, 209, 18, 248, 226, 252, 188,
]);

export function getUpdateOracleDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ORACLE_DISCRIMINATOR,
  );
}

export type UpdateOracleInstruction<
  TProgram extends string = typeof LEND_BORROW_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVaultAdmin extends string | AccountMeta<string> = string,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountVaultConfig extends string | AccountMeta<string> = string,
  TAccountNewOracle extends string | AccountMeta<string> = string,
  TAccountSupplyTokenReservesLiquidity extends string | AccountMeta<string> =
    string,
  TAccountBorrowTokenReservesLiquidity extends string | AccountMeta<string> =
    string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVaultAdmin extends string
        ? ReadonlyAccount<TAccountVaultAdmin>
        : TAccountVaultAdmin,
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountVaultConfig extends string
        ? WritableAccount<TAccountVaultConfig>
        : TAccountVaultConfig,
      TAccountNewOracle extends string
        ? ReadonlyAccount<TAccountNewOracle>
        : TAccountNewOracle,
      TAccountSupplyTokenReservesLiquidity extends string
        ? ReadonlyAccount<TAccountSupplyTokenReservesLiquidity>
        : TAccountSupplyTokenReservesLiquidity,
      TAccountBorrowTokenReservesLiquidity extends string
        ? ReadonlyAccount<TAccountBorrowTokenReservesLiquidity>
        : TAccountBorrowTokenReservesLiquidity,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateOracleInstructionData = {
  discriminator: ReadonlyUint8Array;
  vaultId: number;
};

export type UpdateOracleInstructionDataArgs = { vaultId: number };

export function getUpdateOracleInstructionDataEncoder(): FixedSizeEncoder<UpdateOracleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vaultId", getU16Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ORACLE_DISCRIMINATOR }),
  );
}

export function getUpdateOracleInstructionDataDecoder(): FixedSizeDecoder<UpdateOracleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vaultId", getU16Decoder()],
  ]);
}

export function getUpdateOracleInstructionDataCodec(): FixedSizeCodec<
  UpdateOracleInstructionDataArgs,
  UpdateOracleInstructionData
> {
  return combineCodec(
    getUpdateOracleInstructionDataEncoder(),
    getUpdateOracleInstructionDataDecoder(),
  );
}

export type UpdateOracleInput<
  TAccountAuthority extends string = string,
  TAccountVaultAdmin extends string = string,
  TAccountVaultState extends string = string,
  TAccountVaultConfig extends string = string,
  TAccountNewOracle extends string = string,
  TAccountSupplyTokenReservesLiquidity extends string = string,
  TAccountBorrowTokenReservesLiquidity extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vaultAdmin: Address<TAccountVaultAdmin>;
  /** @dev Verification inside instruction logic */
  vaultState: Address<TAccountVaultState>;
  /** @dev Verification inside instruction logic */
  vaultConfig: Address<TAccountVaultConfig>;
  newOracle: Address<TAccountNewOracle>;
  /** @dev Verification inside instruction logic */
  supplyTokenReservesLiquidity: Address<TAccountSupplyTokenReservesLiquidity>;
  /** @dev Verification inside instruction logic */
  borrowTokenReservesLiquidity: Address<TAccountBorrowTokenReservesLiquidity>;
  vaultId: UpdateOracleInstructionDataArgs["vaultId"];
};

export function getUpdateOracleInstruction<
  TAccountAuthority extends string,
  TAccountVaultAdmin extends string,
  TAccountVaultState extends string,
  TAccountVaultConfig extends string,
  TAccountNewOracle extends string,
  TAccountSupplyTokenReservesLiquidity extends string,
  TAccountBorrowTokenReservesLiquidity extends string,
  TProgramAddress extends Address = typeof LEND_BORROW_PROGRAM_ADDRESS,
>(
  input: UpdateOracleInput<
    TAccountAuthority,
    TAccountVaultAdmin,
    TAccountVaultState,
    TAccountVaultConfig,
    TAccountNewOracle,
    TAccountSupplyTokenReservesLiquidity,
    TAccountBorrowTokenReservesLiquidity
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateOracleInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountVaultAdmin,
  TAccountVaultState,
  TAccountVaultConfig,
  TAccountNewOracle,
  TAccountSupplyTokenReservesLiquidity,
  TAccountBorrowTokenReservesLiquidity
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_BORROW_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    vaultAdmin: { value: input.vaultAdmin ?? null, isWritable: false },
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    vaultConfig: { value: input.vaultConfig ?? null, isWritable: true },
    newOracle: { value: input.newOracle ?? null, isWritable: false },
    supplyTokenReservesLiquidity: {
      value: input.supplyTokenReservesLiquidity ?? null,
      isWritable: false,
    },
    borrowTokenReservesLiquidity: {
      value: input.borrowTokenReservesLiquidity ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("vaultAdmin", accounts.vaultAdmin),
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("vaultConfig", accounts.vaultConfig),
      getAccountMeta("newOracle", accounts.newOracle),
      getAccountMeta(
        "supplyTokenReservesLiquidity",
        accounts.supplyTokenReservesLiquidity,
      ),
      getAccountMeta(
        "borrowTokenReservesLiquidity",
        accounts.borrowTokenReservesLiquidity,
      ),
    ],
    data: getUpdateOracleInstructionDataEncoder().encode(
      args as UpdateOracleInstructionDataArgs,
    ),
    programAddress,
  } as UpdateOracleInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVaultAdmin,
    TAccountVaultState,
    TAccountVaultConfig,
    TAccountNewOracle,
    TAccountSupplyTokenReservesLiquidity,
    TAccountBorrowTokenReservesLiquidity
  >);
}

export type ParsedUpdateOracleInstruction<
  TProgram extends string = typeof LEND_BORROW_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vaultAdmin: TAccountMetas[1];
    /** @dev Verification inside instruction logic */
    vaultState: TAccountMetas[2];
    /** @dev Verification inside instruction logic */
    vaultConfig: TAccountMetas[3];
    newOracle: TAccountMetas[4];
    /** @dev Verification inside instruction logic */
    supplyTokenReservesLiquidity: TAccountMetas[5];
    /** @dev Verification inside instruction logic */
    borrowTokenReservesLiquidity: TAccountMetas[6];
  };
  data: UpdateOracleInstructionData;
};

export function parseUpdateOracleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateOracleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      vaultAdmin: getNextAccount(),
      vaultState: getNextAccount(),
      vaultConfig: getNextAccount(),
      newOracle: getNextAccount(),
      supplyTokenReservesLiquidity: getNextAccount(),
      borrowTokenReservesLiquidity: getNextAccount(),
    },
    data: getUpdateOracleInstructionDataDecoder().decode(instruction.data),
  };
}
