/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { DEX_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_CENTER_PRICE_ADDRESS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([45, 110, 96, 39, 201, 250, 142, 1]);

export function getUpdateCenterPriceAddressDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_CENTER_PRICE_ADDRESS_DISCRIMINATOR,
  );
}

export type UpdateCenterPriceAddressInstruction<
  TProgram extends string = typeof DEX_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TAccountDex extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDexAdmin extends string
        ? ReadonlyAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      TAccountDex extends string ? WritableAccount<TAccountDex> : TAccountDex,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateCenterPriceAddressInstructionData = {
  discriminator: ReadonlyUint8Array;
  centerPriceAddress: Address;
  percent: number;
  time: number;
};

export type UpdateCenterPriceAddressInstructionDataArgs = {
  centerPriceAddress: Address;
  percent: number;
  time: number;
};

export function getUpdateCenterPriceAddressInstructionDataEncoder(): FixedSizeEncoder<UpdateCenterPriceAddressInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["centerPriceAddress", getAddressEncoder()],
      ["percent", getU32Encoder()],
      ["time", getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_CENTER_PRICE_ADDRESS_DISCRIMINATOR,
    }),
  );
}

export function getUpdateCenterPriceAddressInstructionDataDecoder(): FixedSizeDecoder<UpdateCenterPriceAddressInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["centerPriceAddress", getAddressDecoder()],
    ["percent", getU32Decoder()],
    ["time", getU32Decoder()],
  ]);
}

export function getUpdateCenterPriceAddressInstructionDataCodec(): FixedSizeCodec<
  UpdateCenterPriceAddressInstructionDataArgs,
  UpdateCenterPriceAddressInstructionData
> {
  return combineCodec(
    getUpdateCenterPriceAddressInstructionDataEncoder(),
    getUpdateCenterPriceAddressInstructionDataDecoder(),
  );
}

export type UpdateCenterPriceAddressInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountDex extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  dex: Address<TAccountDex>;
  centerPriceAddress: UpdateCenterPriceAddressInstructionDataArgs["centerPriceAddress"];
  percent: UpdateCenterPriceAddressInstructionDataArgs["percent"];
  time: UpdateCenterPriceAddressInstructionDataArgs["time"];
};

export function getUpdateCenterPriceAddressInstruction<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TAccountDex extends string,
  TProgramAddress extends Address = typeof DEX_PROGRAM_ADDRESS,
>(
  input: UpdateCenterPriceAddressInput<
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateCenterPriceAddressInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDexAdmin,
  TAccountDex
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: false },
    dex: { value: input.dex ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("dex", accounts.dex),
    ],
    data: getUpdateCenterPriceAddressInstructionDataEncoder().encode(
      args as UpdateCenterPriceAddressInstructionDataArgs,
    ),
    programAddress,
  } as UpdateCenterPriceAddressInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex
  >);
}

export type ParsedUpdateCenterPriceAddressInstruction<
  TProgram extends string = typeof DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
    dex: TAccountMetas[2];
  };
  data: UpdateCenterPriceAddressInstructionData;
};

export function parseUpdateCenterPriceAddressInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateCenterPriceAddressInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      dexAdmin: getNextAccount(),
      dex: getNextAccount(),
    },
    data: getUpdateCenterPriceAddressInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
