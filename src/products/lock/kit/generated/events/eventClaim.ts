/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const EVENT_CLAIM_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([171, 144, 1, 189, 120, 200, 38, 11]);

export function getEventClaimEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EVENT_CLAIM_EVENT_DISCRIMINATOR,
  );
}

export type EventClaimEvent = {
  amount: bigint;
  currentTs: bigint;
  escrow: Address;
};

export type EventClaimEventArgs = {
  amount: number | bigint;
  currentTs: number | bigint;
  escrow: Address;
};

/** Gets the encoder for {@link EventClaimEventArgs} event data. */
export function getEventClaimEventEncoder(): FixedSizeEncoder<EventClaimEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["amount", getU64Encoder()],
      ["currentTs", getU64Encoder()],
      ["escrow", getAddressEncoder()],
    ]),
    [getConstantEncoder(EVENT_CLAIM_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link EventClaimEvent} event data. */
export function getEventClaimEventDecoder(): FixedSizeDecoder<EventClaimEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["amount", getU64Decoder()],
      ["currentTs", getU64Decoder()],
      ["escrow", getAddressDecoder()],
    ]),
    [getConstantDecoder(EVENT_CLAIM_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link EventClaimEvent} event data. */
export function getEventClaimEventCodec(): FixedSizeCodec<
  EventClaimEventArgs,
  EventClaimEvent
> {
  return combineCodec(getEventClaimEventEncoder(), getEventClaimEventDecoder());
}

/** Parses the provided bytes into {@link EventClaimEvent} event data. */
export function parseEventClaimEvent(
  data: ReadonlyUint8Array | Uint8Array,
): EventClaimEvent {
  if (containsBytes(data, EVENT_CLAIM_EVENT_DISCRIMINATOR, 0)) {
    return getEventClaimEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "EventClaimEvent" event discriminators.',
  );
}
