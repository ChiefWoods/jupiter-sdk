//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::GovernanceParameters;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

pub const CREATE_GOVERNOR_DISCRIMINATOR: [u8; 8] = [103, 30, 78, 252, 28, 128, 40, 3];

/// Accounts.
#[derive(Debug)]
pub struct CreateGovernor {
    /// Base of the [Governor] key.
    pub base: solana_address::Address,
    /// Governor.
    pub governor: solana_address::Address,
    /// The Smart Wallet.
    pub smart_wallet: solana_address::Address,
    /// Payer.
    pub payer: solana_address::Address,
    /// System program.
    pub system_program: solana_address::Address,
}

impl CreateGovernor {
    pub fn instruction(
        &self,
        args: CreateGovernorInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: CreateGovernorInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.base, true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.governor, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.smart_wallet,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.payer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = CreateGovernorInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::GOVERNANCE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CreateGovernorInstructionData {
    discriminator: [u8; 8],
}

impl CreateGovernorInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [103, 30, 78, 252, 28, 128, 40, 3],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CreateGovernorInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CreateGovernorInstructionArgs {
    pub locker: Address,
    pub params: GovernanceParameters,
}

impl CreateGovernorInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `CreateGovernor`.
///
/// ### Accounts:
///
///   0. `[signer]` base
///   1. `[writable]` governor
///   2. `[]` smart_wallet
///   3. `[writable, signer]` payer
///   4. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct CreateGovernorBuilder {
    base: Option<solana_address::Address>,
    governor: Option<solana_address::Address>,
    smart_wallet: Option<solana_address::Address>,
    payer: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    locker: Option<Address>,
    params: Option<GovernanceParameters>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CreateGovernorBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Base of the [Governor] key.
    #[inline(always)]
    pub fn base(&mut self, base: solana_address::Address) -> &mut Self {
        self.base = Some(base);
        self
    }
    /// Governor.
    #[inline(always)]
    pub fn governor(&mut self, governor: solana_address::Address) -> &mut Self {
        self.governor = Some(governor);
        self
    }
    /// The Smart Wallet.
    #[inline(always)]
    pub fn smart_wallet(&mut self, smart_wallet: solana_address::Address) -> &mut Self {
        self.smart_wallet = Some(smart_wallet);
        self
    }
    /// Payer.
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_address::Address) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    /// System program.
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn locker(&mut self, locker: Address) -> &mut Self {
        self.locker = Some(locker);
        self
    }
    #[inline(always)]
    pub fn params(&mut self, params: GovernanceParameters) -> &mut Self {
        self.params = Some(params);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CreateGovernor {
            base: self.base.expect("base is not set"),
            governor: self.governor.expect("governor is not set"),
            smart_wallet: self.smart_wallet.expect("smart_wallet is not set"),
            payer: self.payer.expect("payer is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = CreateGovernorInstructionArgs {
            locker: self.locker.clone().expect("locker is not set"),
            params: self.params.clone().expect("params is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `create_governor` CPI accounts.
pub struct CreateGovernorCpiAccounts<'a, 'b> {
    /// Base of the [Governor] key.
    pub base: &'b solana_account_info::AccountInfo<'a>,
    /// Governor.
    pub governor: &'b solana_account_info::AccountInfo<'a>,
    /// The Smart Wallet.
    pub smart_wallet: &'b solana_account_info::AccountInfo<'a>,
    /// Payer.
    pub payer: &'b solana_account_info::AccountInfo<'a>,
    /// System program.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `create_governor` CPI instruction.
pub struct CreateGovernorCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Base of the [Governor] key.
    pub base: &'b solana_account_info::AccountInfo<'a>,
    /// Governor.
    pub governor: &'b solana_account_info::AccountInfo<'a>,
    /// The Smart Wallet.
    pub smart_wallet: &'b solana_account_info::AccountInfo<'a>,
    /// Payer.
    pub payer: &'b solana_account_info::AccountInfo<'a>,
    /// System program.
    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: CreateGovernorInstructionArgs,
}

impl<'a, 'b> CreateGovernorCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CreateGovernorCpiAccounts<'a, 'b>,
        args: CreateGovernorInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            base: accounts.base,
            governor: accounts.governor,
            smart_wallet: accounts.smart_wallet,
            payer: accounts.payer,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(5 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.base.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.governor.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.smart_wallet.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.payer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = CreateGovernorInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::GOVERNANCE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(6 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.base.clone());
        account_infos.push(self.governor.clone());
        account_infos.push(self.smart_wallet.clone());
        account_infos.push(self.payer.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CreateGovernor` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` base
///   1. `[writable]` governor
///   2. `[]` smart_wallet
///   3. `[writable, signer]` payer
///   4. `[]` system_program
#[derive(Clone, Debug)]
pub struct CreateGovernorCpiBuilder<'a, 'b> {
    instruction: Box<CreateGovernorCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CreateGovernorCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CreateGovernorCpiBuilderInstruction {
            __program: program,
            base: None,
            governor: None,
            smart_wallet: None,
            payer: None,
            system_program: None,
            locker: None,
            params: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Base of the [Governor] key.
    #[inline(always)]
    pub fn base(&mut self, base: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.base = Some(base);
        self
    }
    /// Governor.
    #[inline(always)]
    pub fn governor(&mut self, governor: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.governor = Some(governor);
        self
    }
    /// The Smart Wallet.
    #[inline(always)]
    pub fn smart_wallet(
        &mut self,
        smart_wallet: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.smart_wallet = Some(smart_wallet);
        self
    }
    /// Payer.
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    /// System program.
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn locker(&mut self, locker: Address) -> &mut Self {
        self.instruction.locker = Some(locker);
        self
    }
    #[inline(always)]
    pub fn params(&mut self, params: GovernanceParameters) -> &mut Self {
        self.instruction.params = Some(params);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = CreateGovernorInstructionArgs {
            locker: self.instruction.locker.clone().expect("locker is not set"),
            params: self.instruction.params.clone().expect("params is not set"),
        };
        let instruction = CreateGovernorCpi {
            __program: self.instruction.__program,

            base: self.instruction.base.expect("base is not set"),

            governor: self.instruction.governor.expect("governor is not set"),

            smart_wallet: self
                .instruction
                .smart_wallet
                .expect("smart_wallet is not set"),

            payer: self.instruction.payer.expect("payer is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CreateGovernorCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    base: Option<&'b solana_account_info::AccountInfo<'a>>,
    governor: Option<&'b solana_account_info::AccountInfo<'a>>,
    smart_wallet: Option<&'b solana_account_info::AccountInfo<'a>>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    locker: Option<Address>,
    params: Option<GovernanceParameters>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
