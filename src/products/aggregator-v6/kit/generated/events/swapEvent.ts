/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SWAP_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([64, 198, 205, 232, 38, 8, 113, 226]);

export function getSwapEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SWAP_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type SwapEventEvent = {
  amm: Address;
  inputMint: Address;
  inputAmount: bigint;
  outputMint: Address;
  outputAmount: bigint;
};

export type SwapEventEventArgs = {
  amm: Address;
  inputMint: Address;
  inputAmount: number | bigint;
  outputMint: Address;
  outputAmount: number | bigint;
};

/** Gets the encoder for {@link SwapEventEventArgs} event data. */
export function getSwapEventEventEncoder(): FixedSizeEncoder<SwapEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["amm", getAddressEncoder()],
      ["inputMint", getAddressEncoder()],
      ["inputAmount", getU64Encoder()],
      ["outputMint", getAddressEncoder()],
      ["outputAmount", getU64Encoder()],
    ]),
    [getConstantEncoder(SWAP_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link SwapEventEvent} event data. */
export function getSwapEventEventDecoder(): FixedSizeDecoder<SwapEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["amm", getAddressDecoder()],
      ["inputMint", getAddressDecoder()],
      ["inputAmount", getU64Decoder()],
      ["outputMint", getAddressDecoder()],
      ["outputAmount", getU64Decoder()],
    ]),
    [getConstantDecoder(SWAP_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link SwapEventEvent} event data. */
export function getSwapEventEventCodec(): FixedSizeCodec<
  SwapEventEventArgs,
  SwapEventEvent
> {
  return combineCodec(getSwapEventEventEncoder(), getSwapEventEventDecoder());
}

/** Parses the provided bytes into {@link SwapEventEvent} event data. */
export function parseSwapEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): SwapEventEvent {
  if (containsBytes(data, SWAP_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getSwapEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "SwapEventEvent" event discriminators.',
  );
}
