/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_DEX_PROGRAM_ADDRESS } from "../programs";
import {
  getAddressBoolDecoder,
  getAddressBoolEncoder,
  type AddressBool,
  type AddressBoolArgs,
} from "../types";

export const UPDATE_GUARDIANS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([43, 62, 250, 138, 141, 117, 132, 97]);

export function getUpdateGuardiansDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_GUARDIANS_DISCRIMINATOR,
  );
}

export type UpdateGuardiansInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDexAdmin extends string
        ? WritableAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateGuardiansInstructionData = {
  discriminator: ReadonlyUint8Array;
  guardianStatus: Array<AddressBool>;
};

export type UpdateGuardiansInstructionDataArgs = {
  guardianStatus: Array<AddressBoolArgs>;
};

export function getUpdateGuardiansInstructionDataEncoder(): Encoder<UpdateGuardiansInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["guardianStatus", getArrayEncoder(getAddressBoolEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_GUARDIANS_DISCRIMINATOR }),
  );
}

export function getUpdateGuardiansInstructionDataDecoder(): Decoder<UpdateGuardiansInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["guardianStatus", getArrayDecoder(getAddressBoolDecoder())],
  ]);
}

export function getUpdateGuardiansInstructionDataCodec(): Codec<
  UpdateGuardiansInstructionDataArgs,
  UpdateGuardiansInstructionData
> {
  return combineCodec(
    getUpdateGuardiansInstructionDataEncoder(),
    getUpdateGuardiansInstructionDataDecoder(),
  );
}

export type UpdateGuardiansInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  guardianStatus: UpdateGuardiansInstructionDataArgs["guardianStatus"];
};

export function getUpdateGuardiansInstruction<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TProgramAddress extends Address = typeof LEND_DEX_PROGRAM_ADDRESS,
>(
  input: UpdateGuardiansInput<TAccountAuthority, TAccountDexAdmin>,
  config?: { programAddress?: TProgramAddress },
): UpdateGuardiansInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDexAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
    ],
    data: getUpdateGuardiansInstructionDataEncoder().encode(
      args as UpdateGuardiansInstructionDataArgs,
    ),
    programAddress,
  } as UpdateGuardiansInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin
  >);
}

export type ParsedUpdateGuardiansInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
  };
  data: UpdateGuardiansInstructionData;
};

export function parseUpdateGuardiansInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateGuardiansInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), dexAdmin: getNextAccount() },
    data: getUpdateGuardiansInstructionDataDecoder().decode(instruction.data),
  };
}
