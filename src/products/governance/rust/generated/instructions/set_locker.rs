//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

pub const SET_LOCKER_DISCRIMINATOR: [u8; 8] = [17, 6, 101, 72, 250, 23, 152, 96];

/// Accounts.
#[derive(Debug)]
pub struct SetLocker {
    /// The [Governor]
    pub governor: solana_address::Address,
    /// The Smart Wallet.
    pub smart_wallet: solana_address::Address,
}

impl SetLocker {
    pub fn instruction(&self, args: SetLockerInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SetLockerInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.governor, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.smart_wallet,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SetLockerInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::GOVERNANCE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetLockerInstructionData {
    discriminator: [u8; 8],
}

impl SetLockerInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [17, 6, 101, 72, 250, 23, 152, 96],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SetLockerInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetLockerInstructionArgs {
    pub new_locker: Address,
}

impl SetLockerInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SetLocker`.
///
/// ### Accounts:
///
///   0. `[writable]` governor
///   1. `[signer]` smart_wallet
#[derive(Clone, Debug, Default)]
pub struct SetLockerBuilder {
    governor: Option<solana_address::Address>,
    smart_wallet: Option<solana_address::Address>,
    new_locker: Option<Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetLockerBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The [Governor]
    #[inline(always)]
    pub fn governor(&mut self, governor: solana_address::Address) -> &mut Self {
        self.governor = Some(governor);
        self
    }
    /// The Smart Wallet.
    #[inline(always)]
    pub fn smart_wallet(&mut self, smart_wallet: solana_address::Address) -> &mut Self {
        self.smart_wallet = Some(smart_wallet);
        self
    }
    #[inline(always)]
    pub fn new_locker(&mut self, new_locker: Address) -> &mut Self {
        self.new_locker = Some(new_locker);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SetLocker {
            governor: self.governor.expect("governor is not set"),
            smart_wallet: self.smart_wallet.expect("smart_wallet is not set"),
        };
        let args = SetLockerInstructionArgs {
            new_locker: self.new_locker.clone().expect("new_locker is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `set_locker` CPI accounts.
pub struct SetLockerCpiAccounts<'a, 'b> {
    /// The [Governor]
    pub governor: &'b solana_account_info::AccountInfo<'a>,
    /// The Smart Wallet.
    pub smart_wallet: &'b solana_account_info::AccountInfo<'a>,
}

/// `set_locker` CPI instruction.
pub struct SetLockerCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The [Governor]
    pub governor: &'b solana_account_info::AccountInfo<'a>,
    /// The Smart Wallet.
    pub smart_wallet: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: SetLockerInstructionArgs,
}

impl<'a, 'b> SetLockerCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SetLockerCpiAccounts<'a, 'b>,
        args: SetLockerInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            governor: accounts.governor,
            smart_wallet: accounts.smart_wallet,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.governor.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.smart_wallet.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = SetLockerInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::GOVERNANCE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.governor.clone());
        account_infos.push(self.smart_wallet.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SetLocker` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` governor
///   1. `[signer]` smart_wallet
#[derive(Clone, Debug)]
pub struct SetLockerCpiBuilder<'a, 'b> {
    instruction: Box<SetLockerCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetLockerCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SetLockerCpiBuilderInstruction {
            __program: program,
            governor: None,
            smart_wallet: None,
            new_locker: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The [Governor]
    #[inline(always)]
    pub fn governor(&mut self, governor: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.governor = Some(governor);
        self
    }
    /// The Smart Wallet.
    #[inline(always)]
    pub fn smart_wallet(
        &mut self,
        smart_wallet: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.smart_wallet = Some(smart_wallet);
        self
    }
    #[inline(always)]
    pub fn new_locker(&mut self, new_locker: Address) -> &mut Self {
        self.instruction.new_locker = Some(new_locker);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SetLockerInstructionArgs {
            new_locker: self
                .instruction
                .new_locker
                .clone()
                .expect("new_locker is not set"),
        };
        let instruction = SetLockerCpi {
            __program: self.instruction.__program,

            governor: self.instruction.governor.expect("governor is not set"),

            smart_wallet: self
                .instruction
                .smart_wallet
                .expect("smart_wallet is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SetLockerCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    governor: Option<&'b solana_account_info::AccountInfo<'a>>,
    smart_wallet: Option<&'b solana_account_info::AccountInfo<'a>>,
    new_locker: Option<Address>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
