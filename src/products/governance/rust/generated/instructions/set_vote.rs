//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const SET_VOTE_DISCRIMINATOR: [u8; 8] = [171, 33, 83, 172, 148, 215, 239, 97];

/// Accounts.
#[derive(Debug)]
pub struct SetVote {
    /// The [Governor].
    pub governor: solana_address::Address,
    /// The [Proposal].
    pub proposal: solana_address::Address,
    /// The [Vote].
    pub vote: solana_address::Address,
    /// The [Governor::locker].
    pub locker: solana_address::Address,
}

impl SetVote {
    pub fn instruction(&self, args: SetVoteInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: SetVoteInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.governor,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.proposal, false));
        accounts.push(solana_instruction::AccountMeta::new(self.vote, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.locker,
            true,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = SetVoteInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::GOVERNANCE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetVoteInstructionData {
    discriminator: [u8; 8],
}

impl SetVoteInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [171, 33, 83, 172, 148, 215, 239, 97],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SetVoteInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetVoteInstructionArgs {
    pub side: u8,
    pub weight: u64,
}

impl SetVoteInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `SetVote`.
///
/// ### Accounts:
///
///   0. `[]` governor
///   1. `[writable]` proposal
///   2. `[writable]` vote
///   3. `[signer]` locker
#[derive(Clone, Debug, Default)]
pub struct SetVoteBuilder {
    governor: Option<solana_address::Address>,
    proposal: Option<solana_address::Address>,
    vote: Option<solana_address::Address>,
    locker: Option<solana_address::Address>,
    side: Option<u8>,
    weight: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetVoteBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The [Governor].
    #[inline(always)]
    pub fn governor(&mut self, governor: solana_address::Address) -> &mut Self {
        self.governor = Some(governor);
        self
    }
    /// The [Proposal].
    #[inline(always)]
    pub fn proposal(&mut self, proposal: solana_address::Address) -> &mut Self {
        self.proposal = Some(proposal);
        self
    }
    /// The [Vote].
    #[inline(always)]
    pub fn vote(&mut self, vote: solana_address::Address) -> &mut Self {
        self.vote = Some(vote);
        self
    }
    /// The [Governor::locker].
    #[inline(always)]
    pub fn locker(&mut self, locker: solana_address::Address) -> &mut Self {
        self.locker = Some(locker);
        self
    }
    #[inline(always)]
    pub fn side(&mut self, side: u8) -> &mut Self {
        self.side = Some(side);
        self
    }
    #[inline(always)]
    pub fn weight(&mut self, weight: u64) -> &mut Self {
        self.weight = Some(weight);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SetVote {
            governor: self.governor.expect("governor is not set"),
            proposal: self.proposal.expect("proposal is not set"),
            vote: self.vote.expect("vote is not set"),
            locker: self.locker.expect("locker is not set"),
        };
        let args = SetVoteInstructionArgs {
            side: self.side.clone().expect("side is not set"),
            weight: self.weight.clone().expect("weight is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `set_vote` CPI accounts.
pub struct SetVoteCpiAccounts<'a, 'b> {
    /// The [Governor].
    pub governor: &'b solana_account_info::AccountInfo<'a>,
    /// The [Proposal].
    pub proposal: &'b solana_account_info::AccountInfo<'a>,
    /// The [Vote].
    pub vote: &'b solana_account_info::AccountInfo<'a>,
    /// The [Governor::locker].
    pub locker: &'b solana_account_info::AccountInfo<'a>,
}

/// `set_vote` CPI instruction.
pub struct SetVoteCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The [Governor].
    pub governor: &'b solana_account_info::AccountInfo<'a>,
    /// The [Proposal].
    pub proposal: &'b solana_account_info::AccountInfo<'a>,
    /// The [Vote].
    pub vote: &'b solana_account_info::AccountInfo<'a>,
    /// The [Governor::locker].
    pub locker: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: SetVoteInstructionArgs,
}

impl<'a, 'b> SetVoteCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SetVoteCpiAccounts<'a, 'b>,
        args: SetVoteInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            governor: accounts.governor,
            proposal: accounts.proposal,
            vote: accounts.vote,
            locker: accounts.locker,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.governor.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.proposal.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.vote.key, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.locker.key,
            true,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = SetVoteInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::GOVERNANCE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.governor.clone());
        account_infos.push(self.proposal.clone());
        account_infos.push(self.vote.clone());
        account_infos.push(self.locker.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SetVote` via CPI.
///
/// ### Accounts:
///
///   0. `[]` governor
///   1. `[writable]` proposal
///   2. `[writable]` vote
///   3. `[signer]` locker
#[derive(Clone, Debug)]
pub struct SetVoteCpiBuilder<'a, 'b> {
    instruction: Box<SetVoteCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetVoteCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SetVoteCpiBuilderInstruction {
            __program: program,
            governor: None,
            proposal: None,
            vote: None,
            locker: None,
            side: None,
            weight: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The [Governor].
    #[inline(always)]
    pub fn governor(&mut self, governor: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.governor = Some(governor);
        self
    }
    /// The [Proposal].
    #[inline(always)]
    pub fn proposal(&mut self, proposal: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.proposal = Some(proposal);
        self
    }
    /// The [Vote].
    #[inline(always)]
    pub fn vote(&mut self, vote: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.vote = Some(vote);
        self
    }
    /// The [Governor::locker].
    #[inline(always)]
    pub fn locker(&mut self, locker: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.locker = Some(locker);
        self
    }
    #[inline(always)]
    pub fn side(&mut self, side: u8) -> &mut Self {
        self.instruction.side = Some(side);
        self
    }
    #[inline(always)]
    pub fn weight(&mut self, weight: u64) -> &mut Self {
        self.instruction.weight = Some(weight);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = SetVoteInstructionArgs {
            side: self.instruction.side.clone().expect("side is not set"),
            weight: self.instruction.weight.clone().expect("weight is not set"),
        };
        let instruction = SetVoteCpi {
            __program: self.instruction.__program,

            governor: self.instruction.governor.expect("governor is not set"),

            proposal: self.instruction.proposal.expect("proposal is not set"),

            vote: self.instruction.vote.expect("vote is not set"),

            locker: self.instruction.locker.expect("locker is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SetVoteCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    governor: Option<&'b solana_account_info::AccountInfo<'a>>,
    proposal: Option<&'b solana_account_info::AccountInfo<'a>>,
    vote: Option<&'b solana_account_info::AccountInfo<'a>>,
    locker: Option<&'b solana_account_info::AccountInfo<'a>>,
    side: Option<u8>,
    weight: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
