//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use crate::generated::types::DexPegOracleKind;
use crate::generated::types::Sources;
use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct DexPegOracleConfig {
    pub discriminator: [u8; 8],
    pub nonce: u16,
    pub dex: Address,
    pub position_token0: Address,
    pub position_token1: Address,
    pub token_reserve0: Address,
    pub token_reserve1: Address,
    /// Which token the combined reserves are denominated in (EVM
    /// `QUOTE_IN_TOKEN0`). Irrelevant for pure 1:1 pegs (both orientations sum
    /// identically); meaningful once a [`Self::conversion_source`] is set.
    pub quote_in_token0: bool,
    /// Optional reserves-conversion price source (EVM
    /// `RESERVES_CONVERSION_ORACLE`), needed for non-1:1 pools like LST/SOL:
    /// prices ONE unit of token0 in token1 units (1e15-scaled after the
    /// entry's own `invert`). `source == Pubkey::default()` → hard 1:1 peg
    /// (USDC/USDT style), no extra account.
    /// Restricted to single-account source types (Pyth, StakePool, MsolPool,
    /// Redstone, Chainlink, ChainlinkDataStreams).
    pub conversion_source: Sources,
    pub peg_buffer_percent: u128,
    pub kind: DexPegOracleKind,
    pub bump: u8,
    pub reserved: [u8; 32],
}

pub const DEX_PEG_ORACLE_CONFIG_DISCRIMINATOR: [u8; 8] = [59, 189, 99, 149, 128, 5, 255, 72];

impl DexPegOracleConfig {
    pub const LEN: usize = 287;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for DexPegOracleConfig {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_dex_peg_oracle_config(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<DexPegOracleConfig>, std::io::Error> {
    let accounts = fetch_all_dex_peg_oracle_config(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_dex_peg_oracle_config(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<DexPegOracleConfig>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<DexPegOracleConfig>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::other(format!(
            "Account not found: {address}"
        )))?;
        let data = DexPegOracleConfig::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_dex_peg_oracle_config(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<DexPegOracleConfig>, std::io::Error> {
    let accounts = fetch_all_maybe_dex_peg_oracle_config(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_dex_peg_oracle_config(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<DexPegOracleConfig>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<DexPegOracleConfig>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = DexPegOracleConfig::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for DexPegOracleConfig {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for DexPegOracleConfig {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for DexPegOracleConfig {
    fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(crate::LEND_ORACLE_ID.to_bytes())
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for DexPegOracleConfig {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for DexPegOracleConfig {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
