/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findCampaignPda } from "../pdas";
import { GENIE_DISTRIBUTOR_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_CAMPAIGN_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([169, 88, 7, 6, 9, 165, 65, 132]);

export function getInitializeCampaignDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CAMPAIGN_DISCRIMINATOR,
  );
}

export type InitializeCampaignInstruction<
  TProgram extends string = typeof GENIE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountCampaign extends string | AccountMeta<string> = string,
  TAccountClawbackReceiver extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountTokenVault extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TAccountAssociatedTokenProgram extends string | AccountMeta<string> =
    "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCampaign extends string
        ? WritableAccount<TAccountCampaign>
        : TAccountCampaign,
      TAccountClawbackReceiver extends string
        ? ReadonlyAccount<TAccountClawbackReceiver>
        : TAccountClawbackReceiver,
      TAccountMint extends string
        ? ReadonlyAccount<TAccountMint>
        : TAccountMint,
      TAccountTokenVault extends string
        ? ReadonlyAccount<TAccountTokenVault>
        : TAccountTokenVault,
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountAssociatedTokenProgram extends string
        ? ReadonlyAccount<TAccountAssociatedTokenProgram>
        : TAccountAssociatedTokenProgram,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeCampaignInstructionData = {
  discriminator: ReadonlyUint8Array;
  campaignId: string;
  claimsPubkey: Address;
  allocatedAmounts: Array<bigint>;
  startTs: bigint;
  endTs: bigint;
};

export type InitializeCampaignInstructionDataArgs = {
  campaignId: string;
  claimsPubkey: Address;
  allocatedAmounts: Array<number | bigint>;
  startTs: number | bigint;
  endTs: number | bigint;
};

export function getInitializeCampaignInstructionDataEncoder(): Encoder<InitializeCampaignInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["campaignId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["claimsPubkey", getAddressEncoder()],
      ["allocatedAmounts", getArrayEncoder(getU64Encoder(), { size: 5 })],
      ["startTs", getI64Encoder()],
      ["endTs", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_CAMPAIGN_DISCRIMINATOR }),
  );
}

export function getInitializeCampaignInstructionDataDecoder(): Decoder<InitializeCampaignInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["campaignId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["claimsPubkey", getAddressDecoder()],
    ["allocatedAmounts", getArrayDecoder(getU64Decoder(), { size: 5 })],
    ["startTs", getI64Decoder()],
    ["endTs", getI64Decoder()],
  ]);
}

export function getInitializeCampaignInstructionDataCodec(): Codec<
  InitializeCampaignInstructionDataArgs,
  InitializeCampaignInstructionData
> {
  return combineCodec(
    getInitializeCampaignInstructionDataEncoder(),
    getInitializeCampaignInstructionDataDecoder(),
  );
}

export type InitializeCampaignAsyncInput<
  TAccountCampaign extends string = string,
  TAccountClawbackReceiver extends string = string,
  TAccountMint extends string = string,
  TAccountTokenVault extends string = string,
  TAccountAdmin extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** [Campaign]. */
  campaign?: Address<TAccountCampaign>;
  /** Clawback receiver token account */
  clawbackReceiver: Address<TAccountClawbackReceiver>;
  /** The mint to distribute. */
  mint: Address<TAccountMint>;
  /**
   * Token vault
   * Should create previously
   */
  tokenVault?: Address<TAccountTokenVault>;
  /**
   * Admin wallet, responsible for creating the campaign and paying for the transaction.
   * Also has the authority to set the clawback receiver and change itself.
   */
  admin: TransactionSigner<TAccountAdmin>;
  /** The [System] program. */
  systemProgram?: Address<TAccountSystemProgram>;
  /** The [Associated Token] program. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** The [Token] program. */
  tokenProgram?: Address<TAccountTokenProgram>;
  campaignId: InitializeCampaignInstructionDataArgs["campaignId"];
  claimsPubkey: InitializeCampaignInstructionDataArgs["claimsPubkey"];
  allocatedAmounts: InitializeCampaignInstructionDataArgs["allocatedAmounts"];
  startTs: InitializeCampaignInstructionDataArgs["startTs"];
  endTs: InitializeCampaignInstructionDataArgs["endTs"];
};

export async function getInitializeCampaignInstructionAsync<
  TAccountCampaign extends string,
  TAccountClawbackReceiver extends string,
  TAccountMint extends string,
  TAccountTokenVault extends string,
  TAccountAdmin extends string,
  TAccountSystemProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GENIE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: InitializeCampaignAsyncInput<
    TAccountCampaign,
    TAccountClawbackReceiver,
    TAccountMint,
    TAccountTokenVault,
    TAccountAdmin,
    TAccountSystemProgram,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeCampaignInstruction<
    TProgramAddress,
    TAccountCampaign,
    TAccountClawbackReceiver,
    TAccountMint,
    TAccountTokenVault,
    TAccountAdmin,
    TAccountSystemProgram,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GENIE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    campaign: { value: input.campaign ?? null, isWritable: true },
    clawbackReceiver: {
      value: input.clawbackReceiver ?? null,
      isWritable: false,
    },
    mint: { value: input.mint ?? null, isWritable: false },
    tokenVault: { value: input.tokenVault ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.campaign.value) {
    accounts.campaign.value = await findCampaignPda({
      admin: getAddressFromResolvedInstructionAccount(
        "admin",
        accounts.admin.value,
      ),
      mint: getAddressFromResolvedInstructionAccount(
        "mint",
        accounts.mint.value,
      ),
      campaignId: getNonNullResolvedInstructionInput(
        "campaignId",
        args.campaignId,
      ),
    });
  }
  if (!accounts.tokenVault.value) {
    accounts.tokenVault.value = await getProgramDerivedAddress({
      programAddress:
        "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">,
      seeds: [
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "campaign",
            accounts.campaign.value,
          ),
        ),
        getBytesEncoder().encode(
          new Uint8Array([
            6, 221, 246, 225, 215, 101, 161, 147, 217, 203, 225, 70, 206, 235,
            121, 172, 28, 180, 133, 237, 95, 91, 55, 145, 58, 140, 245, 133,
            126, 255, 0, 169,
          ]),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount("mint", accounts.mint.value),
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("campaign", accounts.campaign),
      getAccountMeta("clawbackReceiver", accounts.clawbackReceiver),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("tokenVault", accounts.tokenVault),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getInitializeCampaignInstructionDataEncoder().encode(
      args as InitializeCampaignInstructionDataArgs,
    ),
    programAddress,
  } as InitializeCampaignInstruction<
    TProgramAddress,
    TAccountCampaign,
    TAccountClawbackReceiver,
    TAccountMint,
    TAccountTokenVault,
    TAccountAdmin,
    TAccountSystemProgram,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram
  >);
}

export type InitializeCampaignInput<
  TAccountCampaign extends string = string,
  TAccountClawbackReceiver extends string = string,
  TAccountMint extends string = string,
  TAccountTokenVault extends string = string,
  TAccountAdmin extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountAssociatedTokenProgram extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** [Campaign]. */
  campaign: Address<TAccountCampaign>;
  /** Clawback receiver token account */
  clawbackReceiver: Address<TAccountClawbackReceiver>;
  /** The mint to distribute. */
  mint: Address<TAccountMint>;
  /**
   * Token vault
   * Should create previously
   */
  tokenVault: Address<TAccountTokenVault>;
  /**
   * Admin wallet, responsible for creating the campaign and paying for the transaction.
   * Also has the authority to set the clawback receiver and change itself.
   */
  admin: TransactionSigner<TAccountAdmin>;
  /** The [System] program. */
  systemProgram?: Address<TAccountSystemProgram>;
  /** The [Associated Token] program. */
  associatedTokenProgram?: Address<TAccountAssociatedTokenProgram>;
  /** The [Token] program. */
  tokenProgram?: Address<TAccountTokenProgram>;
  campaignId: InitializeCampaignInstructionDataArgs["campaignId"];
  claimsPubkey: InitializeCampaignInstructionDataArgs["claimsPubkey"];
  allocatedAmounts: InitializeCampaignInstructionDataArgs["allocatedAmounts"];
  startTs: InitializeCampaignInstructionDataArgs["startTs"];
  endTs: InitializeCampaignInstructionDataArgs["endTs"];
};

export function getInitializeCampaignInstruction<
  TAccountCampaign extends string,
  TAccountClawbackReceiver extends string,
  TAccountMint extends string,
  TAccountTokenVault extends string,
  TAccountAdmin extends string,
  TAccountSystemProgram extends string,
  TAccountAssociatedTokenProgram extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof GENIE_DISTRIBUTOR_PROGRAM_ADDRESS,
>(
  input: InitializeCampaignInput<
    TAccountCampaign,
    TAccountClawbackReceiver,
    TAccountMint,
    TAccountTokenVault,
    TAccountAdmin,
    TAccountSystemProgram,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeCampaignInstruction<
  TProgramAddress,
  TAccountCampaign,
  TAccountClawbackReceiver,
  TAccountMint,
  TAccountTokenVault,
  TAccountAdmin,
  TAccountSystemProgram,
  TAccountAssociatedTokenProgram,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? GENIE_DISTRIBUTOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    campaign: { value: input.campaign ?? null, isWritable: true },
    clawbackReceiver: {
      value: input.clawbackReceiver ?? null,
      isWritable: false,
    },
    mint: { value: input.mint ?? null, isWritable: false },
    tokenVault: { value: input.tokenVault ?? null, isWritable: false },
    admin: { value: input.admin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    associatedTokenProgram: {
      value: input.associatedTokenProgram ?? null,
      isWritable: false,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }
  if (!accounts.associatedTokenProgram.value) {
    accounts.associatedTokenProgram.value =
      "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">;
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("campaign", accounts.campaign),
      getAccountMeta("clawbackReceiver", accounts.clawbackReceiver),
      getAccountMeta("mint", accounts.mint),
      getAccountMeta("tokenVault", accounts.tokenVault),
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("systemProgram", accounts.systemProgram),
      getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getInitializeCampaignInstructionDataEncoder().encode(
      args as InitializeCampaignInstructionDataArgs,
    ),
    programAddress,
  } as InitializeCampaignInstruction<
    TProgramAddress,
    TAccountCampaign,
    TAccountClawbackReceiver,
    TAccountMint,
    TAccountTokenVault,
    TAccountAdmin,
    TAccountSystemProgram,
    TAccountAssociatedTokenProgram,
    TAccountTokenProgram
  >);
}

export type ParsedInitializeCampaignInstruction<
  TProgram extends string = typeof GENIE_DISTRIBUTOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** [Campaign]. */
    campaign: TAccountMetas[0];
    /** Clawback receiver token account */
    clawbackReceiver: TAccountMetas[1];
    /** The mint to distribute. */
    mint: TAccountMetas[2];
    /**
     * Token vault
     * Should create previously
     */
    tokenVault: TAccountMetas[3];
    /**
     * Admin wallet, responsible for creating the campaign and paying for the transaction.
     * Also has the authority to set the clawback receiver and change itself.
     */
    admin: TAccountMetas[4];
    /** The [System] program. */
    systemProgram: TAccountMetas[5];
    /** The [Associated Token] program. */
    associatedTokenProgram: TAccountMetas[6];
    /** The [Token] program. */
    tokenProgram: TAccountMetas[7];
  };
  data: InitializeCampaignInstructionData;
};

export function parseInitializeCampaignInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeCampaignInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      campaign: getNextAccount(),
      clawbackReceiver: getNextAccount(),
      mint: getNextAccount(),
      tokenVault: getNextAccount(),
      admin: getNextAccount(),
      systemProgram: getNextAccount(),
      associatedTokenProgram: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getInitializeCampaignInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
