/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getTicketStatusDecoder,
  getTicketStatusEncoder,
  type TicketStatus,
  type TicketStatusArgs,
} from "../types";

export const TICKET_CLOSED_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([48, 69, 175, 107, 31, 27, 141, 71]);

export function getTicketClosedEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TICKET_CLOSED_EVENT_DISCRIMINATOR,
  );
}

export type TicketClosedEvent = {
  ticket: Address;
  owner: Address;
  ticketId: string;
  marketId: string;
  status: TicketStatus;
  refundAmount: bigint;
  timestamp: bigint;
  closedBy: Address;
};

export type TicketClosedEventArgs = {
  ticket: Address;
  owner: Address;
  ticketId: string;
  marketId: string;
  status: TicketStatusArgs;
  refundAmount: number | bigint;
  timestamp: number | bigint;
  closedBy: Address;
};

/** Gets the encoder for {@link TicketClosedEventArgs} event data. */
export function getTicketClosedEventEncoder(): Encoder<TicketClosedEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["ticket", getAddressEncoder()],
      ["owner", getAddressEncoder()],
      ["ticketId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["marketId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["status", getTicketStatusEncoder()],
      ["refundAmount", getU64Encoder()],
      ["timestamp", getI64Encoder()],
      ["closedBy", getAddressEncoder()],
    ]),
    [getConstantEncoder(TICKET_CLOSED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link TicketClosedEvent} event data. */
export function getTicketClosedEventDecoder(): Decoder<TicketClosedEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["ticket", getAddressDecoder()],
      ["owner", getAddressDecoder()],
      ["ticketId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["marketId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["status", getTicketStatusDecoder()],
      ["refundAmount", getU64Decoder()],
      ["timestamp", getI64Decoder()],
      ["closedBy", getAddressDecoder()],
    ]),
    [getConstantDecoder(TICKET_CLOSED_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link TicketClosedEvent} event data. */
export function getTicketClosedEventCodec(): Codec<
  TicketClosedEventArgs,
  TicketClosedEvent
> {
  return combineCodec(
    getTicketClosedEventEncoder(),
    getTicketClosedEventDecoder(),
  );
}

/** Parses the provided bytes into {@link TicketClosedEvent} event data. */
export function parseTicketClosedEvent(
  data: ReadonlyUint8Array | Uint8Array,
): TicketClosedEvent {
  if (containsBytes(data, TICKET_CLOSED_EVENT_DISCRIMINATOR, 0)) {
    return getTicketClosedEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "TicketClosedEvent" event discriminators.',
  );
}
