/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CLAIM_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([93, 15, 70, 170, 48, 140, 212, 219]);

export function getClaimEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIM_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type ClaimEventEvent = {
  /** User that claimed. */
  claimant: Address;
  /** Timestamp. */
  timestamp: bigint;
  claimedAmounts: Array<bigint>;
  claimedLootboxes: Array<bigint>;
  accumulatedAmounts: Array<bigint>;
  accumulatedLootboxes: Array<bigint>;
};

export type ClaimEventEventArgs = {
  /** User that claimed. */
  claimant: Address;
  /** Timestamp. */
  timestamp: number | bigint;
  claimedAmounts: Array<number | bigint>;
  claimedLootboxes: Array<number | bigint>;
  accumulatedAmounts: Array<number | bigint>;
  accumulatedLootboxes: Array<number | bigint>;
};

/** Gets the encoder for {@link ClaimEventEventArgs} event data. */
export function getClaimEventEventEncoder(): FixedSizeEncoder<ClaimEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["claimant", getAddressEncoder()],
      ["timestamp", getI64Encoder()],
      ["claimedAmounts", getArrayEncoder(getU64Encoder(), { size: 5 })],
      ["claimedLootboxes", getArrayEncoder(getU64Encoder(), { size: 5 })],
      ["accumulatedAmounts", getArrayEncoder(getU64Encoder(), { size: 5 })],
      ["accumulatedLootboxes", getArrayEncoder(getU64Encoder(), { size: 5 })],
    ]),
    [getConstantEncoder(CLAIM_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ClaimEventEvent} event data. */
export function getClaimEventEventDecoder(): FixedSizeDecoder<ClaimEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["claimant", getAddressDecoder()],
      ["timestamp", getI64Decoder()],
      ["claimedAmounts", getArrayDecoder(getU64Decoder(), { size: 5 })],
      ["claimedLootboxes", getArrayDecoder(getU64Decoder(), { size: 5 })],
      ["accumulatedAmounts", getArrayDecoder(getU64Decoder(), { size: 5 })],
      ["accumulatedLootboxes", getArrayDecoder(getU64Decoder(), { size: 5 })],
    ]),
    [getConstantDecoder(CLAIM_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ClaimEventEvent} event data. */
export function getClaimEventEventCodec(): FixedSizeCodec<
  ClaimEventEventArgs,
  ClaimEventEvent
> {
  return combineCodec(getClaimEventEventEncoder(), getClaimEventEventDecoder());
}

/** Parses the provided bytes into {@link ClaimEventEvent} event data. */
export function parseClaimEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ClaimEventEvent {
  if (containsBytes(data, CLAIM_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getClaimEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ClaimEventEvent" event discriminators.',
  );
}
