//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const CREATE_BENEFACTOR_DISCRIMINATOR: [u8; 8] = [184, 241, 45, 0, 53, 40, 201, 54];

/// Accounts.
#[derive(Debug)]
pub struct CreateBenefactor {
    pub operator_authority: solana_address::Address,

    pub operator: solana_address::Address,

    pub payer: solana_address::Address,

    pub benefactor_authority: solana_address::Address,

    pub benefactor: solana_address::Address,

    pub system_program: solana_address::Address,
}

impl CreateBenefactor {
    pub fn instruction(
        &self,
        args: CreateBenefactorInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: CreateBenefactorInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(6 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.operator_authority,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.operator,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.payer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.benefactor_authority,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.benefactor, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = CreateBenefactorInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::JUP_STABLE_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CreateBenefactorInstructionData {
    discriminator: [u8; 8],
}

impl CreateBenefactorInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [184, 241, 45, 0, 53, 40, 201, 54],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for CreateBenefactorInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct CreateBenefactorInstructionArgs {
    pub mint_fee_rate: u16,
    pub redeem_fee_rate: u16,
}

impl CreateBenefactorInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `CreateBenefactor`.
///
/// ### Accounts:
///
///   0. `[signer]` operator_authority
///   1. `[]` operator
///   2. `[writable, signer]` payer
///   3. `[]` benefactor_authority
///   4. `[writable]` benefactor
///   5. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct CreateBenefactorBuilder {
    operator_authority: Option<solana_address::Address>,
    operator: Option<solana_address::Address>,
    payer: Option<solana_address::Address>,
    benefactor_authority: Option<solana_address::Address>,
    benefactor: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    mint_fee_rate: Option<u16>,
    redeem_fee_rate: Option<u16>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl CreateBenefactorBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn operator_authority(&mut self, operator_authority: solana_address::Address) -> &mut Self {
        self.operator_authority = Some(operator_authority);
        self
    }
    #[inline(always)]
    pub fn operator(&mut self, operator: solana_address::Address) -> &mut Self {
        self.operator = Some(operator);
        self
    }
    #[inline(always)]
    pub fn payer(&mut self, payer: solana_address::Address) -> &mut Self {
        self.payer = Some(payer);
        self
    }
    #[inline(always)]
    pub fn benefactor_authority(
        &mut self,
        benefactor_authority: solana_address::Address,
    ) -> &mut Self {
        self.benefactor_authority = Some(benefactor_authority);
        self
    }
    #[inline(always)]
    pub fn benefactor(&mut self, benefactor: solana_address::Address) -> &mut Self {
        self.benefactor = Some(benefactor);
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn mint_fee_rate(&mut self, mint_fee_rate: u16) -> &mut Self {
        self.mint_fee_rate = Some(mint_fee_rate);
        self
    }
    #[inline(always)]
    pub fn redeem_fee_rate(&mut self, redeem_fee_rate: u16) -> &mut Self {
        self.redeem_fee_rate = Some(redeem_fee_rate);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = CreateBenefactor {
            operator_authority: self
                .operator_authority
                .expect("operator_authority is not set"),
            operator: self.operator.expect("operator is not set"),
            payer: self.payer.expect("payer is not set"),
            benefactor_authority: self
                .benefactor_authority
                .expect("benefactor_authority is not set"),
            benefactor: self.benefactor.expect("benefactor is not set"),
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = CreateBenefactorInstructionArgs {
            mint_fee_rate: self
                .mint_fee_rate
                .clone()
                .expect("mint_fee_rate is not set"),
            redeem_fee_rate: self
                .redeem_fee_rate
                .clone()
                .expect("redeem_fee_rate is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `create_benefactor` CPI accounts.
pub struct CreateBenefactorCpiAccounts<'a, 'b> {
    pub operator_authority: &'b solana_account_info::AccountInfo<'a>,

    pub operator: &'b solana_account_info::AccountInfo<'a>,

    pub payer: &'b solana_account_info::AccountInfo<'a>,

    pub benefactor_authority: &'b solana_account_info::AccountInfo<'a>,

    pub benefactor: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `create_benefactor` CPI instruction.
pub struct CreateBenefactorCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub operator_authority: &'b solana_account_info::AccountInfo<'a>,

    pub operator: &'b solana_account_info::AccountInfo<'a>,

    pub payer: &'b solana_account_info::AccountInfo<'a>,

    pub benefactor_authority: &'b solana_account_info::AccountInfo<'a>,

    pub benefactor: &'b solana_account_info::AccountInfo<'a>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: CreateBenefactorInstructionArgs,
}

impl<'a, 'b> CreateBenefactorCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: CreateBenefactorCpiAccounts<'a, 'b>,
        args: CreateBenefactorInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            operator_authority: accounts.operator_authority,
            operator: accounts.operator,
            payer: accounts.payer,
            benefactor_authority: accounts.benefactor_authority,
            benefactor: accounts.benefactor,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(6 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.operator_authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.operator.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.payer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.benefactor_authority.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.benefactor.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = CreateBenefactorInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::JUP_STABLE_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(7 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.operator_authority.clone());
        account_infos.push(self.operator.clone());
        account_infos.push(self.payer.clone());
        account_infos.push(self.benefactor_authority.clone());
        account_infos.push(self.benefactor.clone());
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `CreateBenefactor` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` operator_authority
///   1. `[]` operator
///   2. `[writable, signer]` payer
///   3. `[]` benefactor_authority
///   4. `[writable]` benefactor
///   5. `[]` system_program
#[derive(Clone, Debug)]
pub struct CreateBenefactorCpiBuilder<'a, 'b> {
    instruction: Box<CreateBenefactorCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> CreateBenefactorCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(CreateBenefactorCpiBuilderInstruction {
            __program: program,
            operator_authority: None,
            operator: None,
            payer: None,
            benefactor_authority: None,
            benefactor: None,
            system_program: None,
            mint_fee_rate: None,
            redeem_fee_rate: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn operator_authority(
        &mut self,
        operator_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.operator_authority = Some(operator_authority);
        self
    }
    #[inline(always)]
    pub fn operator(&mut self, operator: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.operator = Some(operator);
        self
    }
    #[inline(always)]
    pub fn payer(&mut self, payer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.payer = Some(payer);
        self
    }
    #[inline(always)]
    pub fn benefactor_authority(
        &mut self,
        benefactor_authority: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.benefactor_authority = Some(benefactor_authority);
        self
    }
    #[inline(always)]
    pub fn benefactor(
        &mut self,
        benefactor: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.benefactor = Some(benefactor);
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn mint_fee_rate(&mut self, mint_fee_rate: u16) -> &mut Self {
        self.instruction.mint_fee_rate = Some(mint_fee_rate);
        self
    }
    #[inline(always)]
    pub fn redeem_fee_rate(&mut self, redeem_fee_rate: u16) -> &mut Self {
        self.instruction.redeem_fee_rate = Some(redeem_fee_rate);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = CreateBenefactorInstructionArgs {
            mint_fee_rate: self
                .instruction
                .mint_fee_rate
                .clone()
                .expect("mint_fee_rate is not set"),
            redeem_fee_rate: self
                .instruction
                .redeem_fee_rate
                .clone()
                .expect("redeem_fee_rate is not set"),
        };
        let instruction = CreateBenefactorCpi {
            __program: self.instruction.__program,

            operator_authority: self
                .instruction
                .operator_authority
                .expect("operator_authority is not set"),

            operator: self.instruction.operator.expect("operator is not set"),

            payer: self.instruction.payer.expect("payer is not set"),

            benefactor_authority: self
                .instruction
                .benefactor_authority
                .expect("benefactor_authority is not set"),

            benefactor: self.instruction.benefactor.expect("benefactor is not set"),

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct CreateBenefactorCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    operator_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    operator: Option<&'b solana_account_info::AccountInfo<'a>>,
    payer: Option<&'b solana_account_info::AccountInfo<'a>>,
    benefactor_authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    benefactor: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    mint_fee_rate: Option<u16>,
    redeem_fee_rate: Option<u16>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
