/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_SWAP_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  202, 242, 228, 28, 37, 194, 52, 34,
]);

export function getLogSwapEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_SWAP_EVENT_DISCRIMINATOR,
  );
}

export type LogSwapEvent = {
  dexId: number;
  swap0To1: boolean;
  amountIn: bigint;
  amountOut: bigint;
  to: Address;
};

export type LogSwapEventArgs = {
  dexId: number;
  swap0To1: boolean;
  amountIn: number | bigint;
  amountOut: number | bigint;
  to: Address;
};

/** Gets the encoder for {@link LogSwapEventArgs} event data. */
export function getLogSwapEventEncoder(): FixedSizeEncoder<LogSwapEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["dexId", getU16Encoder()],
      ["swap0To1", getBooleanEncoder()],
      ["amountIn", getU64Encoder()],
      ["amountOut", getU64Encoder()],
      ["to", getAddressEncoder()],
    ]),
    [getConstantEncoder(LOG_SWAP_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogSwapEvent} event data. */
export function getLogSwapEventDecoder(): FixedSizeDecoder<LogSwapEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["dexId", getU16Decoder()],
      ["swap0To1", getBooleanDecoder()],
      ["amountIn", getU64Decoder()],
      ["amountOut", getU64Decoder()],
      ["to", getAddressDecoder()],
    ]),
    [getConstantDecoder(LOG_SWAP_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogSwapEvent} event data. */
export function getLogSwapEventCodec(): FixedSizeCodec<
  LogSwapEventArgs,
  LogSwapEvent
> {
  return combineCodec(getLogSwapEventEncoder(), getLogSwapEventDecoder());
}

/** Parses the provided bytes into {@link LogSwapEvent} event data. */
export function parseLogSwapEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogSwapEvent {
  if (containsBytes(data, LOG_SWAP_EVENT_DISCRIMINATOR, 0)) {
    return getLogSwapEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogSwapEvent" event discriminators.',
  );
}
