/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findClaimAccountPda } from "../pdas";
import { LEND_LIQUIDITY_PROGRAM_ADDRESS } from "../programs";

export const INIT_CLAIM_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([112, 141, 47, 170, 42, 99, 144, 145]);

export function getInitClaimAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_CLAIM_ACCOUNT_DISCRIMINATOR,
  );
}

export type InitClaimAccountInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountClaimAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountClaimAccount extends string
        ? WritableAccount<TAccountClaimAccount>
        : TAccountClaimAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitClaimAccountInstructionData = {
  discriminator: ReadonlyUint8Array;
  mint: Address;
  user: Address;
};

export type InitClaimAccountInstructionDataArgs = {
  mint: Address;
  user: Address;
};

export function getInitClaimAccountInstructionDataEncoder(): FixedSizeEncoder<InitClaimAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["mint", getAddressEncoder()],
      ["user", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_CLAIM_ACCOUNT_DISCRIMINATOR }),
  );
}

export function getInitClaimAccountInstructionDataDecoder(): FixedSizeDecoder<InitClaimAccountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["mint", getAddressDecoder()],
    ["user", getAddressDecoder()],
  ]);
}

export function getInitClaimAccountInstructionDataCodec(): FixedSizeCodec<
  InitClaimAccountInstructionDataArgs,
  InitClaimAccountInstructionData
> {
  return combineCodec(
    getInitClaimAccountInstructionDataEncoder(),
    getInitClaimAccountInstructionDataDecoder(),
  );
}

export type InitClaimAccountAsyncInput<
  TAccountSigner extends string = string,
  TAccountClaimAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  claimAccount?: Address<TAccountClaimAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  mint: InitClaimAccountInstructionDataArgs["mint"];
  user: InitClaimAccountInstructionDataArgs["user"];
};

export async function getInitClaimAccountInstructionAsync<
  TAccountSigner extends string,
  TAccountClaimAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: InitClaimAccountAsyncInput<
    TAccountSigner,
    TAccountClaimAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitClaimAccountInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountClaimAccount,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    claimAccount: { value: input.claimAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.claimAccount.value) {
    accounts.claimAccount.value = await findClaimAccountPda(
      {
        user: getNonNullResolvedInstructionInput("user", args.user),
        mint: getNonNullResolvedInstructionInput("mint", args.mint),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("claimAccount", accounts.claimAccount),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitClaimAccountInstructionDataEncoder().encode(
      args as InitClaimAccountInstructionDataArgs,
    ),
    programAddress,
  } as InitClaimAccountInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountClaimAccount,
    TAccountSystemProgram
  >);
}

export type InitClaimAccountInput<
  TAccountSigner extends string = string,
  TAccountClaimAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  claimAccount: Address<TAccountClaimAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  mint: InitClaimAccountInstructionDataArgs["mint"];
  user: InitClaimAccountInstructionDataArgs["user"];
};

export function getInitClaimAccountInstruction<
  TAccountSigner extends string,
  TAccountClaimAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
>(
  input: InitClaimAccountInput<
    TAccountSigner,
    TAccountClaimAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitClaimAccountInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountClaimAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LEND_LIQUIDITY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    claimAccount: { value: input.claimAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("claimAccount", accounts.claimAccount),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitClaimAccountInstructionDataEncoder().encode(
      args as InitClaimAccountInstructionDataArgs,
    ),
    programAddress,
  } as InitClaimAccountInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountClaimAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitClaimAccountInstruction<
  TProgram extends string = typeof LEND_LIQUIDITY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    claimAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitClaimAccountInstructionData;
};

export function parseInitClaimAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitClaimAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      claimAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitClaimAccountInstructionDataDecoder().decode(instruction.data),
  };
}
