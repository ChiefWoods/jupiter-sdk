/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { FLASHLOAN_PROGRAM_ADDRESS } from "../programs";

export const ACTIVATE_PROTOCOL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([230, 235, 188, 19, 120, 91, 11, 94]);

export function getActivateProtocolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACTIVATE_PROTOCOL_DISCRIMINATOR,
  );
}

export type ActivateProtocolInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountFlashloanAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountFlashloanAdmin extends string
        ? WritableAccount<TAccountFlashloanAdmin>
        : TAccountFlashloanAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type ActivateProtocolInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ActivateProtocolInstructionDataArgs = {};

export function getActivateProtocolInstructionDataEncoder(): FixedSizeEncoder<ActivateProtocolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ACTIVATE_PROTOCOL_DISCRIMINATOR }),
  );
}

export function getActivateProtocolInstructionDataDecoder(): FixedSizeDecoder<ActivateProtocolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getActivateProtocolInstructionDataCodec(): FixedSizeCodec<
  ActivateProtocolInstructionDataArgs,
  ActivateProtocolInstructionData
> {
  return combineCodec(
    getActivateProtocolInstructionDataEncoder(),
    getActivateProtocolInstructionDataDecoder(),
  );
}

export type ActivateProtocolInput<
  TAccountAuthority extends string = string,
  TAccountFlashloanAdmin extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  flashloanAdmin: Address<TAccountFlashloanAdmin>;
};

export function getActivateProtocolInstruction<
  TAccountAuthority extends string,
  TAccountFlashloanAdmin extends string,
  TProgramAddress extends Address = typeof FLASHLOAN_PROGRAM_ADDRESS,
>(
  input: ActivateProtocolInput<TAccountAuthority, TAccountFlashloanAdmin>,
  config?: { programAddress?: TProgramAddress },
): ActivateProtocolInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountFlashloanAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLASHLOAN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    flashloanAdmin: { value: input.flashloanAdmin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("flashloanAdmin", accounts.flashloanAdmin),
    ],
    data: getActivateProtocolInstructionDataEncoder().encode({}),
    programAddress,
  } as ActivateProtocolInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountFlashloanAdmin
  >);
}

export type ParsedActivateProtocolInstruction<
  TProgram extends string = typeof FLASHLOAN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    flashloanAdmin: TAccountMetas[1];
  };
  data: ActivateProtocolInstructionData;
};

export function parseActivateProtocolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedActivateProtocolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), flashloanAdmin: getNextAccount() },
    data: getActivateProtocolInstructionDataDecoder().decode(instruction.data),
  };
}
