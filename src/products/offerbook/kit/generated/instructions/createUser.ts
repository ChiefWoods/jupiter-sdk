/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findSignerUserPda } from "../pdas";
import { OFFERBOOK_PROGRAM_ADDRESS } from "../programs";

export const CREATE_USER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  108, 227, 130, 130, 252, 109, 75, 218,
]);

export function getCreateUserDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_USER_DISCRIMINATOR);
}

export type CreateUserInstruction<
  TProgram extends string = typeof OFFERBOOK_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountSignerUser extends string | AccountMeta<string> = string,
  TAccountReferrer extends string | AccountMeta<string> = string,
  TAccountReferrerUser extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountSignerUser extends string
        ? WritableAccount<TAccountSignerUser>
        : TAccountSignerUser,
      TAccountReferrer extends string
        ? ReadonlyAccount<TAccountReferrer>
        : TAccountReferrer,
      TAccountReferrerUser extends string
        ? WritableAccount<TAccountReferrerUser>
        : TAccountReferrerUser,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateUserInstructionData = { discriminator: ReadonlyUint8Array };

export type CreateUserInstructionDataArgs = {};

export function getCreateUserInstructionDataEncoder(): FixedSizeEncoder<CreateUserInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CREATE_USER_DISCRIMINATOR }),
  );
}

export function getCreateUserInstructionDataDecoder(): FixedSizeDecoder<CreateUserInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCreateUserInstructionDataCodec(): FixedSizeCodec<
  CreateUserInstructionDataArgs,
  CreateUserInstructionData
> {
  return combineCodec(
    getCreateUserInstructionDataEncoder(),
    getCreateUserInstructionDataDecoder(),
  );
}

export type CreateUserAsyncInput<
  TAccountSigner extends string = string,
  TAccountSignerUser extends string = string,
  TAccountReferrer extends string = string,
  TAccountReferrerUser extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  signerUser?: Address<TAccountSignerUser>;
  referrer?: Address<TAccountReferrer>;
  referrerUser?: Address<TAccountReferrerUser>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCreateUserInstructionAsync<
  TAccountSigner extends string,
  TAccountSignerUser extends string,
  TAccountReferrer extends string,
  TAccountReferrerUser extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OFFERBOOK_PROGRAM_ADDRESS,
>(
  input: CreateUserAsyncInput<
    TAccountSigner,
    TAccountSignerUser,
    TAccountReferrer,
    TAccountReferrerUser,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateUserInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountReferrer,
    TAccountReferrerUser,
    TAccountConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? OFFERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    signerUser: { value: input.signerUser ?? null, isWritable: true },
    referrer: { value: input.referrer ?? null, isWritable: false },
    referrerUser: { value: input.referrerUser ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.signerUser.value) {
    accounts.signerUser.value = await findSignerUserPda({
      signer: getAddressFromResolvedInstructionAccount(
        "signer",
        accounts.signer.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("signerUser", accounts.signerUser),
      getAccountMeta("referrer", accounts.referrer),
      getAccountMeta("referrerUser", accounts.referrerUser),
      getAccountMeta("config", accounts.config),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateUserInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateUserInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountReferrer,
    TAccountReferrerUser,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type CreateUserInput<
  TAccountSigner extends string = string,
  TAccountSignerUser extends string = string,
  TAccountReferrer extends string = string,
  TAccountReferrerUser extends string = string,
  TAccountConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  signerUser: Address<TAccountSignerUser>;
  referrer?: Address<TAccountReferrer>;
  referrerUser?: Address<TAccountReferrerUser>;
  config: Address<TAccountConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCreateUserInstruction<
  TAccountSigner extends string,
  TAccountSignerUser extends string,
  TAccountReferrer extends string,
  TAccountReferrerUser extends string,
  TAccountConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof OFFERBOOK_PROGRAM_ADDRESS,
>(
  input: CreateUserInput<
    TAccountSigner,
    TAccountSignerUser,
    TAccountReferrer,
    TAccountReferrerUser,
    TAccountConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateUserInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountSignerUser,
  TAccountReferrer,
  TAccountReferrerUser,
  TAccountConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? OFFERBOOK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    signerUser: { value: input.signerUser ?? null, isWritable: true },
    referrer: { value: input.referrer ?? null, isWritable: false },
    referrerUser: { value: input.referrerUser ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("signerUser", accounts.signerUser),
      getAccountMeta("referrer", accounts.referrer),
      getAccountMeta("referrerUser", accounts.referrerUser),
      getAccountMeta("config", accounts.config),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateUserInstructionDataEncoder().encode({}),
    programAddress,
  } as CreateUserInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountSignerUser,
    TAccountReferrer,
    TAccountReferrerUser,
    TAccountConfig,
    TAccountSystemProgram
  >);
}

export type ParsedCreateUserInstruction<
  TProgram extends string = typeof OFFERBOOK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    signerUser: TAccountMetas[1];
    referrer?: TAccountMetas[2] | undefined;
    referrerUser?: TAccountMetas[3] | undefined;
    config: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CreateUserInstructionData;
};

export function parseCreateUserInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateUserInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === OFFERBOOK_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      signerUser: getNextAccount(),
      referrer: getNextOptionalAccount(),
      referrerUser: getNextOptionalAccount(),
      config: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateUserInstructionDataDecoder().decode(instruction.data),
  };
}
