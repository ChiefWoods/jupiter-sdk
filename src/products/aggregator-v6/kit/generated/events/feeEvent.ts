/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const FEE_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [73, 79, 78, 127, 184, 213, 13, 220],
);

export function getFeeEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FEE_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type FeeEventEvent = { account: Address; mint: Address; amount: bigint };

export type FeeEventEventArgs = {
  account: Address;
  mint: Address;
  amount: number | bigint;
};

/** Gets the encoder for {@link FeeEventEventArgs} event data. */
export function getFeeEventEventEncoder(): FixedSizeEncoder<FeeEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["account", getAddressEncoder()],
      ["mint", getAddressEncoder()],
      ["amount", getU64Encoder()],
    ]),
    [getConstantEncoder(FEE_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link FeeEventEvent} event data. */
export function getFeeEventEventDecoder(): FixedSizeDecoder<FeeEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["account", getAddressDecoder()],
      ["mint", getAddressDecoder()],
      ["amount", getU64Decoder()],
    ]),
    [getConstantDecoder(FEE_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link FeeEventEvent} event data. */
export function getFeeEventEventCodec(): FixedSizeCodec<
  FeeEventEventArgs,
  FeeEventEvent
> {
  return combineCodec(getFeeEventEventEncoder(), getFeeEventEventDecoder());
}

/** Parses the provided bytes into {@link FeeEventEvent} event data. */
export function parseFeeEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): FeeEventEvent {
  if (containsBytes(data, FEE_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getFeeEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "FeeEventEvent" event discriminators.',
  );
}
