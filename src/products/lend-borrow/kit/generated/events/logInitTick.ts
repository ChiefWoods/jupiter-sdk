/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_INIT_TICK_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([56, 182, 35, 79, 249, 114, 9, 175]);

export function getLogInitTickEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_INIT_TICK_EVENT_DISCRIMINATOR,
  );
}

export type LogInitTickEvent = { tick: Address };

export type LogInitTickEventArgs = LogInitTickEvent;

/** Gets the encoder for {@link LogInitTickEventArgs} event data. */
export function getLogInitTickEventEncoder(): FixedSizeEncoder<LogInitTickEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([["tick", getAddressEncoder()]]),
    [getConstantEncoder(LOG_INIT_TICK_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogInitTickEvent} event data. */
export function getLogInitTickEventDecoder(): FixedSizeDecoder<LogInitTickEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([["tick", getAddressDecoder()]]),
    [getConstantDecoder(LOG_INIT_TICK_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogInitTickEvent} event data. */
export function getLogInitTickEventCodec(): FixedSizeCodec<
  LogInitTickEventArgs,
  LogInitTickEvent
> {
  return combineCodec(
    getLogInitTickEventEncoder(),
    getLogInitTickEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LogInitTickEvent} event data. */
export function parseLogInitTickEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogInitTickEvent {
  if (containsBytes(data, LOG_INIT_TICK_EVENT_DISCRIMINATOR, 0)) {
    return getLogInitTickEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogInitTickEvent" event discriminators.',
  );
}
