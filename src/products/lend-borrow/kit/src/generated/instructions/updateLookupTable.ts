/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { VAULTS_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_LOOKUP_TABLE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([221, 59, 30, 246, 106, 223, 137, 55]);

export function getUpdateLookupTableDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_LOOKUP_TABLE_DISCRIMINATOR,
  );
}

export type UpdateLookupTableInstruction<
  TProgram extends string = typeof VAULTS_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountVaultAdmin extends string | AccountMeta<string> = string,
  TAccountVaultMetadata extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountVaultAdmin extends string
        ? ReadonlyAccount<TAccountVaultAdmin>
        : TAccountVaultAdmin,
      TAccountVaultMetadata extends string
        ? WritableAccount<TAccountVaultMetadata>
        : TAccountVaultMetadata,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateLookupTableInstructionData = {
  discriminator: ReadonlyUint8Array;
  vaultId: number;
  lookupTable: Address;
};

export type UpdateLookupTableInstructionDataArgs = {
  vaultId: number;
  lookupTable: Address;
};

export function getUpdateLookupTableInstructionDataEncoder(): FixedSizeEncoder<UpdateLookupTableInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["vaultId", getU16Encoder()],
      ["lookupTable", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_LOOKUP_TABLE_DISCRIMINATOR }),
  );
}

export function getUpdateLookupTableInstructionDataDecoder(): FixedSizeDecoder<UpdateLookupTableInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["vaultId", getU16Decoder()],
    ["lookupTable", getAddressDecoder()],
  ]);
}

export function getUpdateLookupTableInstructionDataCodec(): FixedSizeCodec<
  UpdateLookupTableInstructionDataArgs,
  UpdateLookupTableInstructionData
> {
  return combineCodec(
    getUpdateLookupTableInstructionDataEncoder(),
    getUpdateLookupTableInstructionDataDecoder(),
  );
}

export type UpdateLookupTableInput<
  TAccountAuthority extends string = string,
  TAccountVaultAdmin extends string = string,
  TAccountVaultMetadata extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  vaultAdmin: Address<TAccountVaultAdmin>;
  /** @dev Verification inside instruction logic */
  vaultMetadata: Address<TAccountVaultMetadata>;
  vaultId: UpdateLookupTableInstructionDataArgs["vaultId"];
  lookupTable: UpdateLookupTableInstructionDataArgs["lookupTable"];
};

export function getUpdateLookupTableInstruction<
  TAccountAuthority extends string,
  TAccountVaultAdmin extends string,
  TAccountVaultMetadata extends string,
  TProgramAddress extends Address = typeof VAULTS_PROGRAM_ADDRESS,
>(
  input: UpdateLookupTableInput<
    TAccountAuthority,
    TAccountVaultAdmin,
    TAccountVaultMetadata
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateLookupTableInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountVaultAdmin,
  TAccountVaultMetadata
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    vaultAdmin: { value: input.vaultAdmin ?? null, isWritable: false },
    vaultMetadata: { value: input.vaultMetadata ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("vaultAdmin", accounts.vaultAdmin),
      getAccountMeta("vaultMetadata", accounts.vaultMetadata),
    ],
    data: getUpdateLookupTableInstructionDataEncoder().encode(
      args as UpdateLookupTableInstructionDataArgs,
    ),
    programAddress,
  } as UpdateLookupTableInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountVaultAdmin,
    TAccountVaultMetadata
  >);
}

export type ParsedUpdateLookupTableInstruction<
  TProgram extends string = typeof VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    vaultAdmin: TAccountMetas[1];
    /** @dev Verification inside instruction logic */
    vaultMetadata: TAccountMetas[2];
  };
  data: UpdateLookupTableInstructionData;
};

export function parseUpdateLookupTableInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateLookupTableInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      vaultAdmin: getNextAccount(),
      vaultMetadata: getNextAccount(),
    },
    data: getUpdateLookupTableInstructionDataDecoder().decode(instruction.data),
  };
}
