/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getSwapEventV2Decoder,
  getSwapEventV2Encoder,
  type SwapEventV2,
  type SwapEventV2Args,
} from "../types";

export const SWAPS_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([152, 47, 78, 235, 192, 96, 110, 106]);

export function getSwapsEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SWAPS_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type SwapsEventEvent = { swapEvents: Array<SwapEventV2> };

export type SwapsEventEventArgs = { swapEvents: Array<SwapEventV2Args> };

/** Gets the encoder for {@link SwapsEventEventArgs} event data. */
export function getSwapsEventEventEncoder(): Encoder<SwapsEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["swapEvents", getArrayEncoder(getSwapEventV2Encoder())],
    ]),
    [getConstantEncoder(SWAPS_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link SwapsEventEvent} event data. */
export function getSwapsEventEventDecoder(): Decoder<SwapsEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["swapEvents", getArrayDecoder(getSwapEventV2Decoder())],
    ]),
    [getConstantDecoder(SWAPS_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link SwapsEventEvent} event data. */
export function getSwapsEventEventCodec(): Codec<
  SwapsEventEventArgs,
  SwapsEventEvent
> {
  return combineCodec(getSwapsEventEventEncoder(), getSwapsEventEventDecoder());
}

/** Parses the provided bytes into {@link SwapsEventEvent} event data. */
export function parseSwapsEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): SwapsEventEvent {
  if (containsBytes(data, SWAPS_EVENT_EVENT_DISCRIMINATOR, 0)) {
    return getSwapsEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "SwapsEventEvent" event discriminators.',
  );
}
