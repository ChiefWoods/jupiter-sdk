/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DEX_ADMIN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  82, 155, 122, 221, 230, 96, 118, 155,
]);

export function getDexAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DEX_ADMIN_DISCRIMINATOR);
}

export type DexAdmin = {
  discriminator: ReadonlyUint8Array;
  authority: Address;
  liquidityProgram: Address;
  nextDexId: number;
  auths: Array<Address>;
  /** Guardians can pause a dex; unpause remains auth-only. */
  guardians: Array<Address>;
  reserved: ReadonlyUint8Array;
  bump: number;
};

export type DexAdminArgs = {
  authority: Address;
  liquidityProgram: Address;
  nextDexId: number;
  auths: Array<Address>;
  /** Guardians can pause a dex; unpause remains auth-only. */
  guardians: Array<Address>;
  reserved: ReadonlyUint8Array;
  bump: number;
};

/** Gets the encoder for {@link DexAdminArgs} account data. */
export function getDexAdminEncoder(): Encoder<DexAdminArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["authority", getAddressEncoder()],
      ["liquidityProgram", getAddressEncoder()],
      ["nextDexId", getU16Encoder()],
      ["auths", getArrayEncoder(getAddressEncoder())],
      ["guardians", getArrayEncoder(getAddressEncoder())],
      ["reserved", fixEncoderSize(getBytesEncoder(), 28)],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DEX_ADMIN_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link DexAdmin} account data. */
export function getDexAdminDecoder(): Decoder<DexAdmin> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["authority", getAddressDecoder()],
    ["liquidityProgram", getAddressDecoder()],
    ["nextDexId", getU16Decoder()],
    ["auths", getArrayDecoder(getAddressDecoder())],
    ["guardians", getArrayDecoder(getAddressDecoder())],
    ["reserved", fixDecoderSize(getBytesDecoder(), 28)],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link DexAdmin} account data. */
export function getDexAdminCodec(): Codec<DexAdminArgs, DexAdmin> {
  return combineCodec(getDexAdminEncoder(), getDexAdminDecoder());
}

export function decodeDexAdmin<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DexAdmin, TAddress>;
export function decodeDexAdmin<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DexAdmin, TAddress>;
export function decodeDexAdmin<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<DexAdmin, TAddress> | MaybeAccount<DexAdmin, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDexAdminDecoder(),
  );
}

export async function fetchDexAdmin<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DexAdmin, TAddress>> {
  const maybeAccount = await fetchMaybeDexAdmin(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDexAdmin<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DexAdmin, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDexAdmin(maybeAccount);
}

export async function fetchAllDexAdmin(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DexAdmin>[]> {
  const maybeAccounts = await fetchAllMaybeDexAdmin(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDexAdmin(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DexAdmin>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDexAdmin(maybeAccount));
}
