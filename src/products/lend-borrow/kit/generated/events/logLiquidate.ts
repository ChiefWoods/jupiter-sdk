/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_LIQUIDATE_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([154, 128, 202, 147, 65, 233, 195, 73]);

export function getLogLiquidateEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_LIQUIDATE_EVENT_DISCRIMINATOR,
  );
}

export type LogLiquidateEvent = {
  signer: Address;
  colAmount: bigint;
  debtAmount: bigint;
  to: Address;
};

export type LogLiquidateEventArgs = {
  signer: Address;
  colAmount: number | bigint;
  debtAmount: number | bigint;
  to: Address;
};

/** Gets the encoder for {@link LogLiquidateEventArgs} event data. */
export function getLogLiquidateEventEncoder(): FixedSizeEncoder<LogLiquidateEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["signer", getAddressEncoder()],
      ["colAmount", getU64Encoder()],
      ["debtAmount", getU64Encoder()],
      ["to", getAddressEncoder()],
    ]),
    [getConstantEncoder(LOG_LIQUIDATE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogLiquidateEvent} event data. */
export function getLogLiquidateEventDecoder(): FixedSizeDecoder<LogLiquidateEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["signer", getAddressDecoder()],
      ["colAmount", getU64Decoder()],
      ["debtAmount", getU64Decoder()],
      ["to", getAddressDecoder()],
    ]),
    [getConstantDecoder(LOG_LIQUIDATE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogLiquidateEvent} event data. */
export function getLogLiquidateEventCodec(): FixedSizeCodec<
  LogLiquidateEventArgs,
  LogLiquidateEvent
> {
  return combineCodec(
    getLogLiquidateEventEncoder(),
    getLogLiquidateEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LogLiquidateEvent} event data. */
export function parseLogLiquidateEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogLiquidateEvent {
  if (containsBytes(data, LOG_LIQUIDATE_EVENT_DISCRIMINATOR, 0)) {
    return getLogLiquidateEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogLiquidateEvent" event discriminators.',
  );
}
