/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_AUTHORITY_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([32, 46, 64, 28, 149, 75, 243, 88]);

export function getUpdateAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_AUTHORITY_DISCRIMINATOR,
  );
}

export type UpdateAuthorityInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountOracleAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountOracleAdmin extends string
        ? WritableAccount<TAccountOracleAdmin>
        : TAccountOracleAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAuthority: Address;
};

export type UpdateAuthorityInstructionDataArgs = { newAuthority: Address };

export function getUpdateAuthorityInstructionDataEncoder(): FixedSizeEncoder<UpdateAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAuthority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_AUTHORITY_DISCRIMINATOR }),
  );
}

export function getUpdateAuthorityInstructionDataDecoder(): FixedSizeDecoder<UpdateAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAuthority", getAddressDecoder()],
  ]);
}

export function getUpdateAuthorityInstructionDataCodec(): FixedSizeCodec<
  UpdateAuthorityInstructionDataArgs,
  UpdateAuthorityInstructionData
> {
  return combineCodec(
    getUpdateAuthorityInstructionDataEncoder(),
    getUpdateAuthorityInstructionDataDecoder(),
  );
}

export type UpdateAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountOracleAdmin extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  oracleAdmin: Address<TAccountOracleAdmin>;
  newAuthority: UpdateAuthorityInstructionDataArgs["newAuthority"];
};

export function getUpdateAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountOracleAdmin extends string,
  TProgramAddress extends Address = typeof LEND_ORACLE_PROGRAM_ADDRESS,
>(
  input: UpdateAuthorityInput<TAccountAuthority, TAccountOracleAdmin>,
  config?: { programAddress?: TProgramAddress },
): UpdateAuthorityInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountOracleAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    oracleAdmin: { value: input.oracleAdmin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("oracleAdmin", accounts.oracleAdmin),
    ],
    data: getUpdateAuthorityInstructionDataEncoder().encode(
      args as UpdateAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as UpdateAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountOracleAdmin
  >);
}

export type ParsedUpdateAuthorityInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    oracleAdmin: TAccountMetas[1];
  };
  data: UpdateAuthorityInstructionData;
};

export function parseUpdateAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), oracleAdmin: getNextAccount() },
    data: getUpdateAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
