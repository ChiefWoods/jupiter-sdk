//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const MIGRATE_VAULT_CONTRACT_UNITS_DISCRIMINATOR: [u8; 8] =
    [241, 231, 0, 150, 218, 48, 154, 189];

/// Accounts.
#[derive(Debug)]
pub struct MigrateVaultContractUnits {
    pub admin: solana_address::Address,

    pub vault: solana_address::Address,
}

impl MigrateVaultContractUnits {
    pub fn instruction(
        &self,
        args: MigrateVaultContractUnitsInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: MigrateVaultContractUnitsInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.admin, true));
        accounts.push(solana_instruction::AccountMeta::new(self.vault, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = MigrateVaultContractUnitsInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::PREDICTION_MARKET_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct MigrateVaultContractUnitsInstructionData {
    discriminator: [u8; 8],
}

impl MigrateVaultContractUnitsInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [241, 231, 0, 150, 218, 48, 154, 189],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for MigrateVaultContractUnitsInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct MigrateVaultContractUnitsInstructionArgs {
    pub expected_current_contracts: u64,
    pub expected_global_max_contracts: u64,
    pub expected_position_max_contracts: u64,
}

impl MigrateVaultContractUnitsInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `MigrateVaultContractUnits`.
///
/// ### Accounts:
///
///   0. `[writable, signer, optional]` admin (default to `8rNYGp2wKnAGDm2SMaoJRFFGvfmt7iCTuu2GUvdMbZ1H`)
///   1. `[writable]` vault
#[derive(Clone, Debug, Default)]
pub struct MigrateVaultContractUnitsBuilder {
    admin: Option<solana_address::Address>,
    vault: Option<solana_address::Address>,
    expected_current_contracts: Option<u64>,
    expected_global_max_contracts: Option<u64>,
    expected_position_max_contracts: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl MigrateVaultContractUnitsBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// `[optional account, default to '8rNYGp2wKnAGDm2SMaoJRFFGvfmt7iCTuu2GUvdMbZ1H']`
    #[inline(always)]
    pub fn admin(&mut self, admin: solana_address::Address) -> &mut Self {
        self.admin = Some(admin);
        self
    }
    #[inline(always)]
    pub fn vault(&mut self, vault: solana_address::Address) -> &mut Self {
        self.vault = Some(vault);
        self
    }
    #[inline(always)]
    pub fn expected_current_contracts(&mut self, expected_current_contracts: u64) -> &mut Self {
        self.expected_current_contracts = Some(expected_current_contracts);
        self
    }
    #[inline(always)]
    pub fn expected_global_max_contracts(
        &mut self,
        expected_global_max_contracts: u64,
    ) -> &mut Self {
        self.expected_global_max_contracts = Some(expected_global_max_contracts);
        self
    }
    #[inline(always)]
    pub fn expected_position_max_contracts(
        &mut self,
        expected_position_max_contracts: u64,
    ) -> &mut Self {
        self.expected_position_max_contracts = Some(expected_position_max_contracts);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = MigrateVaultContractUnits {
            admin: self.admin.unwrap_or(solana_address::address!(
                "8rNYGp2wKnAGDm2SMaoJRFFGvfmt7iCTuu2GUvdMbZ1H"
            )),
            vault: self.vault.expect("vault is not set"),
        };
        let args = MigrateVaultContractUnitsInstructionArgs {
            expected_current_contracts: self
                .expected_current_contracts
                .clone()
                .expect("expected_current_contracts is not set"),
            expected_global_max_contracts: self
                .expected_global_max_contracts
                .clone()
                .expect("expected_global_max_contracts is not set"),
            expected_position_max_contracts: self
                .expected_position_max_contracts
                .clone()
                .expect("expected_position_max_contracts is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `migrate_vault_contract_units` CPI accounts.
pub struct MigrateVaultContractUnitsCpiAccounts<'a, 'b> {
    pub admin: &'b solana_account_info::AccountInfo<'a>,

    pub vault: &'b solana_account_info::AccountInfo<'a>,
}

/// `migrate_vault_contract_units` CPI instruction.
pub struct MigrateVaultContractUnitsCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub admin: &'b solana_account_info::AccountInfo<'a>,

    pub vault: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: MigrateVaultContractUnitsInstructionArgs,
}

impl<'a, 'b> MigrateVaultContractUnitsCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: MigrateVaultContractUnitsCpiAccounts<'a, 'b>,
        args: MigrateVaultContractUnitsInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            admin: accounts.admin,
            vault: accounts.vault,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(2 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.admin.key, true));
        accounts.push(solana_instruction::AccountMeta::new(*self.vault.key, false));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = MigrateVaultContractUnitsInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::PREDICTION_MARKET_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(3 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.admin.clone());
        account_infos.push(self.vault.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `MigrateVaultContractUnits` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` admin
///   1. `[writable]` vault
#[derive(Clone, Debug)]
pub struct MigrateVaultContractUnitsCpiBuilder<'a, 'b> {
    instruction: Box<MigrateVaultContractUnitsCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> MigrateVaultContractUnitsCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(MigrateVaultContractUnitsCpiBuilderInstruction {
            __program: program,
            admin: None,
            vault: None,
            expected_current_contracts: None,
            expected_global_max_contracts: None,
            expected_position_max_contracts: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.admin = Some(admin);
        self
    }
    #[inline(always)]
    pub fn vault(&mut self, vault: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.vault = Some(vault);
        self
    }
    #[inline(always)]
    pub fn expected_current_contracts(&mut self, expected_current_contracts: u64) -> &mut Self {
        self.instruction.expected_current_contracts = Some(expected_current_contracts);
        self
    }
    #[inline(always)]
    pub fn expected_global_max_contracts(
        &mut self,
        expected_global_max_contracts: u64,
    ) -> &mut Self {
        self.instruction.expected_global_max_contracts = Some(expected_global_max_contracts);
        self
    }
    #[inline(always)]
    pub fn expected_position_max_contracts(
        &mut self,
        expected_position_max_contracts: u64,
    ) -> &mut Self {
        self.instruction.expected_position_max_contracts = Some(expected_position_max_contracts);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = MigrateVaultContractUnitsInstructionArgs {
            expected_current_contracts: self
                .instruction
                .expected_current_contracts
                .clone()
                .expect("expected_current_contracts is not set"),
            expected_global_max_contracts: self
                .instruction
                .expected_global_max_contracts
                .clone()
                .expect("expected_global_max_contracts is not set"),
            expected_position_max_contracts: self
                .instruction
                .expected_position_max_contracts
                .clone()
                .expect("expected_position_max_contracts is not set"),
        };
        let instruction = MigrateVaultContractUnitsCpi {
            __program: self.instruction.__program,

            admin: self.instruction.admin.expect("admin is not set"),

            vault: self.instruction.vault.expect("vault is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct MigrateVaultContractUnitsCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    vault: Option<&'b solana_account_info::AccountInfo<'a>>,
    expected_current_contracts: Option<u64>,
    expected_global_max_contracts: Option<u64>,
    expected_position_max_contracts: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
