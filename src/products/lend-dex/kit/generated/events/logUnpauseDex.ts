/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_UNPAUSE_DEX_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([6, 190, 255, 207, 165, 71, 170, 212]);

export function getLogUnpauseDexEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_UNPAUSE_DEX_EVENT_DISCRIMINATOR,
  );
}

export type LogUnpauseDexEvent = { dexId: number };

export type LogUnpauseDexEventArgs = LogUnpauseDexEvent;

/** Gets the encoder for {@link LogUnpauseDexEventArgs} event data. */
export function getLogUnpauseDexEventEncoder(): FixedSizeEncoder<LogUnpauseDexEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([["dexId", getU16Encoder()]]),
    [getConstantEncoder(LOG_UNPAUSE_DEX_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogUnpauseDexEvent} event data. */
export function getLogUnpauseDexEventDecoder(): FixedSizeDecoder<LogUnpauseDexEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([["dexId", getU16Decoder()]]),
    [getConstantDecoder(LOG_UNPAUSE_DEX_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogUnpauseDexEvent} event data. */
export function getLogUnpauseDexEventCodec(): FixedSizeCodec<
  LogUnpauseDexEventArgs,
  LogUnpauseDexEvent
> {
  return combineCodec(
    getLogUnpauseDexEventEncoder(),
    getLogUnpauseDexEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LogUnpauseDexEvent} event data. */
export function parseLogUnpauseDexEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogUnpauseDexEvent {
  if (containsBytes(data, LOG_UNPAUSE_DEX_EVENT_DISCRIMINATOR, 0)) {
    return getLogUnpauseDexEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogUnpauseDexEvent" event discriminators.',
  );
}
