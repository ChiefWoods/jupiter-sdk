/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_PAUSE_DEX_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([107, 202, 204, 255, 100, 73, 92, 117]);

export function getLogPauseDexEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_PAUSE_DEX_EVENT_DISCRIMINATOR,
  );
}

export type LogPauseDexEvent = { dexId: number };

export type LogPauseDexEventArgs = LogPauseDexEvent;

/** Gets the encoder for {@link LogPauseDexEventArgs} event data. */
export function getLogPauseDexEventEncoder(): FixedSizeEncoder<LogPauseDexEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([["dexId", getU16Encoder()]]),
    [getConstantEncoder(LOG_PAUSE_DEX_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogPauseDexEvent} event data. */
export function getLogPauseDexEventDecoder(): FixedSizeDecoder<LogPauseDexEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([["dexId", getU16Decoder()]]),
    [getConstantDecoder(LOG_PAUSE_DEX_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogPauseDexEvent} event data. */
export function getLogPauseDexEventCodec(): FixedSizeCodec<
  LogPauseDexEventArgs,
  LogPauseDexEvent
> {
  return combineCodec(
    getLogPauseDexEventEncoder(),
    getLogPauseDexEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LogPauseDexEvent} event data. */
export function parseLogPauseDexEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogPauseDexEvent {
  if (containsBytes(data, LOG_PAUSE_DEX_EVENT_DISCRIMINATOR, 0)) {
    return getLogPauseDexEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogPauseDexEvent" event discriminators.',
  );
}
