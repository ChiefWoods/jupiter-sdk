/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CLAWBACK_EVENT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  239, 144, 30, 69, 80, 59, 142, 64,
]);

export function getClawbackEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAWBACK_EVENT_DISCRIMINATOR,
  );
}

export type ClawbackEvent = { sender: Address; amount: bigint };

export type ClawbackEventArgs = { sender: Address; amount: number | bigint };

/** Gets the encoder for {@link ClawbackEventArgs} event data. */
export function getClawbackEventEncoder(): FixedSizeEncoder<ClawbackEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["sender", getAddressEncoder()],
      ["amount", getU64Encoder()],
    ]),
    [getConstantEncoder(CLAWBACK_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link ClawbackEvent} event data. */
export function getClawbackEventDecoder(): FixedSizeDecoder<ClawbackEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["sender", getAddressDecoder()],
      ["amount", getU64Decoder()],
    ]),
    [getConstantDecoder(CLAWBACK_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link ClawbackEvent} event data. */
export function getClawbackEventCodec(): FixedSizeCodec<
  ClawbackEventArgs,
  ClawbackEvent
> {
  return combineCodec(getClawbackEventEncoder(), getClawbackEventDecoder());
}

/** Parses the provided bytes into {@link ClawbackEvent} event data. */
export function parseClawbackEvent(
  data: ReadonlyUint8Array | Uint8Array,
): ClawbackEvent {
  if (containsBytes(data, CLAWBACK_EVENT_DISCRIMINATOR, 0)) {
    return getClawbackEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "ClawbackEvent" event discriminators.',
  );
}
