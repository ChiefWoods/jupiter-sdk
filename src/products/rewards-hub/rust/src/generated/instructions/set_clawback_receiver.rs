//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const SET_CLAWBACK_RECEIVER_DISCRIMINATOR: [u8; 8] = [153, 217, 34, 20, 19, 29, 229, 75];

/// Accounts.
#[derive(Debug)]
pub struct SetClawbackReceiver {
    /// The [Campaign].
    pub campaign: solana_address::Address,
    /// New clawback account
    pub new_clawback_account: solana_address::Address,
    /// Admin signer
    pub admin: solana_address::Address,
}

impl SetClawbackReceiver {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.campaign, false));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.new_clawback_account,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.admin, true));
        accounts.extend_from_slice(remaining_accounts);
        let data = SetClawbackReceiverInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::GENIE_DISTRIBUTOR_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct SetClawbackReceiverInstructionData {
    discriminator: [u8; 8],
}

impl SetClawbackReceiverInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [153, 217, 34, 20, 19, 29, 229, 75],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for SetClawbackReceiverInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `SetClawbackReceiver`.
///
/// ### Accounts:
///
///   0. `[writable]` campaign
///   1. `[]` new_clawback_account
///   2. `[writable, signer]` admin
#[derive(Clone, Debug, Default)]
pub struct SetClawbackReceiverBuilder {
    campaign: Option<solana_address::Address>,
    new_clawback_account: Option<solana_address::Address>,
    admin: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl SetClawbackReceiverBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// The [Campaign].
    #[inline(always)]
    pub fn campaign(&mut self, campaign: solana_address::Address) -> &mut Self {
        self.campaign = Some(campaign);
        self
    }
    /// New clawback account
    #[inline(always)]
    pub fn new_clawback_account(
        &mut self,
        new_clawback_account: solana_address::Address,
    ) -> &mut Self {
        self.new_clawback_account = Some(new_clawback_account);
        self
    }
    /// Admin signer
    #[inline(always)]
    pub fn admin(&mut self, admin: solana_address::Address) -> &mut Self {
        self.admin = Some(admin);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = SetClawbackReceiver {
            campaign: self.campaign.expect("campaign is not set"),
            new_clawback_account: self
                .new_clawback_account
                .expect("new_clawback_account is not set"),
            admin: self.admin.expect("admin is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `set_clawback_receiver` CPI accounts.
pub struct SetClawbackReceiverCpiAccounts<'a, 'b> {
    /// The [Campaign].
    pub campaign: &'b solana_account_info::AccountInfo<'a>,
    /// New clawback account
    pub new_clawback_account: &'b solana_account_info::AccountInfo<'a>,
    /// Admin signer
    pub admin: &'b solana_account_info::AccountInfo<'a>,
}

/// `set_clawback_receiver` CPI instruction.
pub struct SetClawbackReceiverCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// The [Campaign].
    pub campaign: &'b solana_account_info::AccountInfo<'a>,
    /// New clawback account
    pub new_clawback_account: &'b solana_account_info::AccountInfo<'a>,
    /// Admin signer
    pub admin: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> SetClawbackReceiverCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: SetClawbackReceiverCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            campaign: accounts.campaign,
            new_clawback_account: accounts.new_clawback_account,
            admin: accounts.admin,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.campaign.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.new_clawback_account.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.admin.key, true));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let data = SetClawbackReceiverInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::GENIE_DISTRIBUTOR_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.campaign.clone());
        account_infos.push(self.new_clawback_account.clone());
        account_infos.push(self.admin.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `SetClawbackReceiver` via CPI.
///
/// ### Accounts:
///
///   0. `[writable]` campaign
///   1. `[]` new_clawback_account
///   2. `[writable, signer]` admin
#[derive(Clone, Debug)]
pub struct SetClawbackReceiverCpiBuilder<'a, 'b> {
    instruction: Box<SetClawbackReceiverCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> SetClawbackReceiverCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(SetClawbackReceiverCpiBuilderInstruction {
            __program: program,
            campaign: None,
            new_clawback_account: None,
            admin: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// The [Campaign].
    #[inline(always)]
    pub fn campaign(&mut self, campaign: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.campaign = Some(campaign);
        self
    }
    /// New clawback account
    #[inline(always)]
    pub fn new_clawback_account(
        &mut self,
        new_clawback_account: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.new_clawback_account = Some(new_clawback_account);
        self
    }
    /// Admin signer
    #[inline(always)]
    pub fn admin(&mut self, admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.admin = Some(admin);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = SetClawbackReceiverCpi {
            __program: self.instruction.__program,

            campaign: self.instruction.campaign.expect("campaign is not set"),

            new_clawback_account: self
                .instruction
                .new_clawback_account
                .expect("new_clawback_account is not set"),

            admin: self.instruction.admin.expect("admin is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct SetClawbackReceiverCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    campaign: Option<&'b solana_account_info::AccountInfo<'a>>,
    new_clawback_account: Option<&'b solana_account_info::AccountInfo<'a>>,
    admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
