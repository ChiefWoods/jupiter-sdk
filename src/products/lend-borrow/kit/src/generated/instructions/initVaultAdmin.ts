/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findVaultAdminPda } from "../pdas";
import { VAULTS_PROGRAM_ADDRESS } from "../programs";

export const INIT_VAULT_ADMIN_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([22, 133, 2, 244, 123, 100, 249, 230]);

export function getInitVaultAdminDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_VAULT_ADMIN_DISCRIMINATOR,
  );
}

export type InitVaultAdminInstruction<
  TProgram extends string = typeof VAULTS_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountVaultAdmin extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountVaultAdmin extends string
        ? WritableAccount<TAccountVaultAdmin>
        : TAccountVaultAdmin,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitVaultAdminInstructionData = {
  discriminator: ReadonlyUint8Array;
  liquidity: Address;
  authority: Address;
};

export type InitVaultAdminInstructionDataArgs = {
  liquidity: Address;
  authority: Address;
};

export function getInitVaultAdminInstructionDataEncoder(): FixedSizeEncoder<InitVaultAdminInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["liquidity", getAddressEncoder()],
      ["authority", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_VAULT_ADMIN_DISCRIMINATOR }),
  );
}

export function getInitVaultAdminInstructionDataDecoder(): FixedSizeDecoder<InitVaultAdminInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["liquidity", getAddressDecoder()],
    ["authority", getAddressDecoder()],
  ]);
}

export function getInitVaultAdminInstructionDataCodec(): FixedSizeCodec<
  InitVaultAdminInstructionDataArgs,
  InitVaultAdminInstructionData
> {
  return combineCodec(
    getInitVaultAdminInstructionDataEncoder(),
    getInitVaultAdminInstructionDataDecoder(),
  );
}

export type InitVaultAdminAsyncInput<
  TAccountSigner extends string = string,
  TAccountVaultAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  vaultAdmin?: Address<TAccountVaultAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  liquidity: InitVaultAdminInstructionDataArgs["liquidity"];
  authority: InitVaultAdminInstructionDataArgs["authority"];
};

export async function getInitVaultAdminInstructionAsync<
  TAccountSigner extends string,
  TAccountVaultAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULTS_PROGRAM_ADDRESS,
>(
  input: InitVaultAdminAsyncInput<
    TAccountSigner,
    TAccountVaultAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitVaultAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVaultAdmin,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    vaultAdmin: { value: input.vaultAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultAdmin.value) {
    accounts.vaultAdmin.value = await findVaultAdminPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("vaultAdmin", accounts.vaultAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitVaultAdminInstructionDataEncoder().encode(
      args as InitVaultAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitVaultAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVaultAdmin,
    TAccountSystemProgram
  >);
}

export type InitVaultAdminInput<
  TAccountSigner extends string = string,
  TAccountVaultAdmin extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  vaultAdmin: Address<TAccountVaultAdmin>;
  systemProgram?: Address<TAccountSystemProgram>;
  liquidity: InitVaultAdminInstructionDataArgs["liquidity"];
  authority: InitVaultAdminInstructionDataArgs["authority"];
};

export function getInitVaultAdminInstruction<
  TAccountSigner extends string,
  TAccountVaultAdmin extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULTS_PROGRAM_ADDRESS,
>(
  input: InitVaultAdminInput<
    TAccountSigner,
    TAccountVaultAdmin,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitVaultAdminInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountVaultAdmin,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    vaultAdmin: { value: input.vaultAdmin ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("vaultAdmin", accounts.vaultAdmin),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitVaultAdminInstructionDataEncoder().encode(
      args as InitVaultAdminInstructionDataArgs,
    ),
    programAddress,
  } as InitVaultAdminInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVaultAdmin,
    TAccountSystemProgram
  >);
}

export type ParsedInitVaultAdminInstruction<
  TProgram extends string = typeof VAULTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    vaultAdmin: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitVaultAdminInstructionData;
};

export function parseInitVaultAdminInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitVaultAdminInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      vaultAdmin: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitVaultAdminInstructionDataDecoder().decode(instruction.data),
  };
}
