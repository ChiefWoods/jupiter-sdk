/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getEventAssetDecoder,
  getEventAssetEncoder,
  type EventAsset,
  type EventAssetArgs,
} from "../types";

export const ESCROW_DEPOSIT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([43, 90, 49, 176, 134, 148, 50, 32]);

export function getEscrowDepositEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ESCROW_DEPOSIT_EVENT_DISCRIMINATOR,
  );
}

export type EscrowDepositEvent = {
  user: Address;
  asset: EventAsset;
  depositAmount: bigint;
  totalAmount: bigint;
  timestamp: bigint;
};

export type EscrowDepositEventArgs = {
  user: Address;
  asset: EventAssetArgs;
  depositAmount: number | bigint;
  totalAmount: number | bigint;
  timestamp: number | bigint;
};

/** Gets the encoder for {@link EscrowDepositEventArgs} event data. */
export function getEscrowDepositEventEncoder(): Encoder<EscrowDepositEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["user", getAddressEncoder()],
      ["asset", getEventAssetEncoder()],
      ["depositAmount", getU64Encoder()],
      ["totalAmount", getU64Encoder()],
      ["timestamp", getU64Encoder()],
    ]),
    [getConstantEncoder(ESCROW_DEPOSIT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link EscrowDepositEvent} event data. */
export function getEscrowDepositEventDecoder(): Decoder<EscrowDepositEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["user", getAddressDecoder()],
      ["asset", getEventAssetDecoder()],
      ["depositAmount", getU64Decoder()],
      ["totalAmount", getU64Decoder()],
      ["timestamp", getU64Decoder()],
    ]),
    [getConstantDecoder(ESCROW_DEPOSIT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link EscrowDepositEvent} event data. */
export function getEscrowDepositEventCodec(): Codec<
  EscrowDepositEventArgs,
  EscrowDepositEvent
> {
  return combineCodec(
    getEscrowDepositEventEncoder(),
    getEscrowDepositEventDecoder(),
  );
}

/** Parses the provided bytes into {@link EscrowDepositEvent} event data. */
export function parseEscrowDepositEvent(
  data: ReadonlyUint8Array | Uint8Array,
): EscrowDepositEvent {
  if (containsBytes(data, ESCROW_DEPOSIT_EVENT_DISCRIMINATOR, 0)) {
    return getEscrowDepositEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "EscrowDepositEvent" event discriminators.',
  );
}
