//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const GET_EXCHANGE_PRICES_DISCRIMINATOR: [u8; 8] = [237, 128, 83, 152, 52, 21, 231, 86];

/// Accounts.
#[derive(Debug)]
pub struct GetExchangePrices {
    pub vault_state: solana_address::Address,

    pub vault_config: solana_address::Address,

    pub supply_token_reserves: solana_address::Address,

    pub borrow_token_reserves: solana_address::Address,
}

impl GetExchangePrices {
    pub fn instruction(&self) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(&[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.vault_state,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.vault_config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.supply_token_reserves,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.borrow_token_reserves,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let data = GetExchangePricesInstructionData::new()
            .try_to_vec()
            .unwrap();

        solana_instruction::Instruction {
            program_id: crate::LEND_BORROW_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct GetExchangePricesInstructionData {
    discriminator: [u8; 8],
}

impl GetExchangePricesInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [237, 128, 83, 152, 52, 21, 231, 86],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for GetExchangePricesInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

/// Instruction builder for `GetExchangePrices`.
///
/// ### Accounts:
///
///   0. `[]` vault_state
///   1. `[]` vault_config
///   2. `[]` supply_token_reserves
///   3. `[]` borrow_token_reserves
#[derive(Clone, Debug, Default)]
pub struct GetExchangePricesBuilder {
    vault_state: Option<solana_address::Address>,
    vault_config: Option<solana_address::Address>,
    supply_token_reserves: Option<solana_address::Address>,
    borrow_token_reserves: Option<solana_address::Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl GetExchangePricesBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn vault_state(&mut self, vault_state: solana_address::Address) -> &mut Self {
        self.vault_state = Some(vault_state);
        self
    }
    #[inline(always)]
    pub fn vault_config(&mut self, vault_config: solana_address::Address) -> &mut Self {
        self.vault_config = Some(vault_config);
        self
    }
    #[inline(always)]
    pub fn supply_token_reserves(
        &mut self,
        supply_token_reserves: solana_address::Address,
    ) -> &mut Self {
        self.supply_token_reserves = Some(supply_token_reserves);
        self
    }
    #[inline(always)]
    pub fn borrow_token_reserves(
        &mut self,
        borrow_token_reserves: solana_address::Address,
    ) -> &mut Self {
        self.borrow_token_reserves = Some(borrow_token_reserves);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = GetExchangePrices {
            vault_state: self.vault_state.expect("vault_state is not set"),
            vault_config: self.vault_config.expect("vault_config is not set"),
            supply_token_reserves: self
                .supply_token_reserves
                .expect("supply_token_reserves is not set"),
            borrow_token_reserves: self
                .borrow_token_reserves
                .expect("borrow_token_reserves is not set"),
        };

        accounts.instruction_with_remaining_accounts(&self.__remaining_accounts)
    }
}

/// `get_exchange_prices` CPI accounts.
pub struct GetExchangePricesCpiAccounts<'a, 'b> {
    pub vault_state: &'b solana_account_info::AccountInfo<'a>,

    pub vault_config: &'b solana_account_info::AccountInfo<'a>,

    pub supply_token_reserves: &'b solana_account_info::AccountInfo<'a>,

    pub borrow_token_reserves: &'b solana_account_info::AccountInfo<'a>,
}

/// `get_exchange_prices` CPI instruction.
pub struct GetExchangePricesCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub vault_state: &'b solana_account_info::AccountInfo<'a>,

    pub vault_config: &'b solana_account_info::AccountInfo<'a>,

    pub supply_token_reserves: &'b solana_account_info::AccountInfo<'a>,

    pub borrow_token_reserves: &'b solana_account_info::AccountInfo<'a>,
}

impl<'a, 'b> GetExchangePricesCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: GetExchangePricesCpiAccounts<'a, 'b>,
    ) -> Self {
        Self {
            __program: program,
            vault_state: accounts.vault_state,
            vault_config: accounts.vault_config,
            supply_token_reserves: accounts.supply_token_reserves,
            borrow_token_reserves: accounts.borrow_token_reserves,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(4 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.vault_state.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.vault_config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.supply_token_reserves.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.borrow_token_reserves.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let data = GetExchangePricesInstructionData::new()
            .try_to_vec()
            .unwrap();

        let instruction = solana_instruction::Instruction {
            program_id: crate::LEND_BORROW_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(5 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.vault_state.clone());
        account_infos.push(self.vault_config.clone());
        account_infos.push(self.supply_token_reserves.clone());
        account_infos.push(self.borrow_token_reserves.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `GetExchangePrices` via CPI.
///
/// ### Accounts:
///
///   0. `[]` vault_state
///   1. `[]` vault_config
///   2. `[]` supply_token_reserves
///   3. `[]` borrow_token_reserves
#[derive(Clone, Debug)]
pub struct GetExchangePricesCpiBuilder<'a, 'b> {
    instruction: Box<GetExchangePricesCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> GetExchangePricesCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(GetExchangePricesCpiBuilderInstruction {
            __program: program,
            vault_state: None,
            vault_config: None,
            supply_token_reserves: None,
            borrow_token_reserves: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn vault_state(
        &mut self,
        vault_state: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.vault_state = Some(vault_state);
        self
    }
    #[inline(always)]
    pub fn vault_config(
        &mut self,
        vault_config: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.vault_config = Some(vault_config);
        self
    }
    #[inline(always)]
    pub fn supply_token_reserves(
        &mut self,
        supply_token_reserves: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.supply_token_reserves = Some(supply_token_reserves);
        self
    }
    #[inline(always)]
    pub fn borrow_token_reserves(
        &mut self,
        borrow_token_reserves: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.borrow_token_reserves = Some(borrow_token_reserves);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let instruction = GetExchangePricesCpi {
            __program: self.instruction.__program,

            vault_state: self
                .instruction
                .vault_state
                .expect("vault_state is not set"),

            vault_config: self
                .instruction
                .vault_config
                .expect("vault_config is not set"),

            supply_token_reserves: self
                .instruction
                .supply_token_reserves
                .expect("supply_token_reserves is not set"),

            borrow_token_reserves: self
                .instruction
                .borrow_token_reserves
                .expect("borrow_token_reserves is not set"),
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct GetExchangePricesCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    vault_state: Option<&'b solana_account_info::AccountInfo<'a>>,
    vault_config: Option<&'b solana_account_info::AccountInfo<'a>>,
    supply_token_reserves: Option<&'b solana_account_info::AccountInfo<'a>>,
    borrow_token_reserves: Option<&'b solana_account_info::AccountInfo<'a>>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
