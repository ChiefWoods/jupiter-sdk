//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct Dex {
    pub discriminator: [u8; 8],
    pub dex_id: u16,
    pub re_entrancy: u8,
    pub token0: Address,
    pub token1: Address,
    pub token0_decimals: u8,
    pub token1_decimals: u8,
    pub last_to_last_stored_price: u128,
    pub last_stored_price: u128,
    pub center_price: u128,
    pub last_update_timestamp: u64,
    pub last_update_slot: u64,
    pub is_smart_collateral_enabled: u8,
    pub is_smart_debt_enabled: u8,
    pub fee: u32,
    pub revenue_cut: u8,
    pub percent_change_active: u8,
    pub upper_percent: u32,
    pub lower_percent: u32,
    pub threshold_change_active: u8,
    pub upper_shift_threshold_percent: u16,
    pub lower_shift_threshold_percent: u16,
    pub shifting_time: u32,
    pub center_price_address: Address,
    pub max_center_price: u64,
    pub min_center_price: u64,
    pub token0_max_utilization: u16,
    pub token1_max_utilization: u16,
    pub is_center_price_shift_active: u8,
    pub swap_and_arbitrage_paused: u8,
    pub total_supply_shares: u64,
    pub max_supply_shares: u64,
    pub total_borrow_shares: u64,
    pub max_borrow_shares: u64,
    pub range_old_upper_shift: u32,
    pub range_old_lower_shift: u32,
    pub range_shift_duration: u32,
    pub range_shift_start_timestamp: u32,
    pub threshold_old_upper_shift: u16,
    pub threshold_old_lower_shift: u16,
    pub threshold_shift_duration: u32,
    pub threshold_shift_start_timestamp: u32,
    pub threshold_shift_old_timestamp: u32,
    pub center_price_shift_start_timestamp: u32,
    pub center_price_shift_percent: u32,
    pub center_price_shift_time: u32,
    pub reserved: [u8; 32],
    pub bump: u8,
}

pub const DEX_DISCRIMINATOR: [u8; 8] = [236, 30, 181, 80, 209, 217, 25, 163];

impl Dex {
    pub const LEN: usize = 329;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for Dex {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_dex(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<Dex>, std::io::Error> {
    let accounts = fetch_all_dex(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_dex(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<Dex>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<Dex>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::other(format!(
            "Account not found: {address}"
        )))?;
        let data = Dex::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_dex(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<Dex>, std::io::Error> {
    let accounts = fetch_all_maybe_dex(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_dex(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<Dex>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<Dex>> = Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = Dex::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for Dex {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for Dex {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for Dex {
    fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(crate::LEND_DEX_ID.to_bytes())
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for Dex {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for Dex {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
