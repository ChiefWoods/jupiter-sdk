//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct LendingRewardsRateModel {
    pub discriminator: [u8; 8],
    /// @dev mint address
    pub mint: Address,
    /// @dev tvl below which rewards rate is 0. If current TVL is below this value, triggering `update_rate()` on the fToken
    /// might bring the total TVL above this cut-off.
    pub start_tvl: u64,
    /// @dev for how long current rewards should run
    pub duration: u64,
    /// @dev when current rewards got started
    pub start_time: u64,
    /// @dev current annualized reward based on input params (duration, rewardAmount)
    pub yearly_reward: u64,
    /// @dev Duration for the next rewards phase
    pub next_duration: u64,
    /// @dev Amount of rewards for the next phase
    pub next_reward_amount: u64,
    pub bump: u8,
}

pub const LENDING_REWARDS_RATE_MODEL_DISCRIMINATOR: [u8; 8] = [166, 72, 71, 131, 172, 74, 166, 181];

impl LendingRewardsRateModel {
    pub const LEN: usize = 89;

    #[inline(always)]
    pub fn from_bytes(data: &[u8]) -> Result<Self, std::io::Error> {
        let mut data = data;
        Self::deserialize(&mut data)
    }
}

impl<'a> TryFrom<&solana_account_info::AccountInfo<'a>> for LendingRewardsRateModel {
    type Error = std::io::Error;

    fn try_from(account_info: &solana_account_info::AccountInfo<'a>) -> Result<Self, Self::Error> {
        let mut data: &[u8] = &(*account_info.data).borrow();
        Self::deserialize(&mut data)
    }
}

#[cfg(feature = "fetch")]
pub fn fetch_lending_rewards_rate_model(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::DecodedAccount<LendingRewardsRateModel>, std::io::Error> {
    let accounts = fetch_all_lending_rewards_rate_model(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_lending_rewards_rate_model(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::DecodedAccount<LendingRewardsRateModel>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::DecodedAccount<LendingRewardsRateModel>> =
        Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        let account = accounts[i].as_ref().ok_or(std::io::Error::other(format!(
            "Account not found: {address}"
        )))?;
        let data = LendingRewardsRateModel::from_bytes(&account.data)?;
        decoded_accounts.push(crate::shared::DecodedAccount {
            address,
            account: account.clone(),
            data,
        });
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "fetch")]
pub fn fetch_maybe_lending_rewards_rate_model(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    address: &solana_address::Address,
) -> Result<crate::shared::MaybeAccount<LendingRewardsRateModel>, std::io::Error> {
    let accounts = fetch_all_maybe_lending_rewards_rate_model(rpc, &[*address])?;
    Ok(accounts[0].clone())
}

#[cfg(feature = "fetch")]
pub fn fetch_all_maybe_lending_rewards_rate_model(
    rpc: &solana_rpc_client::rpc_client::RpcClient,
    addresses: &[solana_address::Address],
) -> Result<Vec<crate::shared::MaybeAccount<LendingRewardsRateModel>>, std::io::Error> {
    let accounts = rpc
        .get_multiple_accounts(addresses)
        .map_err(|e| std::io::Error::other(e.to_string()))?;
    let mut decoded_accounts: Vec<crate::shared::MaybeAccount<LendingRewardsRateModel>> =
        Vec::new();
    for i in 0..addresses.len() {
        let address = addresses[i];
        if let Some(account) = accounts[i].as_ref() {
            let data = LendingRewardsRateModel::from_bytes(&account.data)?;
            decoded_accounts.push(crate::shared::MaybeAccount::Exists(
                crate::shared::DecodedAccount {
                    address,
                    account: account.clone(),
                    data,
                },
            ));
        } else {
            decoded_accounts.push(crate::shared::MaybeAccount::NotFound(address));
        }
    }
    Ok(decoded_accounts)
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountDeserialize for LendingRewardsRateModel {
    fn try_deserialize_unchecked(buf: &mut &[u8]) -> anchor_lang::Result<Self> {
        Ok(Self::deserialize(buf)?)
    }
}

#[cfg(feature = "anchor")]
impl anchor_lang::AccountSerialize for LendingRewardsRateModel {}

#[cfg(feature = "anchor")]
impl anchor_lang::Owner for LendingRewardsRateModel {
    fn owner() -> anchor_lang::solana_program::pubkey::Pubkey {
        anchor_lang::solana_program::pubkey::Pubkey::from(crate::LENDING_ID.to_bytes())
    }
}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::IdlBuild for LendingRewardsRateModel {}

#[cfg(feature = "anchor-idl-build")]
impl anchor_lang::Discriminator for LendingRewardsRateModel {
    const DISCRIMINATOR: &[u8] = &[0; 8];
}
