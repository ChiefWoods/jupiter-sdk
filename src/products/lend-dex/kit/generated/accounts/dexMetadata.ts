/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const DEX_METADATA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  177, 231, 57, 253, 64, 12, 205, 37,
]);

export function getDexMetadataDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DEX_METADATA_DISCRIMINATOR,
  );
}

export type DexMetadata = {
  discriminator: ReadonlyUint8Array;
  dexId: number;
  lookupTable: Address;
  reserved: ReadonlyUint8Array;
};

export type DexMetadataArgs = {
  dexId: number;
  lookupTable: Address;
  reserved: ReadonlyUint8Array;
};

/** Gets the encoder for {@link DexMetadataArgs} account data. */
export function getDexMetadataEncoder(): FixedSizeEncoder<DexMetadataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["dexId", getU16Encoder()],
      ["lookupTable", getAddressEncoder()],
      ["reserved", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: DEX_METADATA_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link DexMetadata} account data. */
export function getDexMetadataDecoder(): FixedSizeDecoder<DexMetadata> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["dexId", getU16Decoder()],
    ["lookupTable", getAddressDecoder()],
    ["reserved", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

/** Gets the codec for {@link DexMetadata} account data. */
export function getDexMetadataCodec(): FixedSizeCodec<
  DexMetadataArgs,
  DexMetadata
> {
  return combineCodec(getDexMetadataEncoder(), getDexMetadataDecoder());
}

export function decodeDexMetadata<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<DexMetadata, TAddress>;
export function decodeDexMetadata<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<DexMetadata, TAddress>;
export function decodeDexMetadata<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<DexMetadata, TAddress> | MaybeAccount<DexMetadata, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getDexMetadataDecoder(),
  );
}

export async function fetchDexMetadata<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DexMetadata, TAddress>> {
  const maybeAccount = await fetchMaybeDexMetadata(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDexMetadata<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DexMetadata, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDexMetadata(maybeAccount);
}

export async function fetchAllDexMetadata(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DexMetadata>[]> {
  const maybeAccounts = await fetchAllMaybeDexMetadata(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDexMetadata(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DexMetadata>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDexMetadata(maybeAccount));
}

export function getDexMetadataSize(): number {
  return 74;
}
