//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_address::Address;

pub const INIT_VAULT_CONFIG_DISCRIMINATOR: [u8; 8] = [41, 194, 69, 254, 196, 246, 226, 195];

/// Accounts.
#[derive(Debug)]
pub struct InitVaultConfig {
    pub authority: solana_address::Address,

    pub vault_admin: solana_address::Address,

    pub vault_config: solana_address::Address,

    pub vault_metadata: solana_address::Address,

    pub oracle: solana_address::Address,

    pub supply_token: Option<solana_address::Address>,

    pub borrow_token: Option<solana_address::Address>,

    pub supply_dex: Option<solana_address::Address>,

    pub borrow_dex: Option<solana_address::Address>,

    pub system_program: solana_address::Address,
}

impl InitVaultConfig {
    pub fn instruction(
        &self,
        args: InitVaultConfigInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: InitVaultConfigInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(10 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.authority, true));
        accounts.push(solana_instruction::AccountMeta::new(
            self.vault_admin,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.vault_config,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.vault_metadata,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.oracle,
            false,
        ));
        if let Some(supply_token) = self.supply_token {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                supply_token,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::VAULTS_ID,
                false,
            ));
        }
        if let Some(borrow_token) = self.borrow_token {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                borrow_token,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::VAULTS_ID,
                false,
            ));
        }
        if let Some(supply_dex) = self.supply_dex {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                supply_dex, false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::VAULTS_ID,
                false,
            ));
        }
        if let Some(borrow_dex) = self.borrow_dex {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                borrow_dex, false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::VAULTS_ID,
                false,
            ));
        }
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.system_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = InitVaultConfigInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::VAULTS_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitVaultConfigInstructionData {
    discriminator: [u8; 8],
}

impl InitVaultConfigInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [41, 194, 69, 254, 196, 246, 226, 195],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for InitVaultConfigInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct InitVaultConfigInstructionArgs {
    pub vault_id: u16,
    pub supply_rate_magnifier: i16,
    pub borrow_rate_magnifier: i16,
    pub collateral_factor: u16,
    pub liquidation_threshold: u16,
    pub liquidation_max_limit: u16,
    pub withdraw_gap: u16,
    pub liquidation_penalty: u16,
    pub borrow_fee: u8,
    pub vault_type: u8,
    pub rebalancer: Address,
    pub liquidity_program: Address,
    pub oracle_program: Address,
}

impl InitVaultConfigInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `InitVaultConfig`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[writable]` vault_admin
///   2. `[writable]` vault_config
///   3. `[writable]` vault_metadata
///   4. `[]` oracle
///   5. `[optional]` supply_token
///   6. `[optional]` borrow_token
///   7. `[optional]` supply_dex
///   8. `[optional]` borrow_dex
///   9. `[optional]` system_program (default to `11111111111111111111111111111111`)
#[derive(Clone, Debug, Default)]
pub struct InitVaultConfigBuilder {
    authority: Option<solana_address::Address>,
    vault_admin: Option<solana_address::Address>,
    vault_config: Option<solana_address::Address>,
    vault_metadata: Option<solana_address::Address>,
    oracle: Option<solana_address::Address>,
    supply_token: Option<solana_address::Address>,
    borrow_token: Option<solana_address::Address>,
    supply_dex: Option<solana_address::Address>,
    borrow_dex: Option<solana_address::Address>,
    system_program: Option<solana_address::Address>,
    vault_id: Option<u16>,
    supply_rate_magnifier: Option<i16>,
    borrow_rate_magnifier: Option<i16>,
    collateral_factor: Option<u16>,
    liquidation_threshold: Option<u16>,
    liquidation_max_limit: Option<u16>,
    withdraw_gap: Option<u16>,
    liquidation_penalty: Option<u16>,
    borrow_fee: Option<u8>,
    vault_type: Option<u8>,
    rebalancer: Option<Address>,
    liquidity_program: Option<Address>,
    oracle_program: Option<Address>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl InitVaultConfigBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_address::Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn vault_admin(&mut self, vault_admin: solana_address::Address) -> &mut Self {
        self.vault_admin = Some(vault_admin);
        self
    }
    #[inline(always)]
    pub fn vault_config(&mut self, vault_config: solana_address::Address) -> &mut Self {
        self.vault_config = Some(vault_config);
        self
    }
    #[inline(always)]
    pub fn vault_metadata(&mut self, vault_metadata: solana_address::Address) -> &mut Self {
        self.vault_metadata = Some(vault_metadata);
        self
    }
    #[inline(always)]
    pub fn oracle(&mut self, oracle: solana_address::Address) -> &mut Self {
        self.oracle = Some(oracle);
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn supply_token(&mut self, supply_token: Option<solana_address::Address>) -> &mut Self {
        self.supply_token = supply_token;
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn borrow_token(&mut self, borrow_token: Option<solana_address::Address>) -> &mut Self {
        self.borrow_token = borrow_token;
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn supply_dex(&mut self, supply_dex: Option<solana_address::Address>) -> &mut Self {
        self.supply_dex = supply_dex;
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn borrow_dex(&mut self, borrow_dex: Option<solana_address::Address>) -> &mut Self {
        self.borrow_dex = borrow_dex;
        self
    }
    /// `[optional account, default to '11111111111111111111111111111111']`
    #[inline(always)]
    pub fn system_program(&mut self, system_program: solana_address::Address) -> &mut Self {
        self.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn vault_id(&mut self, vault_id: u16) -> &mut Self {
        self.vault_id = Some(vault_id);
        self
    }
    #[inline(always)]
    pub fn supply_rate_magnifier(&mut self, supply_rate_magnifier: i16) -> &mut Self {
        self.supply_rate_magnifier = Some(supply_rate_magnifier);
        self
    }
    #[inline(always)]
    pub fn borrow_rate_magnifier(&mut self, borrow_rate_magnifier: i16) -> &mut Self {
        self.borrow_rate_magnifier = Some(borrow_rate_magnifier);
        self
    }
    #[inline(always)]
    pub fn collateral_factor(&mut self, collateral_factor: u16) -> &mut Self {
        self.collateral_factor = Some(collateral_factor);
        self
    }
    #[inline(always)]
    pub fn liquidation_threshold(&mut self, liquidation_threshold: u16) -> &mut Self {
        self.liquidation_threshold = Some(liquidation_threshold);
        self
    }
    #[inline(always)]
    pub fn liquidation_max_limit(&mut self, liquidation_max_limit: u16) -> &mut Self {
        self.liquidation_max_limit = Some(liquidation_max_limit);
        self
    }
    #[inline(always)]
    pub fn withdraw_gap(&mut self, withdraw_gap: u16) -> &mut Self {
        self.withdraw_gap = Some(withdraw_gap);
        self
    }
    #[inline(always)]
    pub fn liquidation_penalty(&mut self, liquidation_penalty: u16) -> &mut Self {
        self.liquidation_penalty = Some(liquidation_penalty);
        self
    }
    #[inline(always)]
    pub fn borrow_fee(&mut self, borrow_fee: u8) -> &mut Self {
        self.borrow_fee = Some(borrow_fee);
        self
    }
    #[inline(always)]
    pub fn vault_type(&mut self, vault_type: u8) -> &mut Self {
        self.vault_type = Some(vault_type);
        self
    }
    #[inline(always)]
    pub fn rebalancer(&mut self, rebalancer: Address) -> &mut Self {
        self.rebalancer = Some(rebalancer);
        self
    }
    #[inline(always)]
    pub fn liquidity_program(&mut self, liquidity_program: Address) -> &mut Self {
        self.liquidity_program = Some(liquidity_program);
        self
    }
    #[inline(always)]
    pub fn oracle_program(&mut self, oracle_program: Address) -> &mut Self {
        self.oracle_program = Some(oracle_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = InitVaultConfig {
            authority: self.authority.expect("authority is not set"),
            vault_admin: self.vault_admin.expect("vault_admin is not set"),
            vault_config: self.vault_config.expect("vault_config is not set"),
            vault_metadata: self.vault_metadata.expect("vault_metadata is not set"),
            oracle: self.oracle.expect("oracle is not set"),
            supply_token: self.supply_token,
            borrow_token: self.borrow_token,
            supply_dex: self.supply_dex,
            borrow_dex: self.borrow_dex,
            system_program: self
                .system_program
                .unwrap_or(solana_address::address!("11111111111111111111111111111111")),
        };
        let args = InitVaultConfigInstructionArgs {
            vault_id: self.vault_id.clone().expect("vault_id is not set"),
            supply_rate_magnifier: self
                .supply_rate_magnifier
                .clone()
                .expect("supply_rate_magnifier is not set"),
            borrow_rate_magnifier: self
                .borrow_rate_magnifier
                .clone()
                .expect("borrow_rate_magnifier is not set"),
            collateral_factor: self
                .collateral_factor
                .clone()
                .expect("collateral_factor is not set"),
            liquidation_threshold: self
                .liquidation_threshold
                .clone()
                .expect("liquidation_threshold is not set"),
            liquidation_max_limit: self
                .liquidation_max_limit
                .clone()
                .expect("liquidation_max_limit is not set"),
            withdraw_gap: self.withdraw_gap.clone().expect("withdraw_gap is not set"),
            liquidation_penalty: self
                .liquidation_penalty
                .clone()
                .expect("liquidation_penalty is not set"),
            borrow_fee: self.borrow_fee.clone().expect("borrow_fee is not set"),
            vault_type: self.vault_type.clone().expect("vault_type is not set"),
            rebalancer: self.rebalancer.clone().expect("rebalancer is not set"),
            liquidity_program: self
                .liquidity_program
                .clone()
                .expect("liquidity_program is not set"),
            oracle_program: self
                .oracle_program
                .clone()
                .expect("oracle_program is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `init_vault_config` CPI accounts.
pub struct InitVaultConfigCpiAccounts<'a, 'b> {
    pub authority: &'b solana_account_info::AccountInfo<'a>,

    pub vault_admin: &'b solana_account_info::AccountInfo<'a>,

    pub vault_config: &'b solana_account_info::AccountInfo<'a>,

    pub vault_metadata: &'b solana_account_info::AccountInfo<'a>,

    pub oracle: &'b solana_account_info::AccountInfo<'a>,

    pub supply_token: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub borrow_token: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub supply_dex: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub borrow_dex: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `init_vault_config` CPI instruction.
pub struct InitVaultConfigCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub authority: &'b solana_account_info::AccountInfo<'a>,

    pub vault_admin: &'b solana_account_info::AccountInfo<'a>,

    pub vault_config: &'b solana_account_info::AccountInfo<'a>,

    pub vault_metadata: &'b solana_account_info::AccountInfo<'a>,

    pub oracle: &'b solana_account_info::AccountInfo<'a>,

    pub supply_token: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub borrow_token: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub supply_dex: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub borrow_dex: Option<&'b solana_account_info::AccountInfo<'a>>,

    pub system_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: InitVaultConfigInstructionArgs,
}

impl<'a, 'b> InitVaultConfigCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: InitVaultConfigCpiAccounts<'a, 'b>,
        args: InitVaultConfigInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            vault_admin: accounts.vault_admin,
            vault_config: accounts.vault_config,
            vault_metadata: accounts.vault_metadata,
            oracle: accounts.oracle,
            supply_token: accounts.supply_token,
            borrow_token: accounts.borrow_token,
            supply_dex: accounts.supply_dex,
            borrow_dex: accounts.borrow_dex,
            system_program: accounts.system_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(10 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.vault_admin.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.vault_config.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.vault_metadata.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.oracle.key,
            false,
        ));
        if let Some(supply_token) = self.supply_token {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                *supply_token.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::VAULTS_ID,
                false,
            ));
        }
        if let Some(borrow_token) = self.borrow_token {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                *borrow_token.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::VAULTS_ID,
                false,
            ));
        }
        if let Some(supply_dex) = self.supply_dex {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                *supply_dex.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::VAULTS_ID,
                false,
            ));
        }
        if let Some(borrow_dex) = self.borrow_dex {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                *borrow_dex.key,
                false,
            ));
        } else {
            accounts.push(solana_instruction::AccountMeta::new_readonly(
                crate::VAULTS_ID,
                false,
            ));
        }
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.system_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = InitVaultConfigInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::VAULTS_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(11 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.vault_admin.clone());
        account_infos.push(self.vault_config.clone());
        account_infos.push(self.vault_metadata.clone());
        account_infos.push(self.oracle.clone());
        if let Some(supply_token) = self.supply_token {
            account_infos.push(supply_token.clone());
        }
        if let Some(borrow_token) = self.borrow_token {
            account_infos.push(borrow_token.clone());
        }
        if let Some(supply_dex) = self.supply_dex {
            account_infos.push(supply_dex.clone());
        }
        if let Some(borrow_dex) = self.borrow_dex {
            account_infos.push(borrow_dex.clone());
        }
        account_infos.push(self.system_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `InitVaultConfig` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` authority
///   1. `[writable]` vault_admin
///   2. `[writable]` vault_config
///   3. `[writable]` vault_metadata
///   4. `[]` oracle
///   5. `[optional]` supply_token
///   6. `[optional]` borrow_token
///   7. `[optional]` supply_dex
///   8. `[optional]` borrow_dex
///   9. `[]` system_program
#[derive(Clone, Debug)]
pub struct InitVaultConfigCpiBuilder<'a, 'b> {
    instruction: Box<InitVaultConfigCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> InitVaultConfigCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(InitVaultConfigCpiBuilderInstruction {
            __program: program,
            authority: None,
            vault_admin: None,
            vault_config: None,
            vault_metadata: None,
            oracle: None,
            supply_token: None,
            borrow_token: None,
            supply_dex: None,
            borrow_dex: None,
            system_program: None,
            vault_id: None,
            supply_rate_magnifier: None,
            borrow_rate_magnifier: None,
            collateral_factor: None,
            liquidation_threshold: None,
            liquidation_max_limit: None,
            withdraw_gap: None,
            liquidation_penalty: None,
            borrow_fee: None,
            vault_type: None,
            rebalancer: None,
            liquidity_program: None,
            oracle_program: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn vault_admin(
        &mut self,
        vault_admin: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.vault_admin = Some(vault_admin);
        self
    }
    #[inline(always)]
    pub fn vault_config(
        &mut self,
        vault_config: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.vault_config = Some(vault_config);
        self
    }
    #[inline(always)]
    pub fn vault_metadata(
        &mut self,
        vault_metadata: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.vault_metadata = Some(vault_metadata);
        self
    }
    #[inline(always)]
    pub fn oracle(&mut self, oracle: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.oracle = Some(oracle);
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn supply_token(
        &mut self,
        supply_token: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.supply_token = supply_token;
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn borrow_token(
        &mut self,
        borrow_token: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.borrow_token = borrow_token;
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn supply_dex(
        &mut self,
        supply_dex: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.supply_dex = supply_dex;
        self
    }
    /// `[optional account]`
    #[inline(always)]
    pub fn borrow_dex(
        &mut self,
        borrow_dex: Option<&'b solana_account_info::AccountInfo<'a>>,
    ) -> &mut Self {
        self.instruction.borrow_dex = borrow_dex;
        self
    }
    #[inline(always)]
    pub fn system_program(
        &mut self,
        system_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.system_program = Some(system_program);
        self
    }
    #[inline(always)]
    pub fn vault_id(&mut self, vault_id: u16) -> &mut Self {
        self.instruction.vault_id = Some(vault_id);
        self
    }
    #[inline(always)]
    pub fn supply_rate_magnifier(&mut self, supply_rate_magnifier: i16) -> &mut Self {
        self.instruction.supply_rate_magnifier = Some(supply_rate_magnifier);
        self
    }
    #[inline(always)]
    pub fn borrow_rate_magnifier(&mut self, borrow_rate_magnifier: i16) -> &mut Self {
        self.instruction.borrow_rate_magnifier = Some(borrow_rate_magnifier);
        self
    }
    #[inline(always)]
    pub fn collateral_factor(&mut self, collateral_factor: u16) -> &mut Self {
        self.instruction.collateral_factor = Some(collateral_factor);
        self
    }
    #[inline(always)]
    pub fn liquidation_threshold(&mut self, liquidation_threshold: u16) -> &mut Self {
        self.instruction.liquidation_threshold = Some(liquidation_threshold);
        self
    }
    #[inline(always)]
    pub fn liquidation_max_limit(&mut self, liquidation_max_limit: u16) -> &mut Self {
        self.instruction.liquidation_max_limit = Some(liquidation_max_limit);
        self
    }
    #[inline(always)]
    pub fn withdraw_gap(&mut self, withdraw_gap: u16) -> &mut Self {
        self.instruction.withdraw_gap = Some(withdraw_gap);
        self
    }
    #[inline(always)]
    pub fn liquidation_penalty(&mut self, liquidation_penalty: u16) -> &mut Self {
        self.instruction.liquidation_penalty = Some(liquidation_penalty);
        self
    }
    #[inline(always)]
    pub fn borrow_fee(&mut self, borrow_fee: u8) -> &mut Self {
        self.instruction.borrow_fee = Some(borrow_fee);
        self
    }
    #[inline(always)]
    pub fn vault_type(&mut self, vault_type: u8) -> &mut Self {
        self.instruction.vault_type = Some(vault_type);
        self
    }
    #[inline(always)]
    pub fn rebalancer(&mut self, rebalancer: Address) -> &mut Self {
        self.instruction.rebalancer = Some(rebalancer);
        self
    }
    #[inline(always)]
    pub fn liquidity_program(&mut self, liquidity_program: Address) -> &mut Self {
        self.instruction.liquidity_program = Some(liquidity_program);
        self
    }
    #[inline(always)]
    pub fn oracle_program(&mut self, oracle_program: Address) -> &mut Self {
        self.instruction.oracle_program = Some(oracle_program);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = InitVaultConfigInstructionArgs {
            vault_id: self
                .instruction
                .vault_id
                .clone()
                .expect("vault_id is not set"),
            supply_rate_magnifier: self
                .instruction
                .supply_rate_magnifier
                .clone()
                .expect("supply_rate_magnifier is not set"),
            borrow_rate_magnifier: self
                .instruction
                .borrow_rate_magnifier
                .clone()
                .expect("borrow_rate_magnifier is not set"),
            collateral_factor: self
                .instruction
                .collateral_factor
                .clone()
                .expect("collateral_factor is not set"),
            liquidation_threshold: self
                .instruction
                .liquidation_threshold
                .clone()
                .expect("liquidation_threshold is not set"),
            liquidation_max_limit: self
                .instruction
                .liquidation_max_limit
                .clone()
                .expect("liquidation_max_limit is not set"),
            withdraw_gap: self
                .instruction
                .withdraw_gap
                .clone()
                .expect("withdraw_gap is not set"),
            liquidation_penalty: self
                .instruction
                .liquidation_penalty
                .clone()
                .expect("liquidation_penalty is not set"),
            borrow_fee: self
                .instruction
                .borrow_fee
                .clone()
                .expect("borrow_fee is not set"),
            vault_type: self
                .instruction
                .vault_type
                .clone()
                .expect("vault_type is not set"),
            rebalancer: self
                .instruction
                .rebalancer
                .clone()
                .expect("rebalancer is not set"),
            liquidity_program: self
                .instruction
                .liquidity_program
                .clone()
                .expect("liquidity_program is not set"),
            oracle_program: self
                .instruction
                .oracle_program
                .clone()
                .expect("oracle_program is not set"),
        };
        let instruction = InitVaultConfigCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            vault_admin: self
                .instruction
                .vault_admin
                .expect("vault_admin is not set"),

            vault_config: self
                .instruction
                .vault_config
                .expect("vault_config is not set"),

            vault_metadata: self
                .instruction
                .vault_metadata
                .expect("vault_metadata is not set"),

            oracle: self.instruction.oracle.expect("oracle is not set"),

            supply_token: self.instruction.supply_token,

            borrow_token: self.instruction.borrow_token,

            supply_dex: self.instruction.supply_dex,

            borrow_dex: self.instruction.borrow_dex,

            system_program: self
                .instruction
                .system_program
                .expect("system_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct InitVaultConfigCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    vault_admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    vault_config: Option<&'b solana_account_info::AccountInfo<'a>>,
    vault_metadata: Option<&'b solana_account_info::AccountInfo<'a>>,
    oracle: Option<&'b solana_account_info::AccountInfo<'a>>,
    supply_token: Option<&'b solana_account_info::AccountInfo<'a>>,
    borrow_token: Option<&'b solana_account_info::AccountInfo<'a>>,
    supply_dex: Option<&'b solana_account_info::AccountInfo<'a>>,
    borrow_dex: Option<&'b solana_account_info::AccountInfo<'a>>,
    system_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    vault_id: Option<u16>,
    supply_rate_magnifier: Option<i16>,
    borrow_rate_magnifier: Option<i16>,
    collateral_factor: Option<u16>,
    liquidation_threshold: Option<u16>,
    liquidation_max_limit: Option<u16>,
    withdraw_gap: Option<u16>,
    liquidation_penalty: Option<u16>,
    borrow_fee: Option<u8>,
    vault_type: Option<u8>,
    rebalancer: Option<Address>,
    liquidity_program: Option<Address>,
    oracle_program: Option<Address>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
