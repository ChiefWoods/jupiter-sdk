/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getI128Decoder,
  getI128Encoder,
  getStructDecoder,
  getStructEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const LOG_REBALANCE_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([90, 67, 219, 41, 181, 118, 132, 9]);

export function getLogRebalanceEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    LOG_REBALANCE_EVENT_DISCRIMINATOR,
  );
}

export type LogRebalanceEvent = { supplyAmt: bigint; borrowAmt: bigint };

export type LogRebalanceEventArgs = {
  supplyAmt: number | bigint;
  borrowAmt: number | bigint;
};

/** Gets the encoder for {@link LogRebalanceEventArgs} event data. */
export function getLogRebalanceEventEncoder(): FixedSizeEncoder<LogRebalanceEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["supplyAmt", getI128Encoder()],
      ["borrowAmt", getI128Encoder()],
    ]),
    [getConstantEncoder(LOG_REBALANCE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link LogRebalanceEvent} event data. */
export function getLogRebalanceEventDecoder(): FixedSizeDecoder<LogRebalanceEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["supplyAmt", getI128Decoder()],
      ["borrowAmt", getI128Decoder()],
    ]),
    [getConstantDecoder(LOG_REBALANCE_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link LogRebalanceEvent} event data. */
export function getLogRebalanceEventCodec(): FixedSizeCodec<
  LogRebalanceEventArgs,
  LogRebalanceEvent
> {
  return combineCodec(
    getLogRebalanceEventEncoder(),
    getLogRebalanceEventDecoder(),
  );
}

/** Parses the provided bytes into {@link LogRebalanceEvent} event data. */
export function parseLogRebalanceEvent(
  data: ReadonlyUint8Array | Uint8Array,
): LogRebalanceEvent {
  if (containsBytes(data, LOG_REBALANCE_EVENT_DISCRIMINATOR, 0)) {
    return getLogRebalanceEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "LogRebalanceEvent" event discriminators.',
  );
}
