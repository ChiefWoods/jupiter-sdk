/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_EARN_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_REBALANCER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([206, 187, 54, 228, 145, 8, 203, 111]);

export function getUpdateRebalancerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_REBALANCER_DISCRIMINATOR,
  );
}

export type UpdateRebalancerInstruction<
  TProgram extends string = typeof LEND_EARN_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountLendingAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountLendingAdmin extends string
        ? WritableAccount<TAccountLendingAdmin>
        : TAccountLendingAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateRebalancerInstructionData = {
  discriminator: ReadonlyUint8Array;
  newRebalancer: Address;
};

export type UpdateRebalancerInstructionDataArgs = { newRebalancer: Address };

export function getUpdateRebalancerInstructionDataEncoder(): FixedSizeEncoder<UpdateRebalancerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newRebalancer", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_REBALANCER_DISCRIMINATOR }),
  );
}

export function getUpdateRebalancerInstructionDataDecoder(): FixedSizeDecoder<UpdateRebalancerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newRebalancer", getAddressDecoder()],
  ]);
}

export function getUpdateRebalancerInstructionDataCodec(): FixedSizeCodec<
  UpdateRebalancerInstructionDataArgs,
  UpdateRebalancerInstructionData
> {
  return combineCodec(
    getUpdateRebalancerInstructionDataEncoder(),
    getUpdateRebalancerInstructionDataDecoder(),
  );
}

export type UpdateRebalancerInput<
  TAccountSigner extends string = string,
  TAccountLendingAdmin extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  lendingAdmin: Address<TAccountLendingAdmin>;
  newRebalancer: UpdateRebalancerInstructionDataArgs["newRebalancer"];
};

export function getUpdateRebalancerInstruction<
  TAccountSigner extends string,
  TAccountLendingAdmin extends string,
  TProgramAddress extends Address = typeof LEND_EARN_PROGRAM_ADDRESS,
>(
  input: UpdateRebalancerInput<TAccountSigner, TAccountLendingAdmin>,
  config?: { programAddress?: TProgramAddress },
): UpdateRebalancerInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountLendingAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_EARN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    lendingAdmin: { value: input.lendingAdmin ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("lendingAdmin", accounts.lendingAdmin),
    ],
    data: getUpdateRebalancerInstructionDataEncoder().encode(
      args as UpdateRebalancerInstructionDataArgs,
    ),
    programAddress,
  } as UpdateRebalancerInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountLendingAdmin
  >);
}

export type ParsedUpdateRebalancerInstruction<
  TProgram extends string = typeof LEND_EARN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    lendingAdmin: TAccountMetas[1];
  };
  data: UpdateRebalancerInstructionData;
};

export function parseUpdateRebalancerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateRebalancerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount(), lendingAdmin: getNextAccount() },
    data: getUpdateRebalancerInstructionDataDecoder().decode(instruction.data),
  };
}
