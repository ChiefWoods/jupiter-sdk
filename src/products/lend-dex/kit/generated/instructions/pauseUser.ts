/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LEND_DEX_PROGRAM_ADDRESS } from "../programs";

export const PAUSE_USER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  18, 63, 43, 94, 239, 53, 101, 14,
]);

export function getPauseUserDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PAUSE_USER_DISCRIMINATOR);
}

export type PauseUserInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountDexAdmin extends string | AccountMeta<string> = string,
  TAccountDex extends string | AccountMeta<string> = string,
  TAccountPosition extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountDexAdmin extends string
        ? ReadonlyAccount<TAccountDexAdmin>
        : TAccountDexAdmin,
      TAccountDex extends string ? ReadonlyAccount<TAccountDex> : TAccountDex,
      TAccountPosition extends string
        ? WritableAccount<TAccountPosition>
        : TAccountPosition,
      ...TRemainingAccounts,
    ]
  >;

export type PauseUserInstructionData = {
  discriminator: ReadonlyUint8Array;
  pauseSupply: boolean;
  pauseBorrow: boolean;
};

export type PauseUserInstructionDataArgs = {
  pauseSupply: boolean;
  pauseBorrow: boolean;
};

export function getPauseUserInstructionDataEncoder(): FixedSizeEncoder<PauseUserInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["pauseSupply", getBooleanEncoder()],
      ["pauseBorrow", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: PAUSE_USER_DISCRIMINATOR }),
  );
}

export function getPauseUserInstructionDataDecoder(): FixedSizeDecoder<PauseUserInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["pauseSupply", getBooleanDecoder()],
    ["pauseBorrow", getBooleanDecoder()],
  ]);
}

export function getPauseUserInstructionDataCodec(): FixedSizeCodec<
  PauseUserInstructionDataArgs,
  PauseUserInstructionData
> {
  return combineCodec(
    getPauseUserInstructionDataEncoder(),
    getPauseUserInstructionDataDecoder(),
  );
}

export type PauseUserInput<
  TAccountAuthority extends string = string,
  TAccountDexAdmin extends string = string,
  TAccountDex extends string = string,
  TAccountPosition extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  dexAdmin: Address<TAccountDexAdmin>;
  dex: Address<TAccountDex>;
  position: Address<TAccountPosition>;
  pauseSupply: PauseUserInstructionDataArgs["pauseSupply"];
  pauseBorrow: PauseUserInstructionDataArgs["pauseBorrow"];
};

export function getPauseUserInstruction<
  TAccountAuthority extends string,
  TAccountDexAdmin extends string,
  TAccountDex extends string,
  TAccountPosition extends string,
  TProgramAddress extends Address = typeof LEND_DEX_PROGRAM_ADDRESS,
>(
  input: PauseUserInput<
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountPosition
  >,
  config?: { programAddress?: TProgramAddress },
): PauseUserInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountDexAdmin,
  TAccountDex,
  TAccountPosition
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_DEX_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    dexAdmin: { value: input.dexAdmin ?? null, isWritable: false },
    dex: { value: input.dex ?? null, isWritable: false },
    position: { value: input.position ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("dexAdmin", accounts.dexAdmin),
      getAccountMeta("dex", accounts.dex),
      getAccountMeta("position", accounts.position),
    ],
    data: getPauseUserInstructionDataEncoder().encode(
      args as PauseUserInstructionDataArgs,
    ),
    programAddress,
  } as PauseUserInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountDexAdmin,
    TAccountDex,
    TAccountPosition
  >);
}

export type ParsedPauseUserInstruction<
  TProgram extends string = typeof LEND_DEX_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    dexAdmin: TAccountMetas[1];
    dex: TAccountMetas[2];
    position: TAccountMetas[3];
  };
  data: PauseUserInstructionData;
};

export function parsePauseUserInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPauseUserInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      dexAdmin: getNextAccount(),
      dex: getNextAccount(),
      position: getNextAccount(),
    },
    data: getPauseUserInstructionDataDecoder().decode(instruction.data),
  };
}
