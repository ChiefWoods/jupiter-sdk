//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;

pub const UPDATE_THRESHOLD_PERCENT_DISCRIMINATOR: [u8; 8] = [177, 125, 99, 134, 42, 254, 140, 234];

/// Accounts.
#[derive(Debug)]
pub struct UpdateThresholdPercent {
    pub authority: solana_address::Address,

    pub dex_admin: solana_address::Address,

    pub dex: solana_address::Address,
}

impl UpdateThresholdPercent {
    pub fn instruction(
        &self,
        args: UpdateThresholdPercentInstructionArgs,
    ) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: UpdateThresholdPercentInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.authority,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.dex_admin,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(self.dex, false));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = UpdateThresholdPercentInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::LEND_DEX_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UpdateThresholdPercentInstructionData {
    discriminator: [u8; 8],
}

impl UpdateThresholdPercentInstructionData {
    pub fn new() -> Self {
        Self {
            discriminator: [177, 125, 99, 134, 42, 254, 140, 234],
        }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for UpdateThresholdPercentInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
pub struct UpdateThresholdPercentInstructionArgs {
    pub upper_threshold_percent: u32,
    pub lower_threshold_percent: u32,
    pub threshold_shift_time: u32,
    pub shift_time: u32,
}

impl UpdateThresholdPercentInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `UpdateThresholdPercent`.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[]` dex_admin
///   2. `[writable]` dex
#[derive(Clone, Debug, Default)]
pub struct UpdateThresholdPercentBuilder {
    authority: Option<solana_address::Address>,
    dex_admin: Option<solana_address::Address>,
    dex: Option<solana_address::Address>,
    upper_threshold_percent: Option<u32>,
    lower_threshold_percent: Option<u32>,
    threshold_shift_time: Option<u32>,
    shift_time: Option<u32>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl UpdateThresholdPercentBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: solana_address::Address) -> &mut Self {
        self.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn dex_admin(&mut self, dex_admin: solana_address::Address) -> &mut Self {
        self.dex_admin = Some(dex_admin);
        self
    }
    #[inline(always)]
    pub fn dex(&mut self, dex: solana_address::Address) -> &mut Self {
        self.dex = Some(dex);
        self
    }
    #[inline(always)]
    pub fn upper_threshold_percent(&mut self, upper_threshold_percent: u32) -> &mut Self {
        self.upper_threshold_percent = Some(upper_threshold_percent);
        self
    }
    #[inline(always)]
    pub fn lower_threshold_percent(&mut self, lower_threshold_percent: u32) -> &mut Self {
        self.lower_threshold_percent = Some(lower_threshold_percent);
        self
    }
    #[inline(always)]
    pub fn threshold_shift_time(&mut self, threshold_shift_time: u32) -> &mut Self {
        self.threshold_shift_time = Some(threshold_shift_time);
        self
    }
    #[inline(always)]
    pub fn shift_time(&mut self, shift_time: u32) -> &mut Self {
        self.shift_time = Some(shift_time);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = UpdateThresholdPercent {
            authority: self.authority.expect("authority is not set"),
            dex_admin: self.dex_admin.expect("dex_admin is not set"),
            dex: self.dex.expect("dex is not set"),
        };
        let args = UpdateThresholdPercentInstructionArgs {
            upper_threshold_percent: self
                .upper_threshold_percent
                .clone()
                .expect("upper_threshold_percent is not set"),
            lower_threshold_percent: self
                .lower_threshold_percent
                .clone()
                .expect("lower_threshold_percent is not set"),
            threshold_shift_time: self
                .threshold_shift_time
                .clone()
                .expect("threshold_shift_time is not set"),
            shift_time: self.shift_time.clone().expect("shift_time is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `update_threshold_percent` CPI accounts.
pub struct UpdateThresholdPercentCpiAccounts<'a, 'b> {
    pub authority: &'b solana_account_info::AccountInfo<'a>,

    pub dex_admin: &'b solana_account_info::AccountInfo<'a>,

    pub dex: &'b solana_account_info::AccountInfo<'a>,
}

/// `update_threshold_percent` CPI instruction.
pub struct UpdateThresholdPercentCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,

    pub authority: &'b solana_account_info::AccountInfo<'a>,

    pub dex_admin: &'b solana_account_info::AccountInfo<'a>,

    pub dex: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: UpdateThresholdPercentInstructionArgs,
}

impl<'a, 'b> UpdateThresholdPercentCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: UpdateThresholdPercentCpiAccounts<'a, 'b>,
        args: UpdateThresholdPercentInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            authority: accounts.authority,
            dex_admin: accounts.dex_admin,
            dex: accounts.dex,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(3 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.authority.key,
            true,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.dex_admin.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(*self.dex.key, false));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_writable: remaining_account.1,
                is_signer: remaining_account.2,
            })
        });
        let mut data = UpdateThresholdPercentInstructionData::new()
            .try_to_vec()
            .unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::LEND_DEX_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(4 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.authority.clone());
        account_infos.push(self.dex_admin.clone());
        account_infos.push(self.dex.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `UpdateThresholdPercent` via CPI.
///
/// ### Accounts:
///
///   0. `[signer]` authority
///   1. `[]` dex_admin
///   2. `[writable]` dex
#[derive(Clone, Debug)]
pub struct UpdateThresholdPercentCpiBuilder<'a, 'b> {
    instruction: Box<UpdateThresholdPercentCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> UpdateThresholdPercentCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(UpdateThresholdPercentCpiBuilderInstruction {
            __program: program,
            authority: None,
            dex_admin: None,
            dex: None,
            upper_threshold_percent: None,
            lower_threshold_percent: None,
            threshold_shift_time: None,
            shift_time: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    #[inline(always)]
    pub fn authority(&mut self, authority: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.authority = Some(authority);
        self
    }
    #[inline(always)]
    pub fn dex_admin(&mut self, dex_admin: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.dex_admin = Some(dex_admin);
        self
    }
    #[inline(always)]
    pub fn dex(&mut self, dex: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.dex = Some(dex);
        self
    }
    #[inline(always)]
    pub fn upper_threshold_percent(&mut self, upper_threshold_percent: u32) -> &mut Self {
        self.instruction.upper_threshold_percent = Some(upper_threshold_percent);
        self
    }
    #[inline(always)]
    pub fn lower_threshold_percent(&mut self, lower_threshold_percent: u32) -> &mut Self {
        self.instruction.lower_threshold_percent = Some(lower_threshold_percent);
        self
    }
    #[inline(always)]
    pub fn threshold_shift_time(&mut self, threshold_shift_time: u32) -> &mut Self {
        self.instruction.threshold_shift_time = Some(threshold_shift_time);
        self
    }
    #[inline(always)]
    pub fn shift_time(&mut self, shift_time: u32) -> &mut Self {
        self.instruction.shift_time = Some(shift_time);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = UpdateThresholdPercentInstructionArgs {
            upper_threshold_percent: self
                .instruction
                .upper_threshold_percent
                .clone()
                .expect("upper_threshold_percent is not set"),
            lower_threshold_percent: self
                .instruction
                .lower_threshold_percent
                .clone()
                .expect("lower_threshold_percent is not set"),
            threshold_shift_time: self
                .instruction
                .threshold_shift_time
                .clone()
                .expect("threshold_shift_time is not set"),
            shift_time: self
                .instruction
                .shift_time
                .clone()
                .expect("shift_time is not set"),
        };
        let instruction = UpdateThresholdPercentCpi {
            __program: self.instruction.__program,

            authority: self.instruction.authority.expect("authority is not set"),

            dex_admin: self.instruction.dex_admin.expect("dex_admin is not set"),

            dex: self.instruction.dex.expect("dex is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct UpdateThresholdPercentCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    authority: Option<&'b solana_account_info::AccountInfo<'a>>,
    dex_admin: Option<&'b solana_account_info::AccountInfo<'a>>,
    dex: Option<&'b solana_account_info::AccountInfo<'a>>,
    upper_threshold_percent: Option<u32>,
    lower_threshold_percent: Option<u32>,
    threshold_shift_time: Option<u32>,
    shift_time: Option<u32>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
