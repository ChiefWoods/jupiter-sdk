/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  containsBytes,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesEncoder,
  getConstantDecoder,
  getConstantEncoder,
  getHiddenPrefixDecoder,
  getHiddenPrefixEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const INITIALIZE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([121, 167, 244, 185, 162, 207, 107, 234]);

export function getInitializeClaimStatusEventEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR,
  );
}

export type InitializeClaimStatusEventEvent = {
  /** Campaign id. */
  campaignId: string;
  /** Base key of the campaign. */
  base: Address;
  /** Claim status public key. */
  claimsPubkey: Address;
};

export type InitializeClaimStatusEventEventArgs =
  InitializeClaimStatusEventEvent;

/** Gets the encoder for {@link InitializeClaimStatusEventEventArgs} event data. */
export function getInitializeClaimStatusEventEventEncoder(): Encoder<InitializeClaimStatusEventEventArgs> {
  return getHiddenPrefixEncoder(
    getStructEncoder([
      ["campaignId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["base", getAddressEncoder()],
      ["claimsPubkey", getAddressEncoder()],
    ]),
    [getConstantEncoder(INITIALIZE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the decoder for {@link InitializeClaimStatusEventEvent} event data. */
export function getInitializeClaimStatusEventEventDecoder(): Decoder<InitializeClaimStatusEventEvent> {
  return getHiddenPrefixDecoder(
    getStructDecoder([
      ["campaignId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
      ["base", getAddressDecoder()],
      ["claimsPubkey", getAddressDecoder()],
    ]),
    [getConstantDecoder(INITIALIZE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR)],
  );
}

/** Gets the codec for {@link InitializeClaimStatusEventEvent} event data. */
export function getInitializeClaimStatusEventEventCodec(): Codec<
  InitializeClaimStatusEventEventArgs,
  InitializeClaimStatusEventEvent
> {
  return combineCodec(
    getInitializeClaimStatusEventEventEncoder(),
    getInitializeClaimStatusEventEventDecoder(),
  );
}

/** Parses the provided bytes into {@link InitializeClaimStatusEventEvent} event data. */
export function parseInitializeClaimStatusEventEvent(
  data: ReadonlyUint8Array | Uint8Array,
): InitializeClaimStatusEventEvent {
  if (
    containsBytes(data, INITIALIZE_CLAIM_STATUS_EVENT_EVENT_DISCRIMINATOR, 0)
  ) {
    return getInitializeClaimStatusEventEventDecoder().decode(data);
  }
  throw new Error(
    'The provided data does not match the "InitializeClaimStatusEventEvent" event discriminators.',
  );
}
