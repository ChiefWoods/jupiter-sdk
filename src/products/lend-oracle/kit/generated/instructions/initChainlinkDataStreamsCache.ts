/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findChainlinkDsCachePda } from "../pdas";
import { LEND_ORACLE_PROGRAM_ADDRESS } from "../programs";
import {
  getFeedEntryDecoder,
  getFeedEntryEncoder,
  type FeedEntry,
  type FeedEntryArgs,
} from "../types";

export const INIT_CHAINLINK_DATA_STREAMS_CACHE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([47, 174, 206, 189, 94, 253, 25, 233]);

export function getInitChainlinkDataStreamsCacheDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_CHAINLINK_DATA_STREAMS_CACHE_DISCRIMINATOR,
  );
}

export type InitChainlinkDataStreamsCacheInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountOracleAdmin extends string | AccountMeta<string> = string,
  TAccountChainlinkDsCache extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountOracleAdmin extends string
        ? ReadonlyAccount<TAccountOracleAdmin>
        : TAccountOracleAdmin,
      TAccountChainlinkDsCache extends string
        ? WritableAccount<TAccountChainlinkDsCache>
        : TAccountChainlinkDsCache,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitChainlinkDataStreamsCacheInstructionData = {
  discriminator: ReadonlyUint8Array;
  nonce: number;
  feeds: Array<FeedEntry>;
  keepers: Array<Address>;
};

export type InitChainlinkDataStreamsCacheInstructionDataArgs = {
  nonce: number;
  feeds: Array<FeedEntryArgs>;
  keepers: Array<Address>;
};

export function getInitChainlinkDataStreamsCacheInstructionDataEncoder(): Encoder<InitChainlinkDataStreamsCacheInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nonce", getU16Encoder()],
      ["feeds", getArrayEncoder(getFeedEntryEncoder())],
      ["keepers", getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: INIT_CHAINLINK_DATA_STREAMS_CACHE_DISCRIMINATOR,
    }),
  );
}

export function getInitChainlinkDataStreamsCacheInstructionDataDecoder(): Decoder<InitChainlinkDataStreamsCacheInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nonce", getU16Decoder()],
    ["feeds", getArrayDecoder(getFeedEntryDecoder())],
    ["keepers", getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getInitChainlinkDataStreamsCacheInstructionDataCodec(): Codec<
  InitChainlinkDataStreamsCacheInstructionDataArgs,
  InitChainlinkDataStreamsCacheInstructionData
> {
  return combineCodec(
    getInitChainlinkDataStreamsCacheInstructionDataEncoder(),
    getInitChainlinkDataStreamsCacheInstructionDataDecoder(),
  );
}

export type InitChainlinkDataStreamsCacheAsyncInput<
  TAccountSigner extends string = string,
  TAccountOracleAdmin extends string = string,
  TAccountChainlinkDsCache extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  oracleAdmin: Address<TAccountOracleAdmin>;
  chainlinkDsCache?: Address<TAccountChainlinkDsCache>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: InitChainlinkDataStreamsCacheInstructionDataArgs["nonce"];
  feeds: InitChainlinkDataStreamsCacheInstructionDataArgs["feeds"];
  keepers: InitChainlinkDataStreamsCacheInstructionDataArgs["keepers"];
};

export async function getInitChainlinkDataStreamsCacheInstructionAsync<
  TAccountSigner extends string,
  TAccountOracleAdmin extends string,
  TAccountChainlinkDsCache extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_ORACLE_PROGRAM_ADDRESS,
>(
  input: InitChainlinkDataStreamsCacheAsyncInput<
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountChainlinkDsCache,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitChainlinkDataStreamsCacheInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountChainlinkDsCache,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    oracleAdmin: { value: input.oracleAdmin ?? null, isWritable: false },
    chainlinkDsCache: {
      value: input.chainlinkDsCache ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.chainlinkDsCache.value) {
    accounts.chainlinkDsCache.value = await findChainlinkDsCachePda(
      { nonce: getNonNullResolvedInstructionInput("nonce", args.nonce) },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("oracleAdmin", accounts.oracleAdmin),
      getAccountMeta("chainlinkDsCache", accounts.chainlinkDsCache),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitChainlinkDataStreamsCacheInstructionDataEncoder().encode(
      args as InitChainlinkDataStreamsCacheInstructionDataArgs,
    ),
    programAddress,
  } as InitChainlinkDataStreamsCacheInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountChainlinkDsCache,
    TAccountSystemProgram
  >);
}

export type InitChainlinkDataStreamsCacheInput<
  TAccountSigner extends string = string,
  TAccountOracleAdmin extends string = string,
  TAccountChainlinkDsCache extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  oracleAdmin: Address<TAccountOracleAdmin>;
  chainlinkDsCache: Address<TAccountChainlinkDsCache>;
  systemProgram?: Address<TAccountSystemProgram>;
  nonce: InitChainlinkDataStreamsCacheInstructionDataArgs["nonce"];
  feeds: InitChainlinkDataStreamsCacheInstructionDataArgs["feeds"];
  keepers: InitChainlinkDataStreamsCacheInstructionDataArgs["keepers"];
};

export function getInitChainlinkDataStreamsCacheInstruction<
  TAccountSigner extends string,
  TAccountOracleAdmin extends string,
  TAccountChainlinkDsCache extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof LEND_ORACLE_PROGRAM_ADDRESS,
>(
  input: InitChainlinkDataStreamsCacheInput<
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountChainlinkDsCache,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitChainlinkDataStreamsCacheInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountOracleAdmin,
  TAccountChainlinkDsCache,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? LEND_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    oracleAdmin: { value: input.oracleAdmin ?? null, isWritable: false },
    chainlinkDsCache: {
      value: input.chainlinkDsCache ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("oracleAdmin", accounts.oracleAdmin),
      getAccountMeta("chainlinkDsCache", accounts.chainlinkDsCache),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitChainlinkDataStreamsCacheInstructionDataEncoder().encode(
      args as InitChainlinkDataStreamsCacheInstructionDataArgs,
    ),
    programAddress,
  } as InitChainlinkDataStreamsCacheInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountOracleAdmin,
    TAccountChainlinkDsCache,
    TAccountSystemProgram
  >);
}

export type ParsedInitChainlinkDataStreamsCacheInstruction<
  TProgram extends string = typeof LEND_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    oracleAdmin: TAccountMetas[1];
    chainlinkDsCache: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: InitChainlinkDataStreamsCacheInstructionData;
};

export function parseInitChainlinkDataStreamsCacheInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitChainlinkDataStreamsCacheInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      oracleAdmin: getNextAccount(),
      chainlinkDsCache: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitChainlinkDataStreamsCacheInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
